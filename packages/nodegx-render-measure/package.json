{
  "name": "@nodegx/render-measure",
  "version": "1.0.0",
  "description": "The render report's pure half — the measurement expression, the findings and the thresholds. No Node, no browser, no build step.",
  "license": "GPL-3.0",
  "private": true,
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "jest --config jest.config.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
