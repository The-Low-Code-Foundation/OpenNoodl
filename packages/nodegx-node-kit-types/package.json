{
  "name": "@nodegx/node-kit-types",
  "version": "1.0.0",
  "description": "The authoring surface of a NodeGX node kit as one self-contained .d.ts — reachable from a JSDoc @type annotation with no build step, no tsconfig and no node_modules in the kit's project. Mirrors react-component-node.ts and @noodl/types; a drift test fails when they disagree.",
  "license": "GPL-3.0",
  "private": true,
  "types": "src/index.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "jest --config jest.config.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "typescript": "^5.9.3"
  }
}
