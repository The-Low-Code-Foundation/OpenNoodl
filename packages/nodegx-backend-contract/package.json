{
  "name": "@noodl/backend-contract",
  "version": "0.1.0",
  "description": "The backend-neutral data, auth and filter contracts, and the capability descriptor that says what each backend can actually do. Types and data only — no I/O, no adapter implementations. BCN-001.",
  "author": "The Low Code Foundation <contact@thelowcodefoundation.com>",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.0.1",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.3"
  }
}
