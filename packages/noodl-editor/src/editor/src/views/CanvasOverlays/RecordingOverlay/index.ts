/**
 * HUD-001 / HUD-002 — the recording HUD over the node canvas.
 *
 * Record left the Provenance panel here (TALK-003 Q1). The panel keeps the walk and the
 * recorded-interactions list; this owns arming, the live counter and the badges.
 */

export { RecordingOverlay } from './RecordingOverlay';
export type { RecordingOverlayProps } from './RecordingOverlay';

export { RecordingNodeBadge } from './RecordingNodeBadge';
export type { RecordingNodeBadgeProps } from './RecordingNodeBadge';
