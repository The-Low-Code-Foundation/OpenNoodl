# Phase 67 — the tasks (UNI: NodeGX University)

**Created:** 2026-08-14 out of [README.md](README.md). Read the README's two principles and the
rulings queue first — several tasks are blocked on D-rulings, and the two principles (the login
gates nothing; services not features) are acceptance criteria in every task.

> 🔴 **Read [PRIOR-ART-RECONCILIATION.md](PRIOR-ART-RECONCILIATION.md) before starting any task
> here.** The first working session (2026-08-14) did the archaeology §"Two things to check" below
> demands, and found **five** phase-67 premises wrong or incomplete against phases 17, 20 and 51 —
> including that **D12 does not exist** (ruled 2026-08-09) and that **UNI-010's experiment is
> already specced** as LEARN-007…010. Four rulings were made; the Blocked column below reflects them.

**Surfaces:** `platform` = the new repo (D1), `editor` = this repo, `bridge` = the
editor-outbound sync between them. No task opens an inbound connection to the user's machine.

> ✅ **THE RULINGS QUEUE IS EMPTY AGAIN — D15, D16 and D17 all ruled 2026-08-16.** It was emptied
> 2026-08-14 (D2–D9, D11), reopened 2026-08-15 with UNI-011 and D14, and closed again. **Nothing in
> this phase is waiting on a decision.** The Blocked column is once more a record of what each task
> must *honour*, not what it is waiting for. The register is **[RULINGS.md](RULINGS.md)** — read it,
> not this banner.

| Task | One line | Surface | Tier | Effort | Rulings it must honour |
|---|---|---|---|---|---|
| [UNI-001](UNI-001-ONE-LOGIN-THAT-GATES-NOTHING.md) ⭐ | the NodeGX account: OAuth, editor sign-in, consent — the spine everything hangs off | platform + editor | **1** | M/L | 🟡 **SPINE BUILT 2026-08-16 — the platform track is open.** `nodegx-community` `bb3ff8d7`, first commit, D1's stack. **The schema is the deliverable**: D3/D4/D6/D10/D11 are DB constraints and triggers, not conventions, because D14 gives this API two clients and a rule enforced in a client is one the other can disagree with. 🔴 **D3 × AC3 is a real tension and is resolved, not papered over** — UPDATE refused outright *including inside an erasure* (a licence to delete is not a licence to rewrite), DELETE refused unless the transaction declares `app.gdpr_erasure`, so AC3's cascade works and a stray one cannot. **57 specs, every mechanism control-run.** 🔴 **NOT built: OAuth, sessions, the consent screen UI, any editor half** — the button renders D2's string and is **inert**, because callback URLs need the domain and the domain is unregistered. AC1/AC2/AC4 are editor-side and untouched; **AC3 is met and proven** 🔴 **08-18, D19:** the **OIDC provider face is no longer needed for the forum** — there is no second system to federate with. It survives only if a *future* consumer wants it, so **do not build it speculatively**; plain sessions are the whole requirement now 🔴 **08-19, E1 BUILT (session 29):** the issuer exists — `session.ts` mints, GitHub OAuth runs in the browser, a **device flow** pairs the editor (no listener, per this task's own scope), sign-out revokes, and the site header's button is live. **35 platform specs + 24 editor specs.** ⚠️ **Nobody can sign in until Richard creates a GitHub OAuth App (E10)** — 503 without it, by design. ✅ **08-18, AC2's LAUNCHER affordance BUILT (session 33):** a `CommunityAccountCard` on the projects page — the offer, the eight-character code, and the chip with **Sign out** — plus `useCommunityAccount`, D2's string moved into `noodl-core-ui/constants/communityCopy.ts` so **three surfaces share one literal**, and **20 source-analysis specs**. 🔴 **Driven in a real editor**: card renders signed out, a written session brings up the chip, **sign-out deletes the store file**, and a sign-in click reports the unreachable platform (E2/E10) instead of hanging. 🔴 **Two findings**: the store is a **plaintext JSON file in `userData`, not `localStorage`** — the module's own security note said otherwise, and the scope's `0600` is not honoured; and **E1's `COMMUNITY_URL as COMMUNITY_ORIGIN` alias had left `test:main` RED since session 29** (2 failures in `uni-016`), under a handover that claimed the suite was re-run and quoted no count. ✅ **08-18, AC4 SETTLED (session 34) — a WORDING defect, not a code one.** The literal grep returns **one** file and has since UNI-016: the composer. But a *location* test never expressed *"gates nothing"* — a read inside the launcher could withhold a feature and stay invisible, while the composer's read **chooses a transport** (direct post vs the browser hand-off, which needs no account and is the signed-out CTA) and **withholds nothing**. The criterion is re-worded to the property it meant and is now a **gate**: `session-readers.test.ts` walks `packages/*/src` **from disk** (2548 files) so a reader in a new directory is still caught, holds a **recorded reader set with a reason each**, and asserts **no capability sits inside a session branch** — with the signed-in post button as the **known-firing control** that the checker can see a gate when one exists. 🔴 **Proven to fail before it was believed**: an unlisted reader planted in *another package* turned exactly one test red and named the file. **10 tests**; `test:main` **3826/3826 across 250** (floor 3816/249, and the floor was CLEANER than session 33's after-state — a peer had fixed phase 69's red), `tsc` exit 0. 🔴 **No editor code item is left in E1** |
| [UNI-002](UNI-002-POINTS-BADGES-AND-THE-LONG-LIST-OF-CHALLENGES.md) ⭐ | the contribution engine: points, badges, the challenge registry, the event ledger | platform | **1** | M/L | ✅ **BUILT 2026-08-16 — all four ACs met.** `105 specs / 8 files` (baseline 57/5), `tsc` clean, `next build` succeeds. **Nine control runs, every mechanism proved to bite** — the advisory-lock control fails **exactly one** spec (the two-connection race) and nothing else; dropping the whole integrity trigger fails **17**. 🔴 **The rules are in the trigger for a reason sharper than D14's:** one of the three mechanisms is `bridge_event` and the bridge is the **user's own editor**, so the price, the mechanism, the cap and the rate limit are read from the registry *by the database* — a caller naming its own points value is refused, not believed. 🔴 **`challenge_id` is NOT NULL** ⇒ every point traces to a registry row; no adjustment back door. **AC4 is proved by a challenge invented at runtime with a random slug** — no source file can hold a case for it. ⚠️ **`awarded_by` has no FK, deliberately**: `set null` is an UPDATE (D3 refuses absolutely), `restrict` breaks UNI-001 AC3, `cascade` deletes other people's awards — an immutable record cannot hold a reference whose upkeep rewrites it; the erased-admin residue is asserted by a spec. 🔴 **D4's absence spec was on the wrong table** — the ruling names *challenges*, which did not exist when the spec was written; both now asserted, and `badges` is the twelve-row cross-product **derived from the enums**. ⚠️ **NOT built: the 12 SVG artworks** (paths only — UNI-003 will be first to notice), no admin route for manual grants (auth first), threshold challenges award manually. 🔴 **AMENDED 2026-08-16 by UNI-003, and it was a live defect in this task's DATA rather than its code:** `project-first-built` awarded **(building, bronze)** — the badge whose own row reads *"Published your first prefab"* — for saving a project, so the profile claimed a publication that never happened **and D8's evidence bar was cleared by pressing save**. The catalogue is now 48: that row keeps its 20 points and awards no badge, and a once-only **`prefab-first-published`** awards the badge on `prefab.published` beside the repeatable one. ✅ **This task's own *"every one of D4's twelve badges is reachable"* spec is what made the fix safe** — a guard written here caught the second half of a change made one task later. 🔴 **Three findings**: a BEFORE ROW trigger runs **ahead of NOT NULL and FK**; a `number`-typed `bigserial` id lied and every spec passed; and `signature ?? sign(body)` turned the *"no header"* spec into a happy-path one 🔴 **08-18, D19:** the **Discourse webhook award path is deleted, not re-pointed** — `accepted answer → points` becomes a call in the same transaction (UNI-015 AC2). ✅ **The engine does not change**: its `event_key` selection and its *challenge invented at runtime* spec must pass unchanged, which is what proves this is a new caller and not a new mechanism |
| [UNI-003](UNI-003-THE-PROFILE-THAT-FOLLOWS-YOU.md) | the public dev profile: badges, points, published prefabs, offers | platform | **1** | M | ✅ **BUILT 2026-08-16 — all four ACs met.** **164 specs / 9 files** (baseline 105/8), `tsc` clean, `next build` succeeds. **Thirteen control runs, every mechanism proved to bite.** 🔴 **AC1's *"404, not a stub"* is one `null`**: private, hidden and never-created are the same answer, so no route can render a "this is hidden" page — the criterion is met by there being nothing to render. 🔴 **Hiding is deliberately NOT `visibility = 'private'`** — reusing the owner's toggle would leave them unable to tell their own opt-out from a decision about them, and AC4's *"sees why"* nowhere to live. 🔴 **D8's bar gates the LISTING, not the page and not the flags** (AC1 and AC3 are literal about the toggle being unconditional), and `profileBar()` returns its **components** rather than a boolean — D16's *"a threshold nobody can see the approach to gets crossed by rounding"*, applied a task early. ⚠️ Its evidence half reads **D4's taxonomy, not a slug list**, or UNI-002 AC4 breaks the day a second publishing challenge is added. 🔴 **THE FINDING: `project-first-built` awarded (building, bronze) — the badge whose row reads *"Published your first prefab"* — for pressing save.** It rendered a claim the holder never made **and silently cleared D8's evidence bar**, so anyone who saved a project once qualified for professional listing. Fixed as **data** (the row keeps its points and awards no badge; a new once-only `prefab-first-published` awards the badge on `prefab.published`) — ✅ and UNI-002's own *"every one of the twelve badges is reachable"* spec is what made the fix safe. ⚠️ **Nothing mechanically checks that a challenge's MEANING matches its badge**; two specs pin these rows by hand. ✅ **DRIVEN over HTTP** against consequences written first: `/u/quiet-quentin` **404** with her bio nowhere in the body, `/u/nia-new` **200 while `/people` omits her** (the D8 seam in one reading), a hide 404ing on the very next request. 🔴 The contribution line was checked against **a second instrument** (`psql`) — and nearly went unverified, because React's `<!-- -->` between text nodes makes a grep for the number return nothing, which reads exactly like a page rendering no number. ⚠️ **NOT built: no admin route** (sessions first, UNI-002's precedent), **no owner-facing account page** — `ownProfile()` is specced and returns the reason, so AC4's owner half is met **at the API and not yet at a URL** — and **the 12 SVGs still do not exist**: the profile renders the family mark and tier colour rather than a broken image, and it is deliberately not a placeholder pretending to be the badge |
| [UNI-004](UNI-004-RFPS-AND-COACHING-WITHOUT-A-GATE.md) ⭐ | the RFP board + coaching offers/booking/payment, spam-shielded relay | platform | **1** | L | ✅ **BUILT 2026-08-16 — all four ACs met.** **245 specs / 12 files** (baseline 164/9), `tsc` clean, `next build` 11 routes. 🔴 **AC1's *"provably absent"* is a TRIGGER, not a renderer** — a renderer that omits an address is a promise about the renderer; a database that refuses the row is a proof about the row. It searches **`to_jsonb(new)`, the whole row**, so a column added next year is covered the day it is added — **and it catches the bypass nobody designs for: a responder who types their own address into the message body has the response REFUSED AND NOT STORED** (one transaction). 🔴 **D7's *"payment can be absent"* is a MISSING constraint** — nothing ties `state` to `payment_state`, so a **£120** offer confirms with `not_required`; `recordPayment` has **no caller outside the suite**, which is what AC2's *"(once D7 lands)"* honestly means. 🔴 **AC4 is an absence with three witnesses** — one writer with no privileged argument, no `featured`/`approved`/`rank` column (asserted against `information_schema`), and `richard` shown ordered **behind** `ada-builds` by the same rule. ✅ **D8's bar was written 3× and is now written once** (`profile_meets_bar()`, **four callers**) — proved by UNI-003's 56 specs passing unchanged **and** by the control that fails UNI-003's own directory spec. ⚠️ **Two report tables** (`profile_reports` + `content_reports`) — chosen, and the cost named in `moderation.ts`. ⚠️ **`profile_report_state` → `report_state`**: 🔴 the drift test compares tables and columns, **not enum names**, so it would not have caught a half-done rename. ✅ **DRIVEN over HTTP, 13/13 consequences written first** — a hidden request **404s on its own URL**, the board shows a **count and never a body**, an offer below the bar **exists and is not listed**. 🔴 **16 controls: 14 clean, 1 measured NOTHING** (a `perl` replacement containing `$$` expanded to the **PID**, corrupting the dollar-quote ⇒ 71 "failures" that all said *"the schema did not build"*), **1 deliberately nondeterministic** (advisory lock off: **3 of 5** race runs slip, lock on **5 of 5** hold — *one run of a race control is not a measurement*). ⚠️ **NOT built: nothing is SENT** (no SMTP, no inbound mail, relay domain unregistered), **no Paddle**, no admin routes, no sessions. ⚠️ **Judgement call to reverse first if wrong: responding requires D8's bar** — one line in `rfp_response_gate()` |
| [UNI-005](UNI-005-AN-ORG-IS-A-ROSTER-AND-A-SHELF.md) | org workspaces: contact-us provisioning, GitHub org hookup, roster, shared prefab/template shelf | platform | 2 | L | ✅ **BUILT 2026-08-16 — all five ACs met.** **363 specs / 16 files** (baseline 245/12), `tsc` clean, `next build` **14 routes**, **ten controls**, **30/30 driven consequences written first**. 🔴 **AC5 was already met before the session began** — LEARN-005's amendment was written 08-14; checked rather than rewritten. 🔴 **THE FINDING: `orgsFor` had no viewer argument**, and *designing AC3's sweep* is what found it — its comment read *"its own membership, so no viewer is needed"*, true of every call site and false as a property, so an admin holding a member's id (which is on the roster they may see) was one call from learning **which other orgs that member belongs to**. Sixth instance of *build the caller*, first where the caller was a **test being designed**. 🔴 **AC3 is a SENTINEL SWEEP over the modules' own exports** — every export called as the admin about the member, with a recipe-coverage assertion so a new endpoint fails until someone gives it a verdict, and **two known-firing controls** because an absence asserted with no proof the instrument can fire measures nothing. 🔴 **AC4 is a CENSUS, not a document**: all 73 free-text columns classified against `information_schema`, unclassified ⇒ named failure; ⚠️ and the suite states **where the claim stops** — six `adult-authored` columns nothing can police. 🔴 **A control found the shelf module's own header true of ONE reader and false of the other** (`orgShelf` filtered `hidden_at` inline, agreeing with `shelf_item_visible_to` by coincidence) — *and the fix was unmeasurable until one spec was split in two, because a spec that fails for either of two reasons cannot be a control for one of them*: as shipped **2** fail, with the inline copy restored **1**. 🔴 **The GitHub sync's rail scoping is a legitimate read of `source`** — D6's ban is on *consumers*; the spec runs a sync over a roster with all three rails rather than asserting the `where`, which would pass on a query carrying it in the SELECT. ⚠️ **Two reversible judgement calls**: an org-minor account **reads the shelf and does not publish to it** (a free-text payload is where a name arrives — 0003's conclusion about a bio) and **belongs to exactly one org, never as admin**. ⚠️ **NOT built: no GitHub API call** (the reconciliation is the deliverable), no email, **no write path in a browser** — and 🔴 **`src/lib/viewer.ts` is a session READER only**, built because UNI-005 is the phase's first task with **no public half**, so every route would otherwise have one reachable branch and *a drive over a surface that 404s for everybody passes identically when it is broken* |
| [UNI-006](UNI-006-ASSIGN-GRADE-REVIEW.md) | org teaching: push lessons/assignments to members, view graded results, human grading override | platform + bridge | 2 | M/L | ✅ **BUILT 2026-08-16 — all four ACs met platform-side.** **442 specs / 18 files** (baseline 363/16), `tsc` clean, `next build` **16 routes**, **16/16 driven consequences written first**. 🔴 **THE FINDING: UNI-005's AC3 sweep advertised *"any org endpoint"* and enumerated a HAND-LIST OF TWO MODULES** — its call list was genuinely derived from `Object.entries(orgsModule)`, which is what made it look strong, but **the list it derived from was itself a list somebody wrote**, so `assignments.ts` (the module holding graded results about minors) was covered by nothing while the suite stayed green. `ORG_MODULES` is now checked against every `src/lib/` file that consults org membership. ⚠️ *Everything downstream of a derivation passes whether the thing you derived from is complete or not.* 🔴 **`org_is_admin()` returned NULL for a NON-member, so `if not org_is_admin(...)` never fired** — `not NULL` is NULL and `if NULL then` does not run, so every gate let through **exactly the person it existed to stop**, while correctly refusing a member with the wrong role (which is why the first draft passed). Found by adding a **third** candidate — a stranger — to a two-candidate test. *A guard tested only against the near-miss is not proven against the far one.* Harmless in a `where`, which is why UNI-005 never saw it. 🔴 **`lessongrading.ts` restates D10 WIDER than D10 says** (*"project content never leaves the machine"*); obligation 3 is about **third-party MODEL APIs** (PRIOR-ART §F4), and the literal reading forbids the graded results D10 exists to permit. The real bar is the **free-text** argument — which permits a bundle of booleans and integers. *A relayed conclusion that GAINS scope is as wrong as one that loses it, and reads as caution.* 🔴 **AC4's headline census PASSED with the allow-list entirely disabled** — its only writer was `submitWork`, which narrows in TypeScript, so it measured `pickEvidence` and reported it as a property of the schema; control went 15→**16 of 71** once it also wrote through the raw path. *A spec whose only writer already enforces the rule cannot detect the rule's removal.* ✅ **The bundle holds NO string except `provenance`** (closed set of 4) — every other leaf a boolean or non-negative integer, refused otherwise — which is what lets `submissions.evidence`, **the first column a minor can write to**, be censused without a `minor-free-text` class. ✅ **D13 is one enum, not a second path**; ⚠️ **AC2's "notify" cannot be an email** (a minted seat has no address, by 0001's constraint). ⚠️ **NOT built: no bridge endpoint, no write path in a browser.** 🔴 Drive trap: **`curl \| grep` for a literal spanning two JSX expressions never matches** — React inserts `<!-- -->`; two consequences read FALSE-NEGATIVE until the HTML was read |
| [UNI-007](UNI-007-THE-LESSON-BEAMED-INTO-THE-EDITOR.md) ⭐ | intake → personalised path → lesson project pulled into the editor's Learning folder → graded via MCP tooling | all three | 2 | L | 🟡 **SLICES 1–4 BUILT** (08-14, 08-15) — the static check, both grading engines, **both** engine-2 adapters, the Learning folder register, D5's launcher section, and ✅ **"check my work"** — the runner's first caller anywhere. **160 tests** in `test:main`, 21 in `noodl-mcp`. 🔴 Slice 4 found that a Learning-folder lesson **had no lesson layer at all** (fixed, `c38fcb7b`) — the fourth time building a caller exposed a hole, and the first time on a *feature* that a live drive had already passed over. ✅ **Slice 4 DRIVEN**: ten consequences, one failed (*"resumes on the step you left"* — true in code, false through the UI), fixed `393ec7bf` and re-driven. No intake (platform work) |
| [UNI-008](UNI-008-ONLINE-IN-ONE-CLICK-OFF-IN-FORTY-FIVE-DAYS.md) | push-to-share hosting: subdomain, 15-day life, manual restarts, 45-day cap, then the self-host off-ramp | platform + editor | 3 | **L+** | 🔴 **D9 ruled *against* the recommendation** — a record-capped backend is in. **Five new obligations**, effort raised, still deliberately last. [RULINGS.md](RULINGS.md) D9 |
| [UNI-009](UNI-009-THE-COMMUNITY-HOME.md) | the site itself: tutorials/tips, meetup replays, the forum entry point | platform | **1 (minimal cut)** | S/M | 🟡 **CONTENT CUT BUILT 2026-08-16** (`bb3ff8d7`) — home, `/replays`, `/tutorials`, `/university`. ✅ **AC2 MET and verified by serving it, not by building it**: all four routes **200 with no cookie**, replays render newest-first, D2's string on every page from the one constant that owns it. ⚠️ The home is a grid of standing surfaces rather than a feed **on purpose** — D16's composition obligation, which the threshold does *not* discharge. 🔴 **NOT built: Discourse, SSO, the webhook receiver** — AC1 and AC3 are untouched and the forum is a purchase nobody has made 🔴 **AMENDED 2026-08-18 by D19 — AC1 and AC3 STRUCK.** They specified SSO against Discourse and a webhook receiver; there is no Discourse. AC1 becomes *the entry point shows real threads signed out*; AC3's real subject (the UNI-002 hook is live) **moves to UNI-015 AC2 and gets sharper** — same transaction as the accept, so a control can prove the accept rolls back with it. ⚠️ **This task keeps the home, replays and tutorials**; it stops owning the forum |
| [UNI-010](UNI-010-A-TUTORIAL-YOUR-OWN-CLAUDE-CAN-WRITE.md) | the user's own Claude authors a verified lesson into the Learning folder — an experiment with pre-registered kill/keep criteria | editor + MCP | experiment | M | 🟡 **SLICES 1–2 BUILT** (08-15). Slice 1 = the **F1–F4 harness**; slice 2 = **the gate collected and the surface built**: `LearningFolderModel.install()` runs the whole scorecard (was F1 alone), and `create_lesson` / `check_lesson` / `get_lesson_brief` ship as a deferred `lesson` group in `noodl-mcp`. **74 tests** (46 + 16 editor, 12 MCP). ✅ **Criterion 1 met for all four classes.** 🔴 D5 solved by an **asymmetry**: a manifest claim may only *tighten* — `authoredBy: "ai"` spends trust, so it is honoured where `curated` would not be, and the MCP route needs no bridge at all. 🔴 The install gate is a **policy table keyed by provenance**, not slice 1's `installable`: the editor **cannot render a solution folder**, so gating on it would refuse every AI lesson. ⚠️ **F4 is therefore checked by the producer and not the installer** — a recorded hole. 🔴 Slice 2's find: **a lazy `require` defers execution, not resolution** — the sidecar is bundled, so UNI-007's inside-the-function require never made the evaluator sidecar-safe; split to `lessonevalconditions.live.ts`. ✅ **CRITERION 2 DRIVEN 2026-08-15** (tenth session): eight consequences written before the drive, **8/8 passed**. 🔴 The asymmetry's *bite* is driven, not just its label — two directories identical but for one `authoredBy` line, one **refused** naming F2/F3 and the other installed. Grading proved provenance-independent (AI arm and non-AI twin returned the same sentence; an edit moved both engines). 🔴 **New finding: F4 is checked by NOBODY for a real user** — the producer's harness lives in `scripts/`, which is not in `build.files`, a fact UNI-007 had already recorded one slice earlier; *a hole recorded in two halves is not recorded*. **Flagged to Richard, not fixed** — it is a scope call. ✅ The "strictly stricter" precondition of the trust asymmetry is now **two specs, proved to bite**. ✅ **CRITERION 3 RUN 2026-08-16** ([UNI-010-CRITERION-3-RUN.md](UNI-010-CRITERION-3-RUN.md), pre-registered and committed *before* a lesson existed): **KEEP on the rule — 5 of 5 install and are completable, ≥3 clean on the human read.** 🔴 **And the same run gave positive evidence for the kill clause: TWO structural holes in F4**, each with a control pair. (a) F4's verdict **discards the render harness's own defect findings** — a bundle whose Repeater stamps three `dead-placeholder-text` rows scores F1–F4 all pass, and the broken/fixed pair differ by **one JSON key** with **character-identical scorecards**; (b) F4 renders **only the Router's `startPage`**, so a lesson that *teaches* a second page has its subject unscored — a dead placeholder there is invisible to F4 **and** F2 and the bundle writes. ✅ **`create_lesson` IS reachable over the real stdio transport** (item 2 closed without touching Richard's registered servers, which still cannot reach it) — 🔴 the `lesson` group is **deferred**, so the first `tools/list` returns 20 tools without it and `find_tools({group:"lesson"})` is required: **reaching the tool is a conversation, not a call**. 🔴 The human read's finding is one-directional: **five steps across four lessons check LESS than their prose asks**, because **F2 punishes a condition that is too strong and nothing punishes one that is too weak** — two have real consequences and one (the Router's `pages` list) **cannot be expressed in the vocabulary at all**. ⚠️ **The ordering constraint against CN-001 was NOT discharged** — it landed mid-run (`ed28a03c`, on disk at 00:26:13, between L3 and L4); corrected by **re-measuring** rather than arguing, and both findings reproduce post-CN-001. ✅ **SLICE 3 2026-08-16 — finding (a) CLOSED** (`eff91029`, **14 specs**, `test:main` 205/**3171**): `renderDefectCodes()` joins `countDrawnElements` as the second rule both adapters share, `WholeSolutionResult.renderDefects` carries the codes past the prose, and F4 fails on them as **`solution-renders-broken`**. 🔴 It reads the harness's **severity**, not the one code the finding named — `empty-list`, `broken-image` and `content-not-visible` were in the identical hole. ✅ **Graded as a control pair both ways**: re-run with the branch disabled, **3 new specs fail and all 25 old ones pass**; and pinned on *recorded* renders — `phase55-replay-haiku` reports two defects, `phase55-replay-sonnet` (the build phase 55 calls correct) still reports none. ⚠️ The learner's "check my work" sentence changed and its **verdict deliberately did not** — a learner mid-build legitimately has placeholders. 🔴 **Finding (b) §8.2 is untouched and belongs to CN-001**, and the F4 packaged-install scope call is still Richard's. ✅ **SLICE 4 2026-08-16 — the run's two remaining recommendations, §12.3 and §12.4, both closed** (**25 specs**, `test:main` 206/**3196**, `noodl-mcp` 44/**512**). §12.3: the brief now names the one-directional gradient out loud (*"F2 punishes a condition that is too strong and nothing punishes one that is too weak — check what your prose asked for"*) with the two shapes it took, and its **F4 paragraph is corrected** — it still described the pre-slice-3 check and never said F4 renders the start page only. §12.4: **`routerLists`**, the verb for *"a router lists this page"*, with `node` **optional** — unscoped it asks the reachability question the learner's app cares about, scoped it names one router; it compiles to **no `path` key** rather than `path: ''`, which would be a silent never-completes. ✅ **Graded as the control pair the finding was** — both arms differ only in the router's `routes`, the **old condition passes both** (asserted as the finding), and re-run with the branch disabled **14 new specs fail, all 251 pre-existing pass**. 🔴 **The purity claim was MEASURED**, per the fifth amendment: two sidecar builds back to back are 5,911,294 / 5,911,848 bytes and `readRouterPagesValue` is already in the *control*, so borrowing `pageRegistration.ts` costs **554 bytes and no new dependency**. ✅ **Zero MCP tool surface** — the manifest is an opaque passthrough, so a condition verb never reaches a tool schema; **the 57 tokens stay free and CN-006/CN-009 are not competing with this**. ⚠️ The control run found **two of my own F2 specs asserting `F2: 'fail'` alone, which passed with the verb disabled** — a failure indistinguishable from a missing mechanism measures nothing; both now assert the finding **code**. ✅ **SLICE 5 2026-08-16 — `derive_starter`** (`1332e0d1`, **25 specs**, `test:main` 209/**3248**, `noodl-mcp` 45/**529**): the starter is now **subtracted from the solution** rather than written beside it, which is the route the criterion-3 run took for all five lessons and the reason its ghostwriting refusal fired **zero times** — a number §11 reads as an upper bound, because a model building both projects independently is far more exposed. 🔴 **The condition verb decides the granularity** — `hasType`/`exists`/`hasLabel`/`hasPort` remove the node, `hasParams`/`paramsEqual` keep it and unset those parameters, `connection` removes the wire, `routerLists` un-lists the page. 🔴 **The postcondition is MEASURED, not guaranteed**: every graded step is replayed against the derived files through the real evaluator and nothing is written if one still holds — because a derivation that guaranteed F2′ *by construction* would make the gate's F2′ vacuous, and **a check that cannot fail is one nobody audits**. Both arms are in the spec. ✅ Control run: **13 of 15 new specs fail with the branch disabled, all 98 pre-existing pass** — 🔴 and the *first* control had only 11 failing, because **two of my own specs were dead in exactly slice 4's way** (one asserted a refusal indistinguishable from a missing mechanism, one asserted "the solution is unchanged", true of a function that returns immediately). 🔴 **The handover's cost premise was FALSE and is now measured**: the surface is **8,223 tokens with and without** it — a fourth tool in an already-deferred group is free, and CN-006/CN-009 are not competing with this. ✅ Purity **measured** per the fifth amendment: 5,936,471 / 5,917,637 bytes = **+18,834**, and the control was verified to be one |

| [UNI-012](UNI-012-F4-ON-A-PACKAGED-INSTALL.md) | ship the render harness with the sidecar, so F4 is answerable on a packaged install | editor packaging + MCP | falls out of UNI-010 | M | 🆕 **Added 2026-08-16.** ✅ **RULED by Richard — ship the harness**, the first of UNI-010's two options. 🔴 **Scoped, NOT built**: it is verifiable only against a packaged build, and a shipping claim nobody exercised is the artifact this phase has found wrong four times. ⚠️ **Two facts measured while scoping, and the first qualifies the ruling** — the harness probes for a **system Chrome** and refuses without one, so shipping it makes `allow_unrendered` *rare* rather than unnecessary (**both halves of the either/or are probably wanted**); and rendering through the sidecar's own Electron is closed by `ELECTRON_RUN_AS_NODE=1`, which is load-bearing and measured. ✅ CN-001 already did the structural half (`@nodegx/render-measure`) |

| [UNI-011](UNI-011-THE-COMMUNITY-MIRRORED-IN-THE-EDITOR.md) ⭐ | the community mirrored inside the app — same API as the web — plus the editor-only half: ask-about-this-node, share-a-capture | editor + bridge | 2 | M/L | ✅ **AC3 MET AND DRIVEN 2026-08-16 (slice 2b, editor `f72799b7`)** — *share what you're seeing*: a capture of the running preview plus live port values, **a toggle per port**, default **off** for anything record-shaped, nothing sent before the button. **23 specs**; `test:main` **227/3527**. 🔴 **THE FINDING: AC3 cannot use AC2's instrument, and the reason generalises.** The excerpt can claim *"every string comes from a set the editor owns"* because a graph's STRUCTURE has an editor-owned form; **a live port value has none — it IS the user's data**, so a closed vocabulary would publish nothing or be a vocabulary of one entry per value. ⚠️ **Reaching for one anyway yields a control that looks like AC2's and checks nothing.** The instrument becomes **provenance under consent**: every published string must trace to a port whose toggle was **on**, in the displayed form — graded with a known-**broken** probe *and* an assertion of what survived. 🔴 **So port names are deliberately NOT bucketed here**: the excerpt hides a user-authored port name because nothing asked about that port, while here the user reads name and value side by side, and *a consent screen that hides what is being consented to is worse than none*. AC2's rule becomes **the default** instead. 🔴 **Two independent grounds, kept as a LIST** — record-shaped/free-text value, or a port name the library does not declare — because collapsing them lets a fix for one read as clearing the row. ✅ **`isLibraryPort` exported and imported**, one predicate two consumers. 🔴 **The fallback shape is `text`, never `scalar`** — `previewValue()`'s grammar can grow, and a permissive default publishes every shape it fails to recognise. ✅ **The runtime's 200-char cap is a PRIVACY property too**: the payload IS the preview string, so what was reviewed is byte-for-byte what ships. 🔴 **THE DRIVE'S FINDING: the app preview was never in the capture registry.** This task's own scope note asserted *"both halves already exist"* naming `livePreviewCapture.ts` — whose only registrants were the two **AI sandbox** surfaces, so `hasLivePreview()` was **false** with a preview on screen, on the very surface whose port values the other half reads. *Build the caller*, **eighth** instance, and the first where the missing caller was asserted present **in the task's own scope section**. **10/10 driven consequences written first.** ⚠️ **Two instrument defects corrected mid-drive, both from this repo's trap list**: `BaseDialog` renders every dialog **twice** and `querySelector` took the **measuring** copy, so clicks hit the real dialog's differently-scrolled list and toggled rows nobody touched. 🔴 **The control that located the fault in the instrument rather than the feature was the excerpt checkbox** — not poll-driven, toggles with the identical technique; without it the honest reading was *"AC3's toggles do not work"*. ⚠️ **An enum reads as free text** (`sizeMode = "contentHeight"` starts off) — deliberate over-redaction, recorded so it is not later read as a defect. ⚠️ Capture is **written to Documents, not uploaded**, and 🔴 **the saved path is shown in the dialog and appears nowhere in the payload**. Previously: ✅ **AC2 MET AND DRIVEN 2026-08-16 (slice 2a, editor `f73b1bd6`)** — *Ask about this node*: menu item, composer prefilled with type/warning/version/OS, excerpt **off until enabled**, and the string shown **is** the string sent. **31 specs**; `test:main` **225/3489**. 🔴 **THE FINDING: no payload in this editor had ever had to hide a PORT name.** ALPHA-007's summary is a *histogram*, so no port name has ever left here; AC2's excerpt is *"types and wiring"*, and wiring **is** port names. `getPorts()` is `type.ports` ++ `this.ports` ++ `this.dynamicports` — the last two are user-authored, and **the first is too whenever the type is a component instance**, because a component's ports are its `Component Inputs` declarations. So the rule **composes** with the type rule: a port name survives only when its node's TYPE survived. ✅ `bucketTypeName` **exported and imported, not reimplemented** — two copies of one privacy control is where a fix lands on one of them. 🔴 **The same helper, two ways, means opposite things**: `bucketTypeName(name, null)` publishes the name **verbatim**, and the branch is reachable because the node library loads asynchronously — this module passes an **empty set**. ⚠️ **Recorded as a deliberate divergence, NOT an ALPHA-007 defect**: its own comment names the case and takes the trade knowingly, and the spec asserts **both** branches or the empty set gets simplified back to `null`. 🆕 **A closed-vocabulary sweep beats a secret search when the payload is STRUCTURE** — what leaks out of structure is an ordinary string in an ordinary place, so collect *every* string published and assert each came from a set the editor owns; graded with a known-**broken** probe *and* an assertion of what survived. 🆕 🔴 **Ordering trap**: `WarningsModel` joins with `<br>` so markup must go, but `redact()` **emits** `<url>`/`<path>` — strip **first**, redact **second**, or the markers saying a secret was removed are deleted. **Drive: 8 consequences written first, 8 observed**, against a copy of ALPHA-007's own hostile fixture — `/Acme Legal Client Portal/Invoice Row` → `<component>`, `com.acmelegal.internal.BillingWidget` → `<unknown>`, `clientMatterRef` → `<port>`, while `hoverStart`/`visible` **survive**. ⚠️ **It hands off to the browser rather than posting** — D16, and there is no forum and no issuer. 📋 **AC3 is the one untouched criterion.** Previously: 🟡 **SLICE 1 BUILT 2026-08-16 — the transport and the boundary** (platform `7193f92`, editor `f7b0b280`). 🔴 **THE FINDING: D14's API did not exist and no task owned it.** Its second consequence says *"the API is a deliverable, not an implementation detail"*, and the platform had **one** route under `src/app/api` — the Discourse webhook — with every other surface a page calling `src/lib` in-process. So this task's AC1 (*"from the same API"*) was unbuildable as written, and `communityVisibility()` (D15) and `readThreshold()` (D16) were pure functions whose **only callers were their own test files** — while D15's own header says the rule must live *behind the API* or the clients drift. *Build the caller*, seventh instance. **Platform: 462 specs / 19 files** (baseline 442/18), `tsc` clean, `next build` **21 routes** (was 16), **four controls**, **14/14 driven consequences written first**. **Editor: 55 specs**, `test:main` **223/3458** (221/3403 without them), eslint clean. 🔴 **The suite is built one level out from UNI-005's AC3 sweep**, because that sweep says of itself *"it proves nothing about a route handler that queries the database directly"* — the route list is read **off disk**, every route needs a D15 verdict **with a reason**, and two known-firing controls catch a route that forgot the gate and one that 404s while explaining why. 🔴 **The threshold is proved able to say `in-editor-mirror`** — every natural assertion expects `browser`, which is also what a completely broken query returns. 🔴 **An absent forum is typed, not zero**: `forum_threads` is written only by the webhook receiver, so with it unconfigured the table's emptiness is a fact about our deployment, not the community. 🔴 **`/api/v1/me/assignments` is deliberately NOT D15-gated** — D15 governs public speech by a minor, and gating it would switch UNI-006 off for every org that took D15's default, reading as caution the whole way (UNI-006 made this exact mistake with D10 two days earlier). 🔴 **The boundary is a DATA MODEL, not a sanitiser** — `Block[]` has no field that could hold markup — and its first draft was **safe only because React sets `href` with `setAttribute`**: `[x](&#106;avascript:…)` has no scheme, so it passed as *relative*, and in an HTML string the parser decodes it to `javascript:`. ⚠️ Found by the corpus entry that looked most theoretical. ✅ **`renderMarkdown` was measured on the same corpus and HOLDS on every case** — not a defect report; the objection to reusing it is structural. ⚠️ **NOT built: any view**, deliberately — D16's ship order is the opposite of its build order. AC2/AC3 untouched; nothing has talked to a real platform. Previously: ✅ **D14 ruled** — web canonical, editor **mirrors** it; editor-only features are the transition incentive, not a reduced surface. ✅ **D15 and D16 RULED 2026-08-16 — UNI-011 is fully unblocked, to build *and* to ship.** D15 → org-minor accounts default **absent**, admin may enable **read-only**, and 🔴 **no write path exists to be switched on**; the rule lives **behind the API**, not in each client, or the mirror drifts from the web. D16 → the entry point opens the **browser** until **30 threads · 3 consecutive weeks with a call · median first reply < 24h**, each **computed with its `n` visible**; ⚠️ the never-look-empty *composition* obligation is **not** discharged by the threshold. 🔴 The renderer is `nodeIntegration: true` **and so is the launcher** (`pages/ProjectsPage`, same `BrowserWindow`) — **no post body may render as HTML in it**; pick the `<webview>` island or raw-markdown-plus-sanitiser and prove the boundary with a known-**bad** corpus. ⚠️ Phase 37 (project tabs) is the "reach it mid-project" enabler and is **scoped, not built** — must not be assumed 🔴 **08-18, D19:** AC2/AC3 gain a **posting** path (UNI-016) — they stay met, but this file's ⚠️ *"hands off to the browser"* notes go stale the day it ships, and the hand-off **stays** as the signed-out route. **`saveCaptureNextTo` becomes the upload** this task predicted, which needs blob storage nobody owns |

| [UNI-013](UNI-013-THE-COMMUNITY-SITE-IN-NODEGX-CLOTHES.md) | the community site in NodeGX clothes — port the canonical token system to the web | platform | **1** | M | ✅ **SLICE 5 BUILT 2026-08-18 (session 32) — ALL THREE ARCHETYPES EXIST AND EVERY PAGE IS AN INSTANCE OF ONE.** The **list** archetype over six pages (`/tutorials` `/replays` `/people` `/rfps` `/coaching` `/bench`, three lines of glue each, the composition a value in `src/lib/lists.ts`), the **detail** archetype on three (`/u/[handle]` — where it came from — plus the thread and the brief), and the **fourth kit component, the facet bar**, drawn with **real counts**. 🔴 **The counts needed an instrument: a number on a pill is a promise about what the click returns**, so the suite runs every facet's own href back through the builder and compares row counts — the cardinality assertion where two producers meet. ✅ Also fixed from the review's list: **avatars on `/people`**, the Bench's **two link styles → one**, the **stray azure rule gone**, the queue **declared a lens on the list** rather than a second list, the replay link's **missing `target`/`rel`** (fixed in the kit, not per page), and **two empty states** where the no-match arm's action is asserted to actually clear the filter. 🔴 **THREE FINDINGS BIGGER THAN THE PAGES: (1) `--theme-color-primary-bg` IS TRANSLUCENT and the active filter pill has been UNGRADED since slice 1** — it threw rather than scoring; the contrast suite grew `flatten()` + an `over:` field and a translucent ground with no underlay now fails loudly. **The trigger for a contrast row is a new PAIRING, not a new colour role** (6th instance). **(2) The accent chip's label was graded only as a SHAPE** (3:1, the poster play mark) and carries words. **(3) A link sweep that follows a page into a SHARED module must scope by FUNCTION** — the first version read all of `lists.ts` and would have reported `/coaching` as linked from `/tutorials`, an instrument that launders a dark branch into a lit one. ⚠️ **Deliberately NOT done: search/sort/price bands (UNI-023's), `/tutorials/[slug]` (UNI-020's — the one page whose rows still do not link, with the reason carried in the row TYPE and asserted to be one page), `/university` + the five org pages (written exception list), and slice 4's twelve badge artworks (Richard's).** 🔴 **AND STILL UNMEASURED: nobody has looked at any page in the LIGHT theme.** Previously: 🔴 **SLICE 5 SCOPED 2026-08-18 (s28) — THE PAGES, and it is the largest piece left.** Richard, 08-18: *"we updated the 'people' detail page but the rest of the app still looks sad, dark and simple"* — the **second** time he has said a version of it. 🔴 **THE FINDING: AC4 NAMED THE WRONG UNIT.** It restyled **six components**, and the premise says *"the other pages inherit from the tokens"* — **they inherit tokens, not a design.** Eight pages are the SAME page: `<h1>`, grey two-line sub, filter pills, one stack of `bg-1` rectangles. **The profile page is the PROOF rather than the exception** — it looks designed because it is the one page the pitch artifact drew a specimen of. ✅ **Driven, not inspected**: `next start` on a fresh build, DB seeded with 4 people / 4 threads / an answer, 1440×1000 — ⚠️ **CSS hash checked against `ls .next/static/css/` FIRST** (this task's own trap; an unstyled page is what a "sad" review misreads), and ⚠️ **content seeded BEFORE looking**, because an empty page looks sad for a reason that is not design. **Observed:** home has **no accent colour anywhere** and shows **none of the 4 threads that exist** (⇒ UNI-009 AC1 is visibly unmet too) · `/people` has **NO AVATARS** while the profile has a gradient one · the Bench renders **two different link styles on one page**, the queue's being **raw underlined browser links** · the queue **repeats the list beneath it** · `/replays` is **~90% empty viewport** under one dashed box (**D16's never-empty obligation is discharged nowhere**) · no page uses more than 2 of the 5 surface steps. 🔴 **THE RULE IT GENERALISES TO: every surface built AFTER slice 3 — the Bench (UNI-015), the attachment renderers (UNI-016), the notification surfaces (UNI-014) — reaches for `bg-1` and a bordered rectangle. A design system with no WORKED EXAMPLE of its page shapes gets read as a colour palette.** ✅ So slice 5 is **not "restyle eight pages"**: **draw three archetypes — index, list, detail — and make every page an instance of one**; the profile is already the detail one. ⚠️ **AC1, AC3 and AC5 still bind.** Previously: 🟢 **SLICES 1–3 BUILT, pushed `f64f138` + `d205b47` (session 24, 2026-08-17). AC1–AC3 + AC5 MET; ONLY SLICE 4 (badge artworks, Richard's) REMAINS.** 🔴 **Sequencing lesson: slice 1 was near-invisible and landed first against a complaint explicitly about appearance — Richard's reaction was *"I thought the style was redone??"*. When an ask is about how something looks, ship something that looks different in the same session.** 🔴 **Slices 2–3 found two more AA defects rendering could not show: `--theme-color-on-primary` FLIPS per theme while the avatar gradient does not (white on azure, 2.63:1, light only), and a sticky header on `bg-page` put nav links at 4.47:1.** `scripts/sync-tokens.mjs` vendors `colors.css`/`fonts.css`/`spacing.css` into `src/styles/tokens/`; `globals.css` declares **no colour of its own**; **516 specs / 21 files** (462 unchanged + 22 drift + 32 contrast), `tsc` clean, `next build` clean at 21 routes, both themes screenshotted. 🔴 **AC2 cost a real defect: the first draft pointed all secondary copy at `--theme-color-fg-muted`, which is an editor CHROME label colour and scores 3.19:1 on the light page ground — below AA, on the lede of every page. Two screenshots said it was fine; the arithmetic said it was not.** `--theme-color-fg-default-shy` clears AA in all four combinations, and light-theme link text takes `primary-highlight` (`primary` is 4.03:1 there). The site now owns a thin **role layer** (`--site-fg-secondary`, `--site-fg-accent`) — every value a `var()`, never a literal — asserted in both themes with the two arms **pinned to distinct values**, so a light arm silently returning the dark palette cannot pass. 🔴 **The theme stamp in `layout.tsx` is load-bearing**: the canonical file gates light on `:root[data-theme='light']`, so vendoring without stamping would have *deleted* the light theme the site already had. 🔴 **Found while writing the probes: a Map-based diff collapses a repeated key silently** — the first rename probe hit `--theme-color-accent`, a real token, so one declaration stopped being checked at all; zero duplicates today, now asserted. ⚠️ **Type sizes deliberately NOT vendored** — the editor's 10→24px scale is chrome at forearm distance, and `--font-size-base: 12.5px` for body copy looks right in a diff and is unreadable on a phone. **Original scoping below.** 🆕 **SCOPED 2026-08-17.** Added out of Richard's read of the running site (*"a bit simple and sad … doesn't fit our sleek new NodeGX style"*). 🔴 **THE DIAGNOSIS: it is not a weak design, it has NO design system** — `globals.css` declares **seven** custom properties and all fourteen pages are built on them, while the editor ships a canonical, documented two-tier system (`noodl-core-ui/src/styles/custom-properties/`, whose own header calls itself *"the only definition"*). Measured 08-17: **7 tokens vs 24 on the landing page**; 2 surface steps vs **5**; **no mono face**, no display face, no light-theme convention. 🔴 **The detail that diagnoses it: the site's accent is `#4b9fff` and the editor's is `#4da3ff`** — four hex digits apart, *close enough that someone was aiming at it and far enough that they were doing it by eye*. Nothing was imported, so nothing stayed in sync, which makes "paste better values in" the one fix guaranteed to fail again. ✅ **D18 RULED 08-17 — AZURE**, against the recommendation (the pitch argued teal); ⚠️ **the landing page keeps teal and becomes the odd one out — chosen, not inherited**, and D18 is now the reason `nodegx-web` moves rather than the community site if anyone reopens it. 🔴 **The load-bearing question is how two repos share one token set**: a copy is what produced `#4b9fff`, and a package is real infrastructure — **chosen: vendored + sync script + a DRIFT TEST**, the same pattern the platform's schema mirror already runs. ⚠️ **And it inherits that spec's known hole** — the schema drift test compares tables and columns **and not enum names**, so the token test must compare *every declared property and its value*, never a count: two files with the same number of different colours pass a count. 🔴 **Non-vacuity floor: change one hex, it must go red, in the same commit.** ✅ **AC5 is a control** — the existing **462 specs must pass unchanged**, because presentation-only means a moving suite is a defect. ⚠️ **NOT in v1**: no new pages/routes/schema, no component library, not the editor, not `nodegx-web`. 🔴 **Tier 1 despite being cosmetic**: nobody has seen the site (deployed nowhere), so this is the cheapest it will ever be — and **deployment is the phase's real gap that no task owns** (measured 08-17: no Dockerfile, no CI workflow, no `ops/`, **zero mentions across all twelve task files**), so whenever it lands, whatever is on the site is what the world sees first |
| [UNI-014](UNI-014-THE-MAIL-ROOM.md) ⭐ | notifications as rows, email as one delivery of a row — the drainer, preferences, unsubscribe | platform | **1** | M | ✅ **BUILT and PUSHED 2026-08-18 (`70d8acc`) — all 5 ACs met, D19's condition is discharged.** `0007` adds `notifications` (canonical, every account kind), `notification_deliveries`, `notification_suppressions`, `notification_policy`; `src/lib/notifications.ts` + `src/lib/mail/transport.ts`; `/unsubscribe/[token]` **driven live over HTTP with no session** (200 GET + one-click POST, 404 tampered, rows verified). 🔴 **ONE DEPARTURE from this file's scope, argued in the migration header**: `outbound_emails` could NOT become the notification channel — `thread_id` is `not null references relay_threads` and `outbound_emails_relay_guard` is an **unconditional per-row trigger that IS UNI-004 AC1's proof**, so widening it turns a total guarantee into a skippable branch. The unification lives one level up: **one transport interface, two queues**. 🔴 **AND THE ASSUMPTION INVERTED**: every pre-existing notification-worthy event is relay-backed and **already mailed**, so those write the row with `via:'relay'` and queue nothing. The events with **no** email path today are exactly the ones reaching **org-minor** accounts — assignments, grading, project requests, badges — and those are what this actually puts mail behind. ✅ Found + fixed en route: `declineBooking`/`cancelBooking` told **nobody**; the **runner's** grading is a second `submission_gradings` insert `gradeManually` readers miss. ⚠️ **Still true and stated**: nothing has ever been posted to a real MX — `log` is the only transport; `NOTIFICATION_LINK_SECRET` has a dev default a deployment **must** replace |
| [UNI-015](UNI-015-THE-BENCH.md) ⭐ | the Bench: ask · read · answer · accept — the four surfaces that make it usable by a stranger | platform | **1** | L | ✅ **BUILT 2026-08-18 (session 27) — all five ACs met.** `uni015-bench.test.ts` **63**, `uni015-bench-http.test.ts` **6 over real HTTP** (`next start`), editor-side `uni-015/postbody-corpus.test.ts` **39**. 🔴 **THE FINDING, and it was a lost QUESTION rather than a lost point: `recordEvent` could not be called inside a transaction.** `tryAward` catches a refusal and reports it, which is right standalone — but a `raise exception` has already ABORTED the caller's Postgres transaction and catching it in TypeScript does not un-abort it. `bench.thread.created` has two listeners (`first-thread` capped at 1, `thread-started` repeatable), so **the second question anybody asked failed outright**. Fixed with a **savepoint PER AWARD** in `contribution.ts` — per challenge, because a wider one would forfeit the siblings and `thread-started` would never pay out again. ✅ **UNI-002's 38 specs pass with the files byte-unchanged in git**, which is exactly the proof AC2 asks for. 🔴 **`alter type … add value` is unusable here** (measured, PG 16.14): naming the new value in a CHECK in the same migration is refused, and the `::text` workaround makes a mistyped literal a clause that silently never matches — **the three enums are recreated**. 🔴 **AC4's `grep -ril discourse` instrument is too strong and was NOT implemented literally** — it forbids a comment saying what was removed; the assertion is **no live CODE in `src/`, comments stripped**, plus the live DB has no `forum_threads` and no `discourse.*` key. ⚠️ **The SQL registry re-point matches ZERO rows today** — the catalogue loads from JSON and nothing is deployed; said out loud so it is not read as evidence of a migration. ✅ **Two shipping palette tokens fail AA in LIGHT** — `danger` 4.26, `success` 3.55 on bg-0, the exact two the queue clock and accepted mark want; roles **split by theme**, all three now in the contrast suite (**5th** `fg-muted` instance). 🔴 **`next start` survives `child.kill()`** — the listener is a grandchild (`next-server`); a survivor holding a pool caused **51 failures in 5 unrelated files** reading `type "badge_family" does not exist`. **KILL BY PORT.** Previously: ⚠️ **Deliberately the least distinctive task in the tranche, and it must NOT ship alone** — plain-text Q&A on our own stack is strictly worse than what we chose not to buy, and shipping it by itself is the one outcome that would prove the buy case right. ✅ **Substrate exists**: `relay_threads`/`relay_messages` carry the threading shape, `content_reports`+`moderation.ts` are already generic, UNI-002's ledger takes the accept. 🔴 **The three-copy trap**: `parsePostBody` → `Block[]` lives in the **editor** checkout and the platform needs the same grammar — third instance of a pattern that bit twice (`LessonEvidence`, `renderMarkdown`). Call: **platform-side canonical + a shared golden corpus whose hash is asserted in both repos**. 🔴 **Carries the Discourse decommission — 11 files inventoried by path in its file**; `forum_threads` is a 6-column stub, **drop it, do not migrate it**; and **`{forum:'absent'}` loses its referent and must be removed, not left unreachable** |
| [UNI-016](UNI-016-ARTIFACT-POSTS.md) ⭐ | a question renders as the thing it is about — node excerpts, captures, facets, visible redaction | platform + editor | **1** | M/L | ✅ **BUILT 2026-08-18 (session 28) — all five ACs met.** 🔴 **THE HANDOVER'S PREMISE WAS FALSE IN BOTH HALVES.** It said the platform could *receive* artifact posts and that the dialog *already held* a bearer token. Measured: `POST /api/v1/bench/threads` took `{section,title,body}` and **`attachToPost` had NO caller in `src/`**; the editor had no token, no token store, and **`CommunityApiClient` had no caller outside its own spec** and was GET-only; and **no issuer exists** — the only `insert into sessions` in the platform repo are in TEST FILES. *Build the caller*, **9th and 10th**, and the 10th is the one where the missing caller was the reason the task existed. ✅ Built: platform `parseAttachments` + `attachments` on both write routes + `attachAll` INSIDE the ask's own transaction (a question whose graph was refused is the wrong post, not a degraded one) + three 0009 constraint names mapped to 400s; editor `nodeartifact.ts`, `communitysession.ts`, `CommunityApiClient.askQuestion/.answer` + the `Write` union, and the composer's two routes. **Platform 14 specs + 2 real-HTTP; editor 42 across 3 files.** 🔴 **THE FINDING — AC2's ASSERTION WAS ON A POPULATION WHERE IT COULD NOT FAIL.** Session 27 asserted it platform-side, but **the withheld name was never in the request**, so no implementation could have emitted it. ✅ Moved to `tests-unit/uni-016/nodeartifact.test.ts`, where the withheld port IS in the input rows and dropping it is work the code does; the platform's copy is KEPT and **relabelled as the weak form**. 🔴 **`apiSql()`'s cached pool does not survive `resetSchema`, and the symptom is a suite that PASSES** — postgres.js resolves enum OIDs at connect time, `drop schema public cascade` invalidates them, and `refusalResponse` maps the resulting `XX000` to an **indistinguishable 400**, so a route-handler refusal spec goes green **without reaching the route**. Fixed with `resetApiSql()` in `freshDb()`; ⚠️ it flipped no existing test (692→706 = exactly the 14 new). 🔴 **An escaped quote made an absence assertion pass trivially** — a text preview value carries literal `"`, and against `JSON.stringify` those become `\"`, so `not.toContain` passes either way. ✅ **Compare VALUES via an enumeration, never serialised text.** Both findings were spoken by the known-firing control in the positive arm. ⚠️ **NOT PROVED: the dialog's button was never clicked** — no DOM and no React in this checkout's jest runner, so the button→client wire is read as SOURCE with negative controls (`base-dialog`'s precedent); a real drive needs a token no issuer can mint and a platform deployed nowhere. 🔴 **No real user can post** — `readCommunitySession()` returns null for everybody, so the browser hand-off is **the only reachable route today**, which is AC5 working. 🔴 **Scope calls:** the graph excerpt **stays in the body, NOT a `graph_fragment`** — that kind is UNI-018's *pullable* unit and a bucketed excerpt is not executable or reconstructible; **no `warningCode`** — this editor has no code, only a free-text sentence with user content in it, and a facet renders as a public filter chip; `parseAttachments` owns **only what the DB cannot say** (4 attachments, 32 KB), never a second copy of 0009's shape rules; `localStorage` is the session store and 🔴 **is the wrong one the moment UNI-001 issues a token that reaches anything of value**. `0009_uni016_attachments.sql`, `attachments.ts`, `Attachment.tsx` (node figure, capture panel, graph chips), **24 specs**. ✅ **AC1–AC4 met**; AC3 is **stronger than asked** — the facets are GENERATED columns, so the control is an attempted write Postgres refuses rather than a grep for a missing setter (asserted on INSERT *and* UPDATE, whose messages differ). ✅ **AC2 carries the known-firing control it names** — a shared port that DOES appear, beside the withheld name that appears nowhere. ⚠️ **AC5 is true only trivially: nothing in the editor was touched.** 🔴 **NOT BUILT: `AskAboutNodeDialog` still hands off to the browser and does not POST — the last piece of this task.** 🔴 **`lesson_step` has no renderer, deliberately** (the scope permits it; UNI-007's format is still moving). 🔴 **Blob storage still owned by nobody** — a `capture` carries dimensions + consent and **no image**, which is exactly what the editor publishes today, so **nothing has to be migrated** when the decision lands. ⚠️ **`post_attachments.payload`/`note` are `minor-refused`, NOT `machine-derived`** — the first draft claimed the weaker class and UNI-005's census caught it by demanding a probe the class could not supply. Previously: **This is the task D19 exists for.** The editor already produces the payloads (UNI-011 AC2/AC3, built and driven) — today they arrive as a string in a prefilled browser composer; here they arrive **structured and stay structured**. 🔴 **Facets are DERIVED from the payload, never typed** — node type, version, OS, warning code. 🔴 **Visible redaction**: `<port>`/`<component>`/`<unknown>` render as chips, not as missing fields — consent legible to the *answerer*, who otherwise wastes a reply asking for what was deliberately withheld. ⚠️ **Four things recorded as DONE change**: UNI-011 AC2/AC3 gain posting, `saveCaptureNextTo` becomes an upload (**blob storage, unowned**), UNI-005 AC4's free-text census floor moves (the guard working), and a `node_excerpt` naming a **custom kit node must not be validated** against `lessonverify`'s built-ins-only catalogue |
| [UNI-017](UNI-017-THE-QUEUE-AND-THE-SIGNAL.md) | the unanswered queue (D16's only software lever) + *same here*, bound to a node type and version | platform | **2** | M | 🔴 **NOT BUILT.** Blocked on UNI-015 and on there being a backlog worth queueing. **The queue is designed AT the acceptance criterion**: of D16's three components (30 threads · 3 weeks with a call · **median first reply < 24h**), the first two are things Richard does and the third is the one a surface can move. 🔴 **`readThreshold()`/`mirror.ts` compute from `forum_threads`, which UNI-015 drops** — re-derive from the real tables and prove the numbers move for the right reason. 🔴 **Read D8's bar through `profile_meets_bar()` — make it the FIFTH caller, not a fourth copy.** ⚠️ *Same here* is a roadmap signal **without telemetry**: opt-in per post, bound to (node type, version), never inferred from a view. 🔴 **A metric that punishes the wanted behaviour is the 4th way an instrument lies** — pure age-sorting surfaces the unanswerable question forever; the fix is a recorded retirement, not hiding it |
| [UNI-018](UNI-018-PULL-A-GRAPH.md) | pull an answer's graph fragment into your editor — the loop that makes it a product | editor (+ platform read) | **2** | M | 🔴 **NOT BUILT.** Blocked on UNI-016. ✅ **Stays inside D14** — the bridge is editor-outbound and a pull is the editor reaching out, same shape as *beaming a lesson is a pull*; **no ruling needs reopening**. 🔴 **THE HAZARD, MEASURED WHILE SCOPING** (`simplejavascript.ts`): the node displayed as **`Function`** is `JavaScriptFunction` and carries **`functionScript`, a string that becomes executable JavaScript**; `Script` carries the same under `code`. **A fragment posted by anyone, pulled into an editor, is arbitrary JS on the puller's machine — and the editor is Electron, so that is not a sandbox.** ⚠️ **The delivery is perfectly disguised**: it arrives as the answer to a question the victim asked, from an account with points and badges. **v1 refuses code-bearing nodes at BOTH the platform and the editor** (a rule in one client is one release from disagreeing), the allow-list is **derived from parameter schemas, never a deny-list**, and every pull is **previewed, never applied** |

| [UNI-019](UNI-019-NOTHING-ON-THE-HOME-PAGE-IS-A-LINK.md) | the home is a menu whose entries do not work — make every card a link, put the Bench in the nav, build the index archetype | platform | **1** | S/M | 🆕 **SCOPED 2026-08-18 (s30)** out of Richard's read of the running site and the review artifact **["Every page is the same page"](https://claude.ai/code/artifact/5cc390dd-a8dc-48c9-b960-b74ed89010e6)**. 🔴 **THIS IS CLOSE ITEM E3 GETTING AN OWNER, not new work on the close.** 🔴 **THE FINDING: the home page has no links at all** — six `<section class="card">`, **zero anchors**, for every viewer (there is no session branch on that path to get wrong, so "maybe I am signed out" is not the explanation). **Two of the six cards name surfaces with no public route**: *Forum* (the real thing is `/bench`) and *Prefab shelf* (org-only, `/orgs/[slug]/shelf/[itemId]`). 🔴 **And `/bench` — the one surface with life in it, built by UNI-015/016 — is unreachable by clicking: it is not in the nav, and the only internal link to it in the whole app is the Bench's own "All" filter pill.** ✅ **Discharges D16's composition obligation, which is currently discharged NOWHERE** and which D16 says out loud the threshold does not discharge — compose from rails that cannot empty (a call happens weekly, the tutorials exist) and decorate with rails that can. ⚠️ **AC1 is a sweep over the RENDERED home with the route list read OFF DISK** (the UNI-011 pattern) plus a known-firing control — a card pointing at `/shelf` must turn it red. ⚠️ **AC2 has two arms**: threads present ⇒ they show; `bench_threads` **empty** ⇒ every rail and its empty state still render. *"Shows threads"* alone passes on a page that collapses to nothing, which is the exact failure D16 exists to prevent |
| [UNI-020](UNI-020-A-TUTORIAL-THAT-SAYS-WHAT-IT-TEACHES.md) | the tutorial detail route + the content model: level, minutes, category, tags, node chips, outcomes | platform | **1** | M | 🆕 **SCOPED 2026-08-18 (s30).** 🔴 **THE FINDING: the bodies are already written and there is nowhere to read them.** `articles.body` is `not null` and populated, and **no `/tutorials/[slug]` route exists** — every card on the index is a dead end pointing at prose that is already in the database. That half needs **no migration**. 🔴 **`kind` is the only metadata the card can show because it is the only metadata that exists** (`articles` = slug, title, summary, body, kind, published_at). 🔴 **THE NODE CHIPS ARE THE LOAD-BEARING IDEA**: *"this tutorial is about For Each and Static Data"* is a filter dimension **no other community can offer**, it is how people actually get stuck, and it composes with the Bench's generated facets and (later) the editor. ⚠️ **A tutorial about a CUSTOM KIT node must be taggable** — UNI-016 recorded the same trap: a name is not a claim that the node ships with the product, and it must not be validated against `lessonverify`'s built-ins-only catalogue. 🔴 **"Open in editor" is scoped OUT, not stubbed** — it needs D5's Learning section and UNI-007's install path, and a button that opens nothing is the defect this whole review is about. ⚠️ **AC2 is *build the caller*, 12th instance in this phase**: a `listArticles({level,node})` whose only caller is its test file is not built |
| [UNI-021](UNI-021-REPLAYS-YOU-CAN-ACTUALLY-WATCH.md) | posters, durations, topics, chapters and an in-place player — the video library that shows no video | platform | **1** | S/M | 🆕 **SCOPED 2026-08-18 (s30).** 🔴 **THE FINDING: the title is a bare `<a href={r.videoUrl}>` with no `target` and no `rel`** (`src/app/replays/page.tsx:23`), so a click replaces the page and hands the visitor to a third party with a referrer — ⚠️ **and the seeded URLs are `example.invalid`, so the click fails today regardless, which is how it went unnoticed: this page has never been clicked successfully by anybody.** 🔴 **It cannot be embedded because the schema cannot say what to embed** — a bare URL, no provider, no id, no duration, no topics. ✅ **Picks up UNI-009's orphaned deferral** (*"chaptered replays … deferred; a plain list is the v1"*) — the web half only. 🔴 **Click-to-load facade: nothing is requested from a video host until somebody presses play**, same posture as UNI-004's double-blind relay, ⚠️ **which is why the poster is GENERATED LOCALLY — fetching the provider's thumbnail would undo that sentence**: a thumbnail URL looks like an image and behaves like a tracker. ✅ **Generating it also keeps this task off blob storage, which is owned by nobody.** ⚠️ **AC2 asserts an ABSENCE (no third-party request on load) and therefore carries a known-firing control** |
| [UNI-022](UNI-022-THE-SYLLABUS-PUBLISHED.md) | University stops being a brochure and becomes the curriculum — the spine, per-lesson state, derived progress | platform | 2 | M | 🆕 **SCOPED 2026-08-18 (s30).** Richard: *"University — no idea wtf this page is supposed to be doing yet."* 🔴 **Correct reading: 41 lines, ZERO queries** — static prose holding a name, which says so in its own last paragraph. **A visitor cannot tell a placeholder from an abandoned product**, so the page's honesty is invisible to the only people it matters to. ✅ **THE DECISION: University is the curriculum, published as a syllabus** — the ordered lessons with what each teaches, what it needs first, and its honest state. ✅ **This is D17's v0, not a fork of it**: D17 ruled the index is part of the platform API with **GitHub Pages as the v0**, and a syllabus rendered from a checked-in curriculum file *is* that v0 — v1 changes an origin, not a design. 🔴 **THE PROPERTY WORTH HAVING: a syllabus is a real page when no lesson is installable**, so this page stops depending on UNI-007 shipping (⚠️ whose spine is blocked on phase 61 via CURRICULUM-DESIGN §11). 🔴 **Progress is DERIVED from `points_ledger`, never stored** — UNI-002 has no column to write a balance into, and AC3 asserts this page and `/u/[handle]` **agree**, because two readers of one ledger is where a second quietly-different query appears. ⚠️ **Per-lesson state, never one banner** — a "coming soon" banner over eight real lessons reads as a dead product; eight rows of which three say "in writing" reads as a curriculum being written. ⚠️ **Does NOT define the lesson format — UNI-007 owns it; read the same file the lesson system reads** |
| [UNI-023](UNI-023-ONE-FACET-BAR-SIX-LISTS.md) | one facet bar — search, multi-select facets with counts, sort — across People, Work, Coaching, Tutorials, Replays and the Bench; + profile rate band and skills | platform | **1** | M | 🆕 **SCOPED 2026-08-18 (s30).** 🔴 **Six lists, one filter dimension each, no search anywhere.** ⚠️ **The page with real numbers is the one with no controls**: `coaching_offers` has stored `price_cents`, `duration_minutes` and `currency` since UNI-004, and `/coaching` sorts by price and offers the visitor no say in anything. 🔴 **The rate filter Richard asked for does not exist for people** — `profiles` has no rate and no skills; that migration is the smaller half. ✅ **Takes the per-list half of UNI-009's orphaned *"search across all surfaces"* deferral**; 🔴 **the global box STAYS DEFERRED** (it needs an index, not a `where`) and ⚠️ **per-list search is explicitly not a down-payment on it**. 🔴 **SEARCH IS A `<form method="get">`, NOT A CLIENT ISLAND** — this site has exactly ONE `"use client"` component and every page is readable with no JavaScript; **a search box must not be the thing that makes a public page require JS**, on a site whose one promise is that reading never asks. 🔴 **TWO ASSERTIONS IT LIVES OR DIES ON: (1) ASSERT CARDINALITY** — a facet count comes from a `group by` and the list from a `where`, two producers of one number, and *"does it render?"* cannot see them drift; **(2) every filter needs a POSITIVE ARM** — a filter returning zero and a broken query returning zero are identical on screen with opposite fixes. ⚠️ **AC1 is a GREP, not a judgement** (more than one pill implementation in `src/` fails it) because it is the criterion most likely to be met on the day and broken in a fortnight — `/bench` is already in that state. ⚠️ **The rate band is OPTIONAL and must not gate listing**: D8's bar is the only listing rule, and a rate filter that silently drops everyone who left it blank is a second, accidental bar |

**Effort is per-v1-slice, not per-dream** — every task file carries an explicit "not in v1" list.

## Suggested order, and why

**First sitting: rulings, not code.** ✅ **Held 2026-08-14** — D1 and D10 ruled, R6 clarified, the
UNI-010 verifier question ruled, D12 struck.

**Second sitting: the rest of the rulings.** ✅ **Held 2026-08-14** — **D2, D3, D4, D5, D6, D7, D8,
D9 and D11 all ruled**, and the three curriculum blockers fact-checked against source and git.
**The queue is empty and nothing is waiting on a decision.** The next sitting is code.

✅ **The D2 rename is DONE — checked against GitHub 2026-08-16, not against this table.** The repo is
`The-Low-Code-Foundation/nodegx-community` and **`has_pages: false`**, so the time-critical window
(GitHub Pages does not follow a repo rename) is still open and still safe. Two handovers carried this
as an outstanding action after it had already been done.

✅ **The other half is DONE TOO — 2026-08-16.** The repo description read *"NodeGX University — the
community platform…"* for two days after D2 ruled the site is **NodeGX Community**: the name moved and
the sentence about the name did not, in the one place every future contributor reads first. It now
names Community as the platform and University as **the learning wing**, which is what D2 said.

🔴 **And the number that decided what this sitting does: `size: 0`, no branches, and `/commits`
returns HTTP 409 *"Git Repository is empty"*** — re-measured 2026-08-16, not carried. Eight of this
phase's twelve tasks live in it, and thirteen consecutive sessions were spent in the editor.

✅ **CLOSED THE SAME DAY — the platform track is open.** `bb3ff8d7` on `main`: D1's stack, the
schema with D3/D4/D6/D10/D11 as database constraints, D15 and D16 as one shared module each, 57
specs with a control run behind every mechanism, and UNI-009's content cut served and curled.
🔴 **The local checkout is `/Users/richardosborne/vscode_projects/nodegx-community` — a SIBLING of
this repo, never nested**, so none of this checkout's gates, peers or traps apply there and nothing
here can sweep it. Postgres runs in Docker on **55432**, deliberately not 5432, because this machine
already has one on the conventional port and both `db:seed` and the suite **drop `public`**.

**UNI-001 + UNI-009 (minimal cut) land together.** A login with nothing behind it is a broken
promise — the account must open onto *something* on day one: replays, tutorials index, the
forum. Discourse-with-SSO is bought, not built.

**Then the Tier-1 focus pair: UNI-002 → UNI-003.** The contribution engine before the profile,
because the profile is mostly a *view* of the engine (badges, points, published things). The
challenge list should launch long (R2) even if most challenges award manually at first —
Richard granting "attended the meetup" points by hand for a month is a fine v1 ledger writer.

**UNI-004 rides once profiles exist.** RFPs and coaching offers hang off profiles; payments
(D7) can trail the board itself — a booking form that ends in an email is an acceptable v0
while the merchant-of-record account is set up.

✅ **DONE 2026-08-16.** The sequencing held: profiles existed, so the board hung off
`profile_meets_bar()` and the two professional flags rather than inventing anything. 🔴 **All four
Tier-1 platform tasks are now built** — UNI-001's schema spine, UNI-002's engine, UNI-003's profile
and UNI-004's two marketplaces. What is NOT built is the login (D2's domain is unregistered), which
means every one of them is reachable **as an API and a public read**, and none of them has a
signed-in write path yet.

**Tier 2 as one arc: UNI-005 → UNI-006 → UNI-007.** Orgs before org-teaching before the full
learning bridge — but note UNI-007's grading runner (editor-side, reusing MCP validate/render)
is independently buildable early, and UNI-006 consumes it. If a school pilot materialises,
D10 goes first.

**UNI-010 as soon as UNI-007's format round-trips (its criterion 4).** The experiment needs no
platform at all, so it can run *before* the platform's lesson generator exists — and its verifier
is shared with UNI-007's runner, so building it early hardens the format for both producers.
Cheapest possible test of the whole lesson concept.

**UNI-008 last, deliberately.** The hosting wow is real but it is the only task with a
standing ops/abuse burden, and the 15/45-day lifecycle (R7) needs the account system mature.
Nothing else depends on it.

## 🔴 Phase 69 shares three surfaces with this phase (added 2026-08-15)

[Phase 69 — The Node You Write Yourself](../phase-69-the-node-you-write-yourself/README.md) promotes
custom nodes (`noodl_modules` kits) to a core concept. **Until 2026-08-15 neither phase referenced the
other anywhere** — grepping each phase for the other's vocabulary returned zero hits both ways —
while three surfaces are shared. Phase 69's README §4 carries the mirror of this section.

| Surface | What it means for phase 67 | Phase 69 task |
|---|---|---|
| 🔴 **`lessonverify.ts` is catalog-backed, built-ins only** | **A lesson cannot teach a custom node today.** `unknown-node-type` is `severity: 'error'` there ([`lessonverify.ts:214`](../../../packages/noodl-editor/src/editor/src/models/lessonverify.ts)) — stricter than `diagnostics.ts`, where it is a warning. That error is **F1**, and `REQUIRED_CLASSES` demands F1 of *every* provenance, so the bundle is refused for `curated` as surely as for `local-ai`. Not a bug in UNI-007 — the catalog genuinely cannot enumerate per-project kits — but it **caps what UNI-006/UNI-007 curriculum can cover** until CN-003 lands | **CN-003** routes the overlay in; **CN-004** owns the consequence |
| ✅ ~~**The MCP tool-surface budget**~~ — **NOT a shared surface after all, measured 2026-08-16** | UNI-010's `lesson` group spent 56 of LEG-001's 58 banked tokens and renegotiated the bar 8,200 → **8,280** ([`toolDisclosure.test.ts`](../../../packages/noodl-mcp/tests/toolDisclosure.test.ts)), whose note reads *"there should not be a third."* This row then predicted that **`derive_starter` would compete with phase 69 for the remaining 57**. 🔴 **It does not, and the premise was wrong rather than optimistic.** A fourth tool in the already-deferred `lesson` group costs **zero** resident tokens — measured at **8,223 with it and 8,223 without** — because the only resident mention of a deferred group is `find_tools`' `(N tools)`, and *"3 tools"* and *"4 tools"* are the same length. **What spends the budget is a *resident* tool (276 tokens for this one) or a whole new *group* (25).** ⚠️ **So the constraint on CN-006/CN-009 is real but differently shaped:** if either adds a deferred tool to an existing group it is free, and if either needs a new group or a resident slot the 57 is the whole of it | **CN-009**, **CN-006** |
| ⚠️ **The render harness is UNI-010's F4 instrument** | `NODEGX_RENDER_CLI` → `measure-from-disk.js` → [`render-report.js:41`](../../../scripts/devtools/render-report.js) → `render-from-disk.js`, which CN-001 rewrites. **Do not let criterion 3's five-lesson run straddle CN-001** or the five F4 scores come from two different instruments. Lesson projects use no kits today, so current readings are sound — the risk is a split dataset | **CN-001** (tier 0, S) |

✅ **And one thing CN-001 does *not* do:** it does not close this phase's open F4 hole. That hole is
`scripts/` being absent from `build.files`, so the harness never ships — an injector fix cannot help a
file that is not there. CN-001 *does* move the pure half into a no-build workspace package, which is
the same shape as the "ship the harness with the sidecar" option, so **it makes Richard's F4 scope
call cheaper without pre-empting it.** Worth putting in front of him alongside the call itself.

## What follows this phase

**Phase 68 — LearnBook** ([../phase-68-learnbook/](../phase-68-learnbook/README.md), scoped
2026-08-14, ruling D13): the coaching *delivery* layer — programs, modules, threaded exchange,
media, assignments the coach validates — for the sessions UNI-004 sells. It consumes UNI-001
(accounts), UNI-003 (profiles), UNI-004 (the offer/booking it delivers on), UNI-005 (the roster)
and UNI-006's assignment state machine, so it starts only once those exist. Three phase-67 tasks
carry a D13 note so they are built reusable rather than forked later: UNI-004 (bookings
referenceable), UNI-005 (one roster), UNI-006 (grader can be a runner *or* a person).

## Two things to check before starting any task here

1. ✅ **The old collab scoping (R6) — DONE 2026-08-14.** Located, and it is *two* documents that
   disagree: [phase 51](../phase-51-collaboration/README.md) is async git-merge collaboration and
   rules real-time **out of scope**; [ECO-001](../phase-20-ecosystem/ECO-001-COLLABORATIVE-EDITING.md)
   is the real-time one and is gated at 4–6 months. **Ruled: UNI-005/006 build on phase 51, COL-004
   (advisory component claiming, 3 days) first.** Full write-up:
   [PRIOR-ART-RECONCILIATION.md](PRIOR-ART-RECONCILIATION.md) F2. The archaeology also turned up
   four further collisions — read the whole document, not just F2.
2. ⚠️ **The bridge direction is a constraint, not a preference.** Editor-outbound only
   (README, surface 3 — the OBS-004 lesson). Any design sketch that has the platform "pushing"
   to an editor must be rewritten as the editor pulling on its own schedule.
3. ✅ **The three curriculum blockers — FACT-CHECKED 2026-08-14** against source and git, not
   against task tables. Full write-up in [RULINGS.md](RULINGS.md).
   - **Two of three now CLEAR.** Phase 60 is **7/7 closed** and its signal wording shipped
     (`portCopy.ts`), so **L2 is unblocked** — the curriculum glossary line has been corrected in
     place. Phase 61 is **8 of 9 built on `cline-dev`** with FUN-001 §2's notation **signed**, so
     **L11 is unblocked** — ⚠️ except that FUN-005's ports rail does not exist, so no step may
     reference it.
   - ✅ **The two-vocabulary rule — the surviving blocker — is now CLOSED by a shipped check.**
     `verifyLessonManifest()` in
     [`models/lessonverify.ts`](../../../packages/noodl-editor/src/editor/src/models/lessonverify.ts)
     rejects a condition written in the prose vocabulary and names the type name to use.
     🔴 **Re-derived from the catalog 2026-08-14 and it is much larger than "nine, two ambiguous":
     103 plain divergences, 4 ambiguous, and a third class of 6 nobody had recorded** — *shadowed*
     names (`Variable`, `Button`, `Text Input`, `Checkbox`, `Radio Button`, `Cloud Function`) where
     the string IS a real type name, so an existence check passes, but it names the deprecated node.
     `Variable` is the curriculum's own L6 node. [RULINGS.md](RULINGS.md) "Blocker 1 — the
     amendment".
   - 🔴 **Two task tables were the stale artifact, not the memory.** Phase 61's table marked four
     merged tasks `open`; the curriculum glossary warned "do not author until phase 60 publishes"
     three days after it published. Both corrected in place. **Grep git and read source before
     believing a status column — including these.**
   - 🔴 **AMENDED AGAIN 2026-08-15 — the shipped check had a hole and the format had a sink.**
     A path written `%Group` (component name omitted) contained *nothing the vocabulary check
     looked at* and passed in silence, while never matching at runtime — the same silent failure by
     a different route. And a lesson body naming `javascript:` compiled into a live anchor inside a
     node-integrated renderer. Both closed (`unmatchable-node-path`, `unsafe-url`). **Both were
     found by building the check's caller, not by reading the check.** [RULINGS.md](RULINGS.md)
     "The second amendment".
   - ⚠️ **Two owed items phase 67 never carried**, both from CURRICULUM-DESIGN §11: **curriculum
     hosting** (§9.3, now partly a D2/D9 question) and the **tutor lesson-context overlay** (§9.1,
     *"required before L2 testing"*) — UNI-007 runs straight into the second and does not mention it.
