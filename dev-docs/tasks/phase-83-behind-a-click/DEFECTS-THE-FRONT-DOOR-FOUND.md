# Defects the front door found — phase 83 register

Per [PHASE-EXECUTION.md](../../guidelines/PHASE-EXECUTION.md):

> **A defect becomes the next session's first job ONLY if it blocks an acceptance criterion.
> Otherwise it is filed here with an owner, and the next session builds the next task.**

🔴 **Every row carries an owner or the literal word `NONE`, and a disposition of `BLOCKS <AC>` or
`BACKLOG`.** An unowned row gets rediscovered at full price. A row filed here must outlive the
session that filed it — say what was measured, with the instrument, not what was concluded.

## Rows carried in from the community issues

These are not this phase's findings; they are the ones already filed against the product that this
phase's tasks own or explicitly do not.

| id | what | owner | disposition |
|---|---|---|---|
| C23 | Code export drops component-input bindings and reports them as translated ([#23](https://github.com/The-Low-Code-Foundation/NodeGX/issues/23)) | HLS-005 | ✅ **CLOSED s6.** The rule was *"some builder happened to look at it"*: `planComponent` records a binding for **every** Component Inputs wire into a rendered node, and the emitter's builders then split into two leaking shapes — `styleAttrs` iterates the *rule table* (`WIRED_STYLE_SINKS`, three ports wide) so a binding outside it is never visited, and `contentAttrs` iterates the bindings but `continue`s on any port with no `attr:` role. A port in neither fell through both **in silence**. 🔴 The corpus already carried it and nobody had counted: **9 props declared and never read** across 4 of 21 components, **3 silent**, and `batch-desk/Notify` sat in *"nothing left over"* while carrying one. `emit/component.ts` now keeps a claim ledger written where each binding is consumed and sweeps the difference. **Instrument:** the emitted `.tsx`, parsed — never the plan that decided the drop |
| C24 | A freshly exported project fails `npm run build` ([#24](https://github.com/The-Low-Code-Foundation/NodeGX/issues/24)) | HLS-004 | ✅ **CLOSED s5.** Not one expression needing a default: **every** JS-node input field was `?:` unconditionally (`emit/component.ts`), so any arithmetic on any wire-fed input was TS18048. Three input classes now type apart and the verbatim body reads them in the runtime's own scope; nothing is defaulted (NDA-017 §2). 🔴 The `tsc` gate this was scoped to build **already existed and was green** — the corpus had no fixture with the shape, its only `Expression` being `(name \|\| '').length > 1`, whose guard dodges the read. `budget-desk` is that fixture. Verified by **running** `npm install && npm run build` (exit 0) and server-rendering the page: `1500` |
| C31 | Preview server binds every interface with no auth ([#31](https://github.com/The-Low-Code-Foundation/NodeGX/issues/31)) | HLS-006 | ✅ **CLOSED s7.** Both ports, and the report understated it. `web-server.js` called `.listen(port)` and `design-tool-import-server.js` called `new WebSocketServer({ port })` — **neither passed an address at all**, and both therefore bound `::`. Nobody chose that; the default chose. Now `resolveAccess()` for 8574 (loopback until somebody shares) and an unconditional `LOOPBACK` for 8575, which has no sharing case. **Instrument:** `server.address()` read off the live socket in `hls006-preview-binding.test.js`, with a reverted arm that starts a bare `listen(0)` beside it and reads `::` — plus `lsof -nP -iTCP -sTCP:LISTEN` against the real `nodegx serve` process, which reported `127.0.0.1:8791` where the old shape reports `*` |
| C31b | Shipped Linux `.desktop` passes `--no-sandbox` (#31's tail) | **NONE** | BACKLOG — out of scope for HLS-006, deliberately |
| C38 | No non-GUI route to open a project ([#38](https://github.com/The-Low-Code-Foundation/NodeGX/issues/38)) | HLS-009 | BACKLOG |
| C28 | An MCP-created project never appears in Recent projects ([#28](https://github.com/The-Low-Code-Foundation/NodeGX/issues/28)) | HLS-009 (may close on the way) | BACKLOG |
| C40 | `render_report` does not write screenshots to disk / renders pages serially ([#40](https://github.com/The-Low-Code-Foundation/NodeGX/issues/40)) | **NONE** | 🟡 **HALF CLOSED by HLS-007, and the check its disposition asked for was run: #40 had NOT landed.** Measured 2026-09-10 on a five-page project — `renderReport` measured **5** pages and wrote **2** images, because the UNI-010 §8.2 sweep added the other pages' *measurements* and never their pictures. `screenshotPages: 'all'` now captures inside the sweep and `measure-from-disk.js --out-dir` writes one file per page per viewport (5 pages × 2 viewports = **10** images, +0s wall clock). ⚠️ **The half that remains is `render_report`'s own**: the MCP tool still returns start-page images only, deliberately — the default is `'start'` so an agent asking about a five-page app is not handed ten pictures — and **pages still render serially**, which the cost measurement supports (boot ~7s, page-viewport ~2s: one browser with N navigations beats N browsers). Still **NONE** |
| C12 | Node 22 LTS support ([#12](https://github.com/The-Low-Code-Foundation/NodeGX/issues/12)) | **NONE** | BACKLOG — becomes a dependency if R2 is "publish publicly" |
| C20 | 🔴 `packages/nodegx-export` and `packages/nodegx-core` **do not exist on `origin/main`** (15 packages vs 22); `origin/main` is `d569d2bd`, editor 0.1.0, **1,837** behind. The public cannot read the code they are filing issues about. [PR #20](https://github.com/The-Low-Code-Foundation/NodeGX/pull/20) is open, non-draft and **mergeable** — the fix is written and waiting on a decision | **NONE** | BACKLOG — **ruling R5** |

## Rows this phase found

Session 2 (2026-09-09, HLS-001). Every row says what was measured, with the instrument.

| id | what | owner | disposition |
|---|---|---|---|
| C41 | ✅ **CLOSED by HLS-002.** An export error's wording depended on which realm it was thrown in: `kitSource.ts` and `parseModules.ts` narrowed with `error instanceof Error ? error.message : String(error)`, which is false across a realm boundary, and the fallback carries the constructor name. **Found again from the other end** — HLS-002 AC1 compares the editor's export against `nodegx export` byte for byte, and the two trees agreed on all eighteen files *except* `EXPORT-REPORT.md`. It blocked an acceptance criterion, so it was fixed here: `src/errorMessage.ts`. **Instrument:** 3 of 840 corpus hashes moved, exactly the three HLS-001 recorded, and jest and `ts-node` now agree on all 840 where they disagreed about 3 | HLS-002 | ✅ CLOSED 2026-09-09 |
| C42 | `parse/parseModules.ts:97` writes an **absolute filesystem path** of the exporting machine into `EXPORT-REPORT.md` — measured as `…/packages/nodegx-export/tests/fixtures/kits/noodl_modules/gone-kit/index.js` in the emitted report. Behind a GUI it is noise; in a CI job it is machine-specific content in a published artefact, and it makes the report non-reproducible | HLS-005 | ✅ **CLOSED s6.** The path came from the absolute path Node writes into an `ENOENT`, not from the exporter's own prose. `withoutMachinePaths` rewrites it to the project-relative one the author would type; the diagnostic survives whole (`ENOENT: no such file or directory, open 'noodl_modules/gone-kit/index.js'`). **Instrument:** a sweep for `/Users|/home|/private|/tmp|/var` over every emitted file and note of all 44 corpus projects — it was the **only** absolute path anywhere in the output, and the gate asserts both the absence *and* that the message still says what happened |
| C43 | `analyze/plan.ts` has a **duplicate `case 'collection-clear':`** at lines 18617 and 18759; the second is unreachable. **Instrument:** esbuild warned the moment the package acquired a bundler (HLS-001). Nothing in the repo had ever looked — `tsc` does not flag it and no suite covers the second arm. Worth knowing *which* arm the export actually takes before deleting the other | HLS-004 | ✅ **CLOSED s5.** Same `switch` (`plan.ts:18562`), so the **first** arm is the live one. They were not identical: the live arm also walks `action.minted?.failThen`, so the dead one was a strict subset and deleting it changes no behaviour. `npm run build` is now warning-free |
| C44 | ✅ **CLOSED by HLS-002.** Nothing in this repo executed the published artefact. `tests/hls002-pack-and-run.test.ts` now builds, packs, installs the tarball into `os.tmpdir()` (asserted to be outside the repository) and runs the linked `nodegx` binary — ~7s, no opt-in flag. 🔴 **It paid in its first minute:** the first built bin carried **two shebangs** (esbuild hoists the source's above the banner; a `#!` on line 2 is a syntax error), the build reported success and every suite stayed green. `build.mjs` now fails unless the output begins with exactly one | HLS-002 | ✅ CLOSED 2026-09-09 |
| C45 | 🔴 **`npm pack` does not apply `publishConfig` field overrides** (measured at npm **10.9.4**: the packed `package.json` is byte-identical to the source, including `main`). `publishConfig` overrides npm *config* values, not package.json fields. Filed because it is a trap two tasks on this board could walk into — the natural way to keep an in-repo `src` entry point and a published `dist` one does not exist | **NONE** | BACKLOG — reference, not a fix |

## Rows session 3 found (2026-09-09, HLS-002)

Both of these are **gates that were never wired to anything**, found by running things this task
needed to run rather than by looking for them. Both are fixed; they are filed because the *class*
outlives them — a green repo is a statement about the jobs that actually execute.

| id | what | owner | disposition |
|---|---|---|---|
| C46 | ✅ **CLOSED by HLS-002.** 🔴 **`npm run typecheck` — a PR gate — was red at HEAD.** HLS-001 moved `@nodegx/export`'s `main`/`types` to `dist/` and put its subpaths behind an `exports` map; the root program resolves at `moduleResolution: node`, which does not read an `exports` map, so `exportBadge.ts`'s `@nodegx/export/ledger` stopped resolving and the package root resolved only for somebody who happened to have run its build. **Instrument:** `npx tsc --noEmit` at the repo root, gated on the exit status rather than the tail of a pipe. Fixed with the two `paths` mappings the editor's own tsconfig has always had — and **the identical incident is recorded three lines above them in that file** (CAN-002, `@noodl-versioning`, "18 TS2307s in files nobody had touched") | HLS-002 | ✅ CLOSED 2026-09-09 |
| C47 | ✅ **CLOSED by HLS-002.** 🔴 **The exporter's 86 suites and 3,140 assertions had never run in CI.** `@nodegx/export` is absent from `test:packages`'s scope list and no workflow names it, so every claim this package makes about itself was graded only by somebody running `npx jest` by hand — including HLS-001's three new gates. Added to `test:packages`. ⚠️ **Measured: the suite was 337s (86 suites / 3,140 tests) before this task and is 406s (88 / 3,168) after it, and 336s of that is `typecheck-emitted.test.ts` alone** — worth splitting if that job ever becomes the critical path, and worth knowing before somebody blames the addition | HLS-002 | ✅ CLOSED 2026-09-09 |
| C48 | ⚠️ **`nodegx export` refuses a non-empty output folder unless `--force`, where the editor asks.** Not a defect — a deliberate difference, filed so it is not "fixed" later by somebody making the doors symmetrical. A person can be asked; a pipeline cannot, and silently overwriting is how a job produces a folder that is half of two different apps and exits 0. Asserted from both sides in `two-doors.test.ts` | HLS-002 | ✅ recorded, by design |

## Rows session 4 found (2026-09-09, HLS-003)

| id | what | owner | disposition |
|---|---|---|---|
| C49 | ✅ **CLOSED by HLS-003.** 🔴 **The editor's own File → Export React was on the wrong side of the load seam too** — not just the CLI. `exportSequence.ts:169` calls `parseProject(projectDir, …)`, which reads the project *files*, while the canvas is built from the graph `applyPatches` produced at load. HLS-002 proved the two doors byte-identical, which is exactly consistent with **both being wrong in the same way**. The task file scoped this as a CLI problem; it never was. **Instrument:** the `cheer` fixture with `runOnChange-condition` removed falls from *translated with nothing left over (9)* to *(7)* — two pages and four cascade nodes refused. Fixed in `parse/parseProject.ts`, which now settles each component as it reads it and reports what it settled | HLS-003 | ✅ CLOSED 2026-09-09 |
| C50 | ✅ **CLOSED by HLS-003.** The `def007` scan **could not see the readers most likely to be wrong**, and said so about itself: it finds `ProjectModel.fromJSON` call sites, and the exporter, the MCP server and template generation all reach a project without constructing one. Those three were carried as prose in `NON_FROMJSON_READERS`, where nothing could fail on them — and the exporter sat on the wrong side for as long as its row was a sentence. `GRAPH_READER_SITES` is now a **second, enforced scan** over every shipped file that opens a component graph (17 files, 6 packages), with a four-way disposition. **Instrument:** an unregistered reader added to `noodl-mcp/src` reddens it; removed after measuring | HLS-003 | ✅ CLOSED 2026-09-09 |
| C51 | 🔴 **The task file's account of template generation was stale, and reading it instead of measuring would have wasted the session.** `HLS-003 §2` says `tpl001Template.ts` "has not had the same pass and nobody has measured whether it disagrees". It had: `def038SettledTemplates.test.ts` measured TPL-001 at **57 stored parameters**, fixed it, and gates a *derived* population rather than a named one. Template generation's side of the seam was already closed before this session started. Filed as an instance of the class, not as work | **NONE** | ✅ recorded — the task file is corrected |
| C52 | ⚠️ **Two `noodl-mcp` browser-drive suites are red at HEAD and nobody owns them.** `sbr009ThemeEditorDrive.test.ts` (2) and `def018-def020-layout-drive.test.ts` (1) — 3 failures, stable across a re-run, in a package that is otherwise 95 suites / 1,381 green. The theme one reads `rootPrimary: ""` where it expects a token value and `heroPainted: rgba(0,0,0,0)` where it expects `rgb(0,0,0)`. **Measured, not inherited:** neither suite imports anything HLS-003 touched (the one `runOnChange` match in either file is a comment). ⚠️ **Not re-run at an earlier commit**, so "pre-existing" is inference from the dependency check, not a bisect | **NONE** | OPEN — outlives HLS-003 |

## Rows session 7 found (2026-09-09, HLS-006)

🔴 **Five of these were found by writing the gate, not by reviewing the change**, and two of them
are in code this task wrote. The one that matters most (C54) was not in #31 at all: the report was
about an open port, and the open port was also a **credential disclosure** for a gate another task
had already built.

| id | what | owner | disposition |
|---|---|---|---|
| C53 | ✅ **CLOSED by HLS-006.** `getAccessStatus()` reported `access.host` and the *requested* port — the option, not the socket. It is the exact shape AC2 is written against ("a config value is a fact about the client"), it was written by this task, and it survived until a spec ran with `NOODLPORT=0`: the status then said port `0`, which nothing can connect to, and the share URL would have named it. Reads `httpServer.address()` now. **Instrument:** the editor spec starting the real server on port 0 | HLS-006 | ✅ CLOSED 2026-09-09 |
| C54 | 🔴 ✅ **CLOSED by HLS-006, and it is the reason this task was worth more than #31 said.** The relay token OBS-004 mints is **injected into the index page `web-server.js` serves** (`injectRelayToken`), and the HTTP half had no gate of any kind. OBS-004's module note reasons about a *browser* on another origin and correctly says the page is not readable cross-origin — but a LAN peer is not a browser applying an origin policy, it is `curl`, and `GET http://<host>:8574/` returned the credential in the body. So the WebSocket gate that stops a page reading the project export was, for anyone on the same network, one unauthenticated HTTP request away from being decorative. **Instrument:** `hls006-preview-binding.test.js` asserts `__nodegxRelayToken` **is** in the body served to a token-bearing caller and is **not** in the 401 — the presence half is what makes the absence half mean anything | HLS-006 | ✅ CLOSED 2026-09-09 |
| C55 | ✅ **CLOSED by HLS-006.** `tokenFromRequest` threw `TypeError: Invalid URL` on a request line the WHATWG parser will not take (`//%%` is enough). In a **shared** server that is an exception in the request handler reachable by anything on the network that can send four bytes — the one place a remote caller must not be able to change what the process does. Found by the spec case "does not throw on a URL that is not one", written before the behaviour was checked. Now caught, and a request carrying no readable token carries no token | HLS-006 | ✅ CLOSED 2026-09-09 |
| C56 | 🔴 ✅ **CLOSED by HLS-006.** `server.on('listening')` fires **again on every rebind**, and everything in that handler is once-per-launch work — including `startWebSocketServer(server)`. Three shares therefore attached **four** relays to one HTTP server: measured, not reasoned, by counting `upgrade` listeners with the guard removed. Every peer would register four times and each relay message would reach a viewer four times with nothing to tell the copies apart. Found by *reading the handler after writing the rebind*; no spec existed that could see it, because the spec that existed did not open a WebSocket. There is one now, with the reverted arm that reads 4 | HLS-006 | ✅ CLOSED 2026-09-09 |
| C57 | ✅ **CLOSED by HLS-006.** The same `server.on('error')` that shows *"A problem was encountered while starting Noodl's webserver"* and then calls `app.quit()` was still armed during a rebind. A port the OS declines for a **share** — an optional action — would have closed the author's editor. Guarded, and a failed rebind now re-listens on the previous address rather than leaving the preview down | HLS-006 | ✅ CLOSED 2026-09-09 |
| C58 | ✅ **CLOSED by HLS-006.** `noodl-preview`'s `--host` was an unauthenticated LAN listener. #31 never named it because its *default* was already `127.0.0.1`, which is the whole argument for the enrolment gate: the defect was one flag away and invisible to a report about defaults. It now binds and gates through the same `resolveAccess`/`authoriseRequest` pair as the other two, and `--token` was added so a caller can know the credential in advance | HLS-006 | ✅ CLOSED 2026-09-09 |
| C59 | ⚠️ **`design-tool-import-server.js` writes a file at a path taken from the wire.** `fs.writeFileSync(local.projectDirectory + '/' + name, message)` where `name` arrives as an `exportImageName` message — no traversal check, so `../../../.zshrc` writes there. HLS-006 **reduced** it from LAN-reachable to loopback-only, which moves it into the class `relay-token.js` explicitly declares out of scope ("nothing local can be" protected from a process already running as the user). Filed rather than fixed because it now blocks no acceptance criterion and fixing it is a change to a plugin protocol nobody in this session has driven. 🔴 **Do not read "loopback" as "closed"** — it is two lines to fix and the next person in that file should | **NONE** | OPEN — outlives HLS-006 |
| C60 | ⚠️ **The editor announces itself on the LAN whether or not the preview is shared.** `main.js`'s `broadcastNew()` sends the hostname, the HTTP port and **the project's name** to the multicast group `225.0.0.100:8575` every time a project opens. It is a discovery beacon for a port that is now loopback-only, so it advertises something unreachable — and it leaks the name of what the author is working on to the whole network, which sharing being *off* ought to mean it does not. Measured by reading `main.js:1340-1358`; **not** driven, so whether anything still listens for it is unmeasured | **NONE** | OPEN — a share that is off should be silent |
| C61 | ⚠️ **`grep` silently skipped a 2,411-line TypeScript file while this task was enumerating listeners.** `grep -n "listen(" packages/nodegx-backend/src/server/HttpServer.ts` returned **nothing**; `grep -an` on the same file returned the `server.listen(this.options.port, this.options.host, …)` on line 1304. Filed because the enumeration this session built its registry from would have been one row short and looked complete. The standing note says `-a` always; this is the instance that would have mattered | **NONE** | ✅ recorded — a known trap, met in the wild |
| C62 | 🔴 **One malformed cloud component silently stopped EVERY cloud function in the project from deploying, on every autosave.** `exportComponent` can throw; `CloudFunctionDeployer.pushToBackend` called `exportCloudFunctionsWithKits` **outside** its `try`, and the save-triggered push swallows the rejection wholesale (`start()`'s `.catch(() => undefined)`). So the failure had no reporter at any level: no toast, no card state, no log — the functions simply stopped updating. The explicit-action path fared no better, reporting `Could not export N cloud function(s)` — **a count, which names nothing an author or an agent can act on** | HLS-013 | ✅ CLOSED — `buildCloudBundleParts` isolates per component and NAMES the ones that failed beside the ones that shipped; graded in `tests-unit/hls-013` and `tests/cloud/hls013-editor-door.test.ts` |
| C63 | 🔴 **Loading a project in order to DEPLOY it schedules a write over the author's own files.** A model change queues an autosave whose `doWriteProjectToDisk` calls `project.toDirectory(project._retainedProjectDirectory)` — and preparing a project for cloud export mutates it (the dynamic-port adapters add ports). Found by driving, not by reading: the first headless probe exited 1 on `ReferenceError: localStorage is not defined` from that scheduled write, which is the **harmless half** of the same bug — the probe had no project directory. A project that has one gets rewritten | HLS-013 | ✅ CLOSED — `prepareProjectForCloudExport` sets `_isReadOnly`; asserted by hashing the whole tree before and after a real deploy |
| C64 | 🔴 **The headless deploy was on the wrong side of the DEF-007 project-load seam, and no human review caught it.** `ProjectModel.fromJSON` does not apply patches — the editor calls `applyPatches` on the line before it. The new `bundleEntry.js` did not, so the same project would have deployed a **different graph** depending on which door deployed it: exactly SB-017's shape, in a new place. ⚠️ **Measured on the two drive projects: `writes: 0` with `familyNodes: 97` and `105`** — the instrument is live and those projects already pin their values, so the AC1 drive would have passed either way. **The register gate caught it, the drive could not** | HLS-013 | ✅ CLOSED — `applyPatches(legacy)` before `fromJSON`, registered `applies` in `projectLoadSeam.ts` |
| C65 | ⚠️ **The MCP server's resident tool surface is 7 tokens under its 8,280 budget** (`[surface] 8273 tokens / 20 resident tools`), read off `toolDisclosure.test.ts` while adding `deploy_cloud_functions`. That tool is disclosure-gated so it costs the resident surface nothing — **but the next resident tool, or any wording change to the instructions, has almost no room**. Not caused by this task; recorded because the margin is now small enough that whoever meets it will meet it as a failing gate with no context | **NONE** | OPEN — outlives HLS-013 |
| C66 | ⚠️ **`WorkflowRunner.loadWorkflow` is all-or-nothing per BUNDLE and reports `{success, error}` only** — there is no per-function result anywhere on the backend side. So a *push* failure cannot be attributed to one function, and HLS-013's AC2 verdict is honest only about **build-time** failures (which it names) versus push failures (which it correctly reports against every function in the bundle). A backend that could load a bundle partially, and say which component it rejected, would make the verdict finer | **NONE** | OPEN — a limit, not a defect |
| C67 | 🔴 **A headless export with an unpopulated `NodeLibrary` ships a blank app and reports success.** `exportComponent` pushes a component root only `if (n.type.allowAsChild)`, so with nothing resolving **every `roots` array exports empty** — and `ComponentInstanceNode.render()` returns `null` when `roots` is empty (`noodl-runtime/src/nodes/componentinstance.ts:322`). `deployToFolder` resolves, writes all eight files and logs `copied 0 project file(s)` **identically to the good run**. 🔴 **The natural instrument is blind to it:** HLS-013 §4's trap teaches you to count connections, and the control pair reads **93/93 in both arms** — 21 components, 375 nodes, byte-comparable everywhere except `roots` (**21 with a root vs 0**). **Instrument:** two full deploys of `templates/landing-pages`, with and without `bootstrapNodeLibrary()`, read by `hls010-spike/measure-deploy.js`. ⚠️ Contained today only because `bootstrapNodeLibrary()` throws on an empty library — that guard lives in `noodl-preview`, **not in the exporter**, so any new caller re-opens it | HLS-015 | ✅ **CLOSED 2026-09-10.** `nodegx deploy` refuses it two ways — `bootstrapNodeLibrary()` throws on the cause before a byte is written, and `readDeployedRoots` reads the written folder for the consequence — and exits **9**. 🔴 **And it was finally put in a browser:** `hls015-drive.mjs` empties every `roots` array in a good deploy's own artefact and reads **0 characters** against the good arm's **1,881**. The claim that empty roots means a blank page had, until this session, only ever been derived from source |
| C68 | ⚠️ **The deploy runtime is addressed relative to `process.cwd()`.** `getExternalFolderPath()` is `filesystem.join(platform.getAppPath(), 'src/external')`, and `platform-node`'s `getAppPath()` returns the first ancestor of the **working directory** holding a `package.json`. So a headless `deployToFolder` finds `index.html`/`noodl.deploy.js` only when it happens to be started from `packages/noodl-editor`. **Instrument:** the same binary, same project — `ENOENT … /packages/noodl-preview/src/external/deploy/index.json` from one cwd, a complete 8-file deploy from another. `noodl-preview` already avoids this with an explicit `DEPLOY_DIR = path.resolve(__dirname, …)`; the editor is unaffected because Electron's `app.getAppPath()` is real | HLS-015 | ✅ **CLOSED 2026-09-10** for the deploy: `setExternalFolderPath()` states the folder from the bundle's own `__dirname`, and the deploy run from `/tmp` produces `index-842da8235ea3dad0.js` — **the byte-identical hash the spike recorded from `packages/noodl-editor`**. ⚠️ Its wording was wrong in two ways; see **C79** |
| C69 | ⚠️ **`deployToFolder` never sets `_isReadOnly`** — C63's guard, absent on the folder-deploy path. Measured as **harmless on this run**: the project tree hashed identical before and after (`9a0f6995394b5837aba7528691cd2351`, matching the pristine template), because this path mutates no model and so schedules no autosave — unlike the cloud path, whose dynamic-port adapters do. 🔴 Filed because that is a property of *what this path happens not to do today*, not a guarantee: the thing that made C63 dangerous is one model mutation away, and a longer-lived process (a `serve` that also deploys) is where it would land | HLS-015 | 🟡 **Guarded 2026-09-10** — `project._isReadOnly = true` before `deployToFolder`, and the project tree hashes identical before and after (`82ec23afa75fcc68caa8017d641903eb`). ⚠️ It hashed identical **without** the guard too, which is why the row stays open: the guard is now there, and the mutation that would need it has still not appeared |

## Rows session 10 found (2026-09-09, HLS-009)

🔴 **Both were found by re-measuring HLS-009's §2 rather than by trusting it — the third session
running where a task file's §2 was wrong.** §2 said "no registered `nodegx://` handler". The
handler is registered. What is missing is everything behind it, which is a different defect with a
different fix, and it would have been carried forward as a settled fact.

| id | what | owner | disposition |
|---|---|---|---|
| C70 | ⚠️ **The relay's token is discoverable and its address is not.** The editor writes `<userData>/relay-token` at mode 0600 precisely so a local agent can find it — and then leaves the agent to guess *where to present it*. The port is `process.env.NOODLPORT \|\| 8574` (`web-server.js:92`) and is written nowhere; `nodegx-observe` hardcodes 8574 with a `--port` escape, and HLS-009's `open_in_editor` does the same with `NOODLPORT`. The user-data directory is guessed too, from a list of three product names — so a **dev-launched editor**, which `scripts/start.ts` gives its own `--user-data-dir`, writes its token somewhere neither sidecar looks. 🔴 **The two failures compose into a lie:** a caller that misses the dev editor's token finds the *stale* one at the default path, presents it, and is told "the relay rejected this token" — a true sentence that sends the reader to restart an editor when the real fault is that they are talking to a different install. **Measured 2026-09-09:** a peer session's editor held `127.0.0.1:8574` with `--user-data-dir=<scratchpad>/userdata`, while `~/Library/Application Support/NodeGX/relay-token` held a token minted 30 minutes earlier. The fix is one file: the editor should write the port (and ideally the user-data path) beside the token, at `listening`, where it already knows both — see C53, which is the same "report the socket, not the option" mistake one layer down. ⚠️ **The composed failure is argued, not driven.** HLS-009's drive later that evening ran against an editor on the **default** user-data directory and port 8574, so it exercised neither clause; what is measured is that a peer's editor did use a scratchpad directory while a stale token sat at the default path. 🔎 It did produce the same *shape* one layer out, though: the drive script's own `--user-data-dir=(\S+)` truncated `…/Library/Application Support/NodeGX` at the space and reported `ENOENT` on a path that has never existed — **"no token" as the sentence for "bad parse"** | **NONE** | OPEN — outlives HLS-009 |
| C71 | 🔴 **The app registers the `nodegx://` scheme and then drops every `nodegx://` URI.** `main.js:241` calls `app.setAsDefaultProtocolClient('nodegx')` and `noodl-editor/package.json:54` declares the scheme for the packaged build, so the OS hands `nodegx://…` links to NodeGX. Both ends then discard them: the `second-instance` handler (`main.js:265`, the Windows/Linux path) forwards a URI only when `args[i].indexOf('noodl:') === 0`, and macOS's `open-url` (`main.js:1180`) forwards to the renderer's `open-noodl-uri` listener, whose one consumer is the design-tool import path (`noodl:import/…`). So a `nodegx://` link **launches or focuses the editor and does nothing else, silently, on every platform**. ⚠️ This corrects [HLS-009 §2](HLS-009-SOMETHING-OTHER-THAN-A-MOUSE.md), which recorded the registration as absent; the renderer comment §2 quotes ("deep linking has never actually worked") is about `Launcher.tsx`'s *tab* parsing, which is a third, unrelated thing. 🔴 **Not a blocker for HLS-009 and deliberately not fixed by it:** an OS-registered scheme is reachable by any web page the user visits, so `nodegx://open?path=…` would be an unauthenticated door onto the same capability the relay gates with a token — and this repo already refuses that exact string elsewhere (`aiMarkdownLinkPolicy.test.ts:58`). **Whoever finishes it owes a credential, not just a handler** | **NONE** | OPEN — registered, inert, and a capability question if completed |

## Rows session 11 found (2026-09-10, HLS-008)

🔴 **Both were found by driving the BUILT artefact rather than the checkout** — C72 is invisible to
every in-repo gate by arithmetic, and C73 was found by doing what AC1's person sentence says and
looking at what came out.

| id | what | owner | disposition |
|---|---|---|---|
| C72 | 🔴 **`export_react` would have shipped dead in the packaged app, and every in-repo gate would have stayed green.** `@nodegx/export`'s `loadCatalog()` reads `node-catalog.json` from disk: `__dirname/node-catalog.json` (the packaged copy) then `__dirname/../../noodl-types/src/node-catalog.json` (the in-repo source). Bundled into `packages/noodl-mcp/dist/noodl-mcp.cjs`, `__dirname` is that `dist/` — and the in-repo fallback resolves **by coincidence**, because `packages/noodl-mcp/dist` sits at exactly the same depth as `packages/nodegx-export/src`. `noodl-editor`'s `extraResources` then copies **one file**, `dist/noodl-mcp.cjs` → `Resources/noodl-mcp/noodl-mcp.cjs`, so in the shipped app both candidates miss. **Measured, both arms, on the real bundle in a directory with no `packages/` above it:** without the catalog beside it, `export_react` returns `not-found` / *"The node catalog could not be found"* naming two absolute paths that mean nothing to the reader; with it, the identical call returns the pre-flight. 🔴 **The class is the row, not the instance** — this is the first thing bundled into `noodl-mcp.cjs` that reads a file at runtime, and the next one will be invisible the same way | HLS-008 | ✅ CLOSED — `build.mjs` copies the catalog into `dist/`, `extraResources` ships it beside the bundle, and `hls008ExportReact.test.ts` asserts the two agree with the filename **derived from `catalogPath()`** rather than typed |
| C73 | ⚠️ **"An empty placeholder page" is tested structurally, so a page somebody actually wrote is treated as one.** [AAQ-001](../phase-40-ai-authoring-quality/AAQ-001-A-CREATED-PAGE-IS-REACHABLE.md) deliberately moves `startPage` in three cases, one of which is *"the start page is the placeholder the caller identified — an **empty** page component"*. 🔴 **The rule is intended; the test for it is not the rule.** `isPlaceholderPageGraph` (`AiAssistant/authoring/pageRegistration.ts:323`) returns true for any page whose root has exactly one childless `Text` child — **it never reads the text**. So an agent that authors the scaffolded Home into a real one-line page (a landing page that says one thing is not unusual) and then adds a second page has the app's entry point moved off it. **Measured in HLS-008's AC1 drive:** `update_component` set Home's Text to `Last boil: 08:12 this morning`; the next `create_component` for `Pages/History` wrote `startPage: "/Pages/History"`, and the exported `App.tsx` routed `/` to `HistoryPage`. Isolated repro: `{"startPage":"/Pages/Home","routes":["/Pages/Home"]}` → `{"startPage":"/Pages/History","routes":["/Pages/Home","/Pages/History"]}`. ⚠️ **Reported, not silent** — the response says *"The app opens on History."* — which is why this is a row and not a blocker: an agent that relays its tool results tells the user. It is filed because an agent that does not relay them, and a person reading only the app, get no warning. The export is faithful throughout; nothing here is the exporter's | **NONE** | OPEN — outlives HLS-008; the fix is a content test beside the shape test, and it belongs to whoever owns AAQ-001's rule |
| C74 | ⚠️ **The render sweep reports a redirected route under the component name it asked for.** `renderReport`'s routed-page sweep navigates to `p.url` and files everything it measures under `p.component`, so a route the running app **redirects away from** is measured, photographed and reported as the component nobody reached. **Measured 2026-09-10 on `Puppy test 3`:** `/#admin` and `/#admin-login` produced **byte-identical** images (md5 `766b9334…`), and `report.pages` reported `/Pages/Admin` as measured with *"Rendered clean, 2 texts"* — the login page's numbers under the admin page's name. 🔴 **A control excluded the obvious cause:** a fresh browser navigated straight to `/#admin`, with nothing measured before it, produced the **same md5**, so this is the app's own auth gate and not a capture racing its navigation. Nothing is wrong with the picture; the **label** is a claim the report cannot support, and it has been true of every measurement since UNI-010 §8.2 — a picture surfaced in one run what five sessions of numbers did not. The fix is to read the URL back after navigating and say so when it moved. ⚠️ Documented rather than silent: `nodegx render --help` and `render.ts`'s module note both say the command photographs URLs, not components | **NONE** | OPEN — filed by HLS-007; affects `render_report` and every F4 grader that reads `report.pages`, not only the CLI |

## Rows session 13 found (2026-09-10, HLS-011 — the end-to-end drive)

| id | what | owner | disposition |
|---|---|---|---|
| C75 | 🔴 **A component input wired to a `width` reaches the screen in the viewer and never reaches the exported app.** Measured end to end 2026-09-10 on an app authored over MCP: three `Gauge` instances with `barWidth` of 120, 200 and 64 wired from `Component Inputs` into a `Group`'s `width`. **The viewer applies all three** as inline styles — `120%` / `200%` / `64%`, computed `907.188px` / `1512px` / `483.828px` — and **the built React app applies none**: `Gauge.tsx` declares `barWidth?: number` in its props and never reads it. So the same graph produces two visibly different apps. ✅ **It is refused out loud, and that is why this is a row and not a blocker:** `EXPORT-REPORT.md` names it by node and port (*"wire into gauge_bar.width from component input \"barWidth\" has no rendered sink on Group — the property renders from its authored parameter only, so the wire is dropped, reported"*), the emitted source carries a `TODO(export)` marker at the element, and `nodegx export --dry-run` **exits 4** on this project before a file is written — a pipeline gating on the pre-flight stops at the gate. 🔴 **The class is wider than the instance.** This is HLS-005's rule working exactly as designed: a dynamic *dimension* has no rendered sink, so **no exported app can have a width, height, or any other dimension that changes at runtime** — which is a large and ordinary thing for an app to want, and the report says it one wire at a time rather than once as a capability. Whoever takes this owes either a sink (a computed style on the emitted element) or a sentence in the README saying dimensions are static in an export | **NONE** | OPEN — outlives HLS-011; found by driving, not by reading the emitter |
| C76 | ⚠️ **The phase's own recipe fails on its first command: `npm ci` cannot run on a fresh export.** The export ships **no lockfile**, so `npm ci` exits **1** with `EUSAGE` — *"can only install with an existing package-lock.json or npm-shrinkwrap.json"*. [README §1](README.md#1-the-person-sentence-for-the-whole-phase) and [§6](README.md#6-the-end-condition), and HLS-011's own route, all say **`npm ci && npm run build`**; a person following the phase document into a GitHub Action gets a red first step. ✅ **The generated `EXPORT-REPORT.md` is right** and says `npm install && npm run build`, so the product's own words are correct and the phase's prose is not. HLS-008's *"What is NOT established"* had already spotted it and called it *"the criterion's word is wrong, not the drive"*; this is the same fact with a pipeline behind it. **Two fixes are possible and they are different decisions:** correct the phase's prose (cheap, and enough), or ship a lockfile with the export (a real compatibility promise about the emitted dependency versions — and one nobody has asked for). ⚠️ Not a blocker: HLS-011's drive measures `npm ci` and grades `npm run build`, deliberately | HLS-012 / s16 | ✅ **CLOSED 2026-09-10 — and this row's own ✅ clause was WRONG.** The prose fix landed (README §1 and §6 now say `npm install`). 🔴 **But "the product's own words are correct" was an absence check on the wrong population.** `EXPORT-REPORT.md`, `readme.ts` and `args.ts` do say `npm install`; **`serve/staticServer.ts` said `npm ci` in four places, two of them user-facing refusals** — including the one shown for *"an exported app that has not been built yet"*, i.e. the fresh export, the exact folder where `npm ci` cannot work. The refusal HLS-006 wrote to be helpful (*"refused with the two commands that are missing"*) handed the person a command that fails. All four corrected. See **C85** for the gate that was pinning it |

## Rows session 14 found (2026-09-10, HLS-015 — `nodegx deploy`)

| id | what | owner | disposition |
|---|---|---|---|
| C77 | 🔴 **A bundled CLI's `@noodl/platform` binding is held in place by one call site, and dropping the call drops the platform.** `noodl-preview/src/deploy.ts` and `src/cli.ts` reach the Node platform through `import { bootstrapNodeLibrary } from './headless'` — a NAMED import, whose only value in the bundle is the module's **side effects** (the dom shim, `@noodl/platform-node`, read at module scope by several editor modules). esbuild drops an import whose bindings are all unused, so removing the call removes the binding. **Measured, not feared:** writing HLS-015 AC2's mutant did exactly that and the 6 MB bundle died at module load with `Cannot read properties of undefined (reading 'join')` from an `EditorSettings` static initialiser — a sentence naming neither the platform nor the import. **Instrument:** delete the `bootstrapNodeLibrary()` call, rebuild, run. Fixed in `deploy.ts` with a bare `import './headless';` above the named one, at **zero bytes** (the rebuilt bundle is byte-identical, `f365838b…`) — it changes only what happens when the call is edited away | HLS-015 (`deploy.ts`) / s16 (`cli.ts`) | ✅ **CLOSED 2026-09-10** — `src/cli.ts` now carries the bare `import './headless';` above the named one, the same zero-byte fix `deploy.ts` got. Both call sites are now held by a side-effect import rather than by the presence of a call nobody may edit away |
| C78 | 🔴 **A spec whose whole subject is a command that does not exist named `deploy`, and the day it existed the row performed a real 15 MB deploy and read exit 0.** `hls002-cli.test.ts`'s `EXIT.usage` row ran `nodegx('deploy', CLEAN, path.join(tmp, 'app'))` — a real project, a real output folder — and asserted a usage error. HLS-015 built the command; the row went red on `Expected: 1, Received: 0`, having deployed successfully. It is also why that file took **520 seconds** (70 after the rename). **The general rule, and it is not about deploy:** a row asserting an absence must name something nothing will ever claim, because the day it is claimed the row stops testing what it says and starts exercising whatever took the name — silently, if the new command happens to succeed | HLS-015 (renamed to `upload`) | ✅ CLOSED |
| C79 | ⚠️ **C68's own wording is wrong in two ways, both measured.** The row says `platform-node`'s `getAppPath()` "returns the first ancestor of the working directory holding a `package.json`" — **there is no ancestor walk**: it tests `process.cwd()` exactly, then `__dirname`, and throws if neither has one. And it throws **a bare string from `PlatformNode`'s constructor at module scope**, so the first real run of a bundled CLI from `/tmp` produced `[@noodl/platform] Cannot find package.json` *before any deploy code ran* — not the `ENOENT …/src/external/deploy/index.json` the row predicts — and a thrown string has no `stack`, so a caller reporting `error.stack` reports `undefined`. **Instrument:** run `packages/noodl-preview/dist/nodegx-deploy.cjs` from a directory with no `package.json`. Both halves fixed for the bundles (`build.mjs` writes `dist/package.json`; the deploy states its runtime folder through `setExternalFolderPath`) | HLS-015 / s16 | ✅ **CLOSED 2026-09-10** — `getAppPath()` now throws an `Error` (so `error.stack` is a stack, not `undefined`) and names **both** paths it tried, `cwd` and `__dirname`, because the row's own history shows the failure is read from a directory nobody expected. No consumer matched on the old string — checked before changing it |

## Rows session 15 found (2026-09-10, HLS-014 — the second deploy)

🔴 **Every one of these was found by re-measuring HLS-014's §2 before writing anything, and none of
them is what §2 predicted.** §2 aimed at `checkTarget` — half of which HLS-015 had already fixed —
while the damage was two layers down, in the reading HLS-015 built its refusal on. The instrument
was one real redeploy of `templates/landing-pages` after a one-word edit, and it cost ten minutes.

| id | what | owner | disposition |
|---|---|---|---|
| C80 | 🔴 **From the second deploy onward, the blank-site refusal was reading the PREVIOUS deploy's export.** `readDeployedRoots` resolved the entry with `readdirSync(outDir).find(/^index-.*\.js$/)`. Every generated name in a deploy is a content hash and nothing in the write path deletes, so a redeploy leaves both — **measured: `index.html` named `index-b529d7656259c342.js` and the reading took `index-842da8235ea3dad0.js`**, deploy 1's. So HLS-015's whole instrument, the one thing standing between C67 and an uploaded blank site, became a statement about an app that is no longer served — in the direction that stays quiet: a second deploy that would ship a blank page, into a folder whose first deploy was good, exits **0**. 🔴 **The class:** a folder that has been written into once and a folder that has been written into twice are different artefacts, and a reading that is correct on the first is not thereby correct on the second. Nothing in HLS-015 was wrong when it was written; it was measured on a population of one | HLS-014 | ✅ **CLOSED 2026-09-10** — `resolveEntry` reads the entry out of `index.html` the way a browser does (through a `--base-url` prefix, and refusing to guess between two exports when there is no page naming one). Graded in `noodl-preview/tests/deploy.test.ts`, **with the control that the stale export really does read as blank**, so the old reading is shown to produce the opposite verdict rather than merely a different filename |
| C81 | ⚠️ **The deployed component count grew by one for every page that had ever been edited.** The bundle half of the reading walked `noodl_bundles/` and added every file in it, so after one redeploy the folder held `b2-378e4cd…json` and `b2-6ac1955…json` — the same `/Pages/Business` before and after — and the deploy reported **22 of 22 components render** for a 21-component project. Harmless in itself; not harmless as the denominator of a refusal that fires on `withRoots === 0` | HLS-014 | ✅ **CLOSED 2026-09-10** — the bundles the **export names** (`componentIndex`) are read, not the files in the directory; files the export does not name are reported as `staleBundles`, and a bundle the export names and the folder does **not** hold counts its components as rendering nothing, which is the direction that refuses |
| C82 | 🔴 **A deploy had no record of what it wrote, and a sweep derived from a directory listing removes nothing — by construction.** The first version of HLS-014's sweep compared the previous deploy's entries against `listEntries(outDir)`. A stale `index-<old>.js` is still in the folder at that moment, so "previous entries missing from the folder" is **always empty**: the drive read *"2 entries removed"* → `0`, and every file survived. `deployToFolder` knew the answer and threw it away — `copyDeployFilesToFolder` returned `void`, `_writeFileToFolder` returned a name nobody kept, and the bundle loop wrote filenames it did not collect. 🔴 **The class: a listing of a folder is not a record of a write.** Where the names are content-addressed, no amount of reading the folder afterwards can reconstruct which entries this run put there — the information exists only at the moment of writing | HLS-014 | ✅ **CLOSED 2026-09-10** — `deployToFolder` returns `written: string[]` (copied project files, runtime files, hashed export, `index.html`, every bundle) and `ProjectCopyReport` gained `copiedPaths`. ⚠️ **No in-CI gate of its own** — the engine cannot be required from ts-jest (HLS-013 measured 201 type errors for the alternative), so `hls014-drive.mjs` is what grades it and the drive is not CI |
| C83 | ⚠️ **A help gate assumed every exit code is one digit.** `hls002-cli.test.ts`'s *"--help names every exit code the binary can return"* matched `^\s{2}<code>\s{2}`. HLS-014's `10` is right-aligned under the `9` with **one** leading space, so the gate went red on correctly formatted help — and the tempting fix (misalign the column) would have made the document worse to satisfy a regex. Relaxed to `^\s+<code>\s{2}`; the two spaces **after** are what stops it matching a `10` inside a sentence, so that half stays. Filed because it is the ninth-to-tenth transition of anything, once, and the next table to cross it will fail the same way | HLS-014 | ✅ CLOSED — a gate whose formatting assumption expired, not a defect in the product |
| C84 | ⚠️ **"Nothing else moved" measured with `String.replace` passes a length check and proves nothing about where the change landed.** HLS-014's drive first asserted `after.text === before.text.replace(phrase, MARKER)`. It failed on a **good** deploy: the edited phrase occurs earlier in the page inside a longer line, so `replace` changed a different occurrence than the deploy did. The two page lengths agreed **to the character** (1881 → 1891, exactly the length of the replacement), so a subtraction — the obvious instrument — would have read green while the marker sat on a line nobody checked. Replaced with a line-by-line diff of the whole page that requires **exactly one** differing line and that it carries the marker. 🔴 **The class:** an aggregate that moves by the right amount is not evidence that the right thing moved | HLS-014 | ✅ CLOSED — the drive says which line changed |
| C85 | 🔴 **A gate pinned the defect in place: the test asserted the refusal contains the command that cannot work.** `hls006-serve.test.ts:119` — *"refuses an unbuilt export and says to build it"* — asserted `rejects.toThrow(/npm ci && npm run build/)`. So the one instrument covering that refusal **required** the wrong command to be there, and any session fixing C76 by correcting the refusal would have gone red and been tempted to revert. 🔴 **The class, and it is C83's twin from the other side:** C83 was a gate whose *formatting* assumption expired; this is a gate whose *content* assumption was wrong on the day it was written, and a green gate is what kept it wrong. **A test that pins user-facing text pins whatever that text says, including its mistakes** — so a gate over prose must assert the property ("names a command that works in this folder") or be re-read whenever the prose is corrected, not merely re-run. Instrument: the assertion and the string it matched sat 60 lines apart in different files, and nothing related them | s16 | ✅ **CLOSED 2026-09-10** — assertion and refusal corrected together; 98 suites / 3,399 green |
| C86 | ⚠️ **The `@nodegx/export` suite's test count is derived from a directory listing, so "N tests" was never a reproducible gate readout — and the recorded N was wrong.** HLS-014's §7 and the handoff both record **98 suites / 3,395 tests**. Re-measured at the same `nodegx-export` commit (`2f4850b8`) with a one-regex change that cannot add a test: **98 suites / 3,400 total (3,399 passed, 1 skipped)**. 🔴 **Four causes were excluded rather than assumed:** the peer's `c7f5ea79` touched nothing under `nodegx-export`; the fixture population is 44 on disk / 44 tracked / 0 gitignored; `node-catalog.json` — which lives **outside** this package, in `noodl-types`, and which `catalogPath()` reaches by `../../` — is unchanged since `2f4850b8`; and the edit was a regex. What remains is that **3,395 was a transcription slip, copied identically into two documents**. ⚠️ **The latent half is worse than the slip:** `cascade.test.ts:46` builds its `test.each` population with `fs.readdirSync(FIXTURE_DIR)`, so the count moves whenever any process leaves a directory in `tests/fixtures/` — and the catalog dependency means an edit in *another package* can move it too. A gate readout that a future session diffs against will report a drift that belongs to neither the code nor that session | **NONE** | OPEN — the slip is corrected in this phase's docs; the reproducibility question is unowned. **Suite count is a weak gate here; the exit status is the gate** |
