# AAQ-011 — Found along the way

The phase's register, in the AIB-009 tradition: anything filed-not-fixed gets a ROW (the pol-013
lesson — a register that loses rows is lying). Worked opportunistically beside the numbered tasks.

| # | Filed | Report | Mechanism | Status |
|---|---|---|---|---|
| F1 | 2026-08-04 | Finding #10 — dragging a component from the component menu in **light mode** renders a dark pill with dark text; the label is unreadable while dragging. | Unverified. Suspect the drag-ghost styling is hardcoded to dark-theme tokens or reads editor chrome tokens outside the themed scope. | open |
| F2 | 2026-08-04 | Finding #12 — the custom CSS property row's label clips to "CSS …" in the props panel, and the popup CSS editor opens **downward**, overflowing the panel bottom so it cannot be used for nodes low in the panel. | Unverified. Two defects: a label truncation (width/ellipsis on the property row) and a popup placement that never flips upward when there is no room below. | open |
| F4 | 2026-08-05 | **Every AI-created project on a machine binds to the same backend.** `findReusableBackend` matches on **name**, and the plan always names it "App backend" — so a project created today came up pointing at one provisioned two days earlier, carrying three unrelated apps' collections. Two apps silently share one datastore. | Verified live: `backend:list` shows a single "App backend" (created 2026-08-03) bound to a project created 2026-08-05, with `Conversation`/`Message`/`Inquiry` in its schema. The name-match exists for *per-plan* idempotence, which it achieves; the lookup is machine-wide and the key is a constant. **CLOSED `36ce5669`** — Richard chose "own backend per project": reuse now requires that this project *owns* the backend (`config.projectIds`, stamped at `backend:create` — a field that had existed and been dead since the backend manager was written) **and** that the name still matches, and `backendNameForProject` gives `PlanFromScopeOptions.backendName` its first production caller so the name is no longer a constant. | **closed**, AAQ-002 |
| F5 | 2026-08-05 | **`backend:createTable` never reconciles an existing table's columns.** It returns `created: false` and adds nothing, so a provisioned collection that already exists keeps whatever schema it had — and a collection with no columns yields no `prop-*` ports, forever. | Proven both ways in one session: a NEW table created with columns comes back with them; `Puppy`, pre-existing in the reused backend, comes back `columns: []`. Together with F4 this is the whole of finding #7's live cause. **CLOSED `36ce5669`** — Richard chose full reconcile including type changes. `planSchemaReconciliation` (pure) is the difference; the provisioner adds missing columns and retypes mismatched ones. A type change needed a **fifth** schema mutation that existed nowhere in the stack (SQLite has no `ALTER COLUMN`): `SchemaManager.changeColumnType`, metadata-only when both types share a storage class and add-copy-drop-rename when they do not, refusing system columns and Relations. ⚠️ Lossy by SQLite's CAST rules and says so — the converted-value count is reported to the user. F4 is what makes it defensible: the collection reconciled is now always *this* project's. | **closed**, AAQ-002 |
| F8 | 2026-08-05 | **The project review still tells the model the built-in backend's collections are "unknown, not absent".** `buildBackendSummary`'s outcome 3 fires for a project bound only to `cloudservices` — which is how every built-in backend is bound — on the stated ground that "the editor has no schema introspection for one". | ⚠️ **A seventh stale premise.** That was true when AIX-010 wrote it and stopped being true in this phase's Layer 1: `SchemaHandler._fetch()` now introspects the built-in backend over `backend:list` → `backend:status` → `backend:getSchema` and caches it in `dbCollections`. `collectBackendSummary` never reads that cache, so the review (and `DatabaseSchemaExtractor`, a third consumer of the same idea) still says the collections are unknowable for exactly the backend phase 40 is about. The reader now exists — `projectSchemaCollections` (`437ec919`) — so this is wiring, not discovery. Not fixed here because it is the *review* path, not authoring, and three specs assert the current four-outcome shape. | open |
| F6 | 2026-08-05 | **Authoring a 55-node component took 6m51s of *editor* main-thread time against a zero-latency provider**; a 9-node component in the same run took 0s. The cost is per partial payload published, not the scan (`PartialPayloadScanner.update` measures 0–1ms for the whole component). Superlinear in node count. | Measured, not inferred. A real provider streams far more partials than the eight this pass used, so a live run is worse, not better — and phase 40's goal is components far larger than 55 nodes. Suspect `AuthoringSession.publish` → the Build panel's re-render, not the scanner. | open |
| F7 | 2026-08-05 | **`PageWithoutPageNode` is filed as a warning when it should block.** A routed component with no `Page` node renders a blank screen; promoting the diagnostic requires correcting **57 fixture sites across 15 AI spec files** that build `/Pages/…` components out of bare Groups. | Deliberate half-measure, reasoned in `validate.ts` beside the `BLOCKING_WARNINGS` set. The prompt contract landed; the refusal did not. | open, AAQ-001 |
| F3 | 2026-08-04 | The scoping wizard's `record_scope` tool result *"Recorded. Now answer the user in prose."* produces a visibly redundant second answer even after AAQ-004 keeps the first one — evaluate whether the second turn should be suppressed from the transcript when it adds nothing. | Design question, not a defect; decide inside AAQ-004's fix review. | open |

## Register rules

- A row is closed by a commit hash and a one-line mechanism, or explicitly downgraded with a reason.
- Anything discovered during AAQ work that doesn't belong to a numbered task lands here first —
  including new silent-discard mechanisms, which additionally get called out in the phase README if
  they change its counts.
- Never close a row on "couldn't reproduce" without recording the drive that failed to reproduce it
  (a driver that swallows a click failure reports the absence of a defect).
