# AAQ-011 — Found along the way

The phase's register, in the AIB-009 tradition: anything filed-not-fixed gets a ROW (the pol-013
lesson — a register that loses rows is lying). Worked opportunistically beside the numbered tasks.

Rows are in **numeric order**. They were not until 2026-08-06 — F3 sat below F15 and F8 sat between
F5 and F6, which is how a register loses a row without deleting one.

| # | Filed | Report | Mechanism | Status |
|---|---|---|---|---|
| F1 | 2026-08-04 | Finding #10 — dragging a component from the component menu in **light mode** renders a dark pill with dark text; the label is unreadable while dragging. | **VERIFIED and CLOSED 2026-08-06.** Not "hardcoded to dark-theme tokens" — a **token-tier mismatch inside one rule**. The drag ghost is a single permanently-mounted singleton owned by `PopupLayer` (`popuplayer.ts:289-299` builds it; `startDragging()` at `:1106-1139` only sets the label and follows the cursor — `setDragImage` appears nowhere in the repo), so `popuplayer.css` had exactly **one** rule for it and that rule disagreed with itself: `background-color: var(--base-color-black-transparent-80)` is a `--base-color-*` **primitive**, defined once at `colors.css:99` as `rgba(0,0,0,0.8)` and never redefined for light, while `color: var(--theme-color-fg-highlight)` is a `--theme-color-*` **semantic** that does flip. One half painted for dark, the other for whichever theme was live. Measured in the running editor: light **1.29:1**, dark fine. Because every drag funnels through the singleton, all three drag surfaces were affected — `ComponentsPanelNew/hooks/useDragDrop.ts:27`, `panels/componentports/ComponentPortsView.tsx:47`, `panels/propertyeditor/components/PropListInput.tsx:90` — and one rule fixed all three. Background is now `--theme-color-bg-4` (the raised-surface step): fg-highlight on bg-4 = **11.04:1 dark / 13.18:1 light**, both clear 4.5:1. ⚠️ **A twin sat one rule above it**: `.popup-layer-toast` paired `black-transparent-70` with the same flipping foreground (**1.92:1 light**) and is still reachable — `showToast` is deprecated but `EditorClipboard.ts:267` still raises it. Fixed identically, as was a third instance, `.popup-layer-activity` (unreachable — `ToastLayer` replaced `PopupLayer.showActivity` — but wrong the same way, and a wrong neighbour is what the next edit copies). Verified live by CDP in both themes: exactly one CSSOM rule matches each selector, so nothing can out-specify it, and the computed values are `rgb(44,53,64)`/`rgb(238,242,246)` dark and `rgb(226,232,239)`/`rgb(24,33,43)` light. | **closed** |
| F2 | 2026-08-04 | Finding #12 — the custom CSS property row's label clips to "CSS …" in the props panel, and the popup CSS editor opens **downward**, overflowing the panel bottom so it cannot be used for nodes low in the panel. | **CLOSED 2026-08-06 — superseded by FH-005 + FH-013, and driven to prove it.** Both halves were fixed by phase-42 work aimed at other reports, which is why nobody closed this row. The row is one node in the props panel, both defects real, neither still present. **Label half → FH-013** (`b8cb27f4`): the truncating rule (`white-space: nowrap` + ellipsis in an absolute 62px box) no longer exists; `.property-label` is now `overflow-wrap: anywhere` with an elastic `.property-label-col` (`propertyeditor.css:51-58`, `:74-91`), and `CodeEditor/Property.tsx:18-28` opts this exact row into it. **Popup half → FH-005** (`2ad567b6`): the custom-CSS popout is not a separate popup — it goes through the same `PopupLayer._positionPopout` (`popuplayer.ts:724`), and `CodeEditorType.ts:212` now wraps the render in `flushSync` with a comment naming FH-005. Note which half of that fix is load-bearing here: the popout asks for `position: 'right'`, so the *flip* is horizontal; what keeps it on screen vertically is the clamp at `popuplayer.ts:836`, which was a **no-op while `contentHeight` was 0**. **Live measurement 2026-08-06:** label reads the full `"CSS Style"`, `clipped: false`; trigger at `y=852` in a 900px viewport, popout rendered `y=337 → bottom=890` — on screen. | **closed** |
| F3 | 2026-08-04 | The scoping wizard's `record_scope` tool result *"Recorded. Now answer the user in prose."* produces a visibly redundant second answer even after AAQ-004 keeps the first one — evaluate whether the second turn should be suppressed from the transcript when it adds nothing. | ✅ **DECIDED and CLOSED 2026-08-06 — `4c53950d`. Richard: remove the second turn entirely.** The report was exact: the string was `ScopingSession.ts:264`, its only reader is the model, and nothing else in the AI stack has a "now answer in prose" tool result — a grep of `models/AiAssistant/**` returns that one line. ⚠️ **The row's own remedy would have been the wrong one.** *"Suppress it from the transcript"* hides a round that has already been paid for and already streamed into the user's bubble, and leaves the model's own context claiming it recapped; the round had to stop being **issued**, not stop being shown. ⚠️ **And "just don't loop after `record_scope`" is the trap the task warned about, because that round is sometimes the only prose there is.** The loop continued after *any* tool call (`if (response.toolCalls.length === 0) break;`), and a turn whose entire output is the call — which is the shape **three of this file's own pre-existing specs script**, and the shape the opening turn most often takes — gets all of its prose from the round that follows. Removing it unconditionally answers those turns with silence: `ProjectsPage.tsx:560` gates the bubble on `if (turn.reply)`, so an empty reply renders the user's message with nothing under it and no error either. **The condition is therefore two-part**: break only when every call in the response was `record_scope` **and** something has already been said this turn. The second clause is the silence guard; the first keeps the *shared* mechanism intact — an unknown tool in the same response carries `unknownToolMessage`, an instruction the model has not had a chance to act on, so that round is still owed however much prose came with it. Only `record_scope`'s own result changed. **The tool result is now conditional** (`SCOPE_RECORDED` / `SCOPE_RECORDED_ANSWER_NEEDED`) rather than deleted, and that matters beyond its round: the message stays in `this.messages` for every later turn, so a nudge written when it was not owed is a standing instruction to repeat yourself. A third string, `SCOPE_RECORDED_REPLY`, is the floor for a turn that recorded and never spoke even in the round that asked it to — reached only on the successful exit, never on a cancel or an error, where inventing prose would claim the turn worked. The system prompt's RECORDING section now says to speak in the same message as the call, so the common path is one round rather than two. **Cost falls with the redundancy**: an answered recording turn is now one provider call, not two. ⚠️ **Two existing AAQ-004 specs had to be re-scripted, and that is a real consequence rather than bookkeeping** — both used `record_scope` as their multi-round shape, which no longer produces one. They now use an unknown tool call, which is the shape that still does; the properties (prose accumulated across rounds in order, and prose kept when the round after it fails) are unchanged and still exercised, so `TURN_SEPARATOR` and `withProsePrefix` are not dead. Four new rows in `tests/ai/project-scoping.test.ts` (jasmine, beside its neighbours): one prose answer and one round after an answered recording, a recording-only turn still speaking, the nudge present in exactly that case and absent from the next turn's message list, and a silent recording turn replying anyway. **Red-then-green measured against HEAD's own file** rather than asserted — 4 of 6 assertions fail before the change, 6 of 6 after. | **closed** |
| F4 | 2026-08-05 | **Every AI-created project on a machine binds to the same backend.** `findReusableBackend` matches on **name**, and the plan always names it "App backend" — so a project created today came up pointing at one provisioned two days earlier, carrying three unrelated apps' collections. Two apps silently share one datastore. | Verified live: `backend:list` shows a single "App backend" (created 2026-08-03) bound to a project created 2026-08-05, with `Conversation`/`Message`/`Inquiry` in its schema. The name-match exists for *per-plan* idempotence, which it achieves; the lookup is machine-wide and the key is a constant. **CLOSED `36ce5669`** — Richard chose "own backend per project": reuse now requires that this project *owns* the backend (`config.projectIds`, stamped at `backend:create` — a field that had existed and been dead since the backend manager was written) **and** that the name still matches, and `backendNameForProject` gives `PlanFromScopeOptions.backendName` its first production caller so the name is no longer a constant. | **closed**, AAQ-002 |
| F5 | 2026-08-05 | **`backend:createTable` never reconciles an existing table's columns.** It returns `created: false` and adds nothing, so a provisioned collection that already exists keeps whatever schema it had — and a collection with no columns yields no `prop-*` ports, forever. | Proven both ways in one session: a NEW table created with columns comes back with them; `Puppy`, pre-existing in the reused backend, comes back `columns: []`. Together with F4 this is the whole of finding #7's live cause. **CLOSED `36ce5669`** — Richard chose full reconcile including type changes. `planSchemaReconciliation` (pure) is the difference; the provisioner adds missing columns and retypes mismatched ones. A type change needed a **fifth** schema mutation that existed nowhere in the stack (SQLite has no `ALTER COLUMN`): `SchemaManager.changeColumnType`, metadata-only when both types share a storage class and add-copy-drop-rename when they do not, refusing system columns and Relations. ⚠️ Lossy by SQLite's CAST rules and says so — the converted-value count is reported to the user. F4 is what makes it defensible: the collection reconciled is now always *this* project's. | **closed**, AAQ-002 |
| F6 | 2026-08-05 | **Authoring a 55-node component took 6m51s of *editor* main-thread time against a zero-latency provider**; a 9-node component in the same run took 0s. The cost is per partial payload published, not the scan (`PartialPayloadScanner.update` measures 0–1ms for the whole component). Superlinear in node count. | **CLOSED `4d1520f6` — the wall clock belonged to the fixture's own `setTimeout` pacing in an occluded window, not to the editor.** Measured before anything was touched, which is the only reason the report's own claims did not survive. ⚠️ **Nothing in that pass measured main-thread time.** What the panel prints per operation is `endedAt - startedAt`, `Date.now()` either side of `await session.run()` (`PlanRun.ts:644,700`, rendered at `ProjectAuthoringView.tsx:1329`) — **wall clock, every await included** — and no CPU was sampled anywhere in the drive. ⚠️ **The named suspect is not the cost.** A committed headless driver (`scripts/aaq011-perf/harness.ts`) runs a real `AuthoringSession` → `PlanRun` → the session snapshot `PlanSessionStore.followRun` takes on **every** publish, with the clock taken around each provider callback: **400 nodes streamed as 4000 partial payloads cost 5.7ms in the entire `onToolCallPartial` path**, 43ms end to end; 56 nodes × 8 payloads cost 0.2ms. **Not superlinear, and not linear in fragments either** — an 80-node payload publishes 85 times whether it arrives in 100 fragments or 4000, because `update()` reports `changed` only when an element *closed* (`partial.ts:143`) and the session publishes only then (`AuthoringSession.ts:675`). The Build panel cannot be it either: React 19 on `createRoot` batches, and the panel renders three rows and a feed. ⚠️ **What it was: `wizard-replay.js` awaited `setTimeout(…, 10)` between fragments** — six for the prose, one per partial payload, **fourteen per component** — and the editor's window is occluded every time a script drives it from a terminal. Measured in this Electron build with the editor's own webPreferences (`scripts/aaq011-perf/timer-clamp.js`): 8 × `setTimeout(10)` costs **88ms on screen, 7882ms hidden, and 16992ms after five minutes hidden** (one of those eight waits took 9982ms — Chromium's intensive tier). 8 × `MessagePort` costs 1.4ms / 0.6ms / 0.8ms. `src/main/main.js:311` leaves `backgroundThrottling` at Electron's default; `test.js:120` and `floating-window.js:37` both turn it off, and **`turnDeadline.ts:26-29` had already recorded this same throttling making a scripted run look hung during AIB-002's live QA**. It is also the whole of `--fast`: one payload instead of eight is seven fewer clamped timers, which is the 20-second run against the 7-minute one. **The fix is in the driver, where the cost was** — the pacing is a `MessagePort` task: still a macrotask, so the panel still renders between payloads; not a timer, so nothing clamps it. ⚠️ **"A 9-node component took 0s" is not explained and is probably a misread row.** `Pages/Admin` streams the same fourteen timers as `Pages/Puppies`, so it cannot have been free; the operation that IS free by construction is the **provision**, which `orderPlanOperations` puts first (`plan.ts:214`) and `stageProvisionOperation` completes synchronously without an await. Three specs in `tests/ai/aaq011-publish-cost.test.ts` pin the *shape* rather than a stopwatch, deliberately: the same component measured 31.7ms idle and 2416.8ms under nine concurrent editor-suite runs, so a wall-clock assertion in a shared suite measures the machine. **AAQ-007 is not gated by any of this** — an iteration loop republishing a candidate costs single-digit milliseconds per republish at four times the node count this phase aims at. | **closed** |
| F7 | 2026-08-05 | **`PageWithoutPageNode` is filed as a warning when it should block.** A routed component with no `Page` node renders a blank screen; promoting the diagnostic requires correcting **57 fixture sites across 15 AI spec files** that build `/Pages/…` components out of bare Groups. | **CLOSED `d2b1077b`.** The code is in `AUTHORED_BLOCKING_WARNINGS`, so the editor gate, the MCP write gate and the MCP plan gate all refuse a `/Pages/…` candidate with no `Page` node — one set, three callers, no policy to drift. `validate:project` is untouched: the authored policy was never applied to the corpus, so promoting a code here cannot turn its 6 hits into errors. **The fixture correction was the work, and it is a correction rather than a suppression** — 15 real `Page` roots added, no assertion weakened, no spec deleted. ⚠️ **This row's own count was wrong by ~3.5×.** "57 sites across 15 files" counted *mentions* of `/Pages/`; the sites the gate actually judges are **16 across 9 files** — 41 editor specs in 8 files (`authoring-validate`, `-style`, `-parameter-values`, `-session`, `-apply`, `-staging`, `-plan`, `-doc-session`) plus one MCP spec. ⚠️ **An update is NOT automatically exempt, which is the finding.** The pre-existing-diagnostic exemption forgives only what the BASE already had, and the corpus `/Pages/Article` **has** a `Page` node — so a revision that drops it is charged to the revision, and three `authoring-plan` specs failed exactly there. The `authoring-session` update-mode specs passed only because their base is synthetic and carried the same defect. ⚠️ **One fixture keeps its bare Group on purpose**: `gateParity.test.ts`'s page-shape row is the diagnostic's own test, so its shape is the defect under examination; it now asserts both gates **block** where it used to assert both let it through. Fixtures the authored gate never sees — `authoring-candidate`, `-changeset`, `-review`, `-sandbox`, which test candidate construction, change-set diffing, review rendering and sandbox export — are deliberately left as they are: correcting them is churn on passing specs with no gate to verify it. The authoring prompt now says the validator *refuses* this rather than merely warning. | **closed**, AAQ-001 |
| F8 | 2026-08-05 | **The project review still tells the model the built-in backend's collections are "unknown, not absent".** `buildBackendSummary`'s outcome 3 fires for a project bound only to `cloudservices` — which is how every built-in backend is bound — on the stated ground that "the editor has no schema introspection for one". | ⚠️ **A seventh stale premise.** That was true when AIX-010 wrote it and stopped being true in this phase's Layer 1: `SchemaHandler._fetch()` now introspects the built-in backend over `backend:list` → `backend:status` → `backend:getSchema` and caches it in `dbCollections`. `collectBackendSummary` never reads that cache, so the review (and `DatabaseSchemaExtractor`, a third consumer of the same idea) still says the collections are unknowable for exactly the backend phase 40 is about. The reader now exists — `projectSchemaCollections` (`437ec919`) — so this is wiring, not discovery. **CLOSED `61579634`** — `builtInSchemaCollections`, the built-in *half* of that reader (the whole of it also returns every Backend Services entry's schema, which the summary already walks, so passing it entire would list every BYOB collection twice), is now read by `collectBackendSummary` and passed to `buildBackendSummary`. ⚠️ **No fifth outcome, deliberately.** A cloud endpoint whose collections *are* readable wants exactly what outcome 1 already does, so a fifth branch would have been outcome 1 with a second copy of the partial-read note; outcome 1's *predicate* changed instead, from "a synced Backend Services schema" to "something was read". Outcome 3 **narrowed** rather than being replaced, and keeps its wording: `SchemaHandler._store()` writes `dbCollections = undefined` whenever the fetch failed, so a stopped backend, a backend mid-restart and a foreign Parse server we hold no key for all arrive as an empty list — concluding "no collections" from that is the lie the module exists to prevent. ⚠️ **Two of this row's own claims were wrong.** `DatabaseSchemaExtractor` needed *no* wiring: it does not read a schema, it renders what the one reader returns, so it was fixed by the same change (its header carried the same stale "stub since WF-007" premise, now dated). And **one** spec asserted the four-outcome shape, not three — `project-review-backend.test.ts`; `database-schema.test.ts` and `project-review.test.ts` both hand-build their summaries. That row survives with an explicit empty third argument, beside five new ones, two of them over the reader, because every pure row would pass with the wiring absent. | **closed** |
| F9 | 2026-08-05 | **Every wizard-built app shows a permanent, false warning.** The topbar reads `⚠ 1` and the Problems entry says *"This Router has no Pages configured, so it has nothing to show"* — on a Router whose `pages.routes` lists all three pages, whose `startPage` is set, and whose app renders correctly in the preview. | Verified live, and the mechanism is a missing clear rather than a wrong diagnosis. `router.tsx::resetAsync` raises `router/no-pages` through `_reportReset` → `raiseRuntimeError` whenever `_internal.pages` is `undefined` at the moment a reset runs — which is true on the mount that happens before the `pages` parameter arrives. **`router.tsx` calls `raiseRuntimeError` and never calls `clearWarning`**, so the transient state leaves a permanent warning; the successful reset that follows says nothing. The Record family (`dbmodelcrudbase.clearWarnings`) is the established shape for the fix: clear the codes the node can raise when the operation later succeeds. **CLOSED `339b3009`** — the clear went into `_reportReset`, the single funnel for all five reset outcomes, and runs for *every* one of them: `done`, `unchanged`, and a failure carrying a **different** code all mean the previous diagnosis has stopped being true. ⚠️ **"Clear on success" would have been the wrong fix**, and the `keep` argument is why: the code being raised *this* pass is left alone, so a Router that genuinely has no pages goes on warning — and its warning is never re-sent, since `sendWarning` deduplicates and a clear-then-raise would be a second message the editor has to process. The four codes live in a `RESET_DIAGNOSIS_CODES` list beside the node, not inside the method that raises them: the editor keys a warning by the raised `code`, so a fifth failure added without a fifth entry reinstates this defect for that code. Eight rows in `aaq-011-router-warning-lifecycle.test.ts` drive the product's own bus, subscriber, `sendWarning`/`clearWarning` and `ActiveWarnings` — only the socket is a stand-in — and **four of them fail without the fix**. ⚠️ The phase HANDOVER listed this row under "worth putting to Richard" alongside F10/F13; it was never a product question, and that line is corrected. | **closed** |
| F10 | 2026-08-05 | **Nothing starts a project's backend when the project is opened.** A wizard-built app reopened the next day has a dead backend: the Record nodes fail, the Data Browser cannot count rows, and the only repair is finding Backend Services and pressing Start. | Verified live: after a full editor restart, `backend:status` for the project's own backend reported `running: false`, and the form's Save wrote nothing until it was started by hand. `backend:start` has exactly **two** callers in the editor — `provisionBackend.ts:364` (the apply, once) and `useLocalBackends.ts:183` (the panel's button). Neither is on the project-open path. ⚠️ This is a product decision as much as a defect — auto-starting a child process on project open is a real choice — which is why it is filed rather than patched. It bears directly on the exit criterion *"the backend the scope described is usable … from the graph's data nodes"*. ✅ **DECIDED 2026-08-06 (Richard): yes — start the project's backend on project open, and stop it on close.** ⚠️ **His constraint, verbatim, and it is a hard requirement rather than a nicety:** *"for the love of Mike please make sure that all 'stop' cases are covered, like the editor crashes, the user's computer goes on fire, etc so that there's no chance we leave orphaned backends running that fuck up other projects or fry the user's CPU"*. **So the design owes orphan REAPING, not a close handler.** A crash, a `SIGKILL`, a force-quit and a power cut all run no close handler by definition, and the failure they leave behind is not cosmetic: an orphan holds the project's port, so the next open either collides or silently binds to a process from a previous session — which is F4's class of bug with a process instead of a name. The minimum shape: every spawned backend records its pid, port and owning project somewhere durable at spawn time, and **the next editor launch sweeps that record before starting anything**, killing or adopting what it finds. A close handler is the happy path only. ✅ **CLOSED 2026-08-06 — `240bf0c0`, `2994e48f`, and the toast slice.** ⚠️ **One premise above was wrong and is corrected here: an orphan guard already existed.** `ServiceSupervisor` has passed `--parent-pid` since WF-004 (`ServiceSupervisor.js:169`) and `nodegx-backend`'s `runServe` polls it every 5s, draining itself when the supervisor disappears (`nodegx-backend/src/cli.ts:350-364`). So *"an orphan holds the project's port"* is not the everyday outcome this row describes — the everyday force-quit is already covered. What it does not cover is three cases, and every one is a process that **never exits**: (1) **parent pid reuse** — the guard's test is `process.kill(pid, 0)`, so once the editor's pid is recycled it succeeds forever; (2) **`EPERM` is deliberately read as alive** (`cli.ts:356`), so a recycled pid owned by root is permanently "alive"; (3) **a wedged backend** never runs the timer that is inside it. It also cannot cover a spawner that never passed the flag — which F13 is about to add. **The design, in `BackendProcessRegistry.js`:** reap on startup, *not* a reaper on a timer — a timed reaper has to be a daemon, and the only process guaranteed not to be running is the one that just died; a startup sweep is deterministic, costs nothing while the editor runs, and its one weakness (an orphan lives until something launches) is exactly what the `--parent-pid` guard already covers, so the two are complementary rather than redundant. **Never kill on a pid:** a kill requires the process's own command line to name `--backend-id <id>` **and** look like the service; `/health` on the recorded port corroborates and is what the adopt path uses, but is refused as kill proof because it identifies a *port*, not a pid. The three failing verdicts differ on purpose — *not ours* drops the record and signals nothing (pid reuse), *unverifiable* **keeps** it and signals nothing (so a machine where `ps` works can settle it later), *gone* drops it. ⚠️ **The record is `noodl-mcp`'s, not one of the editor's own — and that was a correction made mid-task.** F13 landed the same afternoon, in parallel, with its own `runtime.json` per backend directory and a heartbeated owner; this file's first version had written `~/.noodl/backend-runtime/processes|owners` with a session-id owner claim. **Two records that cannot see each other is precisely the failure the F13 row names** — each spawner reaps only its own, so an MCP orphan survives every editor launch and vice versa — so the editor was converged onto `<backendsRoot>/<backendId>/runtime.json` in F13's shape, judged by F13's rules, and it now reaps MCP orphans and writes records the MCP reaper judges. There is no shared module and there cannot be (different packages, different builds): this is the `workflow-proposals` arrangement — two processes, one directory, one JSON shape, and a suite on **each** side asserting a file written the way the other writes it is read back whole. ⚠️ **What the convergence cost:** the discarded editor-only design used a session id in a separate owner file and was immune to pid reuse *without a clock*; the shared format uses a 60-second heartbeat staleness threshold instead, so a machine suspended longer than that, waking a moment before another spawner's sweep, could briefly have a live owner judged dead. The window is one beat wide and was accepted deliberately — a record the other spawner cannot read is a **guaranteed** failure, and this is a narrow race. The editor's sweep is one notch **stricter** than the MCP reaper in the one place it can be: `self` means a backend *this session actually started*, not merely a record whose owner pid equals ours, so a stale record that happens to share our pid is judged on its heartbeat rather than skipped forever. **The three questions the decision asked:** *two windows/tabs* — not reachable (single-instance lock at `main.js:176`, one window, in-renderer tabs) but nothing assumes it: start is idempotent at the main process, stop is guarded by an `ourStarts` set, and across processes the owner field separates spawners; *already running* — **adopted, never restarted**, and therefore **not stopped on close either**, because we did not start it; *what the user sees* — a **delayed** spinner (a start that finishes inside 600ms shows nothing, since this fires on every project open), silent success, and a sticky error naming the reason with a button to Backend Services. **Proven, not asserted:** the suite spawns real children, writes the record, runs **no** close handler, and checks the sweep killed them — and equally that a recycled pid and a health-only match both survive it. 31 main-process specs + 16 editor specs. | **closed** |
| F11 | 2026-08-05 | **The Data Browser's first open reports "Failed to load tables"** on a project whose backend is running and bound. Opening Backend Services and pressing the card's Data button fixes it for the rest of the session. | **CLOSED 2026-08-06.** The consequence was exactly as filed and the line reference was exact — `DataBrowser.tsx:113-116` is still the `catch` that set the string. Two corrections to the mechanism as written. **(a) The main process cannot tell these apart either, so this was never only about `undefined`.** `BackendManager.requireRunning` (`BackendManager.js:477-483`) throws the identical `Backend must be running to get schema` for a **missing id, an unknown id and a stopped backend** — verified live, all three, in one `invoke` loop. So four unrelated situations arrived at one `setError('Failed to load tables')`: nothing selected, backend deleted, backend stopped or still starting, and a running backend whose admin request genuinely failed. **(b) "Reached from the sidebar" is not a plain-click path.** The seven backend surfaces are *transient* panels excluded from the rail (`getVisibleItems()` filters them), and the one caller, `LocalBackendCard`'s Data button, only renders when `backend.running` and always passes `backend.id`. The routes that DO mount it propless are (1) the `router.setup` hot-reload handler at `EditorPage.tsx:48-61`, which calls `SidebarModel.reset()` — discarding every `panelProps` — re-registers and switches back to the active id, and (2) `useSetupSettings.ts:84`, whose `panelExists` check does not exclude transient panels, so a restored `editor-sidebar-panel` naming a surface reopens it with nothing. That is also why the fix reads "for the rest of the session": `item.panelProps` persists once written. **Fixed in both halves.** A new pure module `views/panels/databrowser/schemaFailure.ts` classifies the rejection by asking the two questions the message cannot answer — `backend:get` returns `null` for an id not on disk, `backend:status` reports `running` without throwing — and returns `no-selection` / `missing` / `not-running` / `failed`, in that order (testing `running` first would report a *deleted* backend as merely stopped). `loadTables` now returns before the IPC when there is no id; the panel renders a picker naming every backend instead of an error, and the header says *No backend selected* rather than nothing. **Deliberately an offer, not an auto-selection, even when exactly one backend exists** — this surface edits cells and bulk-deletes rows, and choosing the target on the user's behalf is not a default worth having; naming it on a button is one click and no guessing. A stopped backend gets an **amber notice** (phase 23: red is danger only), the wording *"is not running yet"* so it stays true while F10 is starting one, and a `Try again` button. A real failure keeps the red banner and now carries the actual message, with Electron's `Error invoking remote method '…': Error: ` prefix stripped. `loadData`'s `'Failed to load data'` got the same treatment. 11 jest specs in `tests-unit/aaq-011/`; verified live through all four states. | **closed** |
| F12 | 2026-08-05 | **The `noodl-mcp` write gate is component-scoped, so a project-wide rule fires only after the write.** `create_component` reported `errors: 0` and the immediately following `validate_project --strict` reported **3**, all `duplicate-node-id` — the agent had written ids the fixture's own `/Pages/Home` already used, and nothing at write time could see it. | Found while baselining the suite for AAQ-005 slice 1. The rule is right (SUB-012) and the gate cannot express it: `validateCandidate` calls `validateComponent(candidateProject, name)`, which by construction only reports on one component. ⚠️ **The suite had been red under the `test:packages` gate since `7fd3e053` (2026-07-28), 8 days** — the fixture predates the rule and nobody re-ran it. The test is now corrected (distinct ids) and the suite is green at 121, but the **gate scope is not fixed**: an external agent can still write a colliding id and be told the write is clean. Making a project-scoped rule reachable from a component-scoped gate is a design change, which is why it is filed rather than patched here. Note also that `noodl-mcp`'s own `npm run typecheck` is red from the same commit (`duplicateNodeId.ts:86`, an unguarded `Map.get(...)`) — it is not a CI gate, so nothing caught it. ✅ **That tail is CLOSED (`e45bc02a`, F13)** — and there was more of it than one line: the same typecheck carried ~40 further errors because `@types/jasmine` is hoisted at the monorepo root and TypeScript auto-includes every `@types/*`, so jasmine's global `expect` beat `@types/jest`'s in this package's jest specs. ✅ **The `duplicate-node-id` half is CLOSED 2026-08-06 — by making the collision unreachable, NOT by widening the gate.** Richard's call, and the row's original framing (“making a project-scoped rule reachable from a component-scoped gate is a design change”) is what it turns on: the design change is not worth buying for one rule. **The finding reproduced exactly** before anything was touched — `create_component "Pages/Dash"` with ids `page`/`layout`/`title` against this package's own fixture returned `validation.summary.errors: 0`, and the very next `validate_project --strict` returned **3**, all `duplicate-node-id`, because `/Pages/Home` carries those three ids. **The fix is allocation, not refusal** (`src/project/nodeIds.ts`): an id sent to `create_component`, `update_component` (both `set` and `operations`) or `stage_plan_operation` that the project already uses **elsewhere** is reallocated — `title` → `title-2`, keeping the caller's word rather than minting a uuid — and every reference inside that component (`parent`, `children[]`, `visualRoots[]`, `connections[].fromId/toId`) is rewritten with it, before validation, before anything touches disk. Refusal was rejected because a caller cannot know which ids are free without reading every component first, so “rejected, pick other ids” makes the agent redo the project-wide scan we have already done by the time we could refuse. ⚠️ **Silently rewriting a caller-supplied id is only safe because a node id is never referenced from outside its own component** — cross-component references are by component *name* (an instance node's `type`, a router's `pages.routes`, a navigation target) — which is the thing to check first if the substrate ever changes. It is reported anyway (`remappedNodeIds` + `remapNote` on every write response, and in both tool descriptions), because an agent planning a follow-up `update_component` keyed on the id it just sent needs to know it moved. It is the editor's own mechanism, narrowed: `NodeGraphModel.rekeyAllIds()` (`NodeGraphModel.ts:232`) is what every copy/import path already calls. **Two things deliberately NOT done.** Ids the component already had on disk are never touched, whatever they collide with — the gate's rule is “don't make it worse”, not “tidy the project”, and a spec plants a pre-existing collision and asserts the write neither fixes nor worsens it. And a duplicate id *within one payload* stays a hard refusal (`reconcileHierarchy`, `graph.ts:42`): that component's own connections would be genuinely ambiguous and nothing can guess which node a wire meant. ⚠️ **A pre-existing spec was itself writing colliding ids and nobody had noticed** — `roundtrip.test.ts` authored `page`/`layout`/`nav` into `Pages/Settings`, so it was quietly exercising this collision rather than the import path it is named for. Given distinct ids, plus a second spec that drives a *reallocated* graph through the editor's own `reconstructLegacyComponent` — the real risk of this fix is a rewrite that loses a wire, and that is checked against the editor's loader, not against our own files. 9 specs in `tests/nodeIdAllocation.test.ts` drive the MCP tool path end to end; **4 of them fail with the allocator disabled** and the other 5 are the controls that must pass either way. Suite 186 → 196, `npm run typecheck` green. ⚠️ **The gate-scope class is LEFT OPEN, on purpose — this row does not claim it.** `validateCandidate` still calls `validateComponent(candidateProject, name)`; the write gate is still component-scoped; **the next project-wide rule will land in exactly this hole** and will again be reported only by `validate_project` after a clean-looking write. That is Richard's call to make when there is a second rule to justify it. ✅ **The editor's own AI write path had the same hole and it is CLOSED 2026-08-06 — `ef95e9b1` (the drive) + `d15c841f` (the fix).** This sentence was filed as a *question* in the phase-42 register, on the grounds that the editor has four `rekeyAllIds()` callers and a candidate that reached one would mean no bug at all. **It reached none of them**: `duplicateComponent` is *duplicate*, and the other three are `ComponentTemplates`, `RouterAdapter` and the import engine, while the AI path is `reconstructLegacyComponent` → `ComponentModel.fromJSON` → `project.addComponent`. The drive was committed **before a line of the fix existed**, asserting the collisions, so this row does not repeat its own worst habit. ⚠️ **The editor's consequence is worse than the MCP's was**: `ProjectModel.findNodeWithId` (`projectmodel.ts:428`) is **project-wide and FIRST-MATCH**, and `ViewerConnection`, the debug inspector and the Provenance panel all resolve a runtime node id through it — so a collision is not a validator complaint, it is the editor highlighting and labelling the **wrong node**. The gate-scope hole reproduced identically: `validateCandidateComponent` reported `ok`/0 errors while a project-wide check of the same project reported `duplicate-node-id`. Fixed by the same allocation, in `AiAssistant/authoring/nodeIds.ts`, with one deliberate difference: it runs at the **apply seam** (`staging.ts`) rather than the authoring gate, because the session validates against a snapshot taken when authoring started while the apply is the only moment that sees the project as it is — and it is where a plan session restored from disk arrives without re-authoring. The multi-operation case then costs no overlay bookkeeping, since operations apply in sequence. **The `a node id is never referenced from outside its own component` claim was re-verified on the editor side rather than inherited**: `rekeyAllIds()` rewrites node ids, recursed children and connection endpoints and nothing else, so every editor copy path already depends on that closure; `visualRoots` is derived in the live model and stored only in the v2 files; the one project-level pointer at a node id is `project.rootNodeId`, resolved through the same first-match lookup — an argument *for* uniqueness — and untouched here because an update preserves every id the component already had. ⚠️ **And the same spec-plants-the-collision finding recurred**: `authoring-plan-staging.test.ts`'s 3-component plan wrote `upd-root`/`upd-text` into BOTH updated components, exactly as `roundtrip.test.ts` did here. **Still open, on purpose**: the editor's gate is component-scoped too (now asserted by a spec rather than assumed), and this package's `src/project/nodeIds.ts` is a **twin** of the editor's module rather than an import of it — the editor's is Electron-free precisely so it can be imported along the `plan.ts`/`editor-deps.ts` seam, a two-line change here that AAQ-005's one-substrate line should eventually make. Editor suite 2359 → **2382 specs, 0 failures**. | **`duplicate-node-id` closed; gate scope open** |
| F13 | 2026-08-05 | **An external agent cannot build a full-stack app end to end.** `noodl-mcp` can read and administer backends that already run, but it cannot *create* one — provisioning starts and supervises a `nodegx-backend` child process, which is the editor's backend manager over IPC, and `backend/client.ts` here only speaks admin HTTP to backends it finds on disk. So the third of Layer 1's project-level effects does not port with the other two. | Found closing the apply gap (AAQ-005 slice 2). Made **honest rather than silent**: `create_plan` now accepts `kind: 'provision'` — one vocabulary across both clients, since the plan model it shares has carried the kind since AIB-007 — and refuses it with the reason and where to go instead, rather than letting zod reject the enum with "invalid literal". The safety net is real but partial: slice 1 gave this package `checkBackendRequirements`, so a graph with Record nodes and no backend is *diagnosed*; nothing offers to fix it. Deciding whether the MCP server may spawn backend processes (ports, secrets, lifecycle, and who stops them) is a product question of the same class as F4/F5, which is why this is filed. ✅ **DECIDED 2026-08-06 (Richard): yes — `noodl-mcp` may provision backends.** The reason is the finding's own headline: **an external agent must be able to build a full-stack app end to end**, and today the third of Layer 1's project-level effects simply does not port with the other two. That closes the *permission* question and opens the design work it implies, which is the whole of the question as originally filed: **lifecycle, ports and secrets ownership**. Who spawns, who supervises, who stops; how a port is chosen without colliding with the editor's own; and whether an MCP-provisioned backend's secrets are readable by the process that asked for it. ⚠️ **It inherits F10's constraint in full** — Richard's *"no chance we leave orphaned backends running"* is not scoped to the editor, and an MCP server is the **worse** case: it is a sidecar with no window to close, no quit event, and a parent that may be a agent process nobody is watching. The reaping record F10 owes must be the *same* record, or the two spawners will orphan each other's processes. ✅ **It IS the same record — but only after a correction, and the near-miss is worth recording.** F10 and F13 were built in parallel on 2026-08-06 and each wrote its own format: this one's `runtime.json` per backend directory, F10's `~/.noodl/backend-runtime/processes|owners` with a session-id owner claim. Both were correct in isolation and together they were exactly the failure this sentence predicts. **F10 was converged onto this format** (`BackendProcessRegistry.js`, editor main process): same path, same shape, same `verifyIsOurBackend` / `heartbeatIsFresh` rules, same outcome vocabulary, and the editor now heartbeats its records so `ownerIsLive` judges them as it judges yours. Each side has a suite asserting it reads a record written the way the *other* writes it — the `workflow-proposals` arrangement, since there is no shared module to typecheck the agreement. **CLOSED 2026-08-06 — `e45bc02a` (record + reaper) and `460f17e3` (provisioning).** `provision_backend` / `stop_backend` / `list_backend_processes` are registered under `--allow-writes`; the tool creates the `~/.noodl/backends/<id>/` layout `BackendManager` writes, starts the child, seeds the collections and binds `metadata.cloudservices` — the editor's four steps, with `findReusableBackend`'s ownership rule copied verbatim so a second project can never adopt the first's backend. **The design answers, and what was rejected:** (a) **not a plan operation** — `apply_plan` promises all-or-nothing and byte-identical discard, and a spawned process plus a created database are neither; the editor reaches the same conclusion from the other side, putting *only the binding* in its undo group. `create_plan` still refuses `kind: 'provision'`, now pointing at the tool. (b) **not refused when an editor might be running** — there is no liveness signal a sidecar can read (no lockfile; the MCP front door emits a command string, not a marker), so the test would be a guess, and what actually matters is solved directly: reuse, *adoption* of an already-running backend rather than a second process on one data dir, and an endpoint refusal that fires **before** anything is spawned. (c) **Ports** — the editor's line from 8578 plus a real bind probe, because with two spawners the config scan's blind spot is a race; the port recorded is the one `READY` reports. (d) **Secrets** — none are written. The backend mints its own `adminToken` into `<dataDir>/secrets.json` mode 0600 (`SecurityState`), we only read it; the `functions` namespace is untouched; `runtime.json` holds a pid, a port and a path and is 0600 too. ⚠️ **The orphan story is not what the row assumed.** `nodegx-backend` has carried `--parent-pid` since WF-004 (`cli.ts:350`) and *already* kills itself when its supervisor disappears — but the guard is a bare `process.kill(pid, 0)`, so **once the OS recycles the dead owner's pid it reports "alive" forever** and the backend serves on. That hole is what the durable record closes: the owner heartbeats it, and a stale heartbeat is a dead owner even when the pid resolves. The reaper runs at `noodl-mcp` startup and before every provision, and it **never signals a bare pid** — the process's command line must name the backend id, and a pid it cannot read is reported and left running. It is owner-kind-agnostic in both directions, so F10 can adopt the same record additively (the heartbeat is optional and an absent one reads as fresh). 12 reaper specs including a real spawn, a real owner SIGKILL and a real reap; 11 provisioning specs against the built `nodegx-backend`. Suite 161 → 184. ⚠️ **F12's tail is also closed here**: this package's `npm run typecheck` was red and gated by nothing — `duplicateNodeId.ts:86` *and* ~40 more, because `@types/jasmine` is hoisted at the monorepo root and its globals beat `@types/jest` in this package's program. `types: ["jest", "node"]` ends that. | **closed**, AAQ-005 |
| F14 | 2026-08-05 | **A component the editor authors can never have a variant or a visual state.** `variant`, `stateParameters`, `stateTransitions` and `defaultStateTransitions` are all in `CARRIED_NODE_FIELDS`: on an *update* the editor copies them from the base node so an AI revision cannot eat hand-tuned styling. On a **create** there is no base, so they are unreachable — while an external agent driving `noodl-mcp` can set every one of them (`variant` is a declared field; the rest arrive through `.passthrough()`, and `nodes.schema.json` allows additional properties, so they reach disk). | Found converging the two authoring vocabularies (AAQ-005 slice 3), and declared in the table with its reason rather than papered over. The carry mechanism is right and solves the problem it was built for; what it does not do is give the editor's agent any way to *author* the styling surface in the first place — which is AAQ-010's whole subject, and it means the two doors have materially different visual ceilings. ⚠️ Not a schema oversight to patch in passing: adding `variant` to `submit_component` needs the agent to be told which variants exist (the AIB-010 named-reference gap) or it will invent names, and visual states need `State` node guidance the prompt does not carry. | open, AAQ-010 |
| F15 | 2026-08-05 | **The two clients disagreed about what an agent may say, and one of the differences was load-bearing.** `submit_component`'s JSON Schema and `create_component`'s zod schema were hand-written twins: MCP accepted `children` and `variant` the editor did not, the editor accepted `sample_data` MCP did not, and MCP's port schema was `{ name }` with `.passthrough()` — **`plug` was not declared at all**. A declared instance port with no `plug` is *inert*: `NodeGraphNode.getPorts(filter)` selects on `p.plug`, and `componentmodel` derives the component's interface from `getPorts('input')`/`getPorts('output')`, so a `Component Inputs` node authored without one yields a component with **no inputs**, silently. | **CLOSED `873172d0`** — AAQ-005 slice 3. One table (`validation/authoringVocabulary.ts`) rendered into both schema languages, every remaining difference declared with a reason, and two specs: `tests-unit/aaq-005` (byte-for-byte pin on `submit_component`, the undeclared-divergence invariant, and no field absent from `nodes.schema.json`) plus `noodl-mcp/tests/vocabularyParity.test.ts` (both renderings read back — **8 of 23 fail** against the old hand-written schemas). The inert-port half is now `DiagnosticCode.PortWithoutPlug`, an error in the shared precondition set, so both gates refuse it. Calibrated on all three populations before being made an error: the 96-project corpus declares **1483 instance ports, 1483 with a plug** (`input` 512, `output` 879, `input/output` 92 — that last one would have made a naive check flag 92 real ports), and the AI specs and MCP fixtures declare none without one. Zero hits in all three. | **closed**, AAQ-005 |
| F16 | 2026-08-06 | **Undoing a design-token write restores every token's VALUE and leaves the PROJECT byte-different.** `StyleTokensModel._store()` writes `{ version, customTokens: [] }` under the `designTokens` metadata key on every write — including one that ends with nothing custom — and a project that has never had a custom token has **no such key at all**. So write-then-undo adds a metadata key that was not there. ⚠️ **The model's own four undo specs cannot see it**: `tests/models/StyleTokensUndo.test.ts` reads values back out of the model, never out of `project.toJSON()`. | Found driving AAQ-005 criterion 3, whose whole point is that a plan applies as one changeset and undoes as one group — so the first byte-identity assertion over a plan that also wrote a palette failed, and this is why. **Closed at the transaction, not at the model** (`planTokens.ts`, `b5599a91`): `applyAuthoredPlan`'s token writer snapshots the metadata blob before the first token is written and restores it last, because the promise being kept (*one undo restores the project exactly*) is the transaction's. A spec asserts the failure directly with the snapshot omitted, so the repair cannot be deleted as redundant. ⚠️ **The model-side residue is NOT fixed and is deliberately left**: `setToken`, `addCustomToken`, `deleteCustomToken` and `resetAllToDefaults` each still push their own single-action group whose inverse restores values only, so a hand edit undone in the Design Tokens panel leaves the same key behind. It is invisible in the panel and harmless at runtime (`customTokens: []` merges to the defaults), but it makes a project dirty for version control after an edit the user cancelled — which is how it will eventually be noticed. The fix is the same three lines four times, in a file this task was not scoped to. | **transaction closed; model residue open** |

## Register rules

- A row is closed by a commit hash and a one-line mechanism, or explicitly downgraded with a reason.
- **A decision is not a closure.** A row whose product question has been answered reads
  `**decided**, unowned` and stays in the table until something is built. `open` → `decided` →
  `closed`; skipping the middle state is how a decision becomes a thing everyone remembers agreeing
  and nobody remembers scheduling.
- **Rows stay in numeric order.** Not cosmetic: F3 spent two days below F15 and F8 two days between
  F5 and F6, and the phase HANDOVER's one-line summary of this register names 9 of the 15 rows —
  including neither of the two that were out of place.
- Anything discovered during AAQ work that doesn't belong to a numbered task lands here first —
  including new silent-discard mechanisms, which additionally get called out in the phase README if
  they change its counts.
- Never close a row on "couldn't reproduce" without recording the drive that failed to reproduce it
  (a driver that swallows a click failure reports the absence of a defect).
