# Phase 69 — the tasks (CN: custom nodes as a core concept)

**Created:** 2026-08-15 out of [README.md](README.md). Read the README's two principles first —
**P1** a custom node is a node (no *capability* difference; provenance display is wanted) and
**P2** ports are the product, JavaScript is the escape hatch. Both are acceptance criteria in every
task, not sentiment.

> 🔴 **Read [README.md §4 Prior-art reconciliation](README.md) and [RULINGS.md](RULINGS.md) before
> starting any task here.** ✅ **D7 is ruled:** phase 69 owns the catalog spine (CN-003 = "this
> project", exact, feeds validation); **P65 / LBR-008** narrows to "the shelf" — cheap discovery of
> installable library content, layered on top. P65's row has been updated to point here.

> ✅ **THE RULINGS QUEUE IS EMPTY (2026-08-15).** All eight (D1–D8) are ruled in
> [RULINGS.md](RULINGS.md). **No task here is blocked on a decision** — the last column is now a
> record of what each task must *honour*.
>
> ⚠️ **Two rulings carry obligations that are easy to lose.** **D5** was ruled *against* the
> recommendation: the shipped reference kit is deliberately minimal, so **CN-007's docs must carry
> the cashflow kit as the worked example** or P2 has no demonstration anywhere. **D8** means the
> cashflow kit's hardcoded hex must move onto tokens before CN-007 cites it, or the worked example
> teaches the opposite of the ruling.

> 🔴 **CROSS-PHASE, added 2026-08-15: phase 67 shares three surfaces with this phase, and until today
> neither phase mentioned the other anywhere.** [README §4](README.md) carries the reconciliation;
> the three, in the rows below:
>
> | Surface | Who is affected |
> |---|---|
> | `lessonverify.ts` is a **fifth catalog consumer**, and `unknown-node-type` is an **error** there — a lesson teaching a kit node is refused at install under every provenance | **CN-003** (route the overlay in — the seam already exists), **CN-004** (confirm the consequence) |
> | The **MCP token budget** moved: P67 / UNI-010 spent LEG-001's 58 banked tokens and the bar was renegotiated 8,200 → **8,280**, with a written *"there should not be a third"* | **CN-009** (numbers corrected; `$ref` is the sanctioned move), **CN-006** (competing for the same 57) |
> | `render-from-disk.js` is the instrument **UNI-010's F4 grades with**, via `NODEGX_RENDER_CLI` | **CN-001** (don't straddle P67's criterion-3 run; and this task does *not* close P67's F4 packaging hole) |

**Tiers:** 0 = instruments (nothing downstream is trustworthy until these land) · 1 = the spine ·
2 = authoring · 3 = the AI loop · 4 = the full surface · 5 = the dev loop · 6 = distribution & trust.

**Effort:** S ≈ a session · M ≈ 2–3 · L ≈ a week+ · L+ ≈ needs slicing before it starts.

| Task | One line | Surface | Tier | Effort | Depends on / must honour |
|---|---|---|---|---|---|
| **[CN-001](CN-001-THE-EYES-MUST-SEE-KITS.md)** ✅ **CLOSED 2026-08-16** | **The eyes must see kits.** ✅ Landed: `@nodegx/module-inject` (no-build package), `projectmodules.ts` re-exports it byte-identically (golden snapshot 9/9 unchanged), `render-from-disk.js` injects, `--print-html` added, 16 new tests both mutation-proven. 🔴 **The defect was not a blank** — the page rendered *without* the kit node and the report said *"Rendered clean", 0 findings*. 🔴 **This spec's stated control was unusable**: `cashflow-command-centre` is v1, so the harness rejects it; a v2 control (`cn001-kit-drive`) had to be built. 🔴 **P67 ordering: CN-001 went FIRST — score all five UNI-010 lessons after.** ⚠️ Found in passing: `@nodegx/render-measure` was in **no gate** (absent from `test:packages`' scope); now added. *Original spec below.* **The eyes must see kits.** `render-from-disk.js` builds its own `<head>` with two hardcoded module stylesheets and never calls `injectIntoHtml` — so `render_report` renders any project using a custom node as a **blank**. Replace the template with the real injector; add a regression fixture that fails if a module script tag stops appearing | editor / devtools | **0** | **S** | none. **Do this first** — every other task's verification is currently lying. ⚠️ **Cross-phase:** this is the instrument P67 / UNI-010's **F4** grades with — do not land it mid-way through UNI-010's five-lesson criterion-3 run, and note it does **not** close P67's F4 packaging hole |
| **[CN-002](CN-002-NAME-THE-HOLE.md)** ✅ **CLOSED 2026-08-16** | **Name the hole.** ✅ `unknown-type-check-skipped` emitted from the three skip sites (**not** a new rule that restates what others skip — that could drift). Sized first: on `cashflow-command-centre`, **26 parameters and 10 of 28 endpoints** unverified, printed as `0 error, 5 warning, 0 info`; now `0 / 5 / 8` — errors+warnings **unchanged**. 12 tests, 3 of them controls. 🔴 **Finding: `checkParameterValues` has ONE caller and is not in the project validator at all** — `validate:project` checks parameter values for *no* node, kit or built-in. Needs a ruling. 🔴 This spec assumed one pipeline where there are two. *Original spec below.* **Name the hole.** Where validation *skips* a check because a type is unknown, say so as an `info` diagnostic that names the node and the skipped check. Today the skip is silent and reads as a pass | editor | **0** | S | `diagnostics.ts` already has `info` for exactly this purpose |
| **[CN-003](CN-003-THE-PROJECT-CATALOG-OVERLAY.md)** ⭐⭐ ✅ **ALL FIVE ITEMS BUILT 2026-08-16** | **The project catalog overlay — the keystone.** ✅ Slices 1–4 landed: the shared mapping (`@nodegx/kit-catalog`), the headless extractor + MCP caller (**0/5/8 → 0/0/0**, 18→28 endpoints), the editor caller + D3's agreement check, and slice 4's three lesson vocabularies + `CatalogIndex` provenance. 🔴 **Slice 4 found the trap live in a fourth file the trap did not name** — `lessonprojectcontext.ts` defaulted to the open project's catalog, so a bundle's ports were answered by whatever was on screen. 🔴 **The kit name is FIXED** (`__noodl_module_name`; the editor said `'Unknown Module'` for every kit). ✅ **Richard ruled 08-16: an unresolvable kit type still BLOCKS install** — only the false claim was fixed. ⚠️ **Owed: re-record `kit-app.editor-nodelibrary.json` after a viewer build**, it predates the name fix. ✅ **The golden this task broke is CLOSED (2026-08-16, s12)** — `f7da52d1` added the `__noodl_module_name` marker and updated only `@nodegx/module-inject`'s own tests, leaving the editor's `expected-inject.snapshot.txt` two lines short; re-recorded through **the editor's exact composition** (not the package's `injectIntoHtml`) and mutation-proven. 🔴 **Three sessions relayed second-hand that this was `test:ci` contamination "that would not reproduce on committed code"; it reproduced in plain Node every time.** The test's header now carries a do-not-regenerate note. *Original spec below.* **The project catalog overlay — the keystone.** ✅ **D3 ruled:** the **MCP server** extracts (headless, executing the kit's `index.js` and reading the live register, reusing `scripts/node-catalog/dom-shim.js`); the **editor does not extract at all** — it reads what the viewer already sent over `sendNodeLibrary`. 🔴 **No on-disk cache** (in-memory per session only, keyed on mtime, if speed demands it). Must include a check that the two sources **agree**, or a user will find the divergence | catalog / editor / mcp | **1** | **L** | ✅ **unblocked** — D3, D7 ruled. Everything in tiers 1–4 hangs off this. ⚠️ **Cross-phase:** `lessonverify.ts` is a fifth consumer — route the overlay through `VerifyLessonOptions.vocabulary` (the seam exists), built from **the bundle's own** project files, not the open project |
| **[CN-004](CN-004-TURN-THE-CHECKS-BACK-ON.md)** ✅ **CLOSED 2026-08-16** | **Turn the checks back on.** 🔴 **Items 1–3 were already done by CN-003** — every skip site gates on `catalog.hasType()`, which the overlay makes true; the checks ran and **nothing had ever asserted it**. ✅ Now asserted, each with its built-in control: a kit node's wrong parameter draws the same code, severity and message a shipped type does. 🔴 **Item 4 WAS broken, in both directions, and no fixture could see it** — the demo and cashflow kits declare no `dynamicports`, so `toDynamicPorts` called all four exported shapes `declared-port-groups`: a `channelPort` node got `unknown-parameter` **on a correct kit**, and a conditional group's condition was never found (`formatDynamicPorts` emits `{ports}`, the catalog reads `{inputs}`). Both fixed and mutation-proven; new fixture `kit-dynports` is a real kit run by the real extractor. 45 tests. 🔴 **Two findings outlive it:** `@noodl/mcp`'s provisioning suites are **intermittently red under load, and were before this task** (2/6 baseline, interleaved — a sequential before/after on a shared machine measures the machine), **wants a task number**; and packaged `dist/noodl-mcp.cjs` needs a rebuild to carry the fix. *Original spec below.* **Turn the checks back on.** Stop skipping `parameterValues` and unknown-port checks for kit-declared types. ✅ **D4 ruled:** kit-declared ⇒ known and **fully checked**; only truly unresolvable types error under `--strict`. ⚠️ **Do not downgrade the new checks to warnings when they first go red** — Richard took this option over the softer rollout knowingly | editor / mcp | **1** | M | CN-003. ✅ **D4**. ⚠️ **Cross-phase:** the lesson path is *stricter* than the path D4 reasoned about — there `unknown-node-type` is an **error** (class F1) and blocks install for **every** provenance, not a warning promoted under `--strict` |
| **[CN-005](CN-005-THE-DEFINITION-TYPED.md)** ✅ **CLOSED 2026-08-16** | **The definition, typed.** ✅ `@nodegx/node-kit-types` — one self-contained `src/index.d.ts`, no build step, **60 tests / 3 suites**, all four registrations. 🔴 **AC1's premise was FALSE and was measured before anything was written**: a bare specifier resolves only when the package is physically installed, so with no `node_modules` the annotation is dead and the editor offers ~1,200 DOM globals instead. Delivery is a **copy of the `.d.ts` in the kit folder**, reached relatively — what CN-006's scaffold must write. Both arms asserted. 🔴 **Two holes, both found by building the caller, both in the types package not in a kit:** the `Noodl` / `React` globals were undeclared, so every annotated kit errored on line one; and mirroring the runtime's `[extra: string]: unknown` **silently disabled top-level typo detection** — the runtime drops an unforwarded field silently too, so it was silent at both ends. The index signature is now **deliberately omitted** and the divergence asserted separately, because a property-set check is blind to it. 🔴 **Spec item 4 was backwards about `frame`**: traced live for kits (`useFrame = !!def.frame`, and each sub-field registers real shared ports) — publishing it as dead would have been the lie in the other direction. ✅ AC4: cashflow kit annotated, **0 diagnostics**, and three injected faults each caught to prove the zero was real. *Original spec below.* **The definition, typed.** Publish `ReactNodeDefinition` (and the logic-node shape) as a consumable `.d.ts`, reachable from a JSDoc `@type` annotation with **no build step** — the types are the real documentation | runtime / docs | **2** | M | ✅ **D2** — JS is the supported path; **proposing a compile step on the author's side breaks the ruling**. Source of truth is `react-component-node.ts`, already fully typed |
| **[CN-006](CN-006-SCAFFOLD-A-KIT.md)** ⭐ ✅ **BOTH HALVES BUILT 2026-08-16** (MCP s9, editor s10) | **Editor half (s10):** "New node kit" in Project Settings writes the scaffold, re-reads `ProjectModel.modules`, and opens `index.js` in **the editor's first file-backed code surface** — ✅ **D1a**, Richard ruled for the literal reading rather than `shell.openPath`. `CodeFileDocument` + `ProjectCodeFileModel`, with `ProjectDocsModel`'s two shared primitives **extracted, not copied**. **31 editor tests.** 🔴 **A third silent hole, and it only exists past a bundler:** webpack rewrites the scaffold's `require.resolve` to a *module id*, which `fs` then resolves **relative to `process.cwd()`** — so the editor's scaffold worked or threw `ENOENT` by accident of where the process started, under a source comment claiming the opposite. No gate could see it (the package's suite is plain Node; the editor's never runs a bundle). 🔴 **The new gate was written twice** — mutating the fix away left it green because `"../nodegx-node-kit-types/…"` happens to resolve from jest's cwd *and* from `packages/noodl-editor`, which is a **sibling** of the types package; only a packaged layout (bundle beside its `node_modules`) has neither coincidence. ⚠️ **Not driven:** AC1's picker, AC3's computed style, and whether `ProjectModel.modules` was *actually* stale all still want a running editor. **Scaffold a kit.** ✅ `@nodegx/kit-scaffold` (no-build, 66 tests) + `create_node_kit`, proven end-to-end: scaffold through the tool, and CN-003's real extractor answers `get_node_type` with the node. 🔴 **Two silent runtime holes, both found before the scaffold existed.** (1) **D8 landed on the one path still broken** — AIB-001 guarded `set`, not `default`, so a units-typed port defaulting to a token emitted `var(--space-3)px`: invalid CSS, dropped without error, parameter panel still correct. **A colour port has no units, so the colour arm always worked** — reviewing by eye reports success. (2) The overlay installs **once at bind**, so a kit scaffolded mid-session answered *"Unknown node type"* while the tool's success payload was accurate. ✅ **Budget: 0 tokens** — measured 8,223 in `project` vs 8,249 for a new group and **8,464 resident (184 OVER the bar)**; CN-009 keeps all 57. 🔴 **AC4 was a FALSE PASS first time** — the tool was deferred, every call errored, and "same summary before and after" was trivially true. 🔴 `typesCopyStatus` caught the cashflow kit's copy **already stale within a day**; refreshed. ✅ **Editor command now built (s10)** — D1's clause was put to Richard and ruled (**D1a**): a real in-app file editor, not `shell.openPath`. *Original spec below.* **Scaffold a kit.** `create_node_kit` in `noodl-mcp` **and** an editor "New node kit" command that writes the scaffold and **opens `index.js`** (✅ D1). Emits `manifest.json`, `index.js`, one working example node, a README. Output must model **P2** — ports not constants | mcp / editor | **2** | M | CN-005, ✅ **D1**, ✅ **D8** — scaffold emits `var(--token)` ports by default |
| **[CN-006b](CN-006b-THE-KITS-SURFACE.md)** ✅ **BUILT + DRIVEN 2026-08-17 (s21)** | ✅ **AC1, AC2, AC3, AC4 all met.** `listNodeKits` / `removeNodeKit` / `joinKitNodes` in `projectmodules.ts`, the list in `KitsSection` (item 2, the create entry point, was **already built by CN-006** — confirmed by reading before writing), and a provenance row in `NodeLabel`. **29 tests, 15/15 mutations killed**, `test:main` **3,630 / 236** (re-measured), `typecheck:editor` **0**. 🔴 **THREE of this spec's own premises were false, and two would have shipped something wrong.** (1) *"with the right node count and **version**"* — **no manifest in any of the 29 real projects has a `version` field**, the scaffold writes none, and `MANIFEST_SCHEMA` has no `version` property; the row reads one when present and **omits it rather than defaulting**, because "v0.0.0" would be a number nothing on disk ever said (CN-016 owns what a kit version *means*). (2) *"with a link to its docs — the `docs` field already exists and is already carried through `nodelibraryexport.ts`"* — **it is carried, and it is prose, not a URL.** `docs` is **one field over two vocabularies** (CN-008's finding, third consecutive task): measured on the payload a real viewer sent, both kit types carry the author's sentence and **not one of the 175 built-ins carries the field at all**; rendering it as an `href` opens nothing. (3) *"provenance … already carried"* — `module` **was** already on the exported type and **needed a reader, not plumbing**; `NodeLibraryDataNodeType` declared neither it nor a census of anything else, exactly as s20 found. 🔴 **A P1 capability gap found and closed in passing, and it is the reason AC4 exists:** `getNodeDocs` reads the enriched catalog, keyed by type name and generated at repo-build time, so **a kit type can never be in it** — the header's help button was gated on that lookup, so **a kit node had 2 action buttons and a built-in 3**. The kit's own `docs` prose now fills it, with no "Read more" fine type because there is no page behind the click. 🔴 **There is no `kind: 'node-kit'` marker and the rule had to be built from a census**: the 29 projects contain exactly four manifest shapes (iconset / asset module / ERG-002 library / kit), only the first three positively identifiable, so kit-ness is **subtractive** — and a fixture containing only a kit would pass against a `listNodeKits` that returned every module it found. ⚠️ **`joinKitNodes` keeps two states apart that disk cannot tell apart**: ✅ D3 puts node lists in the running viewer's gift, so a kit with no nodes is **"installed, not yet loaded"**, never "0 nodes"; groups with no kit on disk are surfaced as **orphans** rather than dropped, which is exactly AC3's aftermath. 🚗 **DRIVEN** — observations written before launch, [notes/cn-006b-drive.md](notes/cn-006b-drive.md), all 9 met: 3 cards and **no `Inter`/`Lucide` card** while both sat in `noodl_modules/`; 5/1/1 counts; a kit created mid-session reading *"Installed…"* beside three that showed counts; `from Cashflow Kit` on the kit node and **0 provenance elements** on a built-in; **3 header buttons each, and the pre-change 2-vs-3 gap reproduced live** by clearing `type.docs`; removal deleting the folder and drawing the orphan card; and iconset / asset-module / traversal all refused by name. ⚠️ **Two instrument traps recorded:** CDP hover does **not** open this Tooltip (props read instead, and said so), and the naive React fiber walk reports the **last button in the rail** for every button because `sibling` escapes the subtree. ⚠️ **Not done: the `docs` *link* for a kit that wants one** — no kit can express a URL today, which is a scaffold/`ReactNodeDefinition` question, not a panel one. *Original spec below.* **The kits surface** (new, from ✅ **D1**). A kits list in the editor — natural home beside ERG-002's Libraries section — showing each kit, its nodes, version and provenance; plus **provenance in the property panel** ("from Cashflow Kit v1.2" + docs link). P1 allows attribution; it forbids capability gaps | editor | **2** | M | ✅ **D1**. Reuse `ProjectModel.listModules` + the Libraries section pattern |
| **[CN-007](CN-007-THE-DOCS-PAGE-THAT-REPLACES-THE-BROKEN-ONE.md)** ⭐ ✅ **PAGE + GATE + ENTRY POINT BUILT (s14) · FOUR SECTIONS ADDED (s26)** | 🔴 **s26: AC5's clause was STALE and is rewritten.** It said *"do not claim the logic half works — CN-012 has not run"*; CN-012 ran and it works, so the page was disclaiming its own product. Added: **Logic nodes** (a complete `Accumulator`, compiled by the gate, plus the `runOnValueChange` caution that it declares the checkbox and does **not** wire it) · **Where your kit runs** (the `runtimes` table, cloud = logic nodes only and no `require`, and why there is no `"ssr"`) · **Ports that appear only when they are relevant** = ✅ **CN-010 AC4**, with D12's `channelPort` refusal · **`docs` / `docsUrl`** = ✅ **D10**. Rough edges re-measured rather than copied: the preview-refresh edge is corrected for CN-014 AC1, and two are new (no `--success`/`--warning` token; the open panel that does not refresh). Sample gate: fragments **4 → 7**, each named, **plus a floor on the COMPILED count** so the budget cannot be satisfied by turning complete samples into fragments. ⚠️ **AC2 is the only thing left and cannot be self-graded** — someone who has not read this phase must follow the page and record where they stall. *s14–s15 record below.* ✅ **AC1, AC3, AC4 met; AC5 met and it cost the spec a correction; AC2 not started.** Page is `docs-site/docs/custom-nodes.md` (sidebar 4; troubleshooting moved to 5), and **docusaurus builds it with `onBrokenLinks: 'throw'`**, so its three relative links are checked. 🔴 **The D8 prerequisite was real work, not a tidy-up** — the cashflow kit's hex was in the file **twice**, and only one copy was a port default; see [RULINGS D8](RULINGS.md). 🔴 **The spec's item 4 was WRONG and is corrected on the page**: `runtimeVersion` is `'react17' \| 'react19'`, **new projects are created on React 19**, and the legacy slot spelled `react17` actually serves React **18.3.1** — "18.3.1 default, 19 opt-in" was backwards. ✅ **AC1's gate is `packages/nodegx-node-kit-types/tests/docsamples.test.js`** — put there because that package is already in all four registrations, so it needed none of its own. 🔴 **The gate's FIRST version graded nothing**: it used `/// <reference path=>` to a *module* `.d.ts`, which puts no names in scope, and the flagship sample carried no annotation — so "0 diagnostics" meant "nothing resolved". Caught by its own planted-fault control, then rebuilt to **materialise a real kit folder** (`index.js` beside `types/node-kit.d.ts`) so it exercises CN-005's actual delivery path. Mutation-proven against a real typo in the real page. **9 tests; package 62 → 70** (⚠️ the handover's "60" was already stale — re-measured). ✅ **D8 NOW DRIVEN (s15, 2026-08-17):** positive pills paint **`rgb(22,163,74)`** (`--green-600`) with no colour parameter set — not the old `#1F8A4C`, and **not `rgba(0,0,0,0)`, which is what would have shown if s14's fallback removal had been wrong**; that was the real risk and it is disproven. Both banner arms measured by flipping the driving value. 🔴 **Observation O3 was RETIRED as unmeasurable**: an unresolvable `var()` on a colour property computes to `rgba(0,0,0,0)`, never literal text, so "0 `var(` in computed colours" is equally true of a perfect page and a totally broken one — **and its liveness control ("the CSS text contains `var(--` somewhere") did not catch that, because a control has to be a known-broken input fed to the instrument itself, not a sign of life nearby.** 🔴 **The kit is OUTSIDE the repo and only ONE copy is tokenised** — `cn001-kit-drive` and `cn019-drive` still carry the pre-D8 kit (0 × `var(--`, 6 × live hex); driving either would have read as "the change did not land". Unversioned and ungated. ⚠️ **AC2 still not started.** *Original spec below.* **The docs page that replaces the broken one.** The 2.7 `create-react-lib` guide is the single most-cited dead end in the community. New page: the 21-line node, the `window.React` contract, the port table, the P2 rule. Plus an in-editor entry point so it is discoverable without knowing it exists | docs | **2** | M | CN-005. ⚠️ **D5 obligation: this page MUST carry the cashflow kit as the worked example** — the shipped reference kit is deliberately minimal and cannot demonstrate P2 or composition. Dropping it re-opens D5. Artifact drafted 2026-08-15 is the raw material |
| **[CN-008](CN-008-THE-AI-CAN-USE-YOUR-NODES.md)** ⭐ ✅ **BUILT 2026-08-17 (s16)** | ✅ **AC2–AC5 met; AC1 needs a live model.** `nodeKitOverview()` + a `THIS PROJECT'S NODE KITS` block in the cache-stable half right after the catalog, wired at both `AuthoringSession` call sites. **17 tests, 7/7 mutations killed**; editor suite **3555 / 229 suites**. 🔴 **TWO of this task's own clauses were false, and the second is the dangerous one.** (1) *"the AI does not know the lane exists"* — **it does**: a kit node is `inNodePicker`, so `catalogOverview()` already named it and `nodeTypeDetails()` already rendered its full ports. What was missing is **attribution and salience** — it arrived buried in ~30 names on the `- Visual:` line with **no kit named anywhere in the prompt**. (2) *"the ports an instance would actually set — the inputs with no default"* — **that selector matches ZERO ports** on either kit, because an author defaults every port and ✅ **D8** pushes them harder that way; following it prints a node name with an **empty list** under it while `charge()` reports a cost and AC2/AC3/AC4 all pass. The discriminator is the **runtime-added base set** (`react-component-node.ts`), which takes `demo.kit.Badge` from 12 in/9 out to the **8 and 1 its author declared**. 🔴 **Not derived, because both derivations are wrong**: intersecting the shipped catalog's visual nodes gives the **empty set** (`Component Children` has zero inputs); intersecting the project's own kit nodes also swallows `radius`, which both authors declared and meant. 🔴 **`docs` is ONE FIELD OVER TWO VOCABULARIES** — on a shipped node it is a **URL** (158/175 carry one, **158 of 158 are `https://docs.noodl.net/…`, zero prose**); on a kit node it is prose. ✅ **A real defect fixed in passing:** `renderNodeType` sourced its summary from `enrichedNode()`, which is keyed by type name and generated at repo-build time — **a kit type can never be in it** — so the author's `docs` was carried into the overlay by `@nodegx/kit-catalog` and then **dropped**, and every kit node reached the model with no statement of what it is for. ✅ **Budget measured:** 383 chars fixed + ~261/node; demo kit 934, cashflow-shaped ~2.2k, 40-node kit capped at 30 → 8,869 (~7% of the 120k context budget, in the **cached** half; spends none of CN-009's 8,280 bar). Overflow is **stated**, never silent. ⚠️ **The scaffold still emits no `docs`**, so absent is the common case — CN-006 owns the P2-shaped fix. *Original spec below.* **The AI can use your nodes.** `nodeKitOverview()` in `AuthoringContextBuilder`, beside ERG-002's `libraryOverview()`, so the built-in authoring loop composes a project's own nodes instead of ignoring them | editor / ai | **3** | M | CN-003. Follow `libraryOverview`'s `charge()` / absent-means-omitted pattern exactly |
| **[CN-009](CN-009-THE-MCP-SURFACE-INSIDE-THE-BUDGET.md)** ✅ **CLOSED — AC5 DRIVEN 2026-08-18 (s26)** | ✅ **ALL FIVE MET.** A real MCP session on a **scratch-built** bundle (never over `packages/noodl-mcp/dist/` — four peer servers load it) discovered the kit's nodes and **placed one**: 5 kit types of 147 with `providedBy: project-kit`; `Pill` at **23 in / 14 out** with a `summary` from the author's own `docs`; 🔴 **`query:"draggable"` now returns the Pill where CN-009 measured `[]`**, and so do `"unaffordable"` and `"day track"`. ✅ **The consequence:** a component placing `Lane` + `Pill`, written from tool output only, accepted at **0/0/0** and on disk — and the **control**, the same placement with two invented port names, is **rejected with nothing written**, so the acceptance is about real ports. Controls: `Group` unattributed, a kitless project leaks **0**. ⚠️ **`cashflow-command-centre` cannot serve this drive** — legacy monolithic `project.json`, refused by design; used `cn001-kit-drive` with the tokenised kit copied in. 🔴 **The drive found a D13 regression NO SUITE COULD SEE**: `validateCandidate` merges the rules report with the preconditions and both now run `checkParameterValues`, so every parameter finding was reported **twice**, counts included — invisible because every existing assertion asks *"is this reported?"*, which cannot detect *twice*. Deduped on `diagnosticKey`; regression test in `cn004.test.ts`. **A check registered in a second pipeline is a duplicate before it is a feature.** [notes/cn-009-ac5-mcp-drive.md](notes/cn-009-ac5-mcp-drive.md). *s17 record below.* ✅ **AC1–AC4 met; AC5's consequence needs a live model.** `src/catalog.ts` only — **14 tests, 8/8 mutations killed**, `noodl-mcp` jest **599 / 51 suites**. ✅ **AC3 costs ZERO: 8,223 / 8,280 — 57 under, IDENTICAL before and after**, because every fact added travels in a **response**, which the gate does not measure and a model does not pay for on turns it does not ask. **So there is no third renegotiation and the `$ref` move stays unspent.** 🔴 **AC2 was met on arrival and AC1 half was** — confirmed rather than inherited, against the real cashflow project: all five `nodegx.cashflow.*` types listed and `Pill` returning **23 in / 14 out** exactly. What was missing is **provenance** (`providedBy`/`kitModule` dropped by both projections). 🔴 **The finding is bigger than the task: a kit reached the agent with NO STATEMENT OF WHAT IT IS FOR.** `summary` was read from `enrichment`, generated at repo-build time and **keyed by type name — a kit type can never be in it** — so the author's `docs` was carried into the overlay and dropped. **It also cost the kit its only free-text handle:** `query:"draggable"` and `query:"snaps to whole days"` both returned **[]** against a `Money Pill` whose docs open with those words, while `query:"pill"` worked — so every query you would try *knowing the node exists* passed, and only discovery failed. ⚠️ **CN-008 finding 3 in the other consumer** — two bugs, not one, since the two read different enrichment sources. 🔴 **The obvious control CANNOT FAIL:** all 175 built-ins have an `enrichment.summary`, so the docs-URL fallback is never reached for them **gated or not**; the real control is a hand-built node shaped so it *would* fire. ⚠️ **One scope reading, stated out loud:** *"`find_tools` matches kit node names"* is a **no-op as written** — `list_node_types`/`get_node_type` are `core` and **resident**, so there is no deferred tool to reveal; built on `list_node_types({query})` instead, for zero tokens, leaving `kitTools.test.ts`' deliberate `find_tools` control untouched and green | mcp | **3** | M | CN-003, ✅ **D7** (this is "this project"; the shelf stays LBR-008's). 🔴 **Budget numbers CORRECTED 2026-08-15** — the bar is **8,280** (not 8,200), measured **8,223**, **57** tokens free, and the test forbids a third renegotiation: the sanctioned move is a `$ref`ed node schema. P67 / UNI-010 is what spent the old slack |
| **[CN-010](CN-010-DYNAMIC-PORTS.md)** ✅ **AC2+AC3 (s18) · AC1 DRIVEN (s19) · AC4 WRITTEN (s26)** | ✅ **s26: AC4 landed** — *"Ports that appear only when they are relevant"* on the docs page: a worked conditional port group with its condition, the ports it switches, and the statement that the port stays real throughout. ✅ **D12 closes the `channelPort` hole this task found** — now an **error** at kit-load naming the kit, the node, the surfaces the port is missing from and the working form instead. ⚠️ **`parameterEncoding` is still `{known:false}` on every overlay node** and the kit-catalog header names this task as its owner — untouched. *s18/s19 record below.* ✅ **AC2, AC3 met; AC1 half met and half a hole; AC4 not started.** **23 tests, 15/15 mutations killed**; `noodl-mcp` **613 / 52**, editor **3,567 / 231**, surface **8,223 / 8,280 unchanged**, `tsc` 8 pre-existing. 🔴 **AC3's premise was FALSE three times over** — there is no type named `Function` or `Object` (`Javascript2`, `Model2`); 88 of 175 shipped types declare `dynamicPorts` and **none of it reached an answer**; and the projection read **`dp.note`, a field that exists on NO node** — the catalog's field is `description`, *non-optional* on both the shipped and overlay types. So every dynamic type answered *"this list is incomplete"* with no statement of how, and it typechecked because the consumer **re-declared the shape it reads**. ⚠️ **The editor's `dynamicPortNote()` had it right all along — one consumer of two was wrong**, exactly like CN-008's `docs` and CN-009's `summary`: **three fields, three consecutive tasks, one cause.** ✅ Landed: `description` + `declaredPortGroups` (34 types, 165 groups, 158 conditions) in full detail, exporter internals stripped; and a kit's `hasDynamicPorts` flag finally carries a sentence — it never could, since `runtimeBehavior` is `enrichment`, keyed by type name at repo-build time (**88/88 built-ins have one, 0 kits ever can**). 🔴 **The obvious control CANNOT FAIL again** — *"no built-in shows the generic description"* passes against a completely unimplemented fallback; the real control is a hand-built node shaped so it *would* fire. 💰 **Full-detail ratchet moved 12,500 → 13,500 with the arithmetic** (283,483 → 294,894 total, +4.0%; worst 12,069 → 12,899; headroom 431 → 601) — ⚠️ **the ceiling case is no longer `Group`**, that comment was stale; and the cheaper-*looking* `activeWhen`-per-port encoding was measured and is **not** cheaper (29,014 vs 22,911 bytes). ✅ **AC2**: the carve-out was a bare `continue` — **947 set parameters across 321 nodes (6.0%) of the 29 real projects**, reported as a pass; now an `info` per node, reusing the **existing** `DynamicPortSkipped` code that `nonexistentPort` already emitted for *connections* (one of two sibling sites had it). 🔴 **Gating it behind `emitDynamicPortInfo` would have built a diagnostic no production caller can turn on**; ⚠️ the wording could **not** be borrowed from `unknownTypeSkip`, which says *"type X is not in the node catalog"* — false here, and for a kit false *because CN-003 fixed it*. 🔴 **Four silence baselines replaced, not deleted** (CN-002's rule) — one is a scope call for Richard: `Page` declares **neither `title` nor `urlPath`** statically (96 of the 947), so **"clean" is no longer "an empty diagnostics array" for any page**; `warnings` stays 0. 🚗 **AC1 DRIVEN 2026-08-17 (s19)** — observations written down *before* launch, full readings in **[notes/cn-010-ac1-drive.md](notes/cn-010-ac1-drive.md)**. ✅ **The conditional-group form works end to end and needed nothing kit-aware**: the author's `inputs: ['itemCount']` arrives in the editor's own format with the condition intact, `Item Count` appears at `mode = list` and vanishes at `mode = grid` *reversibly*, it is connectable through the real connection popup, and the wire is drawn, saved to `connections.json` and **live at runtime** (the preview renders `Lanes (0)`, the `0` arriving from `Child Index`). 🔴 **The `channelPort` form is erased in EVERY surface and EVERY state** — absent from the static `ports` list (the exporter removes it), the Properties tab, the Ports tab *whose header promises "every port on this node"*, the connection popup, and `getPorts()` **with the runtime live and the node mounted**; `node.dynamicports` is `[]`. 🔴 **Census: `channelPort` occurs ONCE in 177 types and it is the fixture's own kit node** (`conditionalports/basic` 136 · `extended` 21 · `port` 7 · `template` 2) — zero built-ins use it, which is *why* nothing ever went red: the exporter strips the port expecting an editor-side manager, and `DynamicPortChannel` is commented out at `nodelibrary.ts:94-106`. ⚠️ **Not a P1 violation** — a built-in would be erased identically; generic in code, kit-only in consequence. 🔴 **The finding that outlives the criterion: a kit node's definition is FROZEN after first delivery.** One write flipped `Panel`'s condition *and* added a new `Badge` node; one viewer reload delivered the **new type** (177 → 178) and **never** the changed condition (5 polls / 30s), and a full editor reload delivered it — **same file, same write, same reload**, so the only variable is whether the editor already knew the name. **The cause is a `TODO` on the line**: `NodeLibraryImporter.mergeUpdates` takes the `else` branch for a known name and discards the new data (`// TODO: Update the node data?`), and `updateIndex` only reloads on `forceUpdate || removedNodes.length > 0`; ⚠️ `mergeInByName` (`:366-388`) has the mirror-image bug — **s12 flagged that function as wanting a look, and it wanted a look.** ⚠️ **What the author meets is worse than either bug**: dynamic ports work first time, then every tuning edit is silently ignored, so the natural conclusion is *"dynamic ports don't work in kits"* — which is false. ⚠️ **And a consequence for conditional ports generally**: with the condition changed under an open project the property panel hides the row **and with it `Bound to App Root · Child Index`**, while the canvas still draws the wire, the file still holds it and the runtime still delivers the value — **the one surface that answers "what drives this?" is the one that stops mentioning it**. ⚠️ **Two instrument traps, both of which nearly produced false findings**: `node.setParameter()` does **not** re-render the property panel (the first read would have been filed as *"the condition is not evaluated"* about working code — caught because the `Mode` row I was *driving* also failed to move), and `WarningsModel` returned **0 beside a deliberately bogus node type on the same canvas**, so whether the editor warns about the orphaned `channelName` is recorded as **unmeasured, not silent**. *Original spec below.* **Dynamic ports.** `dynamicports` is the difference between a widget and a building block — a node whose ports come from its data. Supported by the runtime, undocumented for kit authors, and it interacts with CN-004's unknown-port checks | runtime / docs | **4** | M | CN-003, CN-004 |
| **[CN-011](CN-011-KITS-LOOK-LIKE-NODEGX.md)** ✅ **CLOSED s28 — ALL FOUR AC MET** | ✅ **s28: AC2 re-scoped and closed.** Its *"both themes"* clause is **struck, not failed** — re-measured independently of s27: `StyleTokenRecord` carries one value per token and `noodl-viewer-react/src` has **zero** `data-theme` / `prefers-color-scheme` / `setTheme` / `themeName`. The capability does not exist, so the clause could never be met **or** failed. ⚠️ **`ThemeManager` will try to reopen this** — five editor files theme the **editor's own chrome**, not the rendered app; measure the viewer before believing you found a second theme. The two halves that *were* measurable (token resolution, live propagation) are met. 🔴 **PREMISE ALSO FALSE — MEASURED s26** | 🔴 **AC1 and AC4 were ALREADY MET and the handover said otherwise.** This spec says *"the cashflow kit hardcodes hex throughout"* and s26's handover carried CN-011 as *all of it left, incl. D8's tokenisation, gating CN-007*. Measured: **0 live hex**, **16 `var(--token)` refs over 10 distinct tokens**; the 2 `#1F8A4C` a grep finds are both inside the **comment** explaining the change. AC1 was **driven in s15** on the computed style against the discriminating triple. ✅ **So D8 does not gate CN-007** — it never did after s15. ⚠️ **The copies still differ**: `cashflow-command-centre` + `cn069-s15-drive` tokenised, `cn001-kit-drive` + `cn019-drive` + `cn015-editor-drive` **pre-D8 (25 hex)** — drive one of those and you measure the old kit. **Left: AC2** (both themes; a token change without reload — s15 read ONE theme) and **AC3** (a variant surviving save/reload, testing the READER path). Both want a stack. 🔴 **A finding wanting a ruling: the semantic set has `--destructive`(+`-foreground`/`-hover`) but NO `--success`, `--warning` or `--info`**, so a kit with three status bands reaches into the palette scale for two — which is what the cashflow kit does. Not fixed here: `DefaultTokens.ts` changes the vocabulary every project sees. *Original spec below.* **Kits look like NodeGX.** Design tokens (`var(--token)`), `visualStates`, `useVariants`. AIB-001's token passthrough already exists in the bridge — this is about making it the **default an author falls into**, not a rule | runtime / editor | **4** | M | ✅ **D8** — scaffold emits tokens, **nothing validates them** (brand colours and data-viz are legitimate). ⚠️ the cashflow kit hardcodes hex and must move onto tokens before CN-007 cites it |
| **[CN-012](CN-012-LOGIC-NODES-TOO.md)** ✅ **MEASURED + BUILT 2026-08-18 (s24)** | ✅ **AC1, AC2, AC3 all met.** Caller built as the spec demanded: `tally-kit`, a kit with `nodes` and **no `reactNodes` at all**, run through the real extractor, real Chromium, the real `CloudRunner` and the real editor. ✅ **The logic half works and needed NOTHING built** — it registers by the same path a built-in does, keeps the author's `category`, places on canvas with its type resolved, and both directions of signalling work including **kit node → kit node**; the catalog overlay's only visual branch is `isVisual = category === 'Visual'` and a logic node takes the false arm cleanly. 🔴 **Contrast worth carrying: a logic kit node gets NO runtime base set** — where CN-008's base set took `demo.kit.Badge` from 12 in/9 out down to the 8 and 1 declared, a logic node's ports are exactly what the author wrote. 🔴 **`runOnValueChange` declares the PORT and wires NOTHING, and my own first kit got it wrong** — the governed input's `set` must ask `this.shouldRunOnValueChange(name)`; omitting it is silent and reads as a runtime bug (the output keeps the confident `0` `connectInput` pushed at boot, which is constraint 4 of `run-on-value-change.ts`' own header). **The published types did not declare `shouldRunOnValueChange` at all**, so an author following them could not find it; now declared and cross-linked. 🔴 **THE HEADLINE — the cloud runtime is WILLING and there is NO CALLER.** Built the caller: the same cloud function **times out** with a kit node, answers `200` with a built-in `Counter`, and answers `200` again the moment `runtime.registerModule` is called by hand. `CloudRunner`'s constructor calls `registerNodes` and nothing else, and `load(exportData, projectSettings)` **has no parameter a module could arrive through**. The failure is a **hang, not an error** — CWF-018's second confirmed instance. 🔴 **And `runtimes: ["cloud"]` makes a kit run NOWHERE**: `buildInjectionTags` filters on `browser`, so the only positive value the field has **removes the kit from the one runtime that would have run it** — measured, with every node missing and the author told nothing. ⚠️ It was also **stated as fact to the AI**: the overlay copied the manifest verbatim into `availableIn`, the same field that means *runtimeTypes* for a built-in. ✅ **Built:** `effectiveKitRuntimes` (state what runs; the wish moves to `declaredRuntimes`, present **only when the two differ**), a `kit-loads-nowhere` **error** in `kitDiagnostics` **keyed on the KIT not its nodes** (a kit no runtime loads registers nothing, so a node-keyed rule is blind exactly where it is needed — and an earlier cut's synthetic node row silently disabled `kit-registered-nothing`, which now has its own pairing test), wired into **both** `validate:project` and the editor's Kits panel (`listNodeKits` now carries `runtimes`), and **CN-005's provisional marker lifted** behind a `kit-logic` fixture with two `never`-probes proving the green is real. 📊 **Census: not one of the 40 manifests in this repo declares `runtimes`**, and the scaffold writes none — the field is entirely aspirational, so settling it breaks nothing. 🔴 **Found in passing, and it is CN-001's instrument:** `render-from-disk.js` hand-writes a **fourth** copy of the `defineModule` shim and was the only one missing CN-003's `__noodl_module_name` adoption, so every kit node in the harness stamped **`Unknown Module`** with the right name one tag away; ✅ **control: identical on a VISUAL kit**, so never about logic nodes. Fixed + gated. ⚠️ **`WarningsModel` read `0` beside a deliberately bogus node type on the same canvas — third confirmation**, so editor warnings here are **unmeasured, not silent**; the property panel's rendering is **unmeasured** too. **kit-catalog 55→63, node-kit-types 71→76, editor `test:main` 3664/240, noodl-mcp 633/53, module-inject 21; `typecheck:editor` 0, `typecheck:mcp` 0; 8/8 mutations killed** — ⚠️ a ninth **could not fail** and the guard it targeted was **deleted** rather than re-tested. 🔴 **OWED TO RICHARD: making kits run in the cloud is a RULING** — the runtime accepts them, but a loader means executing a kit's arbitrary JS **inside the backend process**, and ✅ D6 ruled only on locally-authored kits in the browser. **It gates CN-013.** ⚠️ Also owed: closing `NodeDefinitionOptions`' index signature (measured, works, but makes a **second** deliberate divergence and reddens `drift.test.js`) is **CN-005's call**. *Original spec below.* **Logic nodes too.** `module.nodes` via `defineNode` is the non-visual half and is **untested here** — my proof covered `reactNodes` only. Ports, signals, and whether a logic kit can run in the cloud runtime | runtime | **4** | M | Establish current behaviour by **building the caller** before specifying |
| **[CN-013](CN-013-CLOUD-AND-SSR.md)** ✅ **AC1 CLOSED s29 ON A REAL SSR DEPLOY** · ⚠️ **AC2 + AC4 open (item 4)** | ✅ **s29 — D19 built and AC1 met where three sessions could not reach.** The SSR loader installs a `window` shim carrying **`React` and nothing else** before evaluating a kit and **removes it before the render** (`viewer.jsx` and the runtime's client-only deferral both guard on `typeof window`, so a `window` left standing flips them to their browser branch on the server). Deploy → `npm run build` → serve → curl: **`SSR: loaded 4 kit script(s)`, zero threw** where s27 measured **all four** throwing `window is not defined`, and **four kit nodes' markup is in the served HTML** where s27's S1 was **0**, with the built-in control read **first**. 🔴 **The presence check was made falsifiable before it was believed** — a parameter value also appears in an inlined export, so the occurrences were traced into `nodegx.cashflow.Pill`'s own rendered DOM. ✅ **(b) landed as the BARE `React` global, and `globalThis.React` was WRONG**: the scaffold's cold-typecheck test rejected it (*"Property 'React' does not exist on type 'typeof globalThis'"*) because the shipped kit types declare `const React` in `declare global` — a `const` adds no property to `typeof globalThis`. That d.ts **already recommended bare `React` over `window.React`**; the scaffold and the docs simply had not moved. ⚠️ **New residual, never written down before:** a **stock** `JavaScript Function` node touching `window`/`document` throws under SSR — two of the fixture's own did, and the `window.foo` one is independent proof the shim really is removed. ⚠️ **7 `useLayoutEffect` warnings**, as this task's trap list predicted — now *reachable* because `Cashflow Lane` runs server-side; not a regression. *s25 record below.* ✅ **The caller CN-012 found missing is built** (✅ D18). `kitModules.ts` evaluates a cloud-enabled kit and registers its **logic** nodes; the editor's cloud bundle carries the sources; `WorkflowRunner` names every failure **at load**, which is the only moment the kit and the failure are in the same place. A `require` shim throws D18's sentence, so an SDK reach fails **naming the limit** instead of dying on `require is not defined`. **Opt-in via `runtimes: ["cloud"]`** — D18 does not re-open D6 — and kits that did not opt in still travel **by name without source**, so an unregistered type reads as *"that kit is not cloud-enabled"* rather than as nothing. ✅ **This also fixes CN-012's M4b:** `["cloud"]` no longer makes a kit run nowhere. 🔴 **D18's PREMISE 2 IS FALSE and it changes the obligation, not the ruling:** nothing in this repo loads `sandbox.isolate.js` (its shims call a `_noodl_api_call` with **no implementation anywhere here**), and the editor's "cloud preview" **spawns `nodegx-backend/dist/cli.js`** — the bundle a deploy target runs. **One context, two BUILD SHAPES**, and both were verified: source via the `@cloud-runtime` alias, and the esbuild bundle over real HTTP where the same graph with `modules` deleted **still 504s after 30 s**. ⚠️ The finding was already in the repo, in another phase's test header (`cloud-logic-builder-log.test.ts`), and D18 was ruled without it. ✅ **SSR: ESTABLISHED, then FIXED the same session** (`static/ssr/kit-modules.js`, 11 tests, 4/4 mutations). It was the same defect in a second runtime — The globals are set and **nothing fills them** — no file in `static/ssr/` evaluates a kit, so `__noodl_modules` is `[]` at every server render while the client loads the kits from `public/index.html`: the server renders **without** the kit nodes and hydration renders a different tree. Measured beside a known-firing control. 🔴 **The loader reads the INJECTOR'S OWN `<script>` tags out of `index.html`, never `noodl_modules/`** — a second manifest scanner is the regression LIB-003 exists to end, and an SSR deploy is a standalone folder that cannot import the first one, so it consumes the browser's instructions and **cannot disagree with the browser about which kits load**. ⚠️ **There is no `runtimes: ["ssr"]` and there should not be:** SSR is the browser app rendered on a server, so `browser` is what reaches it and `KIT_LOADERS` stays `['browser','cloud']`. ✅ **Item 4 is unblocked and its SHAPE CHANGED** — warn about a kit that **throws or no-ops server-side**, fed by the loader's own failure list, not about one whose manifest omits a runtime. 🔴 **A shipped diagnostic became false the moment the loader landed** and is fixed in the same slice: `effectiveKitRuntimes` carried its own instruction (*"the day a cloud loader exists, this function is the one place that has to learn about it"*) and `kit-loads-nowhere` still told authors `["cloud"]` runs nowhere. **Three baselines REPLACED, none deleted** — two of them moving *back* to a value CN-012 had moved them away from, for a different reason, **with their titles**. ⚠️ **The health fixture was the sharp one:** it declared `["cloud"]` with `availableIn: []`, a state the producer can no longer emit, and would have gone on **passing while grading fiction**; `["ssr"]` replaces it as the live counter-example. **Mutations 5/5 on the loader and 2/2 on `KIT_LOADERS` (both directions).** **cloud-runtime 189/9, nodegx-backend 1090/101, module-inject 21, kit-catalog 66/3, noodl-mcp 641/54, viewer-react 913/72, editor `test:main` 3752/244; `typecheck:cloud`/`:editor`/`:mcp` all 0.** ⚠️ **Left: AC2** (a warning before deploy) **and AC4**; and the deploy-time half — the editor could name a cloud function using a kit that has not opted in, **before** pushing, since the node library already stamps `module` on every kit node. *Original spec below.* **Cloud and SSR.** The manifest `runtimes` filter and the `ssr` compat field exist; a kit on an SSR/SSG deploy is unproven. ERG-002's `libraryNeedsSsrWarning` is the precedent for warning well | runtime / editor | **4** | M | CN-012 |
| **[CN-014](CN-014-THE-DEV-LOOP.md)** ✅ **CLOSED s29 — AC3 RE-DRIVEN AND MET** | ✅ **s29: AC3 met on a rebuilt bundle.** Console names the **kit and the file** (`Kit "Broken Kit" failed to load: … (in …/broken-kit/index.js)`), Settings → Kits still names it (s23 does not regress), the other three kits load with the viewer mounted (185 types), and the kit **recovers** on one viewer reload (186). ✅ **s28's diagnosis and its refusal to claim the criterion were both right** — the 13:36 bundle still carried the old anonymous text; rebuilt at 18:00:17 it carries the repaired messages. 🔴 **`packages/noodl-editor/src/external` is GITIGNORED** (`git ls-files` → 0 tracked), so the rebuild leaves **no diff**, nothing in the tree records which bundle a reading came from, and **no gate reproduces the artefact a deploy copies** — hashes are stamped in the drive notes for that reason. 🔴 **The launcher listed TWO cards with the same display name and card 0 was the ORIGINAL** — which project opened was verified from `ProjectModel.instance`, not assumed; clicking the obvious card would have dirtied `cn027-drive`. ⚠️ The s20 residual (an open property panel does not re-render on `libraryUpdated`) is still open, still small, and part of no criterion here. *s28 record below.* ⚠️ **s28: the code half of AC3 is done (see CN-015), the criterion is NOT claimed met.** The reason is mechanical, not a hedge: the editor runs a **built** viewer (`src/external/viewer/noodl.viewer.js`) and the repaired messages are in source. Until that artifact is rebuilt and a stack driven, AC3 is **unmeasured** — and a drive against the stale bundle reads the OLD anonymous text, which looks exactly like the change never landed. ✅ Re-drive with the **`cn027-drive`** fixture, which already carries a kit with a deliberate syntax error. ✅ **AC1's LIBRARY HALF BUILT 2026-08-17 (s20)** | ✅ **The frozen kit definition is fixed.** s19's CN-010 drive had already done this task's mandatory measure-first pass without the task being open — *a kit node's definition is frozen after first delivery* is **AC1 verbatim**, and s15's "a viewer reload is necessary and sufficient" is **AC2**. **Two causes, both in `NodeLibraryImporter`:** `mergeUpdates`' known-name branch carried `// TODO: Update the node data?` and discarded the new definition; and `mergeInByName` **did** replace the picker-index entry but never set `updated`, so `updateIndex` published nothing and `reload()` never ran — which is why *adding* a node to a kit refreshed its group and *editing* one did not. 🔴 **The obvious fix would have broken 84 built-ins**: the generated cloud library merges once per session, shares **all 84** of its type names with the browser library, and **all 84 definitions differ** — so an unconditional replace hands `Expression`, `REST2`, `Model2` and 81 others to the cloud library and inverts a precedence that has always been first-writer-wins. Gated instead on a `dataOwner` map — *whose report is this data?*, which `runtimeTypes` cannot answer because it is a **union** and both runtimes are in it for those 84. 🔴 **`clientId` cannot be the key and this was measured before building**: an ordinary viewer mints a fresh `guid()` per socket open (`editorconnection.ts:194`), only a sandbox passes a fixed one, so a fix keyed on it would never fire; ⚠️ and a rule keyed on *"`runtimeTypes` has one entry"* silently stops refreshing exactly those 84. **8 tests, 5/5 mutations killed**, editor `test:main` **3,601 / 234** (re-measured, not inherited), `typecheck:editor` **0**. ✅ **The precedence control asserts the two real payloads DISAGREE before asserting the browser's survived** — overlap alone would have made it vacuous, the third time this phase a control could not have failed. ⚠️ Found in passing: `NodeLibraryDataNodeType` declares **seven** fields and **neither `ports` nor `dynamicports`** — the two a kit author edits; the fix is field-agnostic by construction. 🚗 **DRIVEN the same session** (observations written before launch, [notes/cn-014-ac1-drive.md](notes/cn-014-ac1-drive.md)): one write renamed a port **and** added a node; one viewer reload took the library **177 → 178 *and* `Title` → `Panel Heading`** — the combination that was impossible before. ✅ **O3 held live**: `Expression`/`REST2`/`Model2` still `["browser","cloud"]` with full port sets. ⚠️ **Residual, reproduced twice: an open property panel does not re-render on `libraryUpdated`** — it showed the old label through **six polls over 30 s untouched**, then updated on one re-selection, so the variable is the selection change and **not** elapsed time. **Severity far below the bug behind it** (clicking any other node and back recovers it, and authoring does that constantly, whereas the frozen definition needed a restart and mislabelled itself as *"dynamic ports don't work"*) — **wants a small follow-up, not a reopening**. ⚠️ **AC1's second clause (a connected port that disappears from a kit) is recorded UNMEASURED, not working.** *Original spec below.* **The dev loop.** 🔴 **MEASURED 2026-08-17 (s15), and the spec's own hypothesis is FALSE:** a kit scaffolded into the **open** project is absent from `ProjectModel.modules` and from the node library immediately and at every 5s poll to **t+30s** — no watcher picks it up. ✅ **A viewer reload is sufficient** (177 → 178 types, new kit correctly named), so `KitsSection`'s "reload by hand" is *necessary and sufficient* — the rough edge is having to know, not the cost of recovering. Taken on an undisturbed tree with no peer stack, with `window.__req` surviving every eval as evidence no HMR reload wiped the renderer — **which is exactly why CN-006 s11's reading was unusable**. [notes/cn-006-midsession-kit-pickup.md](notes/cn-006-midsession-kit-pickup.md). *Original spec below.* Editing a kit file should refresh the preview *and* the node library without restarting the editor. `noodl-preview`'s watcher does not exclude `noodl_modules`, so it may already work there — **measure before specifying** | editor / devtools | **5** | M | ⚠️ HMR leaves the mounted editor on the old module — check that trap applies |
| **[CN-015](CN-015-FAILURES-NAME-THE-KIT.md)** ✅ **COMPLETE — CLI/headless s22, EDITOR s23, THE CONSOLE AND THE THROW s28** | 🔴 **s28 — s23's "met" and s27's "not met" were BOTH right, about different surfaces.** A syntax-error kit **was** named in **Settings → Kits**; it was **not** named on the console or in the exception that kills the viewer — which is where an author actually is when a node stops rendering. **Reconciled by reading the code, not by picking a session to believe.** Three naming-only seams, **no behaviour moved**: (1) `defineNode`'s two throws now name the node **and** the kit — the data was always there, `registerModule` stamps `node.module` *before* calling `registerNode` and `opts.name` was literally the next check; a kit definition also gets the consequence stated, because nothing connected *one missing field* to *a blank app*; a **built-in** gets the node name only, no kit and no kit advice. (2) `registerModule` catches **any** throw under the loop — `setup` and `nodeRegister.register` too — prefixes the kit and the node (or its **index**, when the definition is the one missing its name) and **rethrows**. (3) the capture preamble `console.error`s the kit **and the file**, answering s27's exact complaint that `SyntaxError: Unexpected identifier 'Noodl'` named *neither*. ⚠️ **Blast radius deliberately unchanged, with a test asserting so** — one bad definition still takes the viewer down; making it cost only its own node trades a loud dead app for a quietly missing one, which is **RULINGS-OPEN-QUEUE #14**, not a naming fix. **module-inject 21 → 27, runtime +12, 8/8 mutants killed.** 🔴 **The six new module-inject tests EXECUTE the preamble** against a fake `window` — the existing block only string-matched it and its own comment conceded *"a string assertion cannot show that an event fires"*. ⚠️ `expected-inject.snapshot.txt` re-recorded deliberately; diff is exactly the preamble in both prefix sections, checked against `git show HEAD:` first. 🔴 **The SHIPPED viewer bundle is not the repaired one** — `src/external/viewer/noodl.viewer.js` still carries the old bare message (measured), so a drive without a viewer rebuild reads the OLD text and looks like the change never landed. *s23 record:* | ✅ **ALL FIVE AC MET.** 🆕 **s23 — AC1's editor half.** The editor could not tell *"this kit threw"* from *"this kit is not installed"*, and **nowhere downstream still held the fact**: a kit whose `index.js` throws never calls `defineModule`, so `registerModule` is never called and no later inspection of any process can distinguish the two. Caught in the page instead — `@nodegx/module-inject` opens a capture before the first kit script and closes it after the last, `NoodlRuntime.getNodeLibrary` stamps it onto the payload (as `projectsettings` already was), `NodeLibraryImporter` holds it **per client**, `KitsSection` renders `kitDiagnostics`' own words. ✅ **Measured in real Chromium**: a `throw` at import, a syntax error and a 404'd `index.js`, each attributed by CN-003's marker. 🔴 **`capture: true` is load-bearing and the A/B proves it** — a resource error does not bubble, so bubble-phase loses the 404 case and **only** that case; a missing `main` would have stayed silent. ⚠️ **Attribution is bounded by a SECOND flag, not by clearing `__noodl_module_name`** — clearing would break CN-003 for a kit that defers `defineModule` into a callback, and with no bound at all the *last* kit is blamed for every later runtime error. 🔴 **Per-client, NOT merged into `currentNodeLibrary`: `mergeUpdates` walks `nodetypes` and nothing else**, so a top-level field on a second import is silently ignored and the list would freeze, going on accusing a kit already fixed — **driven live with one kit repaired and one left broken: the repaired one showed its node, the other stayed named.** ⚠️ **Scope: a kit's own `main`, not its dependencies** (dep tags are deduped across kits and carry no marker, so a failing dep has no one kit to name). ⚠️ **`expected-inject.snapshot.txt` re-recorded deliberately** — verified matching on committed code first; the diff is exactly the preamble and epilogue. **module-inject 15 → 21, runtime +5, editor `tests-unit/cn-015` +7, 6/6 mutations killed.** *s22, below:* ✅ **AC2, AC3, AC4, AC5 met; AC1 met on the CLI only.** `kitDiagnostics` in `@nodegx/kit-catalog` (`src/health.js`), wired into `scripts/validate-project.ts`. **Package 38 → 55 tests, 8/8 mutations killed**, driven end to end on a fixture project with a broken / silent / shadowing kit — all four fire, and **the same project with the faults removed produces zero** (AC5 measured, not argued). 🔴 **THE HEADLINE OUTLIVES THE TASK: precedence is two rules that disagree.** The catalog gives the **built-in** priority; the **runtime gives the KIT priority** — `NodeRegister.register` is an unguarded assignment and `viewer.jsx` registers built-ins *before* module nodes, so `createNode` returns the kit's constructor. **Measured with a probe that asserted the built-in so the failure printed the truth.** Consequence: **validation checks the built-in's ports while the app runs the kit's code** — and the shipped message said *"The built-in wins; the kit's node is not available"*, i.e. **the opposite of what happens**, so anyone trusting it debugged the wrong node. Corrected, with a regression test forbidding the old wording, and the same half-truth corrected in `responses.ts` and `extract.ts`. ⚠️ **Making the runtime agree is a RULING, not a tidy-up** — an existing module may override deliberately; owed to Richard. 🔴 **AC2 needed NO code and finding that out cost a wrong probe:** `createNodeFromReactComponent` really has no `name` guard, and a probe calling `NodeRegister.register` **directly** registered a nameless definition under the literal key `'undefined'` — **but that probe skipped a layer.** A kit always goes `registerModule` → `registerNode` → `NodeDefinition.defineNode`, which **throws `'Node must have a name'`** (`nodedefinition.ts:252-254`) and the extractor already catches it. **The diagnostic was written, then deleted when the fixture proved it unreachable** — this task's own trap list forbids a guard nobody has seen fail. ⚠️ **What the fixture found instead is worse:** the throw is *mid-module*, so the kit is left **half-registered** — nodes before the bad definition live, everything after silently gone (observed live: one kit produced *both* a load failure *and* a collision, impossible unless a node registered). `kit-load-failed` now carries `partial`; the flat *"none of its nodes are available"* was **an over-claim in my own first draft**, the same false-statement class the task exists to remove. 🔴 **AC4's "three known-zero shipped modules" is a list that does not exist** — LBR-006 asserts it and **names none**. Census of all 29 instead: **15 register ≥1, 1 registers zero** (`form-validation` — AC4's real known-broken input), **5 unmeasured** (need a browser), 7 have no `main` and register zero by design. ⚠️ **The census lied twice first** — without `window.React` 9 healthy modules read as broken, and collecting only `defineModule` missed `defineNode`. **`asked − answered = absent`: the other two are NOT disproven.** ⚠️ **AC1's EDITOR half is open** — the editor still cannot tell *"this kit threw"* from *"not installed"* (it reads the viewer's payload, ✅ D3, and a thrown kit is simply absent); `assumeLoaded:false` exists for that caller so the zero-node check is **skipped rather than guessed**. *Original spec below.* **Failures name the kit.** A kit that throws, registers a malformed definition, or collides on a type name must fail **loudly and by name**. `projectmodules.ts` already promises "loud, never silent" for manifests; extend the promise to the definitions | runtime / editor | **5** | S/M | CN-002's philosophy, different layer |
| **[CN-016](CN-016-PUBLISH-A-KIT.md)** | **Publish a kit.** Kits as library entries: `library.json` `type`, `library:build`, install routing, versioning, `minEditorVersion` / `runtimeVersion` compat gating. Ships the **new minimal reference kit** (✅ D5) | library | **6** | L | ✅ **D5** — the shipped reference is deliberately small; the cashflow kit stays a docs example and is **not** shipped content (so no licence sweep needed, but it must keep working). ⚠️ **0 of 29 existing modules have ever been run** (LBR-004), 3 register zero nodes (LBR-006) — do not assume the fleet is healthy |
| **[CN-017](CN-017-TRUST.md)** | **Trust.** A kit is arbitrary JS with full page access, and the sandbox responder already carries a `noodl_modules/` path carve-out. ✅ **D6:** locally-authored kits run **freely** (gating them would couple authoring to distribution); third-party kits are **verified on install** via ERG-002's `verifyLibrarySource` sandbox pattern, with provenance recorded and **explicit consent** to run | runtime / editor / library | **6** | L | ✅ **D6**. Gates CN-016 for anything not first-party; makes D1's provenance display load-bearing rather than cosmetic |
| **[CN-018](CN-018-THE-PICKER-NAMES-THE-KIT.md)** ✅ **CLOSED 2026-08-17 (s15)** | ✅ **DRIVEN — AC4 and AC5 met, and both owed re-records are done.** Three kits in one project draw three separately-named headings with their own counts; the two `Stat Tile` cards are distinguishable **without `data-test`**. 🔴 **The two modes carry the kit name on DIFFERENT elements** — browsing: group heading (card meta shows the *category*); searching: card's second line (heading shows the *category*). I nearly filed a false defect by reading only browse-mode cards. Preview chip also confirmed (`EXTERNAL LIBRARIES · HARBOUR METRICS`). AC5 measured, not argued: kit heading and the built-in `Logic` heading are identical in tag, class, size, weight, colour, transform and letter-spacing. 🔴 **THE BLOCKER WAS STALE — this is the transferable part.** Three handovers carried "needs a viewer build"; a build carrying the fix had existed since **2026-08-16 21:57**, five minutes after the fix committed at 21:52. Confirmed by *content* (`moduleNodesByKit` compiled at 5 sites in `noodl.viewer.js`), not by timing. **"Blocked on a build" is a claim about an artefact's mtime and contents; nobody checked either.** ✅ Re-records: the CN-003 fixture now says `Demo Kit` (identical 177-name set, exactly 2 content diffs, ports byte-identical; ~400-line diff is pure reordering, recorded in the JSON header), and `kitAgreement.test.ts` asserts `['Demo Kit','Demo Kit']` — **9 pass**, `tests-unit/cn-003` **22 pass**. *Original entry below.* **The picker names the kit.** Found by driving CN-006 s11 while measuring AC1's *"under its kit's name"* — placeable ✅, named ❌. Every kit collapsed into **one subcategory named `''`** under "External libraries": two kits each shipping a `Stat Tile` drew **two identical cards**, separable only by `data-test`. ✅ Fixed at the one hard-coded site (`nodelibraryexport.ts:917-932`) — group by `metadata.module`, sorted by code-unit comparison (**not `localeCompare`**, which varies by locale over a snapshot-compared blob); `'Unknown Module'` kept as a real group rather than merged into a neighbour's. **6 tests, mutation-proven.** 🔴 **The central case registers TWO kits deliberately: with one kit an unnamed group is indistinguishable from a named one, so every existing single-kit fixture passes against the old code** — that is why nothing caught it. 🔴 **Not driven — the editor loads a gitignored viewer bundle**, so a running editor shows the old grouping until a viewer build; **do not report it as visibly fixed without one**. ⚠️ Found in passing: `NodeLibraryImporter.mergeInByName` replaces a group wholesale by name, so under the old `''` a cloud module group **replaced every browser kit group at once** — narrowed, not fixed. ⚠️ Owed on the same build: `kitAgreement.test.ts:182-185`'s dated `['Unknown Module', …]` becomes `['Demo Kit', …]` | runtime / editor | **2** | S | CN-003 (which is what made the name correct). ⚠️ **This is the provenance CN-006b is built on** — do it before that task, not inside it |
| **[CN-019](CN-019-A-FILE-IS-NOT-A-NODE.md)** ✅ **BUILT 2026-08-16 (`175e29d9`)** | **A file is not a node.** Found by driving CN-006 s11. `CodeFileDocument` reuses `JavaScriptEditor`, so a kit's `index.js` renders FUN-006's port bar — *"This node has no ports yet. Type `Inputs.`…"* — and is labelled `SCRIPT`. **Not cosmetic:** it is the first file CN-006's create command sends a new kit author to, and its first sentence names a mechanism that does not exist there. **Two causes:** `'script'` is in `MODES_WITH_DECLARED_PORTS` (chosen for parser permissiveness, not because it is a Script node), and `openNode` is **ambient module state** whose only producer is the property panel — so a stale Function popout makes the file read *that node's* ports, a live wrong answer rather than an empty one. ✅ **The lint pass already has the exact guard and its comment names this case** (`portDiagnostics.ts:634-641`) — but 🔴 **copying it fixes only the observed row and keeps the stale-node row**; the fix is for the consumer to state what it is showing. ✅ **Built:** `JavaScriptEditor` takes `subject: 'node' \| 'file'`, `portBarState`/`modeLabel` take it too, `CodeFileDocument` passes `"file"` and clears the ambient slot on mount. 🔴 **The copied guard was mutation-tested and it PASSES the observed row** — it fails only the stale-slot row, *and* breaks two existing FUN-006 cases, because a blank Function node's `openNode` is real and empty. ⚠️ AC2's unit form **forces** the slot: `CodeEditorType#dispose` clears it on every close path, so *"immediately after closing a popout"* is the observed row in disguise | editor | **2** | S | CN-006 s10. ⚠️ **CN-007 depends on this in spirit** — the docs teach the model this bar contradicts |

---

## Suggested order

1. **CN-001 alone, immediately.** It is small, it is a bug, and while it is broken every
   verification of every other task is unreliable. It is also the one task with no ruling attached.
2. ✅ **CN-003 — unblocked, and next after CN-001.** D3 and D7 are ruled, so the shape is settled:
   MCP extracts headlessly, the editor reuses `sendNodeLibrary`, no disk cache, and the two sources
   must be checked against each other. Nothing in tiers 1–4 is worth starting before the spine
   exists.
3. **CN-004 → CN-005 → CN-006 → CN-007** as one authoring arc — each is the previous one's
   documentation or its consumer.
4. **CN-008/CN-009** once the catalog is real; they are the reason the catalog work pays off.
   - ⚠️ **CN-018 and CN-019 are both out of the drive, not out of the plan** — CN-006 s11 opened a
     running editor and both fell out of it. Neither is on this arc's critical path, but **CN-018
     goes before CN-006b** (it is the provenance that task displays), and **CN-019 goes before or
     with CN-007** (the docs teach the model the port bar currently contradicts).
5. Tier 4–6 as appetite allows. **CN-012 and CN-014 must start by measuring**, not by specifying —
   both describe behaviour nobody has established.

## Standing obligations for every task here

- **Build the caller.** Building the thing that *uses* a change is what exposes the hole in it. A kit
  task that ships without a kit exercising it is not done. ⚠️ This bullet used to quote a running
  score ("5 for 5"), which was already stale when CN-001 shipped — **a tally frozen into a document
  rots, and it rots inside a heading that tells you not to re-check it.** The live count is in the
  session memory (`build-the-caller-to-find-a-gates-hole`); the practice is what is standing here,
  not the number.
- **Verify the consequence, not the mechanism.** Write down what would be observably true of a
  working feature *before* driving it, and make sure the sentence could not also be true of a
  broken one.
- **Two ways an instrument lies.** A check that rejects sound input; a probe that silently
  exonerates. CN-001 is a live example of the second — run new checks against a **known-good** and
  new probes against a **known-broken** input.
- **P2 is an acceptance criterion.** Any task producing a scaffold, doc, prompt or reference kit is
  incomplete if a reader could come away thinking constants belong in the JavaScript.
