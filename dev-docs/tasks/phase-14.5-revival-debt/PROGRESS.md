# Phase 14.5 — Revival Debt — Progress

**Phase status:** Tasks DEBT-001 – DEBT-010 complete as of 2026-07-25 (open remainders tracked per-task below: DEBT-002's two blocked checks, DEBT-006's runtime-detection repro, DEBT-009's zero-diff save, DEBT-010's hover tooltips; DEBT-011/012 added by the salvage audit remain not started)

| Task | Title | Priority | Status | Notes |
|---|---|---|---|---|
| DEBT-001 | Cloud Function node crashes in deployed apps | 🔴 Critical | ✅ Done (code) | Fix + characterisation tests landed 2026-07-24; deployed-build spot-check folded into DEBT-002 |
| DEBT-002 | The live verification pass | 🔴 High | ✅ Done (2 items blocked, recorded) | 6 of 8 checks executed 2026-07-24; findings list below; SUB-001 kill-mid-save and installer launches blocked with precise reasons |
| DEBT-003 | Expressions cannot see Variables | 🔴 High | ✅ Done | Verdict: engine was right, tests mocked a nonexistent API; 2 real defects fixed (dependency detection, error surfacing); baseline 225/20 → 243/4 |
| DEBT-004 | Component port rename silently breaks wirings | 🔴 High | ✅ Done | Single typeModel slot evicted rename listeners; all 3 quarantined specs re-enabled; 1153 specs green |
| DEBT-005 | Editor test-infrastructure debt | 🟡 Medium | ✅ Done | Hazard was already defused; 42 never-run specs now execute; randomized order re-enabled with seed logging |
| DEBT-006 | Runtime latent-defect batch | 🟡 Medium | ✅ Done (1 addendum open) | Ledger emptied: 8 fixes, 4 recorded decisions, 2 deletions; runtime suite 247/0; the DEBT-002 runtime-detection repro stays open |
| DEBT-007 | Release & dependency hygiene | 🔴 High | ✅ Done (verification needs next tag) | passport 0.7 + login verified; retired macos-13 runner replaced; feed-merge job added; empty-secret crash fixed; dead workflows deleted |
| DEBT-008 | Legacy ES5 modules vs class runtime | 🟡 Medium | ✅ Done | Proxy apply-trap shim on Collection; corpus loads crash-free; survey: era idiom confirmed (5 uses across projects) |
| DEBT-009 | External-authoring friction | ◐ 3 of 4 done | In progress | get_node_type capped+summary; v2 hand-off fails loudly; token parity verified (REV-009); zero-diff first save remains |
| DEBT-010 | Small cleanups & record-keeping | 🟢 Low | ✅ Done (1 deferral) | view.ts→ListenableView, Ports root leak, flushSync×4, dead binding deleted, all doc corrections; hover tooltips deferred with reason |
| DEBT-011 | Retire Monaco + dead intellisense subsystem | 🟢 Low-Medium | Not started | Added 2026-07-24 (salvage audit §5); JSON port → CodeMirror, delete ~1,000+ unreachable lines + dep |
| DEBT-012 | Data-flow tracker salvage | 🟡 Medium | Not started | Added 2026-07-24 (salvage audit §4); fix Trigger Chain dedup data loss, unregister broken Data Lineage panel |

## DEBT-002 — the live pass: what ran, what it found (2026-07-24)

Run via the `run-editor` harness (CDP-driven, no human at the keyboard) against a real user
project (read-only), a disposable test project (`DebtLivePass`, created through the real
Quick Start wizard), and the GitHub Actions history. Evidence screenshots in the session
scratchpad; the durable record is this section plus annotations in each originating doc.

### Checklist outcomes

| Check | Outcome |
|---|---|
| PLAT-003 live editor pass | ✅ Real project (Shine Phase 2) opens, canvas paints, preview renders real content, warnings popup works, **zero renderer/viewer exceptions** in a full session log. Second project created from scratch, imported into, merged, validated — same result. Owed since slice 2; paid. |
| SUB-006 validator panel smoke | ✅ Problems panel renders real diagnostics (unknown node type from a legacy import), badge count updates, **click-to-navigate switches component and selects the offending node**. Note: panel is registered `experimental: true` and hidden until `experimental.panel.problems` is enabled — the validator UI is effectively dark-launched. |
| SUB-007 conflict UI | ✅ First-ever live render. Real merge engine (`mergeProject`) produced a `parameter` conflict; the metadata channel (`MERGE_CONFLICTS_KEY`) fed `MergeConflicts`/`GraphConflictList`; panel shows grouped conflicts with Yours/Theirs values, bulk + per-conflict actions; **"Use theirs" applied the value to the live graph** without hand-editing JSON. Git init exercised through the panel's own button. Git-driver plumbing itself not re-run (covered by `parity.test.ts`). |
| SUB-007 large-diff legibility | ✅ Observed at 151 changed components: a flat list with add/change markers and a count in the tab header; **no folder grouping, no collapse, no kind rollup** ("150 added, 1 changed"). Rows navigate the canvas on click. Filed as concrete asks against AIX-003 (below). |
| SUB-001 large-project claims | ⛔ **Not verified.** Two honest attempts, both ended by environment failures (below): (a) the 176-component corpus is React-17-legacy and now hard-gated read-only behind the migration wizard; (b) a synthetic 155-component project opened fine but the session was killed by the HMR full-reload bug before save-latency / kill-mid-save could run. Needs one clean run in a quiet tree; the claim stays unproven. |
| PLAT-002 import-popup variants | ✅ Owed since wave 3; paid. Import variant: tree indentation, folder toggle, implicit-dependency marker (dimmed check) all render; import lands on disk and in the components panel and survives reload. Collisions/overwrite variant: filters to genuinely colliding items only, partial overwrite works, no exceptions. |
| Nightly workflow | ⛔ **Cannot run, and never could**: `nightly.yml` exists only on `cline-dev`; GitHub only schedules (and dispatches) workflows from the default branch (`main`). This is the whole explanation for REV-003's "no scheduled run has executed". Unblock = land it on `main` (human-gated: pushing to the default branch). |
| Installers | ◐ Partially evidence-backed via the v0.1.0 Release run (29995532502): **win32-x64 installer built successfully** (first evidence a Windows installer has ever been produced); **linux-x64 fails** with `electron-builder: Please specify author 'email' in the application package.json`; **darwin-arm64** electron-builder failed; **darwin-x64 hung 24h and was cancelled** (`release.yml` has no `timeout-minutes`). No artifacts were uploaded, so no installer has ever been *launched* — that remains unverified and needs hardware/VMs. All four routed to DEBT-007. |
| DEBT-001 deployed-build spot-check | ◐ The fix is proven by characterisation tests in the exact deploy-shaped context (no `editorConnection`, no `cloudServices` → no throw, `failure` signalled). Live: the deployed bundle (`noodl.deploy.js` via `noodl-preview`) loads a project containing a triggered Cloud Function node with **zero uncaught errors/TypeErrors** in the console — pre-fix, every call threw. A visual assert of the error-output wiring failed on tooling (headless Chrome dumps the DOM before the app's first paint), not on the app. |

### Findings (new, discovered by this pass)

1. **HMR full reload kills the editor window.** When webpack HMR cannot hot-apply (seen: `TypeError ... isLesson` in `EditorPage.tsx` during apply), it forces a full reload, which resolves the SPA's pushed route as `file:///dashboard/projects` → `ERR_FILE_NOT_FOUND` → permanently dead window. Hit **three times** in one session; any source edit while the editor runs can trigger it. This is the single biggest obstacle to long scripted editor sessions and deserves its own small fix (serve index.html for unknown file routes, or pin the reload URL).
2. **Runtime-detection false positive (unconfirmed cause).** `DebtLivePass` has `runtimeVersion: "react19"` in project.json (detection check 1, high confidence), yet after adding 150 components on disk and reopening, the editor showed "Legacy Project (React 17) — Read-Only Mode". Needs a repro in a quiet session; if real, fresh projects can spuriously lock read-only. Routed to DEBT-006 as a new item.
3. **Problems panel is dark-launched** (experimental, off by default) — decide whether SUB-006's surface should be on by default before Phase 17 leans on it.
4. **Dev-stack fragility**: the viewer webpack watcher died once (`lerna ERR! npm run start exited undefined`) and took the whole stack down; "Exit project" quit the entire app once while the window was in the dead-HMR state. Both harness-level, both worth noting in DEBUG-INFRASTRUCTURE docs.
5. **AIX-003 asks from the large diff**: group changed components by folder prefix; add a kind rollup ("N added / M changed / K deleted"); consider virtualizing the list (151 rows render fine, but the corpus-scale case is untested).
6. **`release.yml` needs `timeout-minutes`** (the 24h darwin-x64 hang is a whole day of macOS runner spend) and package.json needs `author.email` for the Linux build — both DEBT-007.

## Log

- **2026-07-24** — DEBT-011 and DEBT-012 added from the [pre-revival salvage audit](../../reviews/PRE-REVIVAL-SALVAGE-AUDIT.md) (phases 3/4/5/9/11 feature attempts). Same day, the audit spawned sibling tasks elsewhere: AIX-006 (style vocabulary, phase 15), SUB-011 (expression-parameter fixtures + posture, phase 13), the phase-19 re-scope (WF-004/005/006 + BACKEND-GAP-ASSESSMENT), and corrections to RUN-004 (wrong paths; `better-sqlite3` never installed) and PLAT-005 (STYLE-005 banner already wired).
- **2026-07-25** — DEBT-010 complete. Code: `shared/view.ts` → `shared/ListenableView.ts` (the name promised a view framework PLAT-002 deleted; 14 importers updated incl. the CommonJS `require(...).default` in viewer.js); `Ports.getViewGroupsFromPorts` now disposes the previous render's row views before rebuilding (the React-root leak); `flushSync` applied at the four popup sites that measured 0×0 (`Pages.tsx`, `TextStylePicker.jsx`, `propertyeditors.jsx` font picker, `ComponentTemplates.ts` — the StringInputPopup precedent); the never-effective webview blur→refocus binding in viewer.js is **deleted with the decision recorded in place** (a hard focus trap is a UX change nobody asked for); dugite log noise confirmed already gone (DEBT-005 measured zero occurrences). Gate: full Electron suite 1194 specs, 0 failures, randomized. Records: phase-14 PROGRESS corrected (NodeGraphEditorNode split shipped 1,290→610; PLAT-003 cell header now says slices 1–8); PLAT-004 spec's 538 annotated with the committed 540/287 baseline at `78ba241`; phase-2 TASK-005 disposition recorded (nothing absorbed it; future feature phase if wanted; NODES-002/004 still 0-byte); REV-002's `Open PR` box annotated moot; REV-010's three verified-but-unchecked boxes ticked citing its own verification record. **Deferred with reason:** property-row hover tooltips — blind UI wiring with no visual verification available this session; belongs to the next property-panel session (PLAT-005 is already in that file set).
- **2026-07-25** — DEBT-009: 3 of 4 items closed. (1) `get_node_type` gains `detail: "summary"` and a ~60 KB in-band byte budget — oversized full responses degrade the tail to summaries with a `summarized` list and re-request hint, never a file pointer; verified by a new end-to-end test requesting 8 heavy enriched types both ways (MCP suite green). (2) The v2 hand-off loud-failure floor: opening a `nodegx.project.json` directory with `formatV2.enabled` off now shows an actionable toast + console error naming the setting instead of failing blank; the default flip stays pending human sign-off + the SUB-001 checks. (3) The preview/deploy render discrepancy was **already fixed by REV-009**: both paths generate `:root` from the shared `buildEffectiveTokens`, and the `noodl-design-tokens` stamp is confirmed in real served exports; residual documented (preview uses live model incl. unsaved token edits, deploy uses persisted metadata). (4) **Open:** first-save normalization zero-diff — recommended approach recorded in AIX-002-NOTES; deliberately not rushed at the tail of this pass since it is fidelity-critical and AIX-002 is mid-flight on the same surfaces.
- **2026-07-25** — DEBT-008 complete. **Survey:** the Backbone idiom is the era's standard — modules define their own `Noodl.Model.extend`/`Noodl.Collection.extend` (found in the corpus module and LearnBook-era modules, 5 uses across surveyed projects) and chain base constructors with `.apply(this, arguments)`. `Model` is still an ES5 function and was never broken; the one real break was `Collection`'s class constructor. **Decision: (a) compat shim**, implemented as a `Proxy` with an `apply` trap on the export — function-style invocation becomes a supported no-op (the class body is empty; all behaviour lives on the patched `Array.prototype`, which ES5 subclass instances reach through their prototype chain), while `new`, statics, `instanceof` and `class X extends Collection` pass through untouched and real instances stay genuine Arrays (the load-bearing property that rules out un-classing). Characterisation tests cover both construction styles plus the exact corpus subclass pattern; runtime suite **251/0**. Acceptance: `big-merge-test-mine` now loads through the rebuilt deploy runtime with **zero uncaught errors** (pre-fix: the documented `Class constructor` TypeError). Paint could not be asserted headlessly — Chrome's DOM dump races first paint for *any* project (same limitation hit in DEBT-002) — so the visual check rides on the next live editor session; the crash, which was the actual defect, is gone.
- **2026-07-25** — DEBT-007 complete. (1) **The v0.1.0 mac failures were mis-diagnosed until now**: the darwin-x64 "24h hang" was a job queued against `macos-13`, which GitHub retired — both workflows now target `macos-15-intel` (the supported Intel image); and darwin-arm64 died because CI passes signing secrets unconditionally, so an absent secret arrives as an **empty string** and electron-builder treats empty-but-set `CSC_LINK` as a certificate path (`…/noodl-editor not a file`) — `build.ts` now strips empty signing vars so absent secrets degrade to unsigned builds as the workflow promises. (2) `latest-mac.yml` overwrite: universal build stays deferred per RELEASE-PROCESS §6's natives evaluation; instead each mac leg preserves its feed as an artifact and a `merge-mac-update-feed` job combines the `files` lists and re-uploads on tag pushes. **Not yet proven by a real run** — deliberately not dispatched, since a dry-run would modify the org's existing v0.1.0 draft release (human-gated surface); the next tag verifies it. (3) `author.email` added (Linux .deb builds). (4) **passport 0.5.3 → 0.7.0** with the two 0.6+ breakers handled (async `req.logout`, cookie-session `regenerate`/`save` shims) and the dashboard login/logout flow exercised end-to-end standalone: login page, wrong-password reject, login redirect + session cookie, authenticated page, logout redirect — all green. DEPENDENCY-POLICY updated (passport row moved out; dugite re-dated — still no non-major fix, decision stands). (5) The three dead node-16 workflows deleted (all dispatch-only, no callers). Local full `build:editor` NOT run — its pipeline deletes `node_modules` mid-build, which would wreck this shared working tree; the packaged-launch check rides with the next CI run.
- **2026-07-25** — DEBT-006 complete (original ledger). All PLAT-003 findings fixed or closed with recorded decisions — full item-by-item record in PLAT-003-NOTES §20. Highlights: Back Actions no longer throw at load (`_createSignal` → `EdgeTriggeredInput.createSetter`); Component Stack writes the same custom page path it matches; deployed apps no longer build tooltip HTML (and the catalog generator, no longer fooled by a truthy noop `runDeployed`, now emits real port descriptions); seven bare-value inspectors render; QueryBuilder's `id` was a **real adapter bug** against the `objectId` schema — aligned, converting its 4 test failures. Runtime suite **247/0** (phase started at 225/20). `catalog:check` green (two regenerations, each own-committed). Still open from the DEBT-002 addendum: the runtime-detection false-positive repro — needs a quiet editor session, deliberately not attempted in this tree while another session works in it.
- **2026-07-25** — DEBT-005 complete. (1) The `@jest/globals` hazard was **already defused**: `tests/models/index.ts:11` resolves to REV-008's converted models-path copy, not the Jest file — recorded, no change needed. (2) The two orphaned Jest specs now execute: `services/StyleAnalyzer.test.ts` (23 specs) and `services/github/GitHubClient.test.ts` (19 specs) converted to Jasmine — Jest module mocks replaced by seeding the `GitHubOAuthService` singleton slot and installing the mock Octokit directly (a real `initializeOctokit` would hit the network). First-ever execution surfaced only harness drift, no product bugs: the StyleAnalyzer specs patched `ProjectModel.instance` through the public setter, which registers the mock as a NodeLibrary module and crashes (they now patch the private slot — the Jest originals would have failed identically); one GitHubClient assertion didn't account for EventDispatcher appending the event name. **For PLAT-005: StyleAnalyzer's real status is 40 passing specs across both suites, zero analyzer-logic defects found.** (3) Randomized order is back on (`SpecRunner.html`), with the seed reported on every run for reproducibility. Four order couplings fixed along the way: the DEBT-004 fixture mutation, split setup/teardown specs in `nodegrapheditor.js`, export-bundle assertions tied to a global bundle counter (`b0…bN` — now normalized to content-keyed comparison) plus lazy type resolution making bundle *grouping* order-dependent (export specs force `updateTypes()`), and `conflictwarnings.js` leaking its registered project into the NodeLibrary (no afterEach). 7 green randomized runs after the fixes, including 4/4 consecutive. Spec count 1194 (doc said 705 — the suite had grown; +42 converted, −1 from merging the setup/teardown pair). (4) The dugite fallback-path log noise from REV-002 is already gone — zero occurrences in current runs.
- **2026-07-25** — DEBT-004 complete. Root cause of the rename bug: `NodeGraphModel.bindTypeModel` kept a **single** `typeModel` slot, so each node type that resolved evicted the previous type's `portRenamed` listener — in any real graph (many types), the component whose port you renamed had almost always been evicted, so instance connections and parameters silently kept the old name. Graphs now keep every distinct type bound (`boundTypeModels`, released in `dispose()`); the propagation handler already filtered by type so stale bindings no-op. Test side: the spec fixture was shared by reference across specs (`fromJSON` doesn't copy), so the rename spec mutated it for all later specs — that leakage is what the other quarantined specs had encoded (`p3b`); the fixture is now deep-copied per spec, the line-47 health spec was rewritten against today's WarningsModel semantics (dated comment in-file), and the line-152 component-rename spec passed as-is once enabled. All three `xit`s restored to `it`; full Electron suite **1153 specs, 0 failures** (includes the ports-panel rename+undo specs and `tests/versioning/`). Live in-editor rename click-through deferred with the same reason as DEBT-002 finding #1 (HMR instability); the model layer the property panel calls is exactly what the re-enabled specs now cover.
- **2026-07-24** — DEBT-003 complete. Root cause: **stale harness, not a broken engine.** The evaluator always resolved `Variables` from the global `'--ndl--global-variables'` model — proven by direct evaluation against the real store (`Variables.x * 2 → 20`) and by code inspection (variablenode2, setvariablenode and `Noodl.Variables` all share that store); the 15 failing tests mocked a `context.Variables` API that never existed. Fixed 2 genuine engine defects found in the same pass: (1) `detectDependencies` destroyed bracket-notation keys and template-literal interpolations by string-stripping before matching, so those reference forms never re-subscribed; (2) expression errors were swallowed inside the evaluator, making the node's `sendWarning` path unreachable — runtime errors now rethrow into it and compile failures warn with the syntax error. Three tests corrected with recorded policy (auto-creating `Objects`, null → fallback, `validateConfigValue` optional-null). Runtime suite: **243 passing / 4 failing** (was 225/20; remaining 4 = QueryBuilder → DEBT-006); viewer suite and `catalog:check` green; tsc clean. PLAT-003-NOTES §4 annotated with the resolution and new baseline; AIX-002-NOTES gains the expression-semantics contract. Live in-editor click-through of a Variable-reading expression not re-run this session (HMR instability, see DEBT-002 finding #1) — the engine-level proof runs the identical module both editor and deploy use.
- **2026-07-24** — DEBT-002 executed (see section above). Six of eight checks done with evidence; SUB-001 large-project claims and installer launches remain open with precise reasons. New findings routed: HMR-reload harness bug (new small task candidate), runtime-detection false positive → DEBT-006, release-workflow fixes → DEBT-007, diff-legibility asks → AIX-003. Originating docs annotated: PLAT-003-NOTES §18, PLAT-002-NOTES §8, SUB-006, SUB-007, REV-003, REV-004.
- **2026-07-24** — DEBT-001 code complete. `cloudfunction2.doCall` no longer throws in deploy-shaped contexts: the missing-`cloudServices` path routes to the `failure` output via `setError` and returns; the `isRunningLocally()` read is guarded on `editorConnection` being present. Characterisation tests added (`noodl-viewer-react/tests/cloudfunction2.test.ts`, 2 tests, both passing); full viewer-react suite green; `catalog:check` clean; no new tsc errors. PLAT-003-NOTES §17.7 #1 marked resolved. The spec's step-5 deployed-build verification is deferred into DEBT-002's live pass, which is the next task and covers exactly this kind of check.
- **2026-07-24** — Phase created. Source: cross-phase audit of phases 12–14 (three parallel doc-vs-repo reviews). All items were already recorded in those phases' NOTES/PROGRESS files but had no owning task; this phase assigns owners. Items with existing owners elsewhere (STYLE-005 → PLAT-005, CF11 pipeline → WF-001, large-diff review UI → AIX-003, migration wizard UI → SUB-003 deferral, PLAT-003 remaining slices → PLAT-003) were deliberately excluded.
