# Phase 14 Progress — Editor Platform Health

**Created:** 2026-07-22, from [NOODL-REVIVAL-ROADMAP.md](../../reviews/NOODL-REVIVAL-ROADMAP.md) Track B
**Overall status:** 🟡 In progress — 3 / 5 original tasks complete (PLAT-001, PLAT-002 both 2026-07-24; **PLAT-003 closed 2026-07-27** after slice 14 converted the last eleven convertible modules — `noodl-runtime/src` ends at 8 `.js` / 117 `.ts`, every remainder a recorded decision: seven modules are the §29.2 viewer-boundary set now owned by the newly filed **PLAT-006** (ship declarations from `@noodl/runtime`), and vendored `events.js` is keep-as-JS by decision). PLAT-005's implementation landed 2026-07-27 and still owes a live pass.

## Status vocabulary

Not started · In progress · **Built–not wired** · Complete · Superseded

(The "Built–not wired" state is used deliberately across the revival phases: the viability assessment found several subsystems that were complete by their task spec but had no call sites in the application. Completion means integrated.)

## Tasks

| ID | Title | Status | Estimate | Notes |
|---|---|---|---|---|
| PLAT-001 | Canvas decomposition | **Complete** (2026-07-24) | 6–8 wks | Coordinator 3,481 → 790; 20 modules; unit + characterisation tests; regression + 500-node perf verified; CHANGELOG in task doc. ~~Optional follow-up: `NodeGraphEditorNode.ts` split (1,290)~~ — shipped in wave 4: 1,290 → 610 (+ stateless `NodeGraphEditorNodePainter`, 466); see PLAT-001-NOTES §11. *(Corrected by DEBT-010, 2026-07-25.)* |
| PLAT-002 | Retire the jQuery islands | **Complete (2026-07-24)** | 6–8 wks | **547 → 0 `$(` calls, 68 → 0 files.** jQuery is gone from the repository: no source usage, no vendored `jquery-min.js`/`jquery.autosize.min.js`, no `<script>` tags, no `@types/jquery`, and **all 34 runtime `.html` templates deleted** along with webpack's now-unused `html-loader` rule. Waves 1a–4 converted the dead code, every `DataTypes` row/widget/picker, the property-editor hosts, the standalone popups and PopupLayer itself (TypeScript + native DOM, React for the popups that are components). Wave 5a took the utility modules (`$.ajax` → fetch, `$(':focus')` → a real focus check). Wave 5b took the framework: `shared/view.js` + `view.d.ts` → a 35-line typed `view.ts` that is only the listener bus, the last two templates rebuilt in code (`CanvasShell.ts` hands the canvas shell's nine layers back as typed refs instead of `el.find('#...')`), `ReactView` de-jQueried, and the `el.jquery ? el[0] : el` normalisers removed everywhere. The original inventory had missed `src/frames/viewer-frame/` entirely — a second `View` subclass with its own template. Narrowing the types surfaced **two live wave-4 regressions**: the detached viewer window threw on boot (`PopupLayer.render().get(0)`) so its inspect menu was dead, and three callers still passed `content: { el: [div] }`. tsc clean, **1060 specs / 0 failures**, editor + detached viewer driven live via CDP. Follow-ups (rename the bus, `flushSync` sweep, `Ports` root disposal, the unverified import-popup variants) are itemised in PLAT-002-NOTES §8 |
| PLAT-003 | Type the runtime and viewer | **Complete (2026-07-27)** | 8–12 wks | **CLOSED after 14 slices — see the spec's CHANGELOG and NOTES §31 for the authoritative record; this cell's prose narrates only the early slices.** Final state: every node file in both packages TypeScript; `noodl-runtime/src` 8 `.js` / 117 `.ts` (seven §29.2 boundary modules → PLAT-006; vendored `events.js` keep-as-JS by decision); runtime jest 132 → 973/0; live editor pass green at slices 10, 13, 14; `catalog:check` byte-identical throughout. Earlier narration follows: **Slices 1–8 done as of 2026-07-24** (this cell's prose below narrates only slices 1–4; see PLAT-003-NOTES for 5–8, and note the live pass it says is owed was paid by DEBT-002 — correction by DEBT-010, 2026-07-25.) Slices 1–2 detail: Slice 1 (toolchain): `noodl-runtime` had **no TS toolchain and no runnable tests** — 11 of 14 suites collected 0 tests; now 0 load failures and `tsc --noEmit` clean. Slice 2 (spec steps 1, 3, 4): **node-definition API published** as `@noodl/types/src/runtime/node-definition.d.ts`, sharing SUB-004's catalog vocabulary by import so the two cannot drift, with all five dynamic-port mechanisms modelled honestly; **12 core files converted** (node, nodedefinition, nodescope, nodecontext, noderegister, outputproperty, eventsender, edgetriggeredinput, variants, guid, utils, async-pool), ~2,500 lines; runtime internals kept unpublished in `src/internal.d.ts`. Conversion pipeline de-risked with a dedicated ts-loader instance (`webpack-ts-rule.js`) since the runtime is CommonJS and both viewers compile ESM; `noodl-viewer-cloud` moved to its own `typecheck:cloud`. Tests 225 pass (was 215) / 20 pre-existing failures; all 3 viewer bundles + 2 cloud bundles + preview esbuild green; `catalog:check` byte-identical across 135 node types. Also removed 16 leftover emoji `console.log`s, three of them per-signal-per-frame. Slice 3 (step 6): `react-component-node.js` → `.ts` (1,189 lines), the compiler between React node authoring and the runtime definition, with `ReactNodeDefinition`/`ReactNodeInstance` published from it; four latent defects found and documented. Slice 4 (step 7, first group): all nine `nodes/visual/*.js` → `.ts`, **annotated** against `ReactNodeDefinition`/`NodeDefinitionOptions` rather than merely renamed. That slice hit the ESM/CJS boundary for the first time — a viewer `.ts` importing `@noodl/runtime/src/utils` pulled a CommonJS runtime file into the viewer's ESM program; fixed by giving that module named exports, and the general fix (ship `.d.ts` from `@noodl/runtime`) is queued as its own slice. Slice 3's `inputs`/`outputs` types were wrong for React nodes and its first consumers proved it — `ReactInputDefinition`/`ReactOutputDefinition` added. New findings: `Noodl.runDeployed` is never set, so deployed bundles still carry editor-only tooltip HTML (the real flag is `Noodl.deployed`), and `propPath` on an ordinary input is inert. Viewer file counts 107/31/35 → **97 `.js` / 41 `.ts` / 35 `.tsx`**; `catalog:check` byte-identical at every step. **Still owed: a live editor pass** — blocked while PLAT-002 has the editor mid-conversion. Next: remaining viewer nodes (std-library, navigation) — see PLAT-003-NOTES.md §12. **Slice 9 merged `e40b0df` (2026-07-27):** the viewer's `src/` root (17 of 18), all of `api/`, `noodl-js-api` and `constants/flex` — ~4,000 lines, annotated not renamed; viewer now **21 `.js` / 10 `.jsx` / 118 `.ts` / 37 `.tsx`**. Three defects the compiler refused to let stand: `Noodl.Arrays`/`Noodl.Objects` Proxy `set` traps returned nothing, so assignment **throws in strict-mode callers after the write has already succeeded**; TextInput's `ref={(ref) => (this.ref.current = ref)}` returns the element, which React 19 reads as a cleanup function (live since RUN-001); `NavigateArgs.hasNavigated` declared required though user JS never passes it. Seven published types corrected (two were outright wrong: `PortTooltip` has an undocumented keyed form, `InputPortDefinition.tab` is not a string), and **`interface Window { Noodl }` had never been in effect** — `global.d.ts` has a top-level `import`, making it a module; fixing it retired six `@ts-expect-error`s, i.e. spec step 9's category, at the declaration. **§20's premise was wrong: `nodes-deprecated/` is NOT unregistered** — `register-nodes.js` registers all 25 (verified), every one is in the committed catalog as `providedBy: noodl-viewer-react` with `inNodePicker: false`, and the prod bundle ships them; that flag *is* the back-compat mechanism, so deleting them would break existing projects silently at load. **Recommendation: keep and convert in slice 10.** `@noodl/runtime` `.d.ts` did not fit, but its blocker is now named concretely: not the tsconfig (`allowJs` is already on) but the absence of a `types` field and of any build step — `main` points at source, which four packages consume. Slice 10: `nodes-deprecated/` (25), the 3 Group scroll plugins, `register-nodes.js` **last** (its imports name explicit `.ts`/`.tsx` extensions, `TS5097`, and it is the CJS/ESM bridge), the runtime `.d.ts` slice, and a decision on six deprecated controls hidden from the picker yet reporting `isDeprecated: false`. Gates: `catalog:check` byte-identical, viewer jest 52/52, all bundles green. **Owed: the live editor/viewer pass** (`lerna exec` drives the main checkout, so it cannot be run from a worktree) **Slice 10 (`6491dad`)**: all 25 `nodes-deprecated/` files → `.ts`/`.tsx`, so **every node file in `noodl-viewer-react` is now TypeScript**; `NodePanels` was published as the wrong shape entirely (a `Record`, when it is a list or the literal `'none'`); the Globals node was found to throw on every global change. **Slice 11 (`82eec928`) moves the task into `@noodl/runtime`**: all 24 non-`data/` files under `src/nodes` → `.ts` (~3,600 lines), runtime **76 `.js` / 46 `.ts` → 52 / 71**. Preceded by its own behaviour-fixing commit (`2fecd32`) closing the two hard failures slice 10 had only documented — Globals' `TypeError` and DbModel's New action, which called a `storageNew` that `git log -S` shows has **never existed in this repo**, so the port has thrown since the OSS release; implemented to match the sibling Model node exactly, with characterisation tests proven able to fail. Slice 11 published **`RuntimeVisualNode`**, the child-tree protocol that `ReactNodeInstance` had been describing locally all along (one contract, two descriptions), plus `ComponentModelLike.getInputPorts()`/`getOutputPorts()` (uncallable through the index signature — the §19.3 shape, fifth occurrence) and an ambient `_noodl_cloud_runtime_version` that seven runtime modules branch on. Nine more defects documented, the worst being that **Run Tasks leaks every live task component when deleted mid-run** — its `_onNodeDeleted` sits at the definition's top level, and `nodedefinition.ts` installs only `methods`/`prototypeExtensions`. Gates: `catalog:check` byte-identical (154 types, 89 dynamic), runtime jest **959 pass / 0 fail**, viewer jest 59/59, all typechecks clean, **`any` contribution zero** (the red `tsfixme` gate is entirely AIX-005's `1a5f7b4`, deliberately not baselined under this slice). Owed: viewer prod bundle and live pass, both blocked by an AIX-008 workstream mid-flight in the same checkout **Slice 13 (`68f55130` fixes, `88a30806`, `c364c781`), 2026-07-27 — the runtime's infrastructure, and the boundary that stops the rest.** Ten leaf modules + `api/queryutils` → `.ts`; `noodl-runtime/src` **28 `.js` / 97 `.ts` → 18 / 107**. `timerscheduler` and `models/nodemodel` were the cheap-and-revealing kind — both already *described* in `@noodl/types`, so conversion is making the implementation agree with its own published type, and it found that **`GraphPortModel.plug` admitted two values where four reach it** (`'input/output'`, plus `'outputs'`, a misspelling real exported projects contain). `api/adapters/types` had been a wall of `@typedef` exporting `{}` — the types existed only as comments and nothing could reference them. **The behaviour-fixing commit came first and test-first, proven able to fail (2 of 5 tests fail against the pre-fix source):** Query Records' `error` output had never carried a message (setter wrote `_internal.err`, getter reads `_internal.error` — §23.4's defect repeated in the node that *replaced* the one it was found in); Query Records leaked its `create` listener (`_onNodeDeleted` unsubscribed `insert`, a name nothing emits, so a deleted node stayed reachable); and HTTP Request's four debug `console.log`s are gone, one of which printed built request headers and so put Bearer/Basic credentials in the browser console. **The headline finding:** `noodl-viewer-react` compiles at `module: es6` while `noodl-viewer-cloud` compiles at `module: CommonJS`, so a runtime `.ts` reached by an ESM `import` from a *react*-viewer `.ts` is type-checked inside that ESM program — where `import … = require()` is `TS1202` and the runtime's ambient `src/globals.d.ts` is out of scope. That is why `nodescope.ts` has used both idioms since slice 2 without trouble (only the *cloud* viewer imports it) and why `cloudstore` broke instantly. `api/cloudstore`, `api/records`, `api/configservice` and `api/cloudfile` were converted, typechecked clean, produced 30+ bundle errors, and are **reverted to `.js` deliberately**; `model`, `collection` and `javascriptnodeparser` sit on the same boundary. Seven modules — the seven the previous slice ranked highest. `allowJs: false` in the viewer tsconfig is why the `.js` majority is unaffected: **converting a file is what puts it in that program.** This **reopens** the "`@noodl/runtime` ships `.d.ts`" item that slice 12 recommended closing as dead — it is in fact the fix. **The `typecheck:viewer` gate could not have caught it**: its filter since slice 5 counts only `packages/noodl-viewer-react/src/`, and every error here was attributed to a `packages/noodl-runtime/…` file — §13.1's trap a third time; the prod bundle is the gate that decides, and `lerna exec … npm run build` can exit 0 with a failed webpack. Also published `ModelLike._class` (set by `CloudStore._fromJSON`, what lets `Noodl.Records.save(id)` work from an id alone, reaching ~12 readers as `unknown` — the §19.3 shape, sixth occurrence) and the `_noodl_cloudservices` ambient. Three defects documented not fixed, the worst being that `matchesQuery`'s `$lte` branch compares against `$lt`, so **local and server-side filtering silently disagree**. Gates: `catalog:check` byte-identical (154/89); runtime, viewer, cloud, editor typechecks 0 under the *corrected* filter; **viewer prod bundle green, 0 `tsl` errors**; runtime jest **973 / 0**; nodegx-backend build green + 619 specs; **live editor pass green**, clearing the debt slices 11 and 12 both owed; `any` contribution zero. **Remaining: 11 convertible modules (itemised in NOTES §30) and 7 blocked ones.** |
| PLAT-004 | `TSFixme` burn-down | In progress — **gate green, baseline 972** (2026-07-27, slice 7) | Ongoing | **Slice 7 merged `b22cfab0`, re-baselined `436cd080`.** The gate had gone red again — `any` **522 vs 465**, `TSFixme` 546 vs 545 — and effectively all of the rise was the cluster §14.6 had already named: AIX-005's agent nodes and the six suites that drive them. The ten *growing* files held 64 markers; the cluster held **184**, all of one cause, and clearing only the 64 would have left the shape to re-grow. One cause: a definition's callbacks are typed `this: NodeInstance`, the published surface, which knows nothing about the `methods` that definition installs beside them — so a method calling a sibling wrote `(this as any).addChunk()`, a test driving one wrote `(node as any).handleFrame(…)`, and a test reaching graph wiring wrote `(receiver as any).connectInput(…)` **even though `RuntimeNode` has described that last one since PLAT-003 slice 2**. Named once in `agent/node-instances.d.ts`; a declaration file because these modules end in `export =`, which forbids exporting anything else beside it. **The trap worth knowing: every `*Internal` there is a `type`, not an `interface`, and must stay one** — `NodeInstance._internal` is `Record<string, unknown>`, only a type alias gets TypeScript's implicit index signature, and an interface makes the node unassignable to `NodeInstance` so *every* callback in the definition fails at once (which is why `globalstorenode.ts` could narrow `_internal` inline and these five could not). `sse-connection.ts` got the `*Like` seam vocabulary its sibling `websocket-connection.ts` already had; along the way `SseTransportEnv` was found **exported and dead** while the same six seam fields were declared three times, and `SseRequestInit` now names what the fetch transport actually sends (the suite asserts on `init.headers.Authorization`, previously unchecked). Four suites had each grown the same harness — now `test/helpers/node-harness.ts`; the transport doubles stay deliberately unshared, per the suites' own isolation note. Editor side: `NodeLibraryData.projectsettings` was `TSFixme` and the Settings-panel model held four more; both now use `NodeLibraryProjectSettings`. Totals **`TSFixme` 545 → 540, `any` 465 → 336**; baseline lowered to 972. Gates: runtime jest 973/0, editor Jasmine **1573/0**, mcp/preview/backend/cloud/viewer suites green, `catalog:check` byte-identical (154/89), **viewer prod bundle 0 `tsl` errors**, six typechecks clean (core-ui/preview/viewer standalone `tsc` carry 45/30/40 pre-existing errors, verified unchanged against a stashed clean tree). **And the live suite was actually run** — `mock-agent-server.mjs` on :4831, `NODEGX_AGENT_LIVE=… jest test/agent-live-endpoint`, **7/7** — which is the only thing that can prove a seam type describes the platform API it stands in for. Two process findings: `npm run tsfixme:baseline` from this checkout would have swept **22 uncommitted files from a concurrent session** into `byFile` (7 of them untracked, so permanently free) — baseline from a detached worktree at the recorded commit instead; and `RuntimeNodeContext.nodeRegister` is still `any` despite `noderegister.ts` having declared the full interface since slice 2, which is what leaves the single remaining cast in the new harness. See PLAT-004-NOTES §15. Earlier slices follow: **Mechanism complete** — `scripts/tsfixme-ratchet.js` + `.tsfixme-baseline.json`, wired into the `lint` job in `pr.yml`, policy documented in CODING-STANDARDS.md, clustering report at `dev-docs/reference/TYPE-ESCAPE-HATCHES.md`. Baseline now **568** `TSFixme` at `0395b24` (581 when the mechanism landed at `3a302b9`), 392 `any`, 22 `@ts-ignore`, 88 `@ts-expect-error`, each gated separately so trading one marker for another still fails. Note the phase-start figure was 554: the count **rose** during Phase 14 even as PLAT-002/003 removed markers, because new work added them faster — which is the argument for the ratchet. Counter uses the TS parser, not grep (a scanner-based draft silently lost comments in 8 files); verified against per-file grep across all 1,549 files. First burn-down slice done: `noodl-preview` 16 → 3 `TSFixme`, the 3 remaining documented in place as blocked on the untyped exporter; typing the test helpers required naming the preview server's HTTP contract (`PreviewStatus`/`PreviewEvent`) and surfaced four real defects in its specs. The rest of the burn-down is deferred to PLAT-002/003, whose files it deliberately does not touch. See PLAT-004-NOTES.md **Slice 6 merged `dadb76c`, reconciliation `c98e4c8`.** The gate had gone red unnoticed: `any` was **557 against a baseline of 287**, effectively all of it `packages/nodegx-backend`, which did not exist at the `78ba241` baseline. Typed rather than re-baselined: **nodegx-backend 108 `any` → 16**. The cause was the same shape slices 1, 3 and 5 each found from a different direction — fifteen spec files each hand-rolled the same request helper declaring `let json: any = null`, which leaked into ~60 downstream `(x: any)` reads, so a field rename on either side of the wire would have kept every one of those specs green. One typed `request<T>` at `tests/helpers/http.ts` replaced all fifteen. Three producers were also declaring less than they knew (`RealtimeHub`'s three SSE payloads under one `data: unknown`, `ExecutionHistory`'s `store: unknown`, `ChangeBus`'s `adapter: any`). Defects the types exposed: `findRole()` returned `Record<string, unknown>` so `role.objectId` (typed `unknown`) went into three relation calls — compiling only because `schemaManager` was `any`, meaning a malformed row would have addressed nothing, silently; `deleteTable` is feature-detected elsewhere but called unguarded by `POST /admin/schema`; `createLogger()` can return null. New `typecheck:backend-tests` gate wired into CI — unlike the editor's equivalent it did **not** start at zero (32 real errors, now 0). **Also closed §10's last PLAT-004-owned item**: the re-baseline step is now in PLAT-002's and PLAT-003's definition-of-done. Deliberate raises are itemised in NOTES §14.6 (`noodl-runtime` +128 and `noodl-viewer-react` +29 are PLAT-003 slice-10 territory plus live agent-SSE work; the backend's residual 16 are genuinely unknowable — untyped `LocalSQLAdapter`, `node:sqlite` via `getBuiltinModule`, three `globalThis` shims). Baseline now **545 `TSFixme` / 461 `any` / 17 `@ts-ignore` / 0 `@ts-nocheck` / 79 `@ts-expect-error`**, verified holding at merge. Open: `AdapterFacade.schemaManager` genuinely can be `undefined` while fifteen call sites assume presence — declared non-optional with the reason in place rather than change fifteen routes' behaviour inside a typing slice. `noodl-runtime` is now the repo's largest single `any` cluster (185, 65 of them the unclaimed agent-SSE nodes); PLAT-003 slice 10's scope should be checked against that |
| PLAT-005 | Editor UX loose ends | In progress — **live QA owed** (2026-07-27) | 2–3 wks | Implementation merged `a77e32a`; see PLAT-005-NOTES.md. The task's premise was stale twice over: the STYLE-005 banner was already wired (known), **and the re-scope note's prescribed fix was itself wrong** — `_variant` is a marker for the compile-time `ElementConfigRegistry`, a module-level `Map` built at import from four hardcoded files and never written to the project, so adding `ElementConfigRegistry.addVariant` would have created nothing either. "Save as Variant" now goes through the real system (`ProjectModel.variants` / `VariantModel` via `NodeGraphNode.createNewVariant`) with name uniquification, and verifies rather than returning an unchecked `true`. Two bugs underneath it: `createNewVariant`'s undo called `deleteVariant(name, type)` but `deleteVariant` matches by **object identity**, so undo left an orphan variant and redo then no-opped; and `applyTokenAction` was **not undoable at all**, making a 30-parameter rewrite a one-way door. Both fixed. StyleAnalyzer verdict — sound in outline, noisy in practice: four user-visible defects fixed (token-name collisions where `1.5rem`/`15rem` both became `--spacing-15rem` and the second silently overwrote the first; `count` reported occurrences not elements; zero/unitless values were tokenisable, writing the *string* `var(--spacing-1)` into a numeric port; every variant named `custom`). Dashboard-routing bug fixed and **the cause in three documents was wrong** — not TASK-001B's store migration but `Launcher.tsx` assigning `url.pathname` on a host-less `file:` origin, producing `file:///dashboard/projects` in session history. 41 new specs. Left with recorded decisions: cross-property bucketing, `width`/`height` in `SPACING_PROPERTIES`, and `isRawSpacingValue` deliberately not narrowed (it would move AIX-002's raw-vs-on-system measurement). CLEANUP-000H + phase-3/0 triage cut as stale. **Owed: the live click-through in NOTES §6 — the save/close/reopen step is the only one that proves persistence, plus a regression pass on the existing Variants editor, which shares the changed code.** |
| PLAT-006 | Ship type declarations from `@noodl/runtime` | Not started (filed 2026-07-27) | 3–5 days | PLAT-003's named residual — decide the NOTES §29.2 module boundary by making the react viewer resolve runtime *declarations* instead of compiling runtime sources, then convert the seven blocked modules (`model`, `collection`, `javascriptnodeparser`, four `api/` files) and the untyped entry point. The hard part is the resolution mechanism: sibling `.d.ts` files do NOT work (`tsc` prefers `.ts`). Spec: [PLAT-006-RUNTIME-DECLARATIONS.md](./PLAT-006-RUNTIME-DECLARATIONS.md). |

## Baseline measurements (2026-07-22)

Captured so progress is provable rather than asserted:

| Metric | Value at phase start |
|---|---|
| jQuery-referencing files | 14 (all in `noodl-editor`; several are vendored bundles) — the real figure at PLAT-002's start was **68 source files / 547 `$(` calls**; now **0** |
| `src/shared/view.js` subclasses | ~21 |
| `TSFixme` occurrences | 554 |
| `nodegrapheditor.ts` | 3,481 lines |
| `views/popuplayer.js` | 1,043 lines (~101 `$(` calls) — now TypeScript, 0 |
| `noodl-runtime` | 98 `.js` / 6 `.ts` |
| `noodl-viewer-react` | 131 `.js` / 32 `.ts` / 35 `.tsx` |
| `noodl-core-ui` | 1 `.js` / 151 `.ts` / 235 `.tsx` (already modern) |

Re-measure these at each task's completion and record the delta in that task's CHANGELOG.

## Blockers

- All large tasks here should wait for **REV-003** (CI merge gates) — refactoring at this scale without automated regression detection is how contained debt becomes real damage.
- ~~**`cline-dev` tip is red**~~ — **cleared 2026-07-27, no action needed.** For a window, `packages/noodl-editor/tests/nodepicker/NodePickerReducer.test.ts` (committed in `78df460`, UIX-012) imported `NodePicker.selectors`, a module that did not exist in the repo, so `typecheck:editor` failed with 6 `TS2307`s and the editor's Jasmine suite could not build. Both PLAT-003 and PLAT-005 hit it from worktrees based at `7779cd6` and worked around it (PLAT-005 via a temporary scoped webpack entry: 474 specs / 2 failures, both in `tests/nodegraph/export.js`, proven pre-existing). **UIX-013 (`8b1886d`) then replaced that spec** with one importing `NodePicker.reducer`, which exists. PLAT-004 confirmed both `typecheck:editor` and `typecheck:editor-tests` clean at `678d1c0`. Kept here because the failure mode recurs: a spec committed ahead of the module it imports is invisible to the session that has both on disk.
- The **runtime jest baseline is 384 / 0**, not the 247 / 0 recorded during phase 14.5 — measured at `7779cd6` both before and after slice 9. The older figure is stale, not a regression.
- `terminal-link` is missing from this install and `@jest/reporters` needs it *only to print a failing suite's header*, so the runtime suite dies opaquely the moment anything fails. Worth fixing properly.
