# Defects the field report found — phase 84 register

Per [PHASE-EXECUTION.md](../../guidelines/PHASE-EXECUTION.md):

> **A defect becomes the next session's first job ONLY if it blocks an acceptance criterion.
> Otherwise it is filed here with an owner, and the next session builds the next task.**

🔴 **Every row carries an owner or the literal word `NONE`, and a disposition of `BLOCKS <AC>` or
`BACKLOG`.** An unowned row gets rediscovered at full price. Say what was **measured**, with the
instrument, not what was concluded.

## 1. Found while scoping — not in any issue

These came out of re-measuring the issues. **None of them is an open GitHub issue.** Two are more
serious than most of what is filed.

| id | what was measured | owner | disposition |
|---|---|---|---|
| N1 | ✅ **MEASURED AND FIXED 2026-09-10 (FLD-009).** Both halves of the row were true at HEAD. ⚠️ **One word of it was wrong and it changed the acceptance criterion:** `saveProjectLevelFiles` does not write *unconditionally* — it writes unconditionally **once its own built hash has moved**, and a component edit never moves it. So AC1's sequence (agent binds a backend, person touches a component, autosave) **does not lose the binding at HEAD and never could**; the loss needs an editor change to a project-level file. Measured three arms on a `MemFs` double before any fix: component edit 🟢 survives, `rootNodeId` change 🔴 binding gone, `rootNodeId` change 🔴 design tokens gone | **FLD-009** | ✅ **CLOSED** — guard, watcher mapping and reload built; see FLD-009 §4b |
| N2 | 🔴 `noodl-runtime/src/node.ts:135` seeds a units port as `{value, type: defaultUnit}`; `:402` reads `.unit`. Present since `b9c60b07d`. Found independently by two research passes, from #26 and from #22 | **FLD-004** | **BLOCKS FLD-004 AC4**, and FLD-003 |
| N3 | 🔴 `withCreationDefaults` (`noodl-mcp/src/tools/author.ts:164-177`) returns a node unchanged when its id is already in the component, and the `set` door replaces the node list wholesale (`:647`). **An agent re-sending its own graph reverts every server-added creation default and seeds only the new node.** Reproduced: setting `useLabel` on the last radio only emits the reporter's exact markup byte-for-byte | **NONE** | BACKLOG — **this is the real defect behind [#25](https://github.com/The-Low-Code-Foundation/NodeGX/issues/25); it deserves a task** |
| N4 | `nodegx-export/tests/visual-controls.test.ts:319-357` asserts the `<label>` wrapper for the **checkbox** (`:320-322`) and, for the radio (`:337-341`), only the group name and `defaultChecked`. The label assertion the radio needed is the missing one | **NONE** | BACKLOG — closes with N3 |
| N5 | The emitter demands `useLabel === true` (`emit/component.ts:5092`) while the runtime accepts any truthy (`RadioButton.tsx:105`). A `"true"` string renders a label in the app and none in the export. All 538 local values are proper booleans, so **latent, not observed** | **NONE** | BACKLOG |
| N6 | `emit/component.ts:5092` uses `effectiveLiteral` (catalog-default fallback) while `emit/style.ts:859` uses `literal` (no fallback) for the same flag — if a catalog default flipped, JSX would emit a `<label>` with no wrapper class | **NONE** | BACKLOG |

## 2. Reframed — filed against something other than the defect they describe

🔴 **These are not in this phase's fifteen.** Each needs a reply saying what we measured, and three
of them hide a real gap that is smaller or different than the issue claims.

| id | issue | what re-measurement found | owner | disposition |
|---|---|---|---|---|
| C25 | [#25](https://github.com/The-Low-Code-Foundation/NodeGX/issues/25) | Not an export bug. The emitter branch is per-node with no last-child logic and is faithful to a flag that defaults false. The real defect is **N3** | **NONE** | BACKLOG — reply owed |
| C27 | [#27](https://github.com/The-Low-Code-Foundation/NodeGX/issues/27) | No mechanism found. `registerInput` **throws** on a real duplicate (`node.ts:124-127`), so a collision would be loud; the corpus has **zero** instances across 67 examples; and the second example cannot exist — no catalog node has both `size` and `color`. Most likely #26 wearing a different hat | **FLD-004** | BACKLOG — 🔴 **re-measure against the reporter's project after FLD-004 lands, before building `input-shadows-root-port`** |
| C30 | [#30](https://github.com/The-Low-Code-Foundation/NodeGX/issues/30) | The handler exists (`autoupdater.js:351`) and is tested; `setupAutoUpdate` returns early on Linux at `:179-181` **with a comment explaining why**, and the renderer's catch is deliberate (`useUpdateState.ts:72-75`). The real gap: **Linux has no update-notification path at all** | **NONE** | BACKLOG — reply owed; re-title, don't close |
| C34 | [#34](https://github.com/The-Low-Code-Foundation/NodeGX/issues/34) | "Only `value` plus min/max" is **false**: 34 inputs including `flexDirection` and `cssClassName` (the latter comes to every React node at `react-component-node.ts:1024-1045`), against Group's 94. Genuinely missing: `rowGap`/`columnGap`, `alignItems`, `justifyContent`, borders, shadows, backgrounds — and those are **not in a mixin**, they are inline in `group.ts:270-375` | **NONE** | BACKLOG — the clean shape is extracting `addFlexInputs` and calling it from both |
| C13 | [#13](https://github.com/The-Low-Code-Foundation/NodeGX/issues/13) | ~~The mechanism asked for **already exists and gates every PR** (`pr.yml:87`). Real parser count is **582**~~ 🔴 **BOTH HALVES CORRECTED s10 — see P30/P31.** The mechanism exists at `pr.yml:87` and **has gated nothing since 2026-08-07**: CI's last green run is that day, and since 2026-09-09 every job died at `npm ci` before reaching it. And **582 is the ratchet's committed BASELINE, not a count** — the scanner reads **819** TSFixme and **806** bare `any` today. The shape is [[a-client-property-read-as-a-fact-about-the-source]]: a number lifted from the gate's config and reported as a measurement of the tree | **NONE** | BACKLOG — reply owed, and it must now carry the drift rather than the reassurance |

## 3. Ready to close — ✅ **ALL FOUR SENT AND CLOSED, 2026-09-10 (session 6)**

Every verdict below was **re-verified at file:line before the reply went out**, and one of them moved: all
four fixes turned out to be in the **published 0.2.2** already (checked with `git show v0.2.2:<path>`),
not pending in 0.2.3 — which is a much better answer than the one this table was going to give.

| issue | verdict | evidence |
|---|---|---|
| [#9](https://github.com/The-Low-Code-Foundation/NodeGX/issues/9) | Fixed | `author` carries an RFC-5322 name+email; fixed in `ee6223c3c`; the Linux release leg has shipped `.deb` since |
| [#12](https://github.com/The-Low-Code-Foundation/NodeGX/issues/12) | Done | `.nvmrc` 22, root and editor `engines >=22.0.0`, `nodegx-backend >=22.13.0`, CI on `.nvmrc`, Electron 43 bundles Node 22 |
| [#1](https://github.com/The-Low-Code-Foundation/NodeGX/issues/1) | Fixed | `dbmodelcrudbase.ts:994` (unset target means `user`) and `:1013` (an unresolvable user contributes nothing rather than a literal `undefined` key). One mixin, so Create **and** Set Record Properties. Arms at `nda-012-data-record-family.test.ts:159-231` |
| [#15](https://github.com/The-Low-Code-Foundation/NodeGX/issues/15) | Obsolete | `AiAssistantStore.ts:57-70` migrates `full-beta → openai`; the page was rebuilt around a provider picker; verify is now a real `GET /v1/models`; keys moved to OS encryption |

## 4. Passing findings — measured, not chased

| id | what | owner | disposition |
|---|---|---|---|
| P1 | ✅ **CLOSED by FLD-012 (session 6, 2026-09-10).** `visible` now excludes `opacity: 0` and `visibility: hidden`, and reports `visibleCount` / `transparentExcluded` so the population change is readable rather than inferred. **It removed 4 findings across the 20-project corpus, all `flat-type-scale`** — those pages had ten text elements of which one was invisible, and that rule is gated on `>= 10`. ⚠️ **Two corrections to this row's own wording:** the accent area/`single-column-grid` inheritors read **0 in both arms** on the corpus (never exercised — they grade nothing), and *"rhythm bands"* is **not a rule at all**: `v.rhythm` is measured from `visible` but no finding in `summarise` reads it | **FLD-012** | ✅ **CLOSED — measured, not asserted** |
| P2 | A full-width Slider's `<input>` is `width:100%`, height ≥ 8, so it qualifies as a **band** and pollutes the vertical-rhythm spacings | **NONE** | BACKLOG — ⚠️ **still open after FLD-012**, and re-measured: the bands are computed but **nothing consumes them**, so this pollutes a number no rule reads. Cheap to fix, and worth doing before any rule is attached to `v.rhythm` |
| P3 | `validation/queryBeforeFilter.ts` fires only on `DbCollection2`, **not** on `noodl.cloud.aggregate`; and the aggregate node has no `runOnValueChange` gating, so it fetches at graph-build time | **NONE** | BACKLOG — ⚠️ **still open after FLD-008.** FLD-008 fixed what the node does with a *refused* filter; it did not touch **when** the node fetches |
| P4 | `utils/electron/appFocusTimer.ts` is `@deprecated` with **zero call sites** | **NONE** | BACKLOG — delete |
| P5 | `.github/workflows/test-platform-node.yml:30` uses `actions/setup-node@v2` where every other workflow uses the shared action | **NONE** | BACKLOG |
| P6 | `nodegx-export/src/ledger.ts:83` docstring says "97 of the 127"; the computed values are `pickerCoverageFloor: 117` / `pickerCoverageTotal: 127` | **FLD-013** | BACKLOG |
| P7 | The Columns runtime uses a strict `<` where the exported `@container max-width` is **inclusive** — the two disagree exactly on a boundary value | **FLD-002** | BACKLOG |
| P8 | `relay-token` is written (`relay-token.js:69`) and **never unlinked on quit** — a stale file authenticates nothing and proves nothing about a live editor | **FLD-010** | BLOCKS FLD-010 AC4 |
| P9 | 🔴 `WIRED_STYLE_SINKS` (`emit/style.ts:270-274`) has three entries, so a wire into `width`/`height`/`paddingTop`/`borderColor` is never visited and never reported. **Phase 83's HLS-005 owns this table** | **HLS-005** (phase 83) | ✅ **RESOLVED 2026-09-11 (s13) — PHASE 84 OWNS IT.** Richard, asked directly. FLD-015 builds here and the `WIRED_STYLE_SINKS` table comes with it; phase 83 is closed. ⚠️ **This does NOT unblock FLD-015 — R2 still gates it.** P9 was one of two blockers and only the cheaper one has moved |
| P13 | 🔴 **FLD-005 and phase 81's VIB-005 are the same defect in two phases.** V1 is `🔴 open` at `phase-81/README.md:173` with owner **VIB-005** (`:149`), listed `⬜ startable now` in phase 81's handoff, owning **V1/V2/V14/V17/V21/V38** (`NEXT-SESSION-PROMPT.md:73`). ⚠️ An earlier reading of this attributed V1 to REL-002a in phase 82 — **wrong**: REL-002a's *"Registered, not built"* (`:211-226`) registers an inert `scrollEnabled` and records `clip: true` as NOT REPRODUCED, both owner `NONE` | **FLD-005** | ✅ **RESOLVED 2026-09-11 (s13) — PHASE 84 OWNS IT.** Richard, asked directly. FLD-005 builds here; phase 81's VIB-005 row **V1 closes as a duplicate** pointing at FLD-005. 🔴 **P13 was FLD-005's ONLY blocker, so the task and [#35](https://github.com/The-Low-Code-Foundation/NodeGX/issues/35)'s reply are now fully unblocked** |
| P10 | This checkout carries ~16 MB of stray macOS duplicate dirs under `noodl-editor/src/external/` (`deploy 2/`, `viewer 3/`, `ssr 3/`, `cloudruntime 3/`). Gitignored, but `build.files` includes `"src"` wholesale | **FLD-017** | BACKLOG — delete locally |
| P11 | ✅ **ANSWERED 2026-09-10, and the answer is that it is the one project-level file that was never exposed.** `ComponentSaver.updateRegistry` (`ComponentSaver.ts:289-332`) **re-reads the registry off disk and merges** its change set into it, so an entry an agent added is not overwritten by the editor's next save — unlike the three files FLD-009 fixed, which were rebuilt wholesale from memory. It stays out of the watcher mapping deliberately: a component an agent *adds* reaches the canvas by its own three files, which the component mapping already reports | **FLD-009** | ✅ **CLOSED — excluded, with a measured reason** |
| P12 | The three runtime copies under `src/external/` total **~46 MB**, not the ~4.8 MB the issue estimated | **FLD-017** | BACKLOG |

## 5. Replies owed — the phase does not close until this table is empty

🔴 **The table was SHORT BY TWO on 2026-09-10 (#21, #41) — and then SHORT BY EIGHT AGAIN.** A
reply table that does not list an issue reads as a reply that is not owed, and patching it one row
at a time is how it stayed wrong twice.

✅ **Fixed by a sweep, not by a row.** Session 5 ran every task file's own issue number against this
table: 17 task files carry 15 distinct issues, and **eight of them had no row** — #22, #26, #29,
**#32**, **#33**, #35, #39, #42. README §6 says *every task in this phase carries a reply*, so each
of those was a reply this table said nobody owed. All eight are in it now, and all eight were
confirmed **OPEN on GitHub** on 2026-09-10 (`gh issue view`), not assumed.

🔴 **Re-run the sweep, do not read the table.** It is one command, and it has caught the same
omission twice:

```sh
cd dev-docs/tasks/phase-84-the-defects-the-field-report-found
for f in FLD-*.md; do grep -oE '\[#[0-9]+\]' "$f" | head -1; done | sort -u   # issues with a task
awk '/^## 5\./,/^## 6\./' DEFECTS-THE-FIELD-REPORT-FOUND.md | grep -oE '^\| #[0-9]+'  # issues with a row
```

⚠️ **The count does not reconcile, and that is a real finding, not a typo.** README §6 says
*fifteen* issues; this table now holds **24 rows** — 15 that carry a task plus 9 that are answered
without one (#1, #9, #12, #13, #15, #25, #27, #30, #34). Whichever number is right, "fifteen" is not
the number of replies owed, and the end condition should be read off **this table**.

✅ **No approval is needed to send these.** Richard granted standing authorisation 2026-09-10:
post and close from his account, no ask. Every reply carries two mandatory things — a first line
saying it is an automated reply generated from Claude, and **the release the fix ships in**
(0.2.3 as of 2026-09-10; re-derive it, do not inherit the number).


The end condition in [README.md](./README.md) §6 is that the reporters have been **told**. A fix
nobody was told about gets re-reported.

| issue | reporter | owed | sent |
|---|---|---|---|
| #9 | @nikdjukic | Closed as fixed, naming `ee6223c3c` and the `.deb`/`AppImage` targets. Also told them their Node 18 is below the floor now (→ #12) | ✅ **replied + CLOSED 2026-09-10** ([comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/9#issuecomment-5617423117)) |
| #12 | @SgtSpork | Closed as done, naming `.nvmrc` 22, the three `engines` blocks, CI reading `.nvmrc`, and Electron 43's bundled Node 22 | ✅ **replied + CLOSED 2026-09-10** ([comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/12#issuecomment-5617424100)) |
| #1 | @richardosborne14 | Closed as fixed, both bugs: the untouched dynamic `-target` port, and the `acl['undefined']` string key that makes a record unreadable by everyone including its author. Named the two controls so the fix cannot read as "rules get dropped now" | ✅ **replied + CLOSED 2026-09-10** ([comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/1#issuecomment-5617432638)) |
| #15 | @echelonsoftdm-source | Closed as **overtaken by the rebuild, not as fixed-and-verified** — their symptoms 1 and 2 have named replacements (provider picker + migration; `Test connection` against `/models`), but symptom 3 was never reproduced here and the reply says so and invites a reopen. 🔴 The #41 lesson applied in the other direction: do not claim a verification you did not do | ✅ **replied + CLOSED 2026-09-10** ([comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/15#issuecomment-5617433614)) |
| #5 | @VitoMinheere | the one-line cause **and** where to patch a lesson — both halves, asked in 2024. 🟢 Fixed `4068d139` | ✅ **SENT + CLOSED 2026-09-10** — [comment 5615685302](https://github.com/The-Low-Code-Foundation/NodeGX/issues/5#issuecomment-5615685302) |
| #14 | @theMeysam | the `and:` rewrite that unblocks them **today**, plus the swallowed-error fix. 🟢 Fixed `d1daabb1` (FLD-008) | ✅ **SENT + CLOSED 2026-09-10** — [comment 5615685570](https://github.com/The-Low-Code-Foundation/NodeGX/issues/14#issuecomment-5615685570) |
| #21 | @richardosborne14 | both defects, and that BOTH change behaviour on existing projects. 🟢 Fixed `3c13818d` (FLD-001) | ✅ **SENT + CLOSED 2026-09-10** — [comment 5615685029](https://github.com/The-Low-Code-Foundation/NodeGX/issues/21#issuecomment-5615685029) |
| #41 | @dishant-kumar-thakur | 🔴 **REPLIED, DELIBERATELY NOT CLOSED.** The overwrite half is fixed (`fa227028`, FLD-009); the `session_status` tool it actually asks for is **FLD-010, unbuilt**. Closing it would have closed an unbuilt feature request on the strength of a different fix | ✅ **SENT 2026-09-10, ISSUE STAYS OPEN** — [comment 5615685841](https://github.com/The-Low-Code-Foundation/NodeGX/issues/41#issuecomment-5615685841) |
| #25 | @dishant-kumar-thakur | 🟡 **REPLIED, DELIBERATELY NOT CLOSED — it is the tracking thread for N3, which is unbuilt.** The reply gives the whole chain, each link read from source: the emitter's label branch is strictly per-node (`component.ts:5140`) and faithful; `useLabel: true` is a DEF-025 **creation default** for radio and checkbox (`newNodeSeed.ts:83-103`); `withCreationDefaults` skips a node whose id already exists (`author.ts:164`, correct in intent); and the `set` door then replaces the stored node list with the client's wholesale (`author.ts:646-655`). So **every server-added creation default is reverted on every re-send and only the newest node keeps one** — their exact markup. 🔴 **The reply also names the one thing the mechanism does NOT explain and asks about it**: their step 2 says all three labels showed in the editor preview, and `RadioButton.tsx:193` gates the label on the same flag, so it should have shown one | ✅ **SENT 2026-09-10 (s10), ISSUE STAYS OPEN** — [comment 5623309452](https://github.com/The-Low-Code-Foundation/NodeGX/issues/25#issuecomment-5623309452) |
| #27 | @dishant-kumar-thakur | 🟡 **REPLIED AS A QUESTION, NOT A VERDICT, AND LEFT OPEN.** Re-verified before repeating: `registerInput` **throws** on a real duplicate (`node.ts:124-127`), so it cannot be a quiet overwrite at that layer; **zero** nodes in the catalog have both `size` and `color` (`Circle` has `size`, `fillColor`, `strokeColor`); zero instances of the shape in the 67-example corpus. The reply gives the #26 hypothesis with its reasoning — `Group.height` and `Circle.size` are both **dimension ports**, which is FLD-004's exact defect — and then **asks for the component JSON**, naming the discriminator: does the Component Input reach the root **over a wire** (⇒ #26) or by name alone (⇒ a real shadowing rule). 🔴 Not closed as a duplicate: *"probably #26"* is a hypothesis, and closing a second bug on the first one's fix is [[a-reading-that-fits-is-not-one-that-excludes]] | ✅ **SENT 2026-09-10 (s10), ISSUE STAYS OPEN** — [comment 5623320568](https://github.com/The-Low-Code-Foundation/NodeGX/issues/27#issuecomment-5623320568) |
| #30 | @dishant-kumar-thakur | 🟡 **REPLIED, DELIBERATELY NOT CLOSED.** ✅ The register row was RIGHT and now has the mechanism: `update:get-state` **is** registered (`autoupdater.js:351`) with six siblings, but all seven sit inside `setupAutoUpdate()`, which **returns early on Linux at `:180`, 171 lines earlier**. The renderer already `.catch`es it (`useUpdateState.ts:72-75`) with a comment naming this exact case, so it is **log noise from Electron’s IPC layer, not a broken UI** — which makes both options they offered wrong. 🔴 Open because their actual sentence — *“a user has no way to know a new version exists”* — is TRUE and unfixed: notifying and replacing the binary are separable and we had treated them as one | ✅ **SENT 2026-09-10 (s9), ISSUE STAYS OPEN** — [comment 5619024881](https://github.com/The-Low-Code-Foundation/NodeGX/issues/30#issuecomment-5619024881) |
| #34 | @dishant-kumar-thakur | 🟡 **REPLIED, LEFT OPEN — half the report is false and the other half is real.** Counted off the catalog: **34 inputs / 11 outputs** against `Group`'s 94/31. 🔴 **`flexDirection` IS there and IS effective** — displayed as **`Layout`**, default `column`, calling `setLayout()` + `setStyle({flexDirection})` at `radiobuttongroup.ts:55-58`, so the horizontal control was one port away and half their CSS was avoidable. `cssClassName` is there too. ✅ **Genuinely absent**: `rowGap`, `columnGap`, `alignItems`, `justifyContent`, borders, shadows, backgrounds — declared **inline in `group.ts`** (`:270`, `:291`, `:350`, `:363`), not in a mixin, so the fix is an extraction that touches **`Group`**, the most-placed node in the product. ⚠️ The display-name gap is the interesting half: `flexDirection` under the label `Layout` is discoverable in the property panel and invisible to an agent listing port names over MCP | ✅ **SENT 2026-09-10 (s10), ISSUE STAYS OPEN** — [comment 5623293667](https://github.com/The-Low-Code-Foundation/NodeGX/issues/34#issuecomment-5623293667) |
| #13 | @SgtSpork | 🔴 **The disposition this row carried was wrong in both halves — see P30/P31.** The reply says so: the ratchet exists and is well built, and it has gated nothing since **2026-08-07**; the count is **819 TSFixme / 806 bare `any`**, not 582 (which was the baseline). Carries the CI cause (`npm ci`, the missing workspace package), the fix (`9b3017f8b`, pushed), the per-package table (**93% of the TSFixmes are in `noodl-editor`**, 397 of them under `src/editor/src/views`) and the proposed shape: per-package floors, not a big-bang. 🔴 **Open** — the mechanism existing is not what they asked for; the count coming down is, and it has risen | ✅ **SENT 2026-09-10 (s10), ISSUE STAYS OPEN** — [comment 5623272232](https://github.com/The-Low-Code-Foundation/NodeGX/issues/13#issuecomment-5623272232) |
| #37 | @dishant-kumar-thakur | 🟢 **Fixed (FLD-013, `e51c8c61d`).** Shipped with the ports beside the status, because the field **as asked would have read `translated` on `Circle`** — the type that produced all 23 of their refusals. `export: {status, badge, structurePorts, contentPorts}` on `get_node_type` (unconditional, at the default detail) and on the `list_node_types` rows that have something to say, plus `exportCoverage` on the payload so the silence on the other 107 rows is readable. Reply also carries the hole the work found: **nine of `Circle`'s thirteen structure ports were graded by no test**, `startAngle` — the port they wired — among them | ✅ **SENT + CLOSED 2026-09-10** — [comment 5623177432](https://github.com/The-Low-Code-Foundation/NodeGX/issues/37#issuecomment-5623177432) |
| #40 | @dishant-kumar-thakur | screenshots are per viewport, not per page | ✅ **SENT 2026-09-10, issue DELIBERATELY LEFT OPEN** — [comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/40#issuecomment-5618047205). 🟡 FLD-011 built the settle budget (**corpus 205.1s → 55.0s; members-area, 11 pages, 53.8s → 10.3s = 5.21x**) and `out_dir`, but **not** the concurrency their TITLE asks for. The #41 shape: they asked for two things and got one plus an alternative, so the reply says exactly that and asks whether 10.3s is the loop they wanted. Also carried the correction that screenshots were per viewport, so a ten-page render returned TWO images, not twenty |
| #43 | @dishant-kumar-thakur | inputs are majority camelCase; the round-trip pair is the sharper bug | ⬜ |
| #33 | @dishant-kumar-thakur | 🟢 **Fixed (FLD-006, `901280af`), AC1 driven.** It was never a fit: the button routed to a *centre* whose scale is the literal `1` | ✅ **SENT + CLOSED 2026-09-10** — [comment 5616099308](https://github.com/The-Low-Code-Foundation/NodeGX/issues/33#issuecomment-5616099308) (cause) + [comment 5616228302](https://github.com/The-Low-Code-Foundation/NodeGX/issues/33#issuecomment-5616228302) (the drive: HUD 100% → 12%, 0 of 10 nodes outside the pane) |
| #32 | @dishant-kumar-thakur | FLD-012 shipped. Reply said what their two suggested discriminators actually do: `ndl-controls-*` works for the radio and **misses the slider** (unclassed track/thumb), and `appearance: none` is a plain div's **computed default** and would have suppressed everything | ✅ **replied + CLOSED 2026-09-10** ([comment](https://github.com/The-Low-Code-Foundation/NodeGX/issues/32#issuecomment-5617357956)) |
| #22 | @richardosborne14 | 🔴 **REPLIED, DELIBERATELY NOT CLOSED — the #41 shape again.** The read-out half is 🟢 built (`4e8ce7ab5`, FLD-002, AC1 driven): `Breakpoint` string plus `At Medium` / `At Small` signals, one decision behind both the band and the layout. The **other half they asked for — more slots, the Advanced Columns prefab — is FLD-003, unbuilt and gated on R3**, so closing this would close a live feature request on the strength of a different fix. The reply states the Auto Fit and inert-pair readings, and that the three ports do not translate at export | ✅ **SENT 2026-09-10, ISSUE STAYS OPEN** — [comment 5618554737](https://github.com/The-Low-Code-Foundation/NodeGX/issues/22#issuecomment-5618554737) |
| #26 | @dishant-kumar-thakur | register row **N2**: `node.ts:135` seeds a units port as `{value, type}` and `:402` reads `.unit`. Present since `b9c60b07d`, found independently from two directions. Needs ruling R4 | ⬜ |
| #29 | @dishant-kumar-thakur | 🟡 **REPLIED, DELIBERATELY NOT CLOSED — the #41 shape.** FLD-016 `556915fa4` fixes all four sub-problems and **two of them were one config line**: the legacy AppImage toolset both links `libfuse.so.2` AND adds `--no-sandbox` as a `defaultArg`. Measured on our own AppImage, reverted arm: `AppRun --no-sandbox %U` + `libfuse.so.2` → `AppRun %U` + no FUSE. Reply also carried the correction that their proposed `--ozone-platform=auto` is **not a valid value** and would be silently ignored. 🔴 Open because **AC1 and AC3 cannot be measured here** — a FUSE-3 Wayland distro is needed, and they have one | ✅ **SENT 2026-09-10 (s9), ISSUE STAYS OPEN** — [comment 5619007041](https://github.com/The-Low-Code-Foundation/NodeGX/issues/29#issuecomment-5619007041) |
| #35 | @dishant-kumar-thakur | 🔴 **the same defect as phase 81's VIB-005 (register row P13) — do not answer either side before the collision is resolved.** FLD-005 | ⬜ |
| #39 | @dishant-kumar-thakur | 🔴 **collides with phase 83's HLS-005 over `WIRED_STYLE_SINKS` (P9)**, and needs ruling R2 (kit or core nodes). FLD-015 | ⬜ |
| #42 | @dishant-kumar-thakur | 🟡 **REPLIED, DELIBERATELY NOT CLOSED — the #41 shape.** FLD-017 `07f6a7e74`: **`app.asar` 283,432,139 → 198,017,608 B (−30.1%), 13,113 → 11,932 files, 0 sourcemaps in the built `.app`** (was 1,181 / 81.2 MB), and **idle 2.42% → 0.14% of a core** on the welcome screen. 🔴 **Neither cause was where the issue said.** The idle CPU was three `bouncedelay 1.4s infinite` dots inside an `opacity: 0` overlay that is built at startup and never leaves the DOM — an `opacity: 0` element still animates; `getAnimations()` 3 → 0. `PrimaryButton` had the same defect and was found by DRIVING. The reply also **refuses two of their items with measurements**: blockly is a webpack **external**, so the '13.4 MB back in the main bundle' is 0 bytes and a cold re-`require` is **4 ms**; the profile poll does not run on the welcome screen at all. ✅ **R5 answered mid-session and minification shipped**, so the reason for holding it open went away | ✅ **SENT + CLOSED 2026-09-10 (s11)** — [comment 5624413158](https://github.com/The-Low-Code-Foundation/NodeGX/issues/42#issuecomment-5624413158) (the size + idle fixes) and [comment 5625121667](https://github.com/The-Low-Code-Foundation/NodeGX/issues/42#issuecomment-5625121667) (**R5 answered, minification shipped `aedc51f64`** — `app.asar` **283,432,139 → 168,143,884 B, −40.7%**). Closed because both halves are done; the remaining items in the issue were declared out of scope and filed |

## 6. Rows this phase found

Every row carries an owner or **NONE**, and stays open until someone closes it. A row with no owner
is one that gets rediscovered later at full price.

| id | what | found by | owner |
|---|---|---|---|
| P14 | 🔴 **The launcher's "New project → Quick Start" hangs.** Name typed, location defaulted to `~/vscode_projects/NodeGX test projects`, "Create Project" clicked: the button's spinner runs indefinitely, **no project directory is created**, no error is shown to the user, and nothing reaches `.logs/dev.log` or the renderer console. Reproduced twice on `cline-dev` HEAD (`11b2d3a9`). `handleCreateProject` (`ProjectsPage.tsx:801`) only opens the wizard, so the failure is downstream in `handleCreateProjectConfirm`. **This is the front door for a first-time user.** | FLD-001 drive, 2026-09-09 — it blocked AC1 until worked around by seeding a recent-projects entry against a copied project | **NONE** — deserves a task |
| P15 | ⚠️ **RE-MEASURED 2026-09-10 (FLD-009 drive) — the row is WRONG at HEAD, and the real behaviour is better than it says.** "Make Home" **is** in the component context menu, on both a page component and `App` (read off the live menu: `Create Visual Component / Create Logic Component / Create Cloud Function Component / Create Folder / Open / Show in workbench / **Make Home** / Rename / Duplicate / Move to… / Delete`). It also **fires**: clicking it on `/#__page__/Home` refused **out loud** with a toast — *"Home" has no visual node at the root of its graph, so it can't be the home component. Add a visual node — a Group, for example — at the top level of the component.* That is track A's own sentence being honoured, not a missing affordance. The 2026-09-09 reading saw the folder-shaped menu because the row that was right-clicked was a folder, not the component. ⚠️ What the row got right and is worth keeping: a project whose root node is unset **and** whose only page has no visual root still has no route out through the UI — the refusal names the fix but the person must go and make the node | FLD-009 drive, 2026-09-10 | ✅ **CLOSED — not a defect. Do not inherit it** |
| P16 | `NodeContextMenu.ts:179` builds "Add new child" with `parentModel: editor.highlighted ? editor.highlighted.model : undefined` — the **hovered** node, not `selectedNodes[0]`, which is what the menu item's own guard (`:167-172`) tested. If the pointer is not over the node when the item is clicked, the new node is added to the root instead of as a child, silently. **Read from source; not driven** — the drive parented nodes by dragging instead | FLD-001, reading the path AC1 needed | **NONE** |
| P17 | 🔴 **Hash routing defeats every `viewerpatheq` condition, before and after FLD-007.** `navigationPathType` defaults to `'hash'` (`noodl-viewer-react/src/nodes/navigation/router.tsx:806-812`), which compiles a URL as `?query#/page` — fragment last, query first. The editor's route is everything after `http://localhost:<port>`, so a hash-routed project reports `/#/task-page`, which no authored condition matches; stripping the fragment would yield `/`. All seven hosted lesson projects set `navigationPathType: 'path'`, which is the only reason the verb works at all. The fix is to read the fragment as the path when the project is hash-routed — **a lesson authored against a hash-routed project cannot be graded on the route today** | FLD-007, 2026-09-10 — read from source while fixing the truncation; **not driven** | **NONE** |
| P18 | 🔴 **Six of the eight shipped lesson route-conditions were passing on a race, not a comparison.** `CanvasView` had two writers for `window.noodlEditorPreviewRoute` — truncating on `load-commit`, untruncated in `setCurrentRoute` — and a step phrased *"select the /X path in the Path Dropdown"* ticked because `setCurrentRoute` emits `viewer-navigated` synchronously (`EditorDocument.tsx:199-205`), so `lessonlayer2`'s refresh (`:171`) read the full route before `load-commit` clobbered it back to `''`. FLD-007 removed the divergence by putting both writers on `previewRoutePath`. **Registered because the shape recurs**: a global with two writers and two meanings reads as a passing test until the timing moves | FLD-007, 2026-09-10 — the sweep AC3 asked for | **CLOSED by FLD-007** (`4068d139`) |
| P20 | `noodl-viewer-react/src/nodes-deprecated/std-library/data/dbcollectionnode.ts:454-465` carries the **third** copy of the FLD-008 defect: the `_convertFilterOp` error callback calls `_this.context.editorConnection.sendWarning(…)` unconditionally, so outside the editor it throws, the throw is swallowed by `catch (e) { console.log(…) }` at `:493`, `_filter` stays `{}` and the query goes out unfiltered. **Read from source, not driven** — this is `nodes-deprecated`, on its own local `_convertFilterOp`, and outside the two nodes FLD-008 §3 named. The fix is the same four lines: capture first, guard the warning, return `{ failed }`. ⚠️ Whether that node is still reachable from a shipped project was **not measured** — measure that before deciding it is worth a task | FLD-008, 2026-09-10 — the `convertFilterOp` caller sweep | **NONE** |
| P21 | 🟢 **FIXED 2026-09-10 (s5) — and the first two diagnoses were both wrong.** The real-filesystem arms of `fld-009/projectLevelWatch` and `rel-009b/projectFileWatcher` write **immediately after `watcher.start()`**. `fs.watch(dir, {recursive:true})` is **FSEvents on macOS and is not live when `start()` returns** — a write that beats the stream produces **no event at all**, not a late one. Control pair, 25 trials each, load the only variable: **write-immediately 12/25 missed (48%) under load; armed-first 0/25**; both arms **0/60 on an idle box**. 🔴 **The reading that kills every budget theory: measure the latency of the runs that SUCCEED — 14 ms idle, 208 ms worst case under load.** So 4000 ms was never the binding constraint and neither is 30_000 ms; a miss is binary. ⚠️ **Every earlier fix to these two specs adjusted how long to wait AFTER the write** — a 600 ms sleep, then an event-wait, then a latch, then a bigger ceiling — for an event that was never coming. This session added a fifth such non-fix (4000 → 30_000) and `test:main` then failed FLD-009 **twice more at 30 s**, which is what falsified it. Fixed by `tests-unit/support/armWatcher.ts`: poke a sentinel until the watcher answers, drain past the debounce, then measure — and **`armed` is asserted**, because an unarmed watcher is silent in exactly the way the original defect was. **Gate: `test:main` 446/446 suites, 7359/7359, exit 0** — the first fully green run, against 2-of-2 red immediately before | a peer session's `test:main`, 2026-09-10 | ✅ **CLOSED.** 🔴 Read as: *green alone / red under load* is compatible with nondeterminism, with a small budget, AND with a stimulus that fires before the instrument exists — and only the first two feel like answers |
| P19 | `getRelativeURL` returns `undefined` when a page has no `path` parameter (`router.tsx:610-611`), yet **every** `Page` node in all seven hosted lesson projects has `path: undefined` and their routes plainly work — so the default is supplied somewhere this reading did not find. Harmless today; noted because a reader of that function alone would conclude the lessons cannot navigate, which is the kind of wrong model that costs an hour later | FLD-007, 2026-09-10 | **NONE** |

⚠️ **P14 stands; P15 does NOT** — see its re-measurement above. The pair was originally recorded as both being about a project that does not exist or cannot render, and both were hit
inside twenty minutes of trying to do the most ordinary thing in the product. Neither is in any of
the fifteen issues, because the field report's agent drove an **existing** project through the MCP
and never opened the launcher.

### Added by session 6

| id | what | found by | owner |
|---|---|---|---|
| P22 | 🔴 **`appearance: none` is the COMPUTED DEFAULT of an ordinary `<div>` in Chrome**, measured on `demo/fld-012`. FLD-012 §3 proposed skipping empty boxes by it; doing so would have suppressed **every finding on every page**, including the author's genuinely empty box, and read as a clean pass. The shipped predicate skips by TAG only. **The general shape: a CSS property whose initial value is the same as the discriminating value discriminates nothing** — check the computed default on a plain element before making any property a test | FLD-012, 2026-09-10 — caught by the AC2 presence control before the fix shipped | ✅ **CLOSED — never shipped; recorded because the next rule author will reach for it too** |
| P23 | ⚠️ **The catalog carries TWO of each UI control and they render different markup.** `Radio Button` / `Range` are `isDeprecated: true, inNodePicker: false`; the picker offers `net.noodl.controls.radiobutton` / `net.noodl.controls.range`. The deprecated Radio Button is a styled `<input type="radio">`; the current one is an `opacity: 0` input beside an unclassed fill div. **A render fixture that picks the wrong one reproduces a real defect that is not the reporter's** — which is what the first version of `demo/fld-012` did. Any fixture built from a display name is exposed to this | FLD-012, 2026-09-10 | **NONE** — no fix owed; a trap for the next fixture author |
| P24 | ⚠️ **`flat-type-scale` is gated on `v.text.elements >= 10`, so a page with nine visible texts and a genuinely flat scale is never judged.** Surfaced by FLD-012: four corpus pages dropped below the gate once invisible text stopped counting, which is correct — but it also means the gate now bites four pages it did not bite before. Pre-existing, not introduced | FLD-012 corpus run, 2026-09-10 | **NONE** |
| P25 | 🔴 **The resident MCP tool surface has 5 tokens of headroom and `toolDisclosure`'s budget gate is hard.** Adding ONE optional parameter (`render_report`'s `out_dir`) took it from 8273 to 8374 against a budget of **8280**. Measured at HEAD by removing the parameter again: **7 tokens spare**, and the gate's own comment records the budget as already renegotiated **twice**, ending *"there should not be a third"*. FLD-011 paid for `out_dir` by shortening its own `describe`, moving the LOOK doctrine into the response manifest (charged only to callers who ask for files) and correcting `Takes ~8s` → `~2s a page`, which this task made wrong by 4x. Net **8275, 5 under**. 🔴 **FLD-014 does not fit**: its scope is *"accept snake-case aliases for the 98 camelCase inputs in `backendTools.ts`"*, which ADDS to the surface. It needs a ruling or the `$ref` fix the gate's comment already names as the honest answer — **before** anyone starts it | FLD-011, 2026-09-10 (s7) — the gate went red on a one-parameter change | **NONE** — 🔴 blocks FLD-014 |
| P26 | ⚠️ **Three corpus lesson projects render differently depending on how long you wait, and nothing said so.** `lessons/snacks`, `snacks/solution` and `it-gets-demanding/solution` reveal a caption on a **~5s timer**; sampling shows it flip between 4.9s and 6.1s. The old fixed settle timers reached the phone measurement at exactly 5.9s, so `flat-type-scale` (gated at `>= 10` texts, see **P24**) fired on a 10th element that a faster read does not see. 🔴 **Proved a sampling artefact, not a regression, by reproducing the new reading with the OLD mechanism at HALVED timers.** The general shape: **a self-agreement control detects nondeterminism, not time-dependence** — both arms here are internally deterministic and each agrees with itself perfectly while disagreeing with each other. Any before/after over a render corpus needs a third arm that varies the sample TIME | FLD-011 corpus run, 2026-09-10 (s7) | **NONE** — no fix owed; the readings are both honest |
| P27 | ⚠️ **A duplicate derivation of one fact is invisible wherever the two derivations agree — so the arm that finds it is the one where the honest answer is "none of the above".** FLD-002's reverted arm C derived the Columns breakpoint a **second** time beside the layout instead of out of it — the States-node workaround written into the component, and precisely the defect the new ports exist to prevent. It reddened **1 spec of 12**: *Auto Fit reports Default*. In `layoutString` mode the second derivation agrees with the first at every width, so the general "the band matches the layout" assertion **passes on the broken code**. Auto Fit discriminates because it consults no layout string at all, so the right answer there is not a band. 🔴 **Generalises beyond this task**: any "two sources of the same fact" assertion is only as strong as the case where the two sources would disagree, and that case is usually the degenerate one nobody writes a test for | FLD-002, 2026-09-10 (s8) — the reverted arms | **NONE** — no fix owed; a rule for the next agreement spec |
| P28 | 🔴 **The repo's own release path CANNOT be run on this checkout: `scripts/noodl-editor/build-editor.ts:62` runs `npx rimraf ./node_modules` and reinstalls.** On a shared checkout with peer sessions live that is destructive, and nothing in the script or the handoffs says so. FLD-016 needed a real Linux artefact and got one by invoking `node_modules/.bin/electron-builder` directly against the existing bundles, with **`--config.npmRebuild=false`** (else `@electron/rebuild` rewrites native modules in the shared tree for linux-x64) and `--config.directories.output=` pointed at the scratchpad. ✅ **The useful half: electron-builder builds a Linux AppImage, `.deb` AND `.rpm` on darwin-arm64, all exit 0** — the `.rpm` needs `brew install rpm`, the other two need nothing. **A Linux packaging change is gradeable here after all** | FLD-016, 2026-09-10 (s9) | **NONE** — a trap for anyone told to "just build the release locally" |
| P29 | 🔴 **Linux has no way to learn a new version exists, and it is not the missing-handler bug it looks like.** `setupAutoUpdate()` returns early on Linux (`autoupdater.js:180`) — **171 lines before** the seven `ipcMain.handle('update:*')` registrations at `:351` — so #30's `No handler registered for 'update:get-state'` is that early return, not an unfinished handler. The renderer already `.catch`es it (`useUpdateState.ts:72-75`) naming this exact case, so it is **log noise, not a broken UI**, and both fixes #30 proposes are therefore wrong. ⚠️ **But the reporter's sentence stands:** the early return is a correct answer to *"can we replace the binary?"* and the wrong answer to *"can we tell them there is a new one?"* — **those are separable and we have been treating them as one.** Notifying needs no AppImage surgery. The 0.2.3 `.rpm` does NOT close it: a locally-installed rpm has no repo behind it | #30 reply, 2026-09-10 (s9) | **NONE** — deserves a task |
| P30 | 🔴 **CI on `cline-dev` ran ZERO gates between 2026-09-09 and this session, and the failure looks identical to a failing test suite.** `.github/actions/setup` runs `npm ci`, which exits 1 with `Missing: @nodegx/project-contract@0.1.0 from lock file` — the package landed 2026-09-09 in `55f657b8a` (P83/HLS-003) and `package-lock.json` was last regenerated 2026-09-05. Every job died in ~19s **before** `typecheck`, `test:packages`, `test:main`, `lint:ci`, `tsfixme` or `colors` ran. ✅ **Fixed `9b3017f8b`** with `npm install --package-lock-only` (node_modules untouched, so no peer's install was disturbed): 19 insertions, 0 deletions, no version moved, `npm ci --dry-run` exit 0. 🔴 **The lesson is the readout, not the lock:** a run list showing `failure` for a month reads as *"the suite is red"*, and nobody opened one to find that the suite never ran. **Open the log, not the status column.** ⚠️ A push is still owed before this is proved in CI | s10, 2026-09-10 — found while verifying #13's disposition | ✅ **FIXED `9b3017f8b`** |
| P31 | 🔴 **`npm run tsfixme` is RED against its own committed baseline, and has been unenforced for 34 days.** TSFixme **819 vs 582**, bare `any` **806 vs 415**, `@ts-nocheck` **4 vs 0**. The last green CI run and the last commit of `.tsfixme-baseline.json` are **the same day, 2026-08-07** — so the ratchet has been drifting for exactly as long as CI has been unable to tell anyone. ⚠️ **This changes what #13 can honestly be told.** The register's C13 row says *"the mechanism asked for already exists and gates every PR"*; the mechanism exists, it is well built, and it has not gated anything since 08-07. 🔴 **And C13's own number is the BASELINE misread as the count** — *"real parser count is 582"* is `.tsfixme-baseline.json`'s `max.TSFixme`, not what the scanner reads today (819). Nothing here is fixed: the drift needs either a burn-down or a reviewed baseline raise, and **a raise is the thing the gate exists to stop being done silently** | s10, 2026-09-10 | **NONE** — 🔴 gates the reply to [#13](https://github.com/The-Low-Code-Foundation/NodeGX/issues/13) |
| P32 | 🔴 **The node-graph canvas icons have NEVER shipped — home, component, AI assistant and warning are simply not painted, in every packaged release.** `CanvasIcons.ts:29-53` `require`s five SVGs and `@svgr/webpack` + `file-loader` emit them as **hashed files into the package root** (325 of them, 375,288 B); `build.files` ships `"*.js"` from that directory and nothing else, so the archive holds the 56 SVGs under `src/assets/` and **zero** hashed ones. Five `Failed to load … icon` console errors on every project open. ✅ **Proved pre-existing for free, against a SHIPPED artefact rather than a rebuild**: `/Applications/NodeGX.app`'s asar has the identical 56 / zero split, so minification (R5) neither caused it nor could. ⚠️ **`"*.svg"` in `build.files` is probably NOT the whole fix** — the bundle names the assets by bare filename while the HTML lives at `src/editor/index.html`, so `publicPath` is half the question. 🔴 The general lesson: **a build that emits assets to a directory the packager does not ship fails SILENTLY in the artefact and works perfectly in dev**, where webpack-dev-server serves them | FLD-017 R5 QA drive, 2026-09-10 (s11) | **NONE** — deserves a task; it is small and it is user-visible |
| P33 | 🔴 **`render_report` was DROPPING every console error a page logged while it was LOADING, and `console-error` is the one rule whose whole evidence is that array.** The window errors were attributed by was opened *after* the navigation (`renderReport`, both the start-page loop and the routed-page sweep), so anything logged on the way up belonged to no row — including every start page's boot. ✅ **FIXED in FLD-011 s12** (`01ea605cc`): the window opens before the navigation, at **0** on the start page — which is what the comment there already claimed — and **closes at each read**, so windows are disjoint and nothing is dropped or double-counted. 🔴 **It surfaced only because AC3's fixture carries a known-firing control**: without *"heard both pages shout"*, four assertions about WHICH page an error landed on all passed on a report containing no errors at all. **13 findings across 3 corpus projects had been invisible** — `members-area` 61 → 71, a `console-error` on its start page and nine of its ten routed pages | FLD-011, 2026-09-11 (s12) | **FLD-011 — CLOSED** |
| P34 | 🔴 **A shipped template references three images that 404, and the report has been saying so all along.** `templates/members-area`, `/Members/InsideTile`: `starter-imagery/people-cafe.webp`, `people-market.webp`, `work-potter.webp` — read off a real drive, on **eleven** page rows. The `broken-image` rule reports them and `brokenSources` names them, so this needed no new instrument; it needed somebody to read the report. ⚠️ Not FLD-011's: the task made it louder (P33 adds a `console-error` beside each), it did not cause it | FLD-011, 2026-09-11 (s12) | **NONE** — small, real, and in a template we ship |
| P35 | ⚠️ **A cloud project rendered with NO backend now earns a `console-error` per page, where it used to earn silence — and this is a DECISION, not a defect.** P33's fix makes two load-time errors visible on every page of `members-area`: `CloudFunction2 (/Members/Standing): No cloud services defined in this project.` (6 rows) and `TypeError: Cannot read properties of undefined (reading 'results')` in `ParseWireAdapter.query`'s success path (`:359`; `distinct` has the same shape at `:454`) reading `response.results` on a response that has none (8 rows). 🔴 **It is NOT established that a deployed app can reach either** — `render-from-disk.js`'s own `/__backend` proxy with no upstream may be the only producer, and claiming otherwise would be [[a-client-property-read-as-a-fact-about-the-source]]. Shipped rather than suppressed, because a report that silently drops the errors its own rule exists to report is broken — but **what "rendered without a backend" ought to report needs its own task and a ruling**, not a suppression invented at the end of a session. #40's reporter has been asked, in the reply, to say whether it reads as noise | FLD-011, 2026-09-11 (s12) | **NONE** — needs a ruling; FLD-012 is the precedent for how not to answer it |
| P36 | 🔴 **`starter-iconset:check` was red because a GENERATED artefact had been hand-edited — so §7's documented "one command" fix would have SHIPPED A REGRESSION.** VIB-007 (`03c327c84`, 2026-08-31) added `icon-inbox`, `icon-recycle` and `icon-sprout` straight into the generated `manifest.json` — 212 → 215, deliberately, so the picker could offer what the corpus teaches — and never added them to `CURATED` in `scripts/library/make-starter-iconset.js`. `npm run starter-iconset:generate`, which is exactly what the gate's own message and the phase handoff both instruct, deletes all three. ✅ **Fixed in the SOURCE list instead (s13, `b1ca71f5b`): the manifest is byte-identical and the change ships zero product difference.** ⚠️ **Measured the hard way** — an exploratory `node -e "require('./scripts/library/make-starter-iconset.js')"` ran the script's main and deleted the glyphs; restored from HEAD and verified 215 with all three present, and the damage diff was exactly those three lines, which is what proved they were the WHOLE of the staleness. 🔴 **The general shape: "the artefact is stale, regenerate it" assumes the artefact has no author. When a generated file has been hand-edited, the generator is the thing that is wrong, and the gate is reporting the hand edit rather than the drift.** `git log` the artefact and diff the regenerate BEFORE committing it | s13, 2026-09-11 — taking §7's two "one command" gates | ✅ **CLOSED `b1ca71f5b`** |
| P37 | 🔴 **A file can go on EXISTING and stop DECLARING, and an `existsSync` guard cannot see it.** `@nodegx/kit-scaffold`'s `output.test.js` validates every token the scaffold emits against the editor's registry, guarded by a probe whose own comment says *"fail rather than skip if the registry moves: a probe that silently exonerates is worse than no probe"*. HLS-001 (`4fd171fc7`, 2026-09-09) moved the vocabulary into `@nodegx/project-contract/tokens` and left `DefaultTokens.ts` a 12-line re-export — **the file still existed, so that probe stayed green for two days**, and it was the neighbouring `declared.size > 100` floor that went red. ✅ **Fixed s13 (`3fe956fd7`)**: repointed at `packages/nodegx-project-contract/tokens.ts` (192 names, identical format), floor kept, comment corrected to credit the assertion that actually worked; armed with a mutation (`--color-surface-2`, the fake the spec's own comment names) which reddens `unknown` toEqual `[]` and nothing else. 🔴 **Same family as P36 and as the same session's `node-kit-types` drift: a source of truth MOVED and its readers did not follow — three instances in one session.** The generalisation worth keeping: **a probe on a file's PATH and a probe on its CONTENT fail in different circumstances, and only the content one survives a move that leaves a stub behind** | s13, 2026-09-11 — measuring what lerna would hit after `node-kit-types` | ✅ **CLOSED `3fe956fd7`** |
| P38 | 🔴 **Three shipped prefabs carry no icon, and `ModuleCard` destructures it unguarded.** `library:verify-dist`: *"[prefabs] Format Date / Format Full Name / Sanitise Email: missing/empty `icon`"* — `library/prefabs/{format-date,format-full-name,sanitise-email}/library.json` have no `icon` key **and no `icon.png` on disk**, against a uniform `"icon": "icon.png"` + file convention across all 42 other prefabs. Added by **`45929d94c`** (2026-09-10 21:21, **phase 85 CMP-004/CMP-005**, *"the shelf gets parts"*), which landed AFTER the last CI baseline — so **CI saw it for the first time on 2026-09-11** and the *Library check (LIB-001)* job went red on it. ⚠️ **Not fixable mechanically and deliberately NOT fixed here**: the convention needs an actual PNG asset, which is content, and phase 85's author was live in that phase at the time. 🔴 **The order matters for the next reader: this step runs BEFORE `starter-iconset:check`, so it now MASKS it** — the iconset step shows `SKIPPED`, which is not the same as passing | s13, 2026-09-11 — read off the first CI run after the push | 🔴 **PHASE 85 (CMP-004/005)** — needs three icons |
| P39 | 🔴 **A gate that reads UNTRACKED files describes one working tree, not the repository — and this one had been red in CI since the day it shipped.** `nodegx-export`'s `hls006-every-listener` sweeps every package's `src` for `.listen()` and reconciles it against an `ENROLLED` table **in both directions**. Two of its ten rows point into `packages/noodl-editor/src/external`, which **`.gitignore:206` ignores entirely** — present on a machine that has built the editor, absent on every fresh clone. So in CI both rows were stale and the sweep failed **from `148ce57a1` (2026-09-09)**, invisible because `test:packages` was dying two packages earlier; locally it passed until **session 11 deleted the stray `src/external/ssr 3/`**, which is the entire difference between session 10's *98 suites / 3,400 passed, exit 0* and session 13's red. ✅ **FIXED s13 (`052330112`) at the SCANNER, not the table**: `external` joins `SKIP`, whose stated purpose is already *"build output, vendored code, or tests"*. Deleting only the stale row would have left the other red in CI forever. Armed by removing a real row (`trampoline-server.ts`), which fires the *unenrolled* assertion. ⚠️ The first attempt removed the two KEY lines and orphaned their multi-line values — `Tests: 0 total`, which grades nothing | s13, 2026-09-11 — surfaced by fixing `node-kit-types`, which let lerna reach this package | ✅ **CLOSED `052330112`** |
| P40 | 🔴 **A kit node's `inputCss` and `defaultCss` are INVISIBLE to the React export, so a kit styled the documented way renders one way in the editor and unstyled in the exported app.** `parse/kitSource.ts`'s `readDefinition` collects `inputProps` and `inputs` and **never looks at `inputCss`**; the emitted shim (`emit/kits.ts`'s `createNode`) applies `inputProps` defaults and `inputs` setters and has **no `setStyle` and no `style` at all**. A wire or a parameter aimed at such a port is reported (*"names no input port"*), but the port's **defaults** vanish with no note — background, padding, border, radius, and every shared Layout port, since `addDimensions` writes `width`/`height` into `inputCss` too. 🔴 **The shipped `example-node-kit` is exactly this shape**: its `StatTile` puts every box style in `inputCss`, so an exported app draws it with none of them. ⚠️ FLD-015's `nodegx-charts` is DESIGNED AROUND this — no `frame:`, no `inputCss`, every visual decision an `inputProps` — which is why it renders identically in both (measured: identical bar heights). That is a workaround in one kit, not a fix in the export | s16, 2026-09-11 — found while choosing how to declare the chart kit's size ports, then confirmed against the shipped example kit | 🔴 **UNOWNED** — wants the export shim to apply `inputCss`, or the kit format to say the field is not portable |
| P41 | 🔴 **The generated node documentation has been stale for weeks, and no CI job reads the gate that says so.** `npm run docs:nodes:check` reports **71 problems** — one page MISSING (`noodl-cloud-listusersinrole`, P80/DEF-005, 2026-08-31) and 70 STALE, among them `animation/states.md`, every `cloud/*` page, and the seven `utilities/net-noodl-date-*` / `hash` / `log` pages P86 documented this morning. The script exists, exits 1, and is wired into **no workflow** (`grep docs:nodes .github/workflows` → nothing), so the docs site has been shipping a node reference that predates two phases of port work. ⚠️ The Columns page was stale in the same way — FLD-002's three Breakpoint ports were added on 2026-09-10 and never regenerated — and **only that page is regenerated here**; the other 70 were restored from `HEAD` rather than swept into an unrelated commit, because a wholesale regeneration of another phase's pages is an unperformed merge | s17, 2026-09-11 — found while checking whether FLD-003's pointer reached every surface that renders a port description | 🔴 **UNOWNED** — wants one `npm run docs:nodes` commit and the gate added to the `library` or `catalog` job |
