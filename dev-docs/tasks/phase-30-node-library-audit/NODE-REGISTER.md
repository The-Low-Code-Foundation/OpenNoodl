# Node Register — all 151 catalogued nodes

Generated from `packages/noodl-types/src/node-catalog.json` (catalogFormatVersion 1.1.0).
Regenerate with `node scripts/node-audit/register.js` — hand-written `Verdict` cells are preserved.

The smell columns are *machine-derivable*, not verdicts.

- **Fail?** — has a signal input and signal outputs, but no failure/error output: the node can go wrong and say nothing.
- **Mute?** — has a signal input and no signal output at all: nothing downstream can sequence off it.
- **Doc%** — share of ports carrying a `description`, i.e. what the property panel and the AI authoring loop can read.

| # | Node | Category | Ports (in/out) | Fail? | Mute? | Doc% | SSR | Runtimes | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Animate To Value | Animation | 4/2 |  |  | 100% | partial | browser | ⚠️ NDA-012 (Animation) — 4 defects, 3 of them shared with siblings. **FIXED**: a running timer outlived its node at all four Animation sites (`Delay` has had the delete listener since it was written, and is filed under *Utilities*, so no per-category read would compare them). Open: `Number(null)` is 0 so a cleared target animates to zero; the *first* target is adopted without firing `At Target Value`; `EaseCurves[value]` is unguarded so an unknown name over a wire throws every frame. FINDINGS **SR-vi** |
| 2 | Animation _(deprecated)_ | Animation | 13/2 | ⚠️ |  | 100% | partial | browser | ⚠️ NDA-012 — 4 own defects + 2 shared, the worst timer-leak site (*n+1* timers, none stopped — **FIXED**). Open: `play()` reports an undefined start/end to `console` only, so the animation just does not happen; `Stop` emits nothing; and **`updateStartValue` latches** — `Play To Start` after the first play uses the start value sampled on the *first* play. Fourth site of NV-iii's one-shot shape, found by the same "and the second time?" question |
| 3 | States | Animation | 4/4 |  |  | 100% | partial | browser | ⚠️ NDA-012 — 3 defects. **FIXED**: the `values` setter called `value.split` unguarded where its sibling `states` has always had a falsy check, so emptying the Values list threw out of an input setter; plus the shared timer leak, noisiest here (one output flagged per value per frame). Open: **a state or value name containing a hyphen resolves to the wrong pair** — `value-a-b-c` is genuinely ambiguous, so it needs an encoding decision not a substring change. FINDINGS **SR-iii** — also: ✅ NDA-004 §2 — `Failure`/`Error`, `states/unknown-state`. A name not in the list animated **every value to 0**, adopted the bogus name on the `State` output and fired `stateChanged`; no `reached-` port exists for it, so the transition read as successful. The guard refuses to move as well as reporting, and fires only for a *truthy* unknown name |
| 4 | Transition _(deprecated)_ | Animation | 6/2 | ⚠️ |  | 100% | partial | browser | ⚠️ NDA-012 — 1 own defect + 4 shared with `Animate To Value`. Own: `setCurrentNumber` stops the timer (suppressing `onFinish`) and re-starts only if the target differs, so **overriding to the value that is already the target never fires `At Target Value`** — on the one path where the author explicitly asked for a jump |
| 5 | Request | Cloud | 2/3 |  |  | 100% | — | cloud | ⚠️ NDA-012 (Cloud) — 2 defects. **`JSON.parse(req.body)` sits in an empty `catch`**, so a malformed body runs the function with no parameters, fires `Received`, and says nothing anywhere — on the entry point of every cloud function. And `User Id` is declared `type: 'boolean'` while returning a string id, the one outright wrong port type in the batch (filed, not fixed: a published type decides what may connect, which is NDA-014's table's business) |
| 6 | Response | Cloud | 4/3 |  |  | 100% | — | cloud | ⚠️ NDA-012 — 1 defect: `Status` = Failure with `Error Message` unset returns a 400 whose body is `{}`, so the caller gets no reason at all — the Failure Contract's complaint pointed one layer out, at the API's consumer. — also: ✅ NDA-004 §3 — `Sent`/`Failure`/`Error`. `response/no-request-in-scope` (the callback was never installed — previously a `TypeError` out of an input setter) and `response/already-sent` (the second answer, previously discarded in silence). `Sent` fires *before* delivery because delivering tears the request scope down synchronously |
| 7 | Send Email | Cloud | 7/3 |  |  | 100% | — | cloud | ⚠️ NDA-012 — 1 defect, and **the best-behaved node in the batch on B1/B2/B3** otherwise: BAK-002 wrote its failure surface first, and a blank `To` is an explicit failure rather than a silent no-op. The gap is the availability guard — `typeof sendEmail === 'undefined'` rather than `!== 'function'`, so a non-callable global throws *synchronously*, past the `.catch` two lines below |
| 8 | Aggregate Records | Cloud Services | 1/3 |  |  | 100% | — | cloud | ⚠️ NDA-012 (Cloud Services) — **6 defects, the worst node in the category and the one no earlier sweep opened.** Two are FIXED here: it wrote `_internal.err` against a getter reading `_internal.error` (third live instance of PLAT-003 §23.4/§27.3) and raised nothing on the error channel (B-iv). It is the only Cloud Services node in `noodl-viewer-cloud`, which both sweeps missed. Open: `getAggregates` throws when an aggregate name has no property set; an unset list aggregates nothing and still fires `Success`; `validateAggNames` checks only spaces; the class/aggregates warnings are editor-only. Rows C1–C5 |
| 9 | Cloud File | Cloud Services | 1/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 shared defect: `null` cannot clear `Cloud File`, so a cleared port keeps the previous file (`cloudfilenode.ts:51-54`). Otherwise the simplest node in the category and clean |
| 10 | Cloud Function _(deprecated)_ | Cloud Services | 3/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 3 defects, all “the failure has no reason”: a `Failure` signal with **no reason port anywhere on the graph** (the message reaches only the inspector and one `console.log`), and a call that *succeeds* returning nothing fires `Failure`. A data point for the deprecated-five question: worse than its replacement, unlike `Number Blend`. — also: ✅ NDA-004 §2 / FINDINGS B-iv, `cloud-function/call-failed` |
| 11 | Cloud Function | Cloud Services | 1/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects: “No function specified” is editor-only and `doCall` has no guard after it, so a deployed app POSTs to `/functions/undefined`; and `resultsValues` is never cleared between calls, so a shorter second result leaves the first call's values on those outputs and still fires `Success`. — also: ✅ NDA-004 §2 / FINDINGS B-iv, `cloud-function/call-failed` |
| 12 | Config | Cloud Services | 0/0 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 3 defects, and the sharpest in the category: a config fetch that fails once **can never succeed again** (`configservice.ts:118-131`; `clearCache()` clears the other field), and the node has **no failure surface of any kind** — no `Failure`, no `Error`, no raise, a `.then` with no `.catch`. Also the only node in the library whose Doc% cannot be measured: `0/0` static ports, all three real ports pushed from `setup`. Rows M1–M4 (`test.failing`) |
| 13 | Log In | Cloud Services | 3/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects. — also: ✅ NDA-004 §2 / FINDINGS B-iv — ports were always right, diagnosis was editor-only. Raises `user/log-in-failed`; ten sibling auth nodes moved in the same batch, each with its own `user/<operation>-failed` |
| 14 | Log Out | Cloud Services | 1/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects. Its `Do` port is internally named `login` and the comment says why (persisted in every project that uses it) |
| 15 | Model _(deprecated)_ | Cloud Services | 8/9 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 1 shared defect (`setModelID` hands a possibly-absent id to the create-on-read tier). The healthiest deprecated node here, and **better sequenced than its replacement**: every one of its six actions has its own completion signal |
| 16 | Query Collection _(deprecated)_ | Cloud Services | 0/8 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 1 defect, the phase's **fourth signal-before-value**: pulses `Modified` and *then* flags the counts the pulse is about (`dbcollectionnode.ts:211-216`). Its replacement deleted the signal rather than moving one line |
| 17 | Query Records | Cloud Services | 0/19 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 3 defects: a change made by *another* node updates `Count`/`First Record Id`/`Is Empty` and not `Items`, while the node's own patch path flags `Items` three times with a comment saying why; an unparseable Javascript filter dereferences `undefined` rather than failing the query; two reports remain editor-only. — also: ✅ NDA-004 §2 / FINDINGS B-iv, `query-records/query-failed` |
| 18 | Record | Cloud Services | 4/5 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 defect: `typeof null === 'object'`, so a cleared `Id` reaches `Model.create(null)` and binds the node to a fresh throwaway record whose generated id it reports. Third instance of the create-on-read trap. — also: 🔵 NDA-004 §2 — `scheduleStore` is dead code; `setModel`'s guard is the class-F path, already reported by `foreachitem.ts` |
| 19 | Request Magic Link | Cloud Services | 3/3 |  |  | 100% | partial | browser | ⚠️ NDA-012 — 2 defects, both reachability: **it cannot be added to a graph** (absent from `nodelibraryexport.ts`'s picker index, which lists four deprecated auth nodes instead), and it has no `docs` URL. FINDINGS **CS-i** — this corrects last session's dismissal of it as picker-exempt. Behaviourally the cleanest of the auth nodes |
| 20 | Request Password Reset _(deprecated)_ | Cloud Services | 2/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects. Deprecated and **still in the picker**, while its modern replacement is not |
| 21 | Reset Password _(deprecated)_ | Cloud Services | 4/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects, both class D: the outcome is decided by `if (response.indexOf('Invalid Link'))` — truthy exactly when the phrase is *absent* — and the unreachable branch below it reports “Failed to verify email” |
| 22 | Send Email Verification _(deprecated)_ | Cloud Services | 2/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects |
| 23 | Set User Properties | Cloud Services | 3/3 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 defect and it is the family's most consequential: triggered while signed out it calls **neither** callback, so `Do` yields no `Success`, no `Failure`, no `Error` and no console line, forever, and re-arms (`userservice.ts:327-354`). FINDINGS **CS-v** |
| 24 | Sign File URL | Cloud Services | 2/9 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 shared defect (`null` cannot clear `File`, and with none ever set the node says “No file specified” to an author who did wire something). Otherwise the **reference node for this category** — the only one that distinguishes “the request never left” from “the backend refused” on a port. — also: ✅ NDA-004 §2 / FINDINGS B-iv, `sign-file-url/sign-failed` |
| 25 | Sign In With | Cloud Services | 3/5 |  |  | 100% | partial | browser | ⚠️ NDA-012 — 3 defects: **it cannot be added to a graph**, it has no `docs` URL (FINDINGS **CS-i**), and it subscribes to `UserService` in `initialize` with no `_onNodeDeleted`. The best-reasoned node in the category and the least reachable |
| 26 | Sign Up | Cloud Services | 4/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects. 🔵 The local session is written from the request merged over the response, so a column the backend rewrites on insert reads stale until a `Fetch` |
| 27 | Upload File | Cloud Services | 8/9 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 1 shared defect: its `File` input is typed `*` and accepts anything including `null`, the *opposite* of its two `cloudfile` siblings' silent rejection; neither is right and they should agree. — also: ✅ NDA-004 §2 / FINDINGS B-iv, `upload-file/upload-failed` |
| 28 | User | Cloud Services | 1/8 |  |  | 100% | partial | browser, cloud | ⚠️ NDA-012 — 1 defect: four `UserService` subscriptions in `initialize`, none removed on delete — `setMaxListeners(100000)` in the service constructor is the workaround already in place. 🔵 Its `loggedIn`/`loggedOut`/`sessionLost` signals are pushed from `setup`, so three of its most-used ports are invisible to the catalog and the validator even though it reads 100% |
| 29 | Verify Email _(deprecated)_ | Cloud Services | 3/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects, both class D: `if (response.indexOf('Invalid Verification Link'))` is truthy when the phrase is *absent*, and the request interpolates username and token into a query string unencoded forty lines above a method that encodes both of its interpolations |
| 30 | Component Inputs | Component Utilities | 0/0 |  |  | 100% | safe | browser, cloud | ✅ NDA-012 (Component Utilities) — no defects. **C1 is not met and cannot be measured**: `0/0` static ports, so the 100% is NDA-005 §2's instrument hole. Its real ports come from the **Port Editor panel** — a third dynamic-port mechanism with no `description` field — so the node that defines every component's public inputs is undocumentable by any means the library has. FINDINGS **SR-ii** |
| 31 | Component Object _(deprecated)_ | Component Utilities | 3/3 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 (Component Utilities) — 1 defect: a property name containing a hyphen is stored under a truncated key while `fetch()` and the change callback look for the full one, so the value is written and then never republished. **Filed, not fixed** — correcting the key would move where existing projects' data lives. Its ports are otherwise better sequenced than its replacement's: `Set`→`Stored` exists here and does not there |
| 32 | Component Object | Component Utilities | 2/2 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects. **FIXED**: the output read a **different key from the one the input wrote** for any hyphenated property — `registerOutputIfNeeded` took the last `-` segment while `registerInputIfNeeded` stripped the prefix, so one node's own reader and writer landed on two keys. Open: **the write path has no completion signal at all** — its deprecated predecessor had `Set`→`Stored`; this node stores implicitly and emits nothing, and with `Fetch` connected `Changed` is suppressed too. FINDINGS **SR-iii** — also: 🔵 NDA-004 §2 by evidence, twice over |
| 33 | Component Outputs | Component Utilities | 0/0 |  |  | 100% | safe | browser, cloud | ✅ NDA-012 — no defects; C1 unmeasurable for the same reason as `Component Inputs` |
| 34 | Parent Component Object _(deprecated)_ | Component Utilities | 3/3 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects: it **walks** and so can miss, and a miss is a bare `return` — no `Failure`, no `Error`, no raise anywhere, which is exactly what NDA-015 §(c) and NDA-004 §2 closed on its replacement; and it has no explicit target, so the binding is implicit and invisible. Its narrow walk type list, by contrast, is deliberate and documented |
| 35 | Parent Component Object | Component Utilities | 3/4 |  |  | 100% | safe | browser | ✅ NDA-012 — **no defects, clean on all twelve checks: the only node in the phase to manage it**, and the most heavily remediated node in the library (NDA-015's whole binding contract plus NDA-004 §2). 🔵 only: `setup` republishes the port list on every `parameterUpdated` where its sibling filters on the one parameter that matters |
| 36 | Set Component Object Properties | Component Utilities | 2/1 | ⚠️ |  | 100% | safe | browser | ✅ NDA-012 — no defects. `validProperties` is split from the same parameter `updatePorts` splits, so the port names and the store filter cannot disagree — also: 🔵 NDA-004 §2 by evidence: `canFailToResolve` is opt-in precisely so this variant carries no port for a failure mode it does not have |
| 37 | Set Parent Component Object Properties | Component Utilities | 3/3 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects — also: ✅ NDA-004 §2 — the worst class-B defect of that batch: a missed walk handed `undefined` to `Model.get`, which mints a fresh anonymous record per store, and the node reported `Done` |
| 38 | CSS Definition | CustomCode | 1/0 |  |  | 100% | safe | browser | ⚠️ NDA-012 (CustomCode) — 1 defect: it declares SSR `safe` and produces **nothing at all** server-side, so a server-rendered page paints without the author's CSS until hydration. `partial` is the honest declaration. The only node in the batch where `null` clears properly |
| 39 | Expression | CustomCode | 2/10 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 defect, and it is sharp: `parsePorts` mints an input port for **every** identifier outside a 30-name ignore list and passes those names as the compiled function's arguments, so any JavaScript built-in the preamble does not cover — `parseInt`, `Number`, `Date`, `JSON`, `Array` — is **shadowed by the seeded 0** and the expression throws *"parseInt is not a function"*. Filed, not fixed: widening the list changes the port set of every existing Expression node, which is a migration. Its `On True`/`On False` pulsing on unchanged values is NDA-017's mechanism, cross-referenced there — also: ✅ NDA-004 §2 — the failure value is `0`, which is *plausible*, so every downstream branch took the path a legitimate zero would take |
| 40 | Function | CustomCode | 4/3 |  |  | 100% | partial | browser, cloud | ⚠️ NDA-012 — 3 defects. **FIXED**: a script that would not **compile** was mute — NDA-004 §3 gave this node ports for code that *throws* and left the parse failure swallowed into a `console.log`, the identical defect §2 had already fixed on `Expression`. Open: `intype-`'s handler tests `'in' + n` and is missing the hyphen, so changing an input's Type never retypes the port; and `_isSignalType` is called with the prefixed name at one site and the stripped name at another — also: ✅ NDA-004 §3 — `Success` is `await`ed, so an async script signals when it has finished rather than when it started |
| 41 | Logic Builder | CustomCode | 3/3 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 6 defects (5 fixed, 1 filed). **FIXED**: a block program whose generated code would not **compile** was mute — the third time the same defect has been found on the library's four script hosts, after `Expression` (NDA-004 §2) and `Function` (NDA-012); a bare-string `throw` reported the empty string on the node's only failure surface; and **an author-declared port could silently collide with one of the node's own** — names are registered verbatim here, unlike `Function`'s `'out-' + name`, so `set output "error"` hit `registerOutputIfNeeded`'s early return and flagged the built-in port instead, and a `Define input` named `run` was published as a second `run`. Six names are now reserved and a collision is raised. `__triggerSignal__` was built on every execution and never passed to the compiled function, so it read `undefined` inside every block program ever run. Filed: changing a block from value input to signal input leaves the live port its old kind, because `registerInputIfNeeded` early-returns on an existing port and `deregisterInput` throws on a connected one — also: ✅ NDA-004 §3 — `Success`/`Failure` added, the tenth and last mute node |
| 42 | Script | CustomCode | 5/0 |  |  | 100% | partial | browser | ⚠️ NDA-012 — 3 defects. **FIXED**: the External File mode had **no failure path at all** — `createFromURL`'s `onerror` never called back, so `isWaitingForExternalFileToLoad` stayed set and the node's `update()` override skipped `Node.prototype.update` for ever: permanently and silently inert, no code, no ports, no warning. A 404 was a second bug underneath (the server's error page was parsed as the author's script). Open: **user code that throws is editor-only** — `sendWarning` plus a `console.log`, no raise and no port, so a throwing Script node is silent in a deployed build; and clearing the Code box leaves the previous script installed and running. FINDINGS **SR-iv** |
| 43 | Action Dispatcher | Data | 12/21 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (both **filed**: L6, L7) · C1 fixed |
| 44 | Action Handler | Data | 8/7 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D4, **fixed**) · C1 fixed |
| 45 | Add Record Relation | Data | 5/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (DA-ii, fixed) · **filed:** resolving the target's class by fetching it, rather than refusing, is the more helpful behaviour but adds a round trip to every relation write — a design decision, not a repair. — also: ✅ NDA-004 §2 — `validateInputs` returned early with no editor connection, so **deployed it validated nothing** and the caller then hit two bare `return`s. Now returns the verdict and the caller fails through `setError` |
| 46 | Array _(deprecated)_ | Data | 9/8 | ⚠️ |  | 0% | safe | browser | 🔵 NDA-004 §2 — its `Id` input is a value arriving, not a `Do`; a `Failure` would fire on the boot path. Its only action, `Fetch`, yields the node's own array, which is a legitimate result. The Object node's answer |
| 47 | Array | Data | 3/6 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-4, fixed) · 🔵 **filed:** clearing the `Id` field re-binds the node to a *fresh anonymous throwaway collection on every set*, and emits a new random guid on the `Id` output each time — measured, two clears gave two different ids. Same shape `collection-failure.ts` exists to prevent for the three mutators, never adopted by the node that owns the id. Unbinding is the right answer and this node has no failure surface to say so on, so the repair and the B1 decision are one decision. Pinned by a row. — also: 🔵 NDA-004 §2 — its `Id` input is a value arriving, not a `Do`; a `Failure` would fire on the boot path. Its only action, `Fetch`, yields the node's own array, which is a legitimate result. The Object node's answer |
| 48 | Array Filter | Data | 4/6 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-8, filed) · **checked and clean:** its `setup` registers `nodeAdded.Filter Collection` and re-runs `updatePorts` on `parameterUpdated` *and* on `metadataChanged.dbCollections`; `GraphModel.addComponent` emits `nodeAdded` for nodes already in the component, so existing nodes are covered — same conclusion as the twin, recorded so it is not re-raised. — also: ✅ NDA-004 §2 — `Failure`/`Error` and **two** failures where the triage predicted one. `array-filter/no-items` (a `Do` with nothing on Items) is gated on a `filterRequested` flag, because five of the scheduler's six callers are value arrivals; `array-filter/filter-failed` is not, because a `RegExp` built from the author's `Value` port threw out of a scheduled callback whenever it arrived. The trigger distinction the register asked for was **already in the source** as `isInputConnected('filter')` |
| 49 | Array Map | Data | 3/5 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-2, fixed, spanning B1/B2/B3) · **note for the script-host register:** this is a `new Function` host (`mapcollectionnode.ts:89`). Whether it is a sixth member of the five-member script-host class recorded in the CustomCode pass, or outside it because the script is `allowEditOnly` and takes a fixed one-argument shape, is the CustomCode owner's call — flagged, not claimed. |
| 50 | Clear Array | Data | 2/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — clean · **one cosmetic non-defect recorded so it is not re-raised:** `_clearCollectionFailure()` runs *after* the guard here and *before* it in its two siblings. The outcome is identical — the editor keys by `code`, so a re-raise overwrites — but the asymmetry invites a "fix" that is not one. — also: ✅ NDA-004 §2 — `Failure`/`Error`, `clear-array/no-array`. Had **no guard at all**: `collection.set([])` on `undefined` threw a `TypeError` out of a scheduled callback |
| 51 | Create New Array | Data | 2/2 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — clean. The `shortDesc` was one of the four wrong ones (§1.2) and is replaced. — also: 🔵 NDA-004 §2 — builds its own collection (`Collection.get()` with no name), so it cannot fail to find one. Correctly carries no `Failure`, and a corpus row pins the absence |
| 52 | Create New Object | Data | 2/2 | ⚠️ |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — clean — the only node of the four with no defect. C1 was the whole of its debt. — also: 🔵 NDA-004 §2 — builds its own object, so it cannot fail to find one. Correctly gets neither `addFailure` nor `repeaterComponent` |
| 53 | Create Record | Data | 3/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (DA-iii (a) and (b), both fixed, both in the shared base) |
| 54 | Delete Record | Data | 4/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (DA-iv, fixed but shadowed) · **standing, not re-raised:** `nodeScope.ModelScope` (capital M) is a documented PLAT-003 defect left verbatim — this node alone ignores the component's record scope. Fixing it is a behaviour change for any scoped-store project and is still unowned. |
| 55 | Filter Records | Data | 3/6 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (DA-v, filed) · **checked and clean:** its `setup` registers only `nodeAdded.FilterDBModels` where the CRUD base also enumerates `getNodesWithType` after `editorImportComplete`. That is **not** a missing-ports defect — `GraphModel.addComponent` emits `nodeAdded` for every node already in the component (`graphmodel.ts:232`), so existing nodes are covered. Recorded so a later pass does not re-raise it. — also: ✅ NDA-004 §2 — Array Filter's twin, read rather than assumed to be one: same six trigger paths, same guard, same bare return. `filter-records/no-items` and `filter-records/filter-failed` |
| 56 | Global Store | Data | 4/6 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D1, **fixed**) · C1 fixed · 🔵 E1 by design, documented |
| 57 | HTTP Request | Data | 3/7 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (OB-iii, fixed) · **filed:** FC-1 stale `Status Code`/`Response` after a failed request (unfixable here — `sendValue` drops `undefined`), FC-2 lifecycle, FC-3 **browser-only while deprecated `REST2` is browser+cloud, which corrects NDA-011 §2**, FC-4 a 200 with an unparseable body loses its status, FC-5 no error-bus raise. |
| 58 | Insert Object Into Array | Data | 3/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 pinned decision (AR-1) · no repaired defect. The `shortDesc` was one of the four wrong ones and is replaced. — also: ✅ NDA-004 §2 — `Failure`/`Error`, `insert-into-array/no-array` and `/no-object-id`. Both branches were `sendWarning` + bare `return` behind an editor-only guard |
| 59 | JSON Stream Parser | Data | 5/10 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (L4, **filed**) · C1 fixed · 🔵 E1, and the deliberate absence of a runtime raise, both documented |
| 60 | Object _(deprecated)_ | Data | 6/5 | ⚠️ |  | 0% | safe | browser | 🔵 NDA-004 §2 — **read and deliberately left silent.** Its `scheduleStore` has the same shape as Set Object Properties' but no `Do`: it is reached from any value arriving at a `prop-…` port, so failing would fire on the ordinary boot path. Values are retained and written when an object arrives |
| 61 | Object | Data | 5/3 | ⚠️ |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (OB-i, OB-ii — both fixed) · **noted:** a `prop-…` output cannot go empty when the bound object changes, for the same runtime-wide reason as FC-1. — also: 🔵 NDA-004 §2 — **read and deliberately left silent.** Its `scheduleStore` has the same shape as Set Object Properties' but no `Do`: it is reached from any value arriving at a `prop-…` port, so failing would fire on the ordinary boot path. Values are retained and written when an object arrives |
| 62 | Optimistic Update | Data | 10/13 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D4, **fixed**) · C1 fixed |
| 63 | Pattern Extractor | Data | 5/10 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D3, **fixed**) · C1 fixed · 🔵 E1 by design, documented |
| 64 | Remove Object From Array | Data | 3/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (DA-vi, fixed) · 🔵 **pinned, not fixed:** removing a record that *exists* but sits in a different array is still reported as `Done`. Genuinely ambiguous — an idempotent "make sure this is not in here" is defensible, and unlike the fixed case the operation is not impossible, merely unnecessary. Pinned by a row so it is a decision rather than an accident. — also: ✅ NDA-004 §2 — `Failure`/`Error`, `remove-from-array/no-array` and `/no-object-id`. Two bare `return`s: no diagnosis in any runtime, including the editor |
| 65 | Remove Record Relation | Data | 5/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (DA-ii, fixed) — also: ✅ NDA-004 §2 — twin of Add Record Relation |
| 66 | Repeater Item | Data | 1/3 | ⚠️ |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — clean · 🔵 **recorded:** F1 is unmet by BINDING-CONTRACT §(a) — the Repeater cannot be named — and the docs URL is under `nodes/ui-controls/` while the node's category is `Data`. Cosmetic, but it is the only node in my ten whose documentation lives outside its category. — also: ✅ NDA-004 §2 / NDA-015 — the **sixth** hand-rolled `_forEachModel` read, missed by the sweep that converged the other five. Now uses `resolveForEachItem`, so it takes the scope chain (a Repeater Item nested one component deep resolves) and raises `repeater-item/no-item-in-scope` once instead of handing out an undefined Item Id for ever |
| 67 | REST _(deprecated)_ | Data | 6/3 |  |  | 0% | safe | browser, cloud |  |
| 68 | Run Tasks | Data | 10/4 |  |  | 0% | safe | browser, cloud | ✅ NDA-004 — raises `run-tasks/no-completion-output` and ends the run instead of hanging (corpus F1/F1′). NDA-009 §1 still owes the editor-time check |
| 69 | Server-Sent Events | Data | 17/17 |  |  | 100% | client-only | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (D2 **fixed**, L5 **filed**) · C1 fixed · 🔵 E1 by design |
| 70 | Set Global Store | Data | 6/3 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D4, **fixed**) · C1 fixed |
| 71 | Set Object Properties | Data | 5/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (OB-ii, fixed — shared with `Object`, counted once) · **filed:** FC-6, FC-7, FC-8 in the shared helper. — also: ✅ NDA-004 §2 — `Failure`/`Error` and `set-object-properties/no-object`. `Do` with no object bound wrote nothing and said nothing. Raises in `explicit` mode only; in `foreach` mode `foreachitem.ts` already raised the precise reason, and the graph surface fires either way |
| 72 | Set Variable | Data | 3/3 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-7, filed). — also: ✅ NDA-004 §2 — **it reported `Done` for a write nothing could read.** `Model.set(undefined, value)` neither throws nor no-ops: it writes a key literally named `undefined` on the shared variables record. Third confirmed instance of the false-success shape, reached by not filling in a field. `set-variable/no-name`, empty string included; refuses to write as well as reporting |
| 73 | State History | Data | 6/8 | ⚠️ |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (D1, D5 — both **fixed**) · C1 fixed · 🔵 B1 and E1 by design — also: 🔵 NDA-004 §2 — its `storeName` setter normalises absent/empty to `'app'`, so there is no target it can fail to find. Its one action always has a real store name and `historyChanged` is a notification, not an action completion. **Two ✅ siblings in the same family** (rows 78, 84); a corpus row pins the absence of the port |
| 74 | State Snapshot | Data | 5/7 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (L1, **filed**) · C1 fixed · 🔵 E1 by design — also: ✅ NDA-004 §2 / FINDINGS B-iv — had *no* channel and *no* `Failure` port: an author could wire `Saved` and had nothing to sequence off a failure, only an `Error` string to poll. `state-snapshot/operation-failed`, guarded on a defined message because `setError(undefined)` is how every success path clears |
| 75 | Static Array | Data | 3/4 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-3, fixed) · **a second, smaller thing fixed with it and worth naming separately:** `parseData` installed a *fresh empty* collection before attempting the parse, so a failed parse left the node internally inconsistent — `Count` read 0 from the new empty collection while `Items` was never re-flagged and downstream still held the previous one. Building the collection only on success is what lets `Items`' new description honestly say "unchanged while the JSON cannot be parsed". |
| 76 | Stream Buffer | Data | 7/10 |  |  | 100% | partial | browser, cloud | ⚠️ NDA-012 (Data) — clean — the only node of the fifteen with no defect on any check · C1 fixed · 🔵 E1 by design — also: ✅ NDA-004 §2 — `Add` with nothing on `Data` returned bare. `stream-buffer/no-data`. `Flush`/`Clear` on an empty buffer deliberately do **not** report: a timed flush with nothing to send is a legitimate empty result, Open File Picker's `Cancelled` question answered the other way |
| 77 | Subscribe to Store | Data | 2/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D1, **fixed**) · C1 fixed |
| 78 | Text Accumulator | Data | 6/14 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — 1 defect (D4, **fixed**) · C1 fixed · 🔵 A3/L8 and E1 by design, both now documented on the ports |
| 79 | Undo / Redo | Data | 5/7 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — clean · C1 fixed · 🔵 B3 by design, documented — also: ✅ NDA-004 §2 / FINDINGS B-iv — twin of row 78. `undo/operation-failed`. Four existing test assertions read `expect(signals).toEqual([])` while being named "reports an out-of-range jump"; they encoded the defect, not the claim |
| 80 | Update Record | Data | 7/4 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Data) — clean on its own terms · inherits DA-iii through the shared base. ⚠️ `Properties to  store` carries a **double space** in its `displayName` — cosmetic, recorded not fixed, because it is the kind of string a screenshot corpus should catch rather than a one-off edit. |
| 81 | Variable _(deprecated)_ | Data | 4/5 | ⚠️ |  | 0% | safe | browser |  |
| 82 | Variable | Data | 3/6 |  |  | 100% | safe | browser | ⚠️ NDA-012 (Data) — 1 defect (AR-5, fixed) · **the lesson is about the previous pass, not the node.** NDA-004 §2 fixed `Set Variable`'s `Model.set(undefined, …)`, wrote the paragraph explaining it, and did not open the file next to it that does the same thing on a value arrival instead of a `Do`. A sweep scoped by *trigger kind* — "which nodes have a `Do` that can go nowhere" — cannot find the same defect on a node that has no `Do`. |
| 83 | WebSocket | Data | 18/18 |  |  | 100% | client-only | browser, cloud | ⚠️ NDA-012 (Data) — 2 defects (L2, L3 — both **filed**) · C1 fixed |
| 84 | Receive Event | Events | 3/1 |  |  | 100% | safe | browser | ✅ **FIXED 2026-07-30 (NDA-012 Events).** `handleEvent` flags the payload outputs dirty *before* pulsing `Received`, so a node acting on the signal reads this event's data rather than the previous one — the whole point of a node that carries a payload with a signal. Corpus row inverted, not deleted; restoring the statement order reddens it while the wiring control stays green. ⚠️ `Signal To Index` still has the shape and is deliberately unfixed |
| 85 | Send Event | Events | 4/3 |  |  | 100% | safe | browser | ✅ NDA-004 — `Sent`/`Failure`; empty channel name reported rather than dispatched into the void |
| 86 | Color Blend | Interpolation | 1/1 |  |  | 100% | safe | browser |  |
| 87 | Number Blend _(deprecated)_ | Interpolation | 2/1 |  |  | 100% | safe | browser |  |
| 88 | Script Downloader _(deprecated)_ | Javascript | 2/1 | ⚠️ |  | 100% | client-only | browser |  |
| 89 | And | Logic | 0/1 |  |  | 100% | safe | browser, cloud |  |
| 90 | Condition | Logic | 2/4 | ⚠️ |  | 100% | safe | browser, cloud |  |
| 91 | Inverter | Logic | 1/1 |  |  | 100% | safe | browser, cloud |  |
| 92 | Or | Logic | 0/1 |  |  | 100% | safe | browser, cloud |  |
| 93 | Signal To Index _(deprecated)_ | Logic | 0/2 |  |  | 100% | safe | browser |  |
| 94 | Switch | Logic | 4/4 | ⚠️ |  | 100% | safe | browser |  |
| 95 | Value Changed | Logic | 1/1 |  |  | 100% | safe | browser |  |
| 96 | Counter | Math | 7/2 | ⚠️ |  | 100% | safe | browser, cloud |  |
| 97 | Number Remapper | Math | 6/1 |  |  | 100% | safe | browser | ✅ **FIXED 2026-07-30 (NDA-012 Math).** Both input endpoints defaulted to `0` — the degenerate branch — so every freshly dropped node reported `Output Minimum` for every input, the Expression-returns-`0` shape sitting in the default configuration. `initialize` now uses an input maximum of 1, so an unconfigured node passes its input through 0..1. Reporting instead would fire on the boot path of a graph still being built. ⚠️ The fix is in `initialize`, not the port `default` — a declared default does not run its setter at construction |
| 98 | Close Popup | Navigation | 4/3 |  |  | 100% | safe | browser | ✅ NDA-004 §2 — `Closed`/`Failure`/`Error`; no-popup-in-scope now reported. Targeting stays NDA-010 §2 / NDA-015 |
| 99 | External Link | Navigation | 3/3 |  |  | 100% | safe | browser | ✅ NDA-004 — `Success`/`Failure`/`Error`; catches the popup-blocker case that made the button look dead |
| 100 | Navigate | Navigation | 2/3 |  |  | 100% | safe | browser | ✅ NDA-004 §2 — `Failure`/`Error`. `navigate/no-target-page` and `navigate/page-not-found`; the latter replaces a bare `return` that carried `//TODO: send error to editor`. The editor adapter's health warning is the edit-time half and does not travel |
| 101 | Navigate To Path | Navigation | 4/3 |  |  | 100% | safe | browser | ✅ NDA-004 — `Success`/`Failure`/`Error`; the `path === undefined` return is no longer silent |
| 102 | Page Inputs | Navigation | 2/0 |  |  | 100% | safe | browser |  |
| 103 | Pop Component Stack | Navigation | 3/3 |  |  | 100% | safe | browser | ✅ NDA-004 §3 — done in NDA-008 §3 (`0db1d770`), not pending: `Popped`/`Failure`/`Error` and `reportFailure` raising on the bus (`navigate-back.ts:63-80,133-138`). **This row said ⏳ until 2026-07-30** — the work landed under another task's commit and nobody came back to the register. A `void` return from `backCallback` counts as success on purpose |
| 104 | Push Component To Stack | Navigation | 3/3 |  |  | 100% | safe | browser | ✅ NDA-004 §2 — `Failure`/`Error` for five drops: three guards in `navigateAsync` and the same three copied into `replaceAsync`. Reported via a `hasFailed` callback because the stack's `asyncQueue` outlives the caller's frame; the *node* owns the port, NDA-008 §3's decision for `back()` |
| 105 | Show Popup | Navigation | 3/4 |  |  | 100% | safe | browser | ✅ NDA-004 §2 — every outcome it had was a *later* one, so a popup that never opened looked like one the user had not finished with. `show-popup/no-target`, and `show-popup/target-failed` for the **unhandled promise rejection**: `showPopup` is `async` and `getComponentModel` throws for an unregistered name, which the node dropped. Residual: a runtime with no popup host resolves successfully having done nothing |
| 106 | Device Orientation _(deprecated)_ | Sensors | 0/3 |  |  | 100% | client-only | browser |  |
| 107 | String Format | String Manipulation | 1/1 |  |  | 100% | safe | browser, cloud |  |
| 108 | Substring | String Manipulation | 3/1 |  |  | 100% | safe | browser, cloud |  |
| 109 | Unique Id | String Manipulation | 1/2 | ⚠️ |  | 100% | safe | browser, cloud | ✅ NDA-004 §3 — `Generated` |
| 110 | Boolean To String | Utilities | 3/2 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 (Utilities) — 1 defect: `undefined` does not abstain, so an upstream with nothing to say is indistinguishable from one saying `false` and `Current Value` switches to `String for false`. Signal ordering is **correct** here, unlike the two G-class nodes found in earlier categories |
| 111 | Date To String | Utilities | 2/3 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 3 defects. **FIXED, and the headline of the batch: `Invalid Date` had never fired.** The catch called `flagOutputDirty('onError')`, which is `sendValue(name, output.value)` — a signal output's `value` is `undefined`, so receivers got a *value* rather than a pulse. PLAT-003 NOTES §25 recorded the line and correctly left it alone; nobody came back. Open: changing `Format` fires `Date Changed` though no date changed (third instance of the emitting-value-port shape); `{monthShort}` is hard-coded `en-US`. FINDINGS **SR-v** |
| 112 | Delay | Utilities | 5/2 | ⚠️ |  | 100% | partial | browser | ⚠️ NDA-012 — 2 defects: `Duration`/`Start Delay` assign straight onto the timer, so a `null` or `undefined` over a wire yields a countdown that never finishes; and `Stop` emits nothing, so a graph sequenced behind `Finished` stops dead. **But it is the model for the Animation fixes** — the only timer-owning node in the library that has always stopped its timer on delete |
| 113 | Globals _(deprecated)_ | Utilities | 0/0 |  |  | 100% | safe | browser | ✅ NDA-012 — no defects; the `_cachedInputValues` crash PLAT-003 slice 10 found is fixed and documented in place. C1 unmeasurable (`0/0`, Port Editor ports). Its lack of namespacing is 🔵 — it is the node's design and the reason it is deprecated |
| 114 | Index To String _(deprecated)_ | Utilities | 1/2 |  |  | 100% | safe | browser | ⚠️ NDA-012 — 2 defects: `value |
| 115 | On App Error | Utilities | 1/7 |  |  | 100% | safe | browser, cloud |  |
| 116 | Open File Picker | Utilities | 3/9 |  |  | 100% | client-only | browser | ⚠️ NDA-012 — 1 defect: no delete listener, so a dialog open when the node is deleted still fires its handlers against a dead node. Otherwise **the fullest completion set in the batch** — `Open` → `Success` *or* `Cancelled` *or* `Failure`, with cancelled deliberately not a failure. 🔵 needing a live check: `input.accept` is assigned `undefined`, which the DOM stringifies to `"undefined"` — harmless only because browsers discard an invalid accept token — also: ✅ NDA-004 §2 |
| 117 | Screen Resolution | Utilities | 0/3 |  |  | 100% | client-only | browser | ⚠️ NDA-012 — 2 defects. **FIXED**: the `resize` listener was an anonymous arrow with no `removeEventListener` anywhere, so every Screen Resolution node ever created stayed subscribed to `window` for the life of the page. Open: `Aspect Ratio` is `NaN` when unset, which propagates where `undefined` would abstain |
| 118 | String Mapper | Utilities | 2/1 |  |  | 100% | safe | browser, cloud | ⚠️ NDA-012 — 1 defect: the numbered setters coerce `undefined` to `''`, so an input row the author left blank matches an *empty* `Input String` and publishes that row's mapping instead of `Default`. Its numbered ports are also **invisible to the catalog entirely** — `registerNumberedInput` writes no metadata (FINDINGS **SR-ii**) |
| 119 | Boolean | Variables | 3/3 | ⚠️ |  | 50% | safe | browser, cloud |  |
| 120 | Color | Variables | 3/3 | ⚠️ |  | 50% | safe | browser |  |
| 121 | Number | Variables | 3/3 | ⚠️ |  | 50% | safe | browser, cloud |  |
| 122 | String | Variables | 3/4 | ⚠️ |  | 57% | safe | browser, cloud |  |
| 123 | Button _(deprecated)_ | Visual | 48/21 |  |  | 42% | safe | browser |  |
| 124 | Button | Visual | 79/20 |  |  | 53% | safe | browser |  |
| 125 | Checkbox _(deprecated)_ | Visual | 39/21 |  |  | 45% | safe | browser |  |
| 126 | Checkbox | Visual | 76/20 | ⚠️ |  | 48% | safe | browser |  |
| 127 | Circle | Visual | 33/14 |  |  | 77% | safe | browser |  |
| 128 | Columns | Visual | 16/9 |  |  | 40% | safe | browser |  |
| 129 | Component Children | Visual | 0/0 |  |  | 100% | safe | browser |  |
| 130 | Component Stack | Visual | 9/11 | ⚠️ |  | 50% | partial | browser |  |
| 131 | Drag | Visual | 16/16 | ⚠️ |  | 31% | safe | browser |  |
| 132 | Dropdown | Visual | 91/20 |  |  | 48% | safe | browser |  |
| 133 | Field Set _(deprecated)_ | Visual | 33/9 |  |  | 88% | safe | browser |  |
| 134 | Form _(deprecated)_ | Visual | 33/10 |  |  | 86% | safe | browser |  |
| 135 | Group | Visual | 85/20 | ⚠️ |  | 52% | safe | browser |  |
| 136 | Icon | Visual | 30/8 |  |  | 92% | safe | browser |  |
| 137 | Image | Visual | 62/16 |  |  | 64% | safe | browser |  |
| 138 | Label _(deprecated)_ | Visual | 57/9 |  |  | 50% | safe | browser |  |
| 139 | Options _(deprecated)_ | Visual | 52/21 |  |  | 51% | safe | browser |  |
| 140 | Page | Visual | 23/9 | ⚠️ |  | 44% | safe | browser |  |
| 141 | Page Router | Visual | 9/11 | ⚠️ |  | 50% | safe | browser |  |
| 142 | Radio Button _(deprecated)_ | Visual | 39/20 |  |  | 46% | safe | browser |  |
| 143 | Radio Button | Visual | 76/19 |  |  | 48% | safe | browser |  |
| 144 | Radio Button Group | Visual | 33/11 |  |  | 84% | safe | browser |  |
| 145 | Range _(deprecated)_ | Visual | 40/22 |  |  | 44% | safe | browser |  |
| 146 | Repeater | Visual | 5/2 | ⚠️ |  | 0% | safe | browser | ✅ NDA-004 §3 — `Items Rendered`, fired when the operation queue drains (not when `refresh()` returns) |
| 147 | Slider | Visual | 92/21 |  |  | 27% | safe | browser |  |
| 148 | Text | Visual | 43/14 |  |  | 75% | safe | browser |  |
| 149 | Text Input _(deprecated)_ | Visual | 55/21 | ⚠️ |  | 49% | safe | browser |  |
| 150 | Text Input | Visual | 97/21 | ⚠️ |  | 45% | safe | browser |  |
| 151 | Video | Visual | 66/23 |  |  | 47% | safe | browser | ✅ NDA-004 §2 — `Playback Failure`/`Error`. `play()`'s rejected promise was dropped at all three sites (`video/play-rejected`), and the element's `error` event had no listener (`video/media-error`). `AbortError` is deliberately **not** reported — a `Pause` superseding a `Play` rejects on a correct graph |
