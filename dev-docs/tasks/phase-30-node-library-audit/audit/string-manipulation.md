# Audit worksheet — String Manipulation (3 nodes)

Generated by `node scripts/node-audit/worksheets.js`. **Existing files are never overwritten** —
delete this file to regenerate it.

Checks are defined in [NDA-012](../NDA-012-PER-NODE-AUDIT.md) and derive from the defect classes
in [FINDINGS.md](../FINDINGS.md). Verdicts: ✅ pass · ⚠️ defect · 🔵 by design, document it ·
⬜ not audited.

**Audited 2026-07-30. 3/3 complete. C1 closed — 9/9 ports documented.**

---

### String Format  `String Format`

1 inputs / 1 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser, cloud · dynamic ports

Source: [`stringformat.ts`](../../../../packages/noodl-runtime/src/nodes/std-library/stringformat.ts) · Docs: [link](https://docs.noodl.net/nodes/string-manipulation/string-format)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | n/a | |
| A3 |  | ✅ | `scheduleFormat` coalesces to one recompute per frame (`:107-116`), which is the right shape for a node whose inputs all arrive together |
| G1 |  | ✅ | `v !== undefined ? String(v) : ''` (`:89`) — an unset placeholder becomes empty rather than the text `"undefined"`. **Correct, and notably better than the String Variable's `cast`** |
| B1 | n/a — no action input | 🔵 | |
| B2 |  | n/a | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/2) | ✅ | **Closed 2026-07-30** — 2/2, and `Format`'s sentence now carries the repeat limitation |
| D1 |  | ⚠️ | The `{placeholder}` contract is a bare regex on both sides — but `setup` mirrors the same parse editor-side (`:129-150`), so the ports an author sees do come from the format string. Better than most class-D instances; the residual gap is that `[A-Za-z0-9_]*` matches `{}`, giving a port with an empty name |
| E1 | ✅ no dead-end types | ✅ | Placeholder inputs are `string` |
| F1 |  | n/a | |
| H1 | declares `safe` | ✅ | |
| — |  | ⚠️ | **A placeholder used twice fills only the first time.** `formatted.replace('{' + name + '}', …)` with a *string* pattern substitutes one occurrence (`:89`), so `"{name} and {name}"` renders the second literally. Documented in place and kept verbatim; `updatePorts` dedupes for the editor, so the author sees one port and reasonably expects it to fill both |

**Verdict:** ⚠️ 2 defects — the repeated placeholder is the visible one

---

### Substring  `Substring`

3 inputs / 1 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser, cloud

Source: [`substring.ts`](../../../../packages/noodl-runtime/src/nodes/std-library/substring.ts) · Docs: [link](https://docs.noodl.net/nodes/string-manipulation/substring)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | n/a | |
| A3 |  | ✅ | Lazy recompute behind `resultDirty` (`:78-88`) |
| G1 |  | ⚠️⚠️ | **`null` down the `String` wire throws.** `value.toString()` (`:65`) was chosen over `String(value)` deliberately, to preserve the original's behaviour — and the original's behaviour is a `TypeError` out of an input setter. Per the Empty-Value Contract `null` should *clear*; per harness fact 4 the throw is not a crash but it abandons the rest of the node's update pass and leaves one unstructured console line |
| B1 | n/a — no action input | 🔵 | |
| B2 |  | n/a | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/4) | ✅ | **Closed 2026-07-30** — 4/4, and `String`'s sentence now warns about the `null` case |
| D1 |  | ✅ | |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | ✅ | |
| — |  | ⚠️ | **The declared default and the real default disagree.** `end` declares `default: 0` but `initialize` sets `endIndex = -1`, and `-1` means "to the end of the string" (`:29`, `:45-48`). So leaving `End` alone returns the whole string while explicitly typing `0` returns nothing — and the property panel shows `0` as though that were the current state. The port's declared default is a statement about the node that is not true of it |

**Verdict:** ⚠️ 2 defects — both are about what an *empty* input means, which is the category's theme

---

### Unique Id  `Unique Id`

1 inputs / 2 outputs · 1 signal in / 1 signal out · docs **100%** · SSR `safe` · browser, cloud

Source: [`uniqueid.ts`](../../../../packages/noodl-runtime/src/nodes/std-library/uniqueid.ts) · Docs: [link](https://docs.noodl.net/nodes/utilities/unique-id)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | n/a | |
| A3 |  | ✅ | |
| G1 |  | n/a | Signal input only |
| B1 | ✅ | 🔵 | `Model.guid()` cannot fail |
| B2 |  | n/a | |
| B3 | ✅ | ✅ | **Fixed by NDA-004 §3** — `New` used to emit nothing at all |
| C1 | ⚠️ **0%** (0/3) | ✅ | **Closed 2026-07-30** — 3/3 |
| D1 |  | ✅ | |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | 🔵 | A remount mints a new id, which is correct for an identity node and worth knowing; the port sentence says so |
| — |  | ✅ | **Values before the signal, done right** (`:33-37`) — `flagOutputDirty('guid')` precedes `sendSignalOnOutput('generated')`, with a comment saying why. The counter-example to Receive Event and Signal To Index, and it is this recent because NDA-004 §3 wrote it |

**Verdict:** ✅ no defects — the second clean node found in this pass

---

## Category summary

| | |
|---|---|
| Nodes audited | 3 / 3 |
| Nodes with ≥1 defect | 2 |
| Distinct defects | 4 |
| **New, not already on record** | **4** |
| C1 closed | 9/9 ports |

Three of the four are about what an absent value means: `null` throwing out of Substring's setter, a
declared default that contradicts the real one, and a placeholder that is used twice but filled once.
`String Format`'s `undefined → ''` is the category's one *correct* empty-value decision and is worth
citing when NDA-003's contract work reaches these nodes.

**Find rate 3/3 audited, 2 with defects, 4 new.** No decline.
