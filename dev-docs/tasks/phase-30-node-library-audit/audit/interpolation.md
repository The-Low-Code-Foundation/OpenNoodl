# Audit worksheet — Interpolation (2 nodes)

Generated by `node scripts/node-audit/worksheets.js`. **Existing files are never overwritten** —
delete this file to regenerate it.

Checks are defined in [NDA-012](../NDA-012-PER-NODE-AUDIT.md) and derive from the defect classes
in [FINDINGS.md](../FINDINGS.md). Verdicts: ✅ pass · ⚠️ defect · 🔵 by design, document it ·
⬜ not audited.

**Audited 2026-07-30. 2/2 complete. C1 closed — 5/5 ports documented.**

---

### Color Blend  `Color Blend`

1 inputs / 1 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser · dynamic ports

Source: [`colorblend.ts`](../../../../packages/noodl-viewer-react/src/nodes/std-library/colorblend.ts) · Docs: [link](https://docs.noodl.net/nodes/utilities/color-blend)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | Every setter calls `updateColor` |
| A2 |  | n/a | |
| A3 |  | ✅ | |
| G1 |  | 🔵 | A missing colour falls back to `#000000` (`:96-98`). Defensible for a blend, and now on the port |
| B1 | n/a — no action input | 🔵 | |
| B2 |  | n/a | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/2) | ✅ | **Closed 2026-07-30** — 2/2, and `Result`'s sentence now states the 6-digit-hex requirement |
| D1 |  | ⚠️ | **The colour format is an unvalidated string contract.** `setRGB` assumes exactly `#RRGGBB` and reads fixed offsets (`:20-25`); anything else — a 3-digit `#RGB`, an `rgba()`, a named colour, an 8-digit hex with alpha — gives `parseInt(…) === NaN`, and `componentToHex` then produces the literal string `"NaN"`. **The output is `#NaNNaNNaN`, silently.** Class D with a class-B consequence: garbage out, nothing said |
| E1 | ✅ no dead-end types | ⚠️ | `Result` is `color`, and `color` casts to **nothing** (`{"from":"color","to":[]}`) — the dead-end type the Variables worksheet found. The blend's answer reaches only `color` inputs and cannot, for instance, be shown as text for debugging |
| F1 |  | n/a | |
| H1 | declares `safe` | ⚠️ | `browser` only; the file is arithmetic and string formatting |
| — |  | 🔵 | `rgb0`/`rgb1`/`rgb2` are module-level scratch arrays shared by every instance (`:36-39`). Safe because `updateColor` is synchronous and single-threaded, and the comment says why — but it is the kind of thing that stops being safe the moment anything here becomes async |

**Verdict:** ⚠️ 3 defects — the `#NaNNaNNaN` output is the one to fix

---

### Number Blend  `Number Blend`

2 inputs / 1 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser · **deprecated** · dynamic ports

Source: [`numberblend.ts`](../../../../packages/noodl-viewer-react/src/nodes-deprecated/std-library/numberblend.ts) · Docs: [link](https://docs.noodl.net/nodes/utilities/number-blend)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | n/a | |
| A3 |  | ✅ | |
| G1 |  | ⚠️ | No empty handling; a cleared number coerces to `0` and blends toward it |
| B1 | ⚠️ **none** | 🔵 | Feeds ⏳ item 9, the deprecated-five policy question |
| B2 |  | n/a | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/3) | ✅ | **Closed 2026-07-30** — 3/3 |
| D1 |  | ✅ | Numbers, not strings — which is exactly why this node has none of Color Blend's parsing problem |
| E1 | ✅ no dead-end types | ✅ | `number` is well connected |
| F1 |  | n/a | |
| H1 | declares `safe` | ⚠️ | `browser` only for pure arithmetic |
| — |  | 🔵 | **Deprecated, and it is the *better* of the pair.** It does the same job as Color Blend over a type that has no format ambiguity and no dead end. Worth a line in whatever decides the deprecated dispositions: the surviving sibling is the one with the defects |

**Verdict:** ⚠️ 2 defects, both minor, and one observation that matters more than either

---

## Category summary

| | |
|---|---|
| Nodes audited | 2 / 2 |
| Nodes with ≥1 defect | 2 |
| Distinct defects | 5 |
| **New, not already on record** | **4** (the `color` dead end is class E, already filed) |
| C1 closed | 5/5 ports |

The finding worth carrying out of this category is not a defect at all: **the deprecated node is
healthier than the one that replaced its type.** Colour is where the format ambiguity and the type
dead end both live, and Number Blend has neither. Anything that decides deprecated dispositions
should not assume deprecation tracks quality.

**Find rate 2/2, 4 new.** No decline.
