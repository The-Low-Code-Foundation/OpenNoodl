# Audit worksheet — Sensors (1 node)

Generated by `node scripts/node-audit/worksheets.js`. **Existing files are never overwritten** —
delete this file to regenerate it.

Checks are defined in [NDA-012](../NDA-012-PER-NODE-AUDIT.md) and derive from the defect classes
in [FINDINGS.md](../FINDINGS.md). Verdicts: ✅ pass · ⚠️ defect · 🔵 by design, document it ·
⬜ not audited.

**Audited 2026-07-30. 1/1 complete. C1 closed — 3/3 ports documented.**

---

### Gyroscope  `Gyroscope`

0 inputs / 3 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser · **deprecated** · not in picker

Source: [`gyroscope.ts`](../../../../packages/noodl-viewer-react/src/nodes-deprecated/std-library/gyroscope.ts) · Docs: [link](https://docs.noodl.net/nodes/sensors/gyroscope)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | Each axis is flagged only when it moves (`:59-72`) |
| A2 |  | n/a | |
| A3 |  | ✅ | |
| G1 |  | 🔵 | `DeviceOrientationEvent` fields are nullable and are passed through unchanged, so "no reading" is distinguishable from "zero". Correct, and better than several non-deprecated nodes manage |
| B1 | n/a — no action input | ⚠️ | See below — this node has a failure mode and no way at all to report it |
| B2 |  | ⚠️ | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/3) | ✅ | **Closed 2026-07-30** — 3/3, and all three sentences carry the iOS warning, which is the only place an author can currently learn it |
| D1 |  | ✅ | |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | ✅ | Removes its `deviceorientation` listener on `applicationDataReloaded` (`:29-32`) |
| — |  | ⚠️⚠️ | **Permanently silent on iOS, with no diagnosis.** Since iOS 13 `deviceorientation` delivers nothing until `DeviceOrientationEvent.requestPermission()` has been called from a user gesture. This node never calls it and has no port through which it could ask, so on the platform where a gyroscope is most likely to be wanted the three outputs sit at their initial values forever and nothing anywhere says why. `declares safe` for SSR is honest; *available on the device* is what is not |
| — |  | 🔵 | `Rotation X` is `-beta` and `Rotation Z` is `-alpha` — the axis remapping and the sign flips are undocumented anywhere but here. Now on the ports |

**Verdict:** ⚠️ 1 defect, and it makes the node useless on the platform it exists for

---

## Category summary

| | |
|---|---|
| Nodes audited | 1 / 1 |
| Nodes with ≥1 defect | 1 |
| Distinct defects | 1 |
| **New, not already on record** | **1** |
| C1 closed | 3/3 ports |

The node is deprecated and out of the picker, so this feeds the deprecated-five policy question
rather than a fix. It is a useful data point *for* that question: the honest options here are
"delete it" or "make it request permission", and **documenting it is not one of them** — a node that
cannot work on the platform it targets is not improved by a port sentence saying so, which is exactly
what this audit was able to give it.

**Find rate 1/1, 1 new.**
