# Audit worksheet — Math (2 nodes)

Generated by `node scripts/node-audit/worksheets.js`. **Existing files are never overwritten** —
delete this file to regenerate it.

Checks are defined in [NDA-012](../NDA-012-PER-NODE-AUDIT.md) and derive from the defect classes
in [FINDINGS.md](../FINDINGS.md). Verdicts: ✅ pass · ⚠️ defect · 🔵 by design, document it ·
⬜ not audited.

**Audited 2026-07-30. 2/2 complete. C1 closed — 16/16 ports documented.**

Counter's boot behaviour is pinned in
[`nda-012-small-categories.test.ts`](../../../../packages/noodl-runtime/test/corpus/nda-012-small-categories.test.ts).

---

### Counter  `Counter`

7 inputs / 2 outputs · 3 signal in / 1 signal out · docs **100%** · SSR `safe` · browser, cloud

Source: [`counter.ts`](../../../../packages/noodl-runtime/src/nodes/std-library/counter.ts) · Docs: [link](https://docs.noodl.net/nodes/math/counter)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | n/a | |
| A3 |  | ⚠️ | **Hitting a limit is completely silent.** `Increase` at Max Value and `Decrease` at Min Value `return` with no signal at all (`counter.ts:44-46`, `:57-59`), so "the button did nothing" and "the node is broken" look identical. By Open File Picker's reasoning (NDA-004 §2) this wants a **completion** signal — the count did not move, but nothing went wrong |
| G1 |  | ⚠️ | `Number(value)` on `Start Value`, `Min Value` and `Max Value` (`:88`, `:106`, `:117`). `Number(undefined)` is `NaN`, and every limit comparison against `NaN` is false — so **clearing Max Value silently disables the limit** while `Limits Enabled` still reads true. Same family as the Variables `Number` finding, with a worse consequence |
| B1 | ⚠️ **none** | 🔵 | Counting cannot fail; see A3 for the completion-signal gap, which is a different question |
| B2 |  | n/a | |
| B3 | ✅ | ⚠️ | `Count Changed` terminates the two paths that *move*; the two that decline to move terminate in nothing. That is the A3 defect stated as a B3 one — one fix |
| C1 | ⚠️ **0%** (0/9) | ✅ | **Closed 2026-07-30** — 9/9, and the limit ports now say they are ignored unless Limits Enabled |
| D1 |  | ✅ | |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | ✅ | Pure state |
| — |  | ⚠️ | **An authored `Start Value` announces a count change at page load** (`:90-95`). Second instance of the shape in this pass — see Switch in the Logic worksheet. Pinned, with the control showing an unset port is silent |
| — |  | 🔵 | `Reset`'s early return reads `this.currentValue`, which is always `undefined`, so it has never fired and Reset always signals even when nothing changed. Known, documented in place, left by PLAT-003 §25. Not counted as new |

**Verdict:** ⚠️ 3 new defects — the silent limit is the one an author meets first

---

### Number Remapper  `Number Remapper`

6 inputs / 1 outputs · 0 signal in / 0 signal out · docs **100%** · SSR `safe` · browser

Source: [`numberremapper.ts`](../../../../packages/noodl-viewer-react/src/nodes/std-library/numberremapper.ts) · Docs: [link](https://docs.noodl.net/nodes/math/number-remapper)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | Every setter recomputes |
| A2 |  | n/a | |
| A3 |  | ✅ | Recomputes and flags on every input, which is right for a pure function this cheap |
| G1 |  | ⚠️ | No empty handling anywhere. A cleared input arrives as `null`, arithmetic coerces it to `0`, and the node reports a confident number from a value the author has just removed |
| B1 | n/a — no action input | 🔵 | |
| B2 |  | n/a | |
| B3 | n/a | n/a | |
| C1 | ⚠️ **0%** (0/7) | ✅ | **Closed 2026-07-30** — 7/7, and the two Input ports now say what happens when they are equal |
| D1 |  | ✅ | |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | ⚠️ | `browser` only, and the file contains nothing but arithmetic |
| — |  | ✅ | ~~**An unconfigured node reports a plausible constant.**~~ **FIXED 2026-07-30** — both input endpoints defaulted to `0`, the degenerate branch (`:127-128`), so `Remapped Value` was `Output Minimum` for every input in the state every freshly dropped node was in. `initialize` now sets an input maximum of 1, so a fresh node passes its input through 0..1. **Reporting it would have been the wrong repair** — an unconfigured node is degenerate for the whole boot path, so a `Failure` would fire on a graph the author is still building. ⚠️ The fix had to be in `initialize`, not the port's `default`: a declared default does not run its setter at construction, and reverting only `initialize` reddens the row |

**Verdict:** ⚠️ 3 defects — the degenerate default is the worst, because it is what the node ships in

---

## Category summary

| | |
|---|---|
| Nodes audited | 2 / 2 |
| Nodes with ≥1 defect | 2 |
| Distinct defects | 6 |
| **New, not already on record** | **6** |
| C1 closed | 16/16 ports |

Two nodes, six new defects, and **both of the worst two are about a node reporting a confident number
it has no basis for**: the Counter whose limits silently stopped applying, and the Number Remapper
that outputs a constant until two ports that both default to `0` are made to differ. Neither says
anything.

**Find rate 2/2, 6 new — the highest per-node rate of the pass.** No decline.
