# Audit worksheet — Javascript (1 node)

Generated by `node scripts/node-audit/worksheets.js`. **Existing files are never overwritten** —
delete this file to regenerate it.

Checks are defined in [NDA-012](../NDA-012-PER-NODE-AUDIT.md) and derive from the defect classes
in [FINDINGS.md](../FINDINGS.md). Verdicts: ✅ pass · ⚠️ defect · 🔵 by design, document it ·
⬜ not audited.

**Audited 2026-07-30. 1/1 complete. C1 closed — 3/3 ports documented.**

⚠️ The category name is misleading and worth correcting somewhere: this category does **not** contain
the Function node (`JavaScriptFunction`, category `CustomCode`). It contains one deprecated script
loader.

---

### Script Downloader  `Script Downloader`

2 inputs / 1 outputs · 1 signal in / 1 signal out · docs **100%** · SSR `safe` · browser · **deprecated** · not in picker · dynamic ports

Source: [`scriptdownloader.ts`](../../../../packages/noodl-viewer-react/src/nodes-deprecated/std-library/scriptdownloader.ts) · Docs: [link](https://docs.noodl.net/nodes/javascript/script-downloader)

| Check | Pre-filled | Verdict | Note |
|---|---|---|---|
| A1 |  | ✅ | |
| A2 |  | ✅ | `Load` re-runs the scan and skips anything already in `<head>` (`:130-150`) |
| A3 |  | ✅ | |
| G1 |  | 🔵 | Empty script slots are trimmed and skipped |
| B1 | ⚠️ **none** | ⚠️⚠️ | **No error handling of any kind.** `scriptObj.onerror` is never wired (`:145-149`), so a 404, a CSP block or a syntax error in a fetched script produces nothing: no `Failure`, no raised code, no console line of the node's own. The author sees a `Loaded` that never fires and has no way to learn why |
| B2 |  | ⚠️ | Same gap; there is nothing to reach a deployed build with |
| B3 | ✅ | ⚠️ | `Load` has a terminating `Loaded`, but only on the success path — the failure path terminates in nothing at all, which is the B3 defect stated precisely |
| C1 | ⚠️ **0%** (0/3) | ✅ | **Closed 2026-07-30** — 3/3, and `Loaded`'s sentence now says a failed script means it never fires |
| D1 |  | ⚠️ | **Completion is inferred from a sentinel request, not observed.** The node appends a final `<script src="load_terminator.js">` and treats *its* load event as "everything before me is done" (`:152-167`), relying on browsers loading non-async scripts in order. `load_terminator.js` does not exist, so `Loaded` in practice fires on that script's **error**, not its load — the mechanism works by accident of `onreadystatechange` semantics. Fragile, and undocumented outside its own comment |
| E1 | ✅ no dead-end types | ✅ | |
| F1 |  | n/a | |
| H1 | declares `safe` | ⚠️ | Declares `safe` for SSR but is **entirely** `document` manipulation — under SSR it does nothing at all, which is arguably correct behaviour but is not what `safe` communicates. Worth checking against `PORT-TYPE-CONTRACT`'s neighbours when the SSR declarations are next reviewed |
| — |  | 🔵 | Its `setup` is an empty guard with no body, documented in place as deliberately not deleted (a registered `setup` being removed is a behaviour change) |
| — |  | 🔵 | It injects author-supplied URLs as `<script>` into the running app. Legitimate — that is the node's entire purpose — but it is the same trust question NDA-007 deferred for inline icon sets, and the two should be answered together if either is |

**Verdict:** ⚠️ 4 defects — a node whose whole job is fetching remote resources and which cannot report a failed fetch

---

## Category summary

| | |
|---|---|
| Nodes audited | 1 / 1 |
| Nodes with ≥1 defect | 1 |
| Distinct defects | 4 |
| **New, not already on record** | **4** |
| C1 closed | 3/3 ports |

Deprecated, so all four feed the deprecated-five policy question rather than a fix. It is the
strongest argument *in* that question found so far: this is a node that does I/O, over the network,
against author-supplied URLs, and has no failure surface whatsoever — the case for "a deprecated node
still deserves a Failure port" is easier to make here than for any of the others.

**Find rate 1/1, 4 new.**
