# Phase 23 Progress — Visual Refresh (Track I)

**Created:** 2026-07-26 · **Status:** ✅ Complete (all 9 UIX tasks; 2026-07-26) · follow-ups UIX-004b / UIX-010 / UIX-011 / UIX-012 / UIX-013 tracked below

Visual-QA corpus + before/after gallery: [corpus/](./corpus/) (`corpus/gallery.html`, `corpus/README.md`). Follow-ups filed by UIX-009 and **specced 2026-07-26**: [UIX-010](./UIX-010-ICON-STROKE-GRID-REDRAW.md) (icon stroke-redraw + IconSize), [UIX-011](./UIX-011-LEGACY-CSS-ICON-RETIREMENT.md) (legacy CSS-background icons).

| ID | Title | Status | Notes |
|---|---|---|---|
| UIX-001 | Design tokens & typography foundation | ✅ Complete (2026-07-26) | Canonical tokens in core-ui; azure primary, danger-only red; see UIX-001-NOTES.md |
| UIX-002 | Legacy hex mop-up + ratchet | ✅ Complete (2026-07-26) | Editor styles 300→16 (16=1 documented exemption); per-package ratchet gate live in CI. ⚠️ core-ui slice (100 occ/16 files) **deferred** — run after UIX-003 (now merged); see UIX-002-NOTES.md |
| UIX-003 | Control kit | ✅ Complete (2026-07-26) | Forms/toast/chips/binding chip/box-model editor; see UIX-003-NOTES.md. Live residuals: box-model set/undo/reload round-trip, binding-chip source label, ToastCard live check |
| UIX-004 | Editor chrome & panels | ✅ Complete (2026-07-26) | Toolbar warnings→amber chip + route pill, Deploy stays azure; icon-rail active/hover accent pill (shared IconButton transparent variant); 62px properties field-grid + section rhythm; preview device-card framing; component-trail accent; dialog primary-right + splitter azure hover. See UIX-004-NOTES.md. **Deferred:** properties-panel node-category type-chip (legacy imperative header render path — live follow-up). Live residuals: properties panel across 5+ node types incl. dynamic-port (top priority), preview sizing/fit unchanged, amber warnings chip with a warning-carrying project, dialog primary-right in both legacy + React paths |
| UIX-004b | Node type-chip (deferred from UIX-004) | ⬜ Not started | Header type-chip needs markup surgery in Ports.ts/propertyeditor.ts imperative render path |
| UIX-005 | Canvas re-palette & node cards | ✅ Complete + live-verified (2026-07-26) | Token-driven CanvasTheme + theme-change repaint hook (UIX-008 dependency demonstrated working); anchor math preserved. Live QA done: cards/wires/grid vs mock, selection ring, diff annotation colours, zoom 25/50/200, theme toggle, long-label wrap, 0.39ms/frame paint bound; screenshots in screenshots/uix-005-live-qa/. Ratchet extended to canvas-paint TS. Residuals: large-corpus perf soak, full SUB-007/AIX-003 flow smoke. See UIX-005-NOTES.md |
| UIX-006 | Launcher & first-run | ✅ Complete (2026-07-26) | NodeGX wordmark + coral brand dot (sole sanctioned coral), inline tabs w/ accent underline, secondary Connect-GitHub, card grid (folder/sort/search/kebab preserved), amber "React 17 runtime" + neutral "Local only" chips, launcher toast migration, first-launch + no-results empty states, mono "NodeGX 0.1.0" footer; every "Noodl 2.9.3" string killed. Thumbnails: deterministic name-hash gradient + ghosted-initial placeholder ships unconditionally; deep persistence (cross-reload thumbURI) intentionally left off (VCS-bloat tradeoff, see NOTES). **Orchestrator fix (e2bd955):** live smoke found solid-white thumbURIs still rendering — added runtime blank-capture detection (≥96% near-white or near-zero spread → placeholder). Live-verified cold-boot. See UIX-006-NOTES.md |
| UIX-007 | Iconography | ✅ Complete (2026-07-26) | One `currentColor` Icon convention; 149 core-ui SVGs converted to `currentColor` (57 redrawn to the 16×16/1.5px round-stroke grid incl. UIX-003 alignment + justify/text-align glyphs), Icon/IconButton SCSS moved `path{fill}`→`.Icon{color}` (UIX-004 token values unchanged), dead `codicon.ttf/md` removed. **Redraw-in-place → zero import migration, no orphaned parallel set.** New `dev-docs/guidelines/ICONOGRAPHY.md`. Hex ratchet unchanged (SVGs unscanned; SCSS swapped fill→color, no hex). Live-verified (see Log): rail/toolbar/panel-tree render as one uniform stroke set in dark + light, amber warning triangle solid, icons re-tint via currentColor on theme flip. **Residuals:** `IconSize` enum still 16/20/24/28 not the mock's 12/14/15/17-per-context (needs a live sizing pass, deferred); 92 node-type/decorative glyphs now `currentColor` but still filled — triage list handed to UIX-009; legacy CSS-background icon sets + per-node art out of scope (inventoried for 009). See UIX-007-NOTES.md |
| UIX-008 | Light theme | ✅ Complete (2026-07-26) | Light token block activated (`data-theme` override on `:root` dark default) + light shadow set; `ThemeManager` (system/light/dark, default **system**, `prefers-color-scheme` live listener, persists `editor.theme` in EditorSettings, fires `nodegx:themechanged`); quiet tri-state selector in Editor Settings → Appearance (no toolbar toggle); canvas re-resolves CanvasTheme + repaints on the UIX-005 hook (cache-invalidating); CodeMirror theme rewritten to derive every color from tokens (auto-switches via CSS vars); Electron window `backgroundColor` per persisted theme (flash guard) + `nativeTheme.themeSource` IPC; dark-island fixes in visualstates/TitleBar/CodeHistoryDiffModal. Default-mode decision: **system** (least-surprising for pre-release; no dark-accustomed base). Hex ratchet unchanged (colors.css is the excluded definition file; shadows/fixes use rgba()/var()). Live-verified (see Log): launcher + editor chrome + **canvas** all light via real `themechanged` AND real persisted-boot; persistence across restart; **no flash-of-wrong-theme on boot** (main.js pre-paints per persisted theme); amber/severity legible on light. **Residual:** CodeMirror light *visual* spot-check (verified by construction — token-derived, typechecks, CSS-var auto-switch — but reaching a live code node via CDP was unreliable in the test project; hand to UIX-009 corpus). See UIX-008-NOTES.md |
| UIX-010 | Icon stroke-grid redraw + IconSize retune | ⬜ Not started | Specced 2026-07-26. 88 of 149 core-ui glyphs still fill-drawn (measured; the "92" estimate was close); `IconSize` is a 4-value class enum vs the mock's per-context 12/14/15/17. Polish only — nothing is broken. See UIX-010-ICON-STROKE-GRID-REDRAW.md |
| UIX-011 | Legacy CSS-background icon retirement | ✅ Implementation complete (2026-07-26) — ⚠️ **visual QA outstanding** | `url()`-to-SVG in editor stylesheets **50 → 0**, asserted by a new hard gate (`npm run icons:css`, wired into pr.yml, verified to fail on a planted violation). Editor icon assets **255 → 12** (243 deleted): 136 of `core-ui-temp/`'s 142 were byte-identical duplicates of core-ui's set, and 73 more were orphans no source referenced. **4 of the 13 stylesheets turned out to be dead code** (`componentspanel.css` superseded by `ComponentsPanel.module.scss`; `createnewnodepanel.css` / `layoutpanel.css` / `deploypopup.css` unreferenced) — 26 of the 50 refs died with them. 24 live refs across 13 call sites migrated to the core-ui `Icon`; every consumer was React except `models/lessonformat.ts` (HTML-string site, glyph inlined with `currentColor`). 5 unique glyphs migrated into core-ui (`lightning`, `pin`, `pin_fill`, `check_circle`, `check_circle_fill`). **`core-ui-temp/` could NOT be deleted entirely** — `CanvasIcons.ts` paints 5 glyphs onto `<canvas>` where `currentColor` cannot apply; reduced 142→5 and renamed `canvas/`, and **the still-theme-frozen canvas marks are handed to UIX-005**. Build + `tsc` green; hex ratchet holds for editor (16/16) and canvas-paint (2/2). **Found: `IconSize`'s classes are defined in no stylesheet, so `size={...}` is inert app-wide** — every migrated icon is therefore explicitly boxed (also what makes the swaps layout-neutral); flagged to UIX-010. Fill-drawn `lightning`/`pin`/`pin_fill` handed to UIX-010. **Residual: the whole visual acceptance test — these glyphs must now change colour between themes, which cannot be verified from a worktree.** Both-theme QA checklist (16 rows, incl. 4 dead-code regression rows) in UIX-011-NOTES.md. See UIX-011-NOTES.md |
| UIX-009 | Long-tail sweep & visual QA | ✅ Complete (2026-07-26) | core-ui hex slice 100→0 (last straggler; ratchet re-baselined); fixed 2 baked-light theme bugs (JS/JSON editor error panels) + AA button-text (`on-primary`) + 2 style.css keyword breakers; deleted 5 dead icon-button SVGs; red-as-action + palette-name coherence = verified non-issues; CodeMirror both-theme verified by construction (0 hex / 59 var refs, both-theme token values). **Screenshot-corpus harness** (`corpus/`, one-command, self-discovering panels, deterministic, both themes) + **14-surface × 2-theme live corpus** (`corpus/captures/2026-07-26/`) + gallery. Audit table (every menu-reachable surface, zero unexplained deferred) in UIX-009-NOTES.md. Follow-ups filed: UIX-010 (icon stroke-redraw + IconSize), UIX-011 (legacy CSS-bg icons), VersionControlPanel functional crash, CodeMirror live syntax pixel. **"Before" corpus never existed** (UIX-001 predated the harness); this corpus is the forward baseline. See UIX-009-NOTES.md |
| UIX-012 | Theme-aware node colours + picker collapse state | ✅ Complete + live-verified (2026-07-26) | Four live-use defects, one cause: literal palettes where tokens belong. (1) Connection popups, (2) node-picker items and (4) the hover JSON inspector all now resolve through **one** path — `CanvasTheme.resolveThemeTokens` → `useThemeTokens` / `useNodeColorScheme` — instead of the dark-only `nodelibraryexport.js` blob and an inline base16 object; the JSON inspector reuses the `--theme-color-syntax-*` tokens UIX-008 already defined (no new palette). Node schemes are **derived** from the category tokens with ratios tuned to reproduce the shipped dark literals (spec-asserted), so the dark look does not regress. (3) Picker collapse state: the reducer is now the single source of truth, and a second cause was found live — every keystroke re-parsed the categories back to collapsed, so **search-expansion had never worked**, not just after a click; a control run with the fix stashed reproduces it. Ratchet `canvas-paint-ts` 2→**0**. Live-verified both themes: picker items, auto-expand, click-then-search, both port popups, JSON inspector expanded, and **re-theme in place** with popups/inspector open. Residual: Node References panel unreachable (`ExperimentalFlag`). Findings handed on: `codemirror-theme.ts` hardcodes `{dark:true}` (report-only, deliberately unfixed); project-close teardown crash in `ProjectDesignTokenContext`/`EditorDocument`. See UIX-012-NOTES.md |
| UIX-013 | Node picker overhaul | ⬜ Not started | Specced 2026-07-26 from live-use complaints. Accordion replaced by a persistent category rail with counts (removes the "search doesn't auto-open categories" bug by construction, and the underlying local-vs-reducer collapse-state drift in NodePickerCategory.tsx:39-70); search results flat + ranked with match reasons; node cards get category-tinted glyphs on neutral surfaces; "Noodl AI (Beta)" promo pane replaced by a live docs preview; keyboard hint bar. **Consumes UIX-012's theme-aware colour schemes — must not fork the palette.** Mock: mocks/nodegx-node-picker-mock.html (3 states, both themes). See UIX-013-NODE-PICKER-OVERHAUL.md |

## Coordination notes

- File-territory rule (UIX-002 vs UIX-003/004): UIX-002 never edits a file a Tier-2 spec claims; log claims here when tasks run concurrently.
- Parallel worktrees: verify base = cline-dev tip; editor live-verification from the primary checkout (lerna trap); commit with pathspecs.
- Mocks: [mocks/](./mocks/) — spec wins over mock; mock wins over silence.

## Log

- 2026-07-26 — **UIX-012 complete + live-verified** (solo, primary checkout). Four defects reported from live use, and they turned out to be one defect wearing four hats: a **literal palette sitting where a token belongs**. The connection popup, the node-picker item and the hover JSON inspector each carried their own hardcoded dark colours that the light theme had no way to reach — so the fix was not four patches but one resolution path (`CanvasTheme.resolveThemeTokens` → `useThemeTokens` / `useNodeColorScheme`), with the node schemes **derived** from the UIX-005 category tokens rather than hand-authored a second time. The derivation is tuned so the dark output reproduces the shipped `nodelibraryexport.js` literals to within a few levels per channel, and a spec asserts it, because "make it work on light" must not become "quietly re-skin dark". The runtime blob stays: it is the runtime→editor export contract, and only three editor call sites read it. The JSON inspector needed **no new palette at all** — UIX-008 had already defined `--theme-color-syntax-*` for both themes and nothing was reading them; the only adjustment was swapping `syntax-meta` (3.5:1 on the light `bg-4`) for `fg-default`. **Driving it live paid for itself twice.** First, the picker bug was bigger than reported: every keystroke re-parsed the categories back to collapsed, so search-expansion had *never* worked, not just after a header click — fixing only the reported local-state drift would have shipped a still-broken picker. A control run with the four picker files stashed reproduces the original behaviour exactly (search → 0 items). Second, the live pass caught nothing wrong with the colours, which is the outcome that makes the screenshots worth keeping. Gates: editor suite 1346/0, editor `tsc` clean, hex ratchet `canvas-paint-ts` **2 → 0** (scope now empty). **Environment note for future parallel work:** most of this session's live pass was spent fighting a concurrent session in the same checkout — its mid-edit `NodeGraphModel.ts` broke the dev-server compile, the shared Electron window kept switching projects, and both sessions raced for port 9222. Two agents cannot live-drive one editor. **Trap worth keeping:** CDP mouse events are silently dropped when the Electron window is unfocused — `Page.bringToFront` first, or a working selector looks broken. Residual: the Node References panel is behind `ExperimentalFlag` and unreachable. Handed on: `codemirror-theme.ts` hardcodes `{dark:true}` on the theme facet (real, but fixing it needs a `Compartment` reconfigure and CodeMirror's light appearance has never been live-checked — deliberately not shipped unverified); and a project-close teardown crash in `ProjectDesignTokenContext`/`EditorDocument` (`reading 'off' of undefined`). See UIX-012-NOTES.md.
- 2026-07-26 — **UIX-011 implementation complete (worktree agent, Opus).** The premise held and got worse on inspection: 0 of 255 editor SVGs carried `currentColor` while 149 of 149 core-ui glyphs did, so every CSS-referenced glyph was frozen at its authored colour — including lesson checkmarks still painted in the pre-azure Noodl yellow `#FCCC73`. **Most of the task was deletion, and more of it than the spec expected.** Four of the thirteen stylesheets were dead code (26 of 50 `url()` refs died with the files); 136 of `core-ui-temp/`'s 142 glyphs were byte-identical duplicates of core-ui's set; 73 further assets were referenced by nothing. Net **255 assets → 12**, **50 `url()` refs → 0**. Only 24 refs needed real migration, across 13 call sites, and — contrary to the spec's "imperative `View` subclasses" framing — every consumer was already React except `models/lessonformat.ts`, whose HTML-string card got the glyph inlined with `currentColor` instead. **Two spec corrections:** `style.css` had 1 icon ref, not 21 (the table counted font `src: url()` loads); and `core-ui-temp/` could not be deleted entirely, because `CanvasIcons.ts` paints 5 glyphs onto `<canvas>` where `currentColor` has nothing to resolve against — `ICONOGRAPHY.md` already carved that out, so the directory was reduced 142→5 and renamed `canvas/`, with the still-frozen canvas marks handed to **UIX-005** (now the last theme-frozen icon surface). **Trap found, with app-wide reach:** `IconSize`'s class names (`is-size-tiny`, …) are defined in **no stylesheet**, so `size={IconSize.Tiny}` is inert and an `<Icon>` has no intrinsic size — every one of the ≈127 existing consumers is sized by its host. Every migrated glyph was therefore pinned to an explicit box matching the rule it replaced, which made the swaps layout-neutral by construction rather than by luck (the exact failure mode the spec flagged); handed to UIX-010, which owns the retune. Also closed a late find: legacy hand-authored lesson HTML emits an *empty* checkmark span that the removed `content: url()` used to fill, and would have rendered nothing — degraded to a themeable CSS-drawn ring. New **hard gate** `npm run icons:css` (a gate at zero, not a ratchet — no legacy tail remains), comment- and data-URI-aware, wired into `pr.yml`, and **verified to fail** on a planted violation. Gates: editor renderer+main webpack build green (0 errors, no unresolved SVG), editor `tsc` clean, hex ratchet holds for `noodl-editor` 16/16 and `canvas-paint-ts` 2/2. **⚠️ Two things the orchestrator must pick up:** (1) `npm run colors` **fails on `noodl-core-ui` +2** — `Logo.module.scss`, from `87b6c6b`, present identically on the base commit `e1914e1`; not this task's, deliberately untouched (brand territory, possible concurrent agent), but it blocks CI. (2) **The visual acceptance test was not run** — a worktree cannot start the editor (`lerna exec` resolves to the main checkout) so the UIX-009 corpus harness was unreachable; the whole point of the task is that these glyphs now change colour between themes, and that was proven only by construction. 16-row both-theme QA checklist — including four rows that specifically probe the dead-code deletions — in UIX-011-NOTES.md.

- 2026-07-26 — **UIX-009 complete — phase closer, run solo (Fable 5, primary checkout).** Swept the long tail, proved the standard surface-by-surface, and built the permanent regression instrument. **(1) Last big straggler closed:** UIX-002's deferred core-ui hex slice, 100 occ / 16 files → **0** (ratchet re-baselined; `noodl-core-ui` dropped out of the `max` block). Method = UIX-002's: strip dead `var(--token,#hexFallback)` fallbacks (6 files to zero; several fallbacks were stale pre-azure red), map real semantic literals to tokens (JSON type badges → semantic + node-category via `color-mix`; diff colours → success/danger/warning). **This caught two real theme bugs, not just debt:** JavaScriptEditor + JSONEditor error panels were baked *light* (`#fef2f2`/`#dc2626`/brown) and never flipped → now danger/warning tokens; `color: white` on azure primary buttons (≈1.9:1, fails AA) → `on-primary`. **(2)** Named-keyword sweep fixed 2 more theme-breakers in legacy `style.css` (`color:black` on azure primary → `on-primary`; invisible `background:white` divider → border token). **(3)** Deleted the 5 dead `assets/icons/icon-button/*.svg` (UIX-007 delete-candidate; confirmed no path ref, no `require.context`, build green). **(4) Verified non-issues:** red-as-action grep = clean (de-red law holds); color-picker palette names come from the user's project color styles, not a static map (nothing to fix); CodeMirror both-theme correct by construction (`codemirror-theme.ts` = 0 hex / 59 `var(--theme-color-syntax-*)`, both-theme token values in colors.css). **(5) The harness:** `corpus/capture.mjs` (raw CDP over one socket, no deps, Node ≥21), `gallery.mjs`, `run.sh`, `README.md` — deterministic (fixed 1600×1000, animations killed, reduced-motion, declarative theme) and **self-discovers the panel list from the live DOM** (the literal menu-walk). **(6) Live corpus** (fixture "Shine Phase 2"): **14 surfaces × 2 themes = 28 PNGs** at `corpus/captures/2026-07-26/` + gallery. Editor/canvas re-theme dark→light verified live (UIX-005+008 contract, WF-007 blank-canvas fix holds); Backend Services + Property Editor + Editor Settings + launcher all token-clean both themes. **Backbone audit fact:** the hex ratchet now proves tokens-only across *every* editor+core-ui stylesheet (0 / 16-documented / 2-documented), a CI gate — so "tokens only" is green by construction for every surface, not spot-checked. Verification: editor renderer webpack build green (exit 0, covers every `color-mix()`). **Findings/follow-ups (zero silent deferrals):** UIX-010 (92-glyph stroke redraw + IconSize retune — "redraw hundreds", parked), UIX-011 (legacy CSS-background icon retirement), VersionControlPanel ErrorBoundary crash (functional, not styling), CodeMirror live syntax-pixel spot-check (CDP reach unreliable per UIX-008). Stale user-docs: in-repo `docs/` has **zero** UI screenshots — the stale shots are in the external docs repo (out of scope), filed. See UIX-009-NOTES.md.
- 2026-07-26 — **UIX-007 + UIX-008 run in parallel** (2 Opus worktrees — the last concurrent pair; UIX-009 runs solo after) and merged to cline-dev. Territory split BY FILE TYPE within shared component dirs: 008 owns all color/theme `.scss`/token/machinery; 007 owns the Icon component, icon assets, and icon markup. `comm -12` preflight showed **zero file overlap** (the anticipated `IconButton.module.scss` collision never materialized — 008 didn't touch it). Both hit and self-corrected the stale worktree base (rooted at 360cdc4 → `git reset --hard e2cace6`). Zero merge conflicts. Post-merge gates green: **hex ratchet unchanged** (canvas-paint-ts 2 / core-ui 100 / editor 16 — 007 swapped fill→color with no hex; 008's new hexes live in the excluded colors.css definition file), editor `tsc` clean (0 errors, covers both agents' TS), core-ui `tsc` 45 pre-existing `@noodl-versioning/@noodl-store/@noodl-viewer-cloud` module-resolution errors unrelated to either task (sibling-package build-state trap, zero in touched files). **Live pass (primary checkout, both themes, real machinery):** launcher + editor chrome + canvas all verified light AND dark; canvas repaints light via the real `nodegx:themechanged` event *and* via a real persisted-theme boot (UIX-005 hook + cache invalidation hold — no stale dark tiles); theme **persists across restart** (injected `editor.theme=light` in EditorSettings, restarted, booted straight into light); **no flash-of-wrong-theme** (main.js sets window `backgroundColor` from the persisted theme at creation — verified in code + boot screenshot); UIX-007 rail/toolbar/component-tree icons read as one uniform 1.5px stroke set in both themes, re-tinting via `currentColor`; amber warning triangle + azure Deploy intact and legible on light. Traps re-encountered: dev-stack port collisions require full teardown before a clean start; synthetic dblclick to open a project card is timing-flaky (reduced-DOM state mid-navigation reads as "no info" but the open completes ~5s later). **The user's `editor.theme` key was removed post-test to restore their `system` default.** Residuals for UIX-009 corpus: CodeMirror light *visual* legibility (mechanism verified by construction — token-derived, typechecks, CSS-var auto-switch — but a live code node wasn't reachable via CDP here); IconSize 12/14/15/17 retune (007); 92 filled node/decorative glyphs → stroke-grid redraw. See UIX-007-NOTES.md + UIX-008-NOTES.md.
- 2026-07-26 — Phase created: critique + mockups session; styling-architecture audit; 9 task specs written.
- 2026-07-26 — **UIX-002, UIX-003, UIX-005 run in parallel** (3 worktrees, one model each: Sonnet/Opus/Fable) and merged to cline-dev in order 005→002→003. All three self-corrected the 376-commit stale worktree base. Territories were disjoint by design (editor stylesheets / core-ui components / canvas paint code); UIX-002's core-ui slice was deferred to keep it clear of UIX-003. One merge conflict, in `assets/css/style.css` marginpadding block — both branches tokenized it; resolved toward UIX-003's box-model redesign. Post-merge gates green: hex ratchet holds (editor 16, core-ui 100), core-ui + editor typecheck clean on all touched files. UIX-005 deleted `constants/NodeGraphColors.ts` (rewired into new `CanvasTheme.ts`). Live-editor verification (from primary checkout) is the outstanding residual per each task's NOTES.md.
- 2026-07-26 — **UIX-004 + UIX-006 run in parallel** (2 Opus worktrees) and merged to cline-dev (disjoint by design — `comm -12` preflight showed zero file overlap; editor chrome/CSS vs core-ui launcher subtree). Both hit and self-corrected the stale worktree base (rooted at 360cdc4 → `git reset --hard 7cbf665`). Zero merge conflicts. Post-merge gates green: hex ratchet holds (editor 16, core-ui 100), editor tsc clean, core-ui tsc clean on all touched files (45 pre-existing baseline errors unrelated). Orchestrator relocated the stray root `NOTES.md` → `UIX-006-NOTES.md`. **Live-editor smoke pass (primary checkout) found + fixed a real UIX-006 bug:** the deterministic thumbnail placeholder did not engage for *blank-white* captures — `hasUsableCapture()` passes any valid data:image>64 chars, and stored `thumbURI` PNGs decode to solid white; the guard only covered missing/errored, not blank. Fixed in `e2bd955` with runtime canvas blank-detection (≥96% near-white, outlier-tolerant, or near-zero min/max spread; cross-origin taint kept). Cold-boot re-verified: 5/7 cards show gradient placeholders (incl. the 2 formerly-white), 2 real captures retained, no white rectangles. UIX-004 chrome verified live (azure Deploy, active-rail accent pill, route pill, layout toggles, component tabs); properties-panel node-type sweep + amber-warnings-chip left as manual-QA residuals (need node selection / a warning-carrying project). Editor SPA-reload trap (RUN-003) re-encountered — raw CDP `reload` re-requests the client route as file:// (ERR_FILE_NOT_FOUND); recovered via full restart. Also hit dev-stack port collision (EADDRINUSE) from overlapping launches — full teardown before a single clean start is the reliable path.
- 2026-07-26 — **UIX-005 live QA completed + P0 editor regression found/fixed.** Live verification (Shine Phase 2, CDP-driven from primary checkout) was blocked by every node graph painting blank: **WF-007 deleted the cloud-runtime sandbox (the second node-library client) but `NodeLibraryImporter` still gated `NodeLibrary.reload()` on `clients.count >= 2`** — the library never loaded, `CanvasPainter.paint()` early-returned, and `ViewerConnection.export()` (design-view preview) was blocked by the same flag. The earlier parallel-batch smoke screenshots all show this state; empty test projects made it read as "boot clean". Fixed: reload on first client import. With that in place, all UIX-005 live checks pass (see UIX-005-NOTES.md live record + screenshots/uix-005-live-qa/): cards/wires/dot-grid per mock, accent selection ring, Created/Changed/Deleted annotation colours legible, zoom 25/50/200 with grid LOD at 40%, `.theme-light` flip re-themes the canvas via MutationObserver (UIX-008 contract proven), long-label wrap consistent, 0.39 ms/frame paint bound. Hex ratchet gained a `canvas-paint-ts` scope (painter/renderer .ts files, baseline 2 = dispositioned ConnectionPopups literals, CanvasTheme.ts excluded as definition site).
- 2026-07-26 — UIX-001 complete: duplicate token files collapsed into core-ui (editor + viewer-frame import via `@noodl-core-ui`), new palette (elevation neutrals, azure primary, danger/warning/success split, node-category + wire tokens, inert light block), typography settled (system UI stack, bundled Bricolage Grotesque 600 display face extracted from the mock, 35 legacy per-weight font-var call sites converted), full primary/notice audit (zero danger-on-primary found; 1 mis-wire fixed, 5 notice-as-accent chrome sites → primary), 32 referenced-but-undefined theme tokens defined as compat aliases, AA contrast matrix in dev-docs/guidelines/DESIGN-TOKENS.md, live-verified (launcher + editor), screenshots in screenshots/after-tier1/. Status: ⚠️ UIX-009's "before" corpus was NOT captured before this landed — only these after-shots exist.

## Findings from the 2026-07-26 live QA pass (new, unowned)

Both surfaced while verifying UIX-011/LIB-005; neither is caused by them.

### 1. Blank-white thumbnails are back — in a *second* card component

The node picker's **Import from project** grid shows pure-white thumbnails for
several projects (3 of 6 in the QA run). This is the same defect UIX-006 fixed
for the launcher in `e2bd955` — stored `thumbURI` PNGs that decode to solid
white pass a naive "is there a data URI?" check and render as white rectangles
instead of falling back to the gradient placeholder.

**Diagnosis (not a guess):** the blank-capture guard `hasUsableCapture` exists
in exactly one file —
`packages/noodl-core-ui/src/preview/launcher/Launcher/components/LauncherProjectCard/LauncherProjectCard.tsx`.
The node picker uses an entirely separate component,
`packages/noodl-editor/src/editor/src/views/NodePicker/components/ProjectCard/ProjectCard.tsx`,
which never received the fix. Two card components, one guard.

**Fix shape:** lift the near-white/low-spread detection out of
`LauncherProjectCard` into a shared helper and use it in both. Fixing it in
place in the second component would leave the same trap for a third.

### 2. Stale "Noodl AI" promo in the node picker — NOT fixable in this repo

The Nodes tab's promo panel reads **"New Feature — Noodl AI (Beta)"** with a
**"Noodl AI Documentation"** button. This was first filed here as a string UIX-006's
sweep missed. **That was wrong** — corrected on investigation: the content is
fetched at runtime by `useGetSliderNews()` in `NodePicker.hooks.ts` from
`getDocsEndpoint() + '/nodepickerdefaults/news.json'`. There is no such string in
this repo to change.

It belongs to the **external docs/marketing repo** — the same one UIX-009 flagged
for stale "Noodl 2.9.3" screenshots. Fix it there, alongside those.

### 1. — FIXED 2026-07-26

Shared helper `packages/noodl-core-ui/src/utils/projectThumbnail.ts` now owns
`hasUsableCapture` / `isBlankCapture` / `placeholderBucket` / `projectInitial`.
Both `LauncherProjectCard` and the node picker's `ProjectCard` consume it.

The node-picker card needed more than the guard: it painted the capture as a CSS
`background-image`, so there was **no `<img>` to hang `onLoad`/`onError` on and no
placeholder at all**. Rewritten to an `<img>` with the same fallback contract plus
the gradient-bucket + ghosted-initial placeholder.

Live-verified after a clean restart: the three cards that rendered as white
rectangles (DebtLivePass, RUN-003 slice-5, Ferell-main) now show gradients with
ghosted initials, real captures are preserved, and the buckets **match the
launcher exactly** for the same projects — which is the shared hash proving it is
one implementation, not two.

Trap worth keeping: editing a `.module.scss` under HMR desyncs the CSS-module class
hashes from the JS that holds them, so *every* class in that file silently stops
applying — the card rendered all four of its absolutely-positioned overlay states
stacked at once. It looks exactly like a catastrophic self-inflicted regression and
is entirely an artifact. Restart before believing a styling result seen after HMR.
