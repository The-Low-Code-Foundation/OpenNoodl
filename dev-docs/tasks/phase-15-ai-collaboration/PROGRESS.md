# Phase 15: AI Collaboration Experience - Progress Tracker

**Last Updated:** 2026-07-27
**Overall Status:** 🟡 In Progress

---

## Quick Summary

| Metric       | Value  |
| ------------ | ------ |
| Total Tasks  | 12     |
| Completed    | 8      |
| In Progress  | 2      |
| Not Started  | 2      |
| Blocked      | 0 — AIX-009's blocker cleared by DEP-008 (phase 26) on 2026-07-27 |
| **Progress** | **67%** |

---

## Task Status

| Task    | Name                | Status         | Notes                                              |
| ------- | ------------------- | -------------- | -------------------------------------------------- |
| AIX-001 | Modern AI Client    | 🟢 Complete    | Client + registry + 4 providers; live runs unverified |
| AIX-002 | The Authoring Loop  | 🟢 Complete    | All 8 spec steps: headless loop, staging/accept/undo, conversation UI, live canvas rendering, live-provider measurement (sonnet 8/8 first-attempt), update mode (post-accept refinement as diff-reviewed revision), opt-in local-first G2 telemetry. Residuals: live-provider run of update mode; exit-criterion demo in a signed build. See AIX-002-NOTES.md |
| AIX-003 | Graph-Native Review | 🟡 In Progress | Slices 1–5 shipped: change-set adapter, annotated diff canvas, review document (list/nav/detail, before/after, granular accept with dependency closure, walkthrough). Residuals: one clean-session UI smoke, 40-node fresh-reviewer test, live-provider round trip. See AIX-003-NOTES.md |
| AIX-004 | Explain Mode        | 🟢 Complete    | Panel shipped + wired (context menu + sidebar); read-only; citations link to canvas; +47 specs. Register/accuracy tuning needs a live provider run. NOTES + CHANGELOG in the task doc |
| AIX-005 | Agentic UI Nodes    | 🟢 Complete (1 criterion undemonstrated) | **Gate G2 was not met; started anyway on instruction — see the Gate G2 section at the top of the task doc.** All seven phase-3.5 tasks (AGENT-001…007) built by six parallel agents: SSE + 4 stream/state utility nodes, WebSocket, global store (+Set/Subscribe), optimistic updates, state history (+Undo/Redo, Snapshot), action dispatcher (+Handler). **15 new nodes**, runtime suite 459 → **891 specs**, catalog 140 → 154 types at **154/154 SUB-005 coverage**, 49/49 examples strict-clean, `project-examples/agent-chat` (262 nodes, all 15 types) with a zero-dependency mock agent server, `docs/runtime/AGENTIC-UI.md`. First real-server pass: `test/agent-live-endpoint.test.ts`, opt-in, 5/5. Three integration defects found by the first consumer to wire all fifteen together, and fixed: the advertised `data → chunk` streaming wiring rendered `[object Object]` for the commonest agent endpoint (SSE gains `text`/`textPath`; TextAccumulator now refuses a non-text chunk instead of concatenating `String(object)`), object-typed inputs were uneditable (needed `typesToSaveInInput` and a parenthesised literal eval as well as the `Ports.ts` branch — the editor half alone would have shipped a row you can type into that does nothing), and built-in dispatcher actions refused the obvious `{type, payload}` shape. Runtime suite now **927**. **Live editor pass done** (`aix005-notes/LIVE-VERIFICATION.md`): all 15 types in the library with none flagged `inNodePicker: false`, correct placement under Streaming/App State/Agent Actions, and the example project opens — 262 nodes, root resolves to `App`, **0 renderer exceptions**. **The app has now been run** (`aix005-notes/LIVE-RUN.md`), and running it found three defects, all fixed. (1) **The SSE node had never worked in a browser**: it called its injected `fetch` as `this._fetch(…)`, and a browser brand-checks the receiver — `Illegal invocation` on the first token. Node's `fetch` ignores its receiver, so all 39 connection specs *and* the real-server pass, which drives from Node, were blind to it. (2) **A runtime defect, not a node one**: `sendSignalOnOutput` queued two entries (`true`, `false`) where a value input queues one, and inputs are drained one entry per port per pass — so a signal advanced at half the rate of the value it pairs with, and three or more messages inside one update iteration dropped every other chunk and repeated the last (`a b c d` → `abcdddd`). A signal is now one `SIGNAL_PULSE` entry expanded on drain; `sendPulse` falls back to the old form for any receiver without `_setPulseFromConnection`. **This affected every node pairing a value with a signal**, not just these. (3) The example's Repeater template read its item from a `Component Object` — per-instance state, unrelated to the repeated item — so every transcript row rendered blank; replaced with `Component Inputs`. **Verified end to end after the fixes**: the streamed answer is byte-identical to the server's (494/494), transcript roles and text correct, Stop cancels with the partial kept, killing the server mid-answer walks `reconnecting`/retries and recovers via `Last-Event-ID` with no duplicated tokens, Tools 3 completed/4 refused + Pattern Extractor, State history + undo/redo, WebSocket 71 values parsed / 0 errors, and the object-typed property row renders and round-trips a literal through to `project.json`. Runtime suite **933**. Remaining, all disclosed in `LIVE-RUN.md`: a WebSocket close that heals leaves no trace on screen (deliberate, README corrected), the object popout is titled `EXPRESSION`, no real provider endpoint, and the editor suite is seed-dependent (two of six runs red, different specs each time, on both trees). **The one success criterion still undemonstrated is "AIX-002 can author with these nodes"** — the catalog side of it is done and gated (154/154 enriched, `catalog:check` and `catalog:merge:check --require-coverage` green, the example validates 262 nodes / 0 failures), but no authoring run has actually built a streaming UI out of them. That needs a live provider and is the last thing between this task and unqualified completion. |
| AIX-006 | Style Vocabulary    | 🟢 Complete (`fa639c8`) | Live per-project vocabulary (tokens/variants/presets, `var(--token)` format verified) injected into ContextBuilder + `ON-SYSTEM STYLING` prompt; StyleAnalyzer detection extracted to a pure `StyleAnalyzerCore` and wired as an advisory post-gen lint into the refine loop; MCP `get_style_vocabulary`/`set_project_tokens`/`set_style_preset`. A/B (sonnet, headless): **2.4× more on-system styling**, 90% vs 85%. mcp jest 39/39; Electron editor suite pending |
| AIX-007 | Token Cost Reduction | 🟢 Complete | **Mean cost/component −82%** ($0.1954 → $0.0352) with first-attempt validity **up** 7/8 → 8/8 and latency 106s → 16s. Prompt caching (3 budgeted breakpoints: system+tools, opening reference blocks, growing turn) verified live — cache reads on turn 2+ of **8/8** multi-turn sessions, 73% hit rate. Opening turns reordered stable-first (create + update); `AiUsage` carries cache read/write tokens and `calculateCostUsd` prices them; sonnet-5 price corrected to the $2/$10 intro rate. `output_config.effort` plumbed per call site: sweep found **`low`** wins on every axis — the only level reaching 8/8, and it recovers `article-list`, which at `high`/`medium` burned ~32k reasoning tokens across 7 turns and never submitted. Default model moved opus-4-8 → **sonnet-5** on measurement (tie at 8/8, 2.6× cheaper). Two defects found and fixed: a 5th cache breakpoint the API would reject (caught by a spec), and cancelled turns misreported as `exhausted`. Suite 1388 green. Residual: our cost figure is not yet reconciled against Anthropic's billed figure. See AIX-007-NOTES.md |

| AIX-008 | Sandbox Preview     | 🟡 In Progress | Built and live-verified. The authoring surface is now a split: the staged candidate **rendered and running** on the left, its graph on the right. A second viewer client registers as `sandbox-<id>` and is fed a *spliced* export (project export + candidate as root) — `ProjectModel` is never touched, so reject stays the absence of a call. Data is sandboxed by default through one seam: a `fetch`/`XMLHttpRequest` shim installed before the runtime exists, answering Parse (`/classes`, `/aggregate`, `/login`, `/users`, `/functions`, `/files`), BYOB (`/items`) and *any* unrecognised endpoint from an in-memory store with working CRUD and a fake signed-in user. The dataset is assembled editor-side from the graph (`collectionName`/`collection` parameters name the classes, `prop-<field>` connection endpoints name the fields) with the authoring model's optional `sample_data` taking precedence; a "Real backend" toggle switches the same pane to live data. +27 runtime jest specs, +12 editor specs (runtime 968/0, editor 1441/0). **Live-verified** by driving a scripted (no-provider) session through the real UI: the split renders the staged candidate as root, `fetch` and `XHR` are both patched in the sandbox window, and the page shows the agent's own sample records (`Reading list / 3 / The Left Hand of Darkness / Ursula K. Le Guin`). **Running it found one defect, fixed and re-verified**: Parse routes were matched only at the *first* path segment, but every configured endpoint carries a mount path (`https://host/parse`, `http://localhost:8577/api`) — so on any project that actually had a backend configured the sandbox answered an empty 200 to every query and the preview rendered a plausible, silently empty list. Routing now finds the Parse segment anywhere in the path. Residuals: the Sample-data/Real-backend toggle is not yet exercised live (scripted driving kept losing the document to the panel’s unmount path), and no run against a real AI provider. |

| AIX-009 | Project Context Documents | 🟢 Complete | Built 2026-07-27 in a parallel worktree batch alongside DEP-008 (its unblocker) and AIX-011. `docs/` (BRIEF / ARCHITECTURE / CONVENTIONS) with `ProjectDocsModel`, context injection through the AIX-006 seam, a `DocsPanel`, reviewed doc diffs, and four MCP tools. **The blocker was cleared in the same batch**: DEP-008 replaced the five-name filter with a `.noodlignore` mechanism whose default set includes `docs/`, so criterion 8 now holds — asserted mechanically in `tests/utils/deploy-ignore.test.ts` against a seven-file nested `docs/`, with a root `README.md` left in place so a blanket markdown filter could not satisfy it. Two decisions worth knowing. **Zero new dependencies**: the spec's `@codemirror/lang-markdown` was rejected because it is absent from `node_modules`, so adding it would leave every checkout unbuildable until someone ran `npm install` (the BAK-002 nodemailer trap) — a `StreamLanguage` mode on the already-present `@codemirror/language` does the job, and swapping in `markdown()` later is one line. **A 2s content poll instead of a file watcher**: `IFileSystem` has no watch API and `FileStat` is `{size}` only, so extending the cross-platform interface would have been bigger than the feature; writes carry optimistic concurrency (`baseline` bytes, `DocsConflictError`, nothing written on drift) and are temp-file + rename, which is exact where a size/mtime heuristic is not — a same-length external edit defeats the heuristic and not this. Doc blocks append **after** the existing reference blocks, so a project without docs produces byte-identical turns and `ARCHITECTURE.md` is pull-only via a conditionally-offered tool, keeping AIX-007's cached prefix the same size on every turn. Verified: editor suite 1521/0 (its 40 previously-unrun Electron specs included), `noodl-mcp` 78/78 under node 22. **Found a live defect in adjacent code**: `UndoActionGroup`'s constructor appends its `{do, undo}` pair but leaves the group pointer at 0, so `undo()` loops from −1 and does nothing. 15 call sites are saved by `pushAndDo` advancing the pointer; the 4 in `StyleTokensModel` (set/add/delete token, reset all) apply first and then plain-`push`, so **⌘Z after a design-token change is a no-op**. Not fixed here — UIX territory. Residuals: criteria 1, 2 and the live half of 4 need a real provider; the `DocsPanel` has never been mounted in a running editor |
| AIX-010 | Project Review & Docs Retrofit | 🔴 Not Started | Specced 2026-07-27. Read an existing hand-built project (Explain Mode's machinery, raised to a `project` scope it does not have today) and draft its docs for review. Uncertain claims must be labelled `> TODO:` rather than smoothed over — a confident wrong doc is worse than none. Surfaced as a dismissible, per-project, never-auto-running recommendation on two surfaces only |
| AIX-011 | Project-Scope Authoring | 🟢 Complete (8 of 8 criteria; 3 need a live provider to *measure*) | **Criterion 7 closed 2026-07-27 in a second slice** — and the gap was not the one the hand-off described. It said "wire a seam"; in fact a `doc` operation carried only `{target, intent}` and `PlanRun` skipped doc ops in the fan-out, so nothing ever authored a body and `docs.write(path, proposed)` had no `proposed`. What closes it: a **`DocSession`** doc-authoring turn (one `submit_doc` tool, whole-file contract, shaped like `PlanningSession`) that runs in a **second fan-out pass after every component op** — deliberately not interleaved, so "the doc sees the finished work" is true by construction rather than by however the caller sorted the plan, and the outcome it is handed names what the plan *failed* to build with an instruction not to document it; a **`docLint`** that makes AIX-009's "docs never restate the graph" mechanical rather than a paragraph of prompt, advisory-only through the AIX-006 style-pass shape (one rewrite offered, and a usable document is never turned into a failure by a cosmetic suggestion); the proposal **reviewed as a diff inside the plan** (`PlanDocReviewDialog`, Keep / Drop-from-plan, feeding the same exclusion closure as component rows); and `createPlanDocWriter` over `ProjectDocsModel`, recording its inverse with `group.push` so the write rides the plan's single `UndoActionGroup`. MCP has parity: doc ops stage `content` and the same `apply_plan` writes them, through a `writeProjectDocFile` extracted from `docsTools.ts` so there is one doc write path rather than a containment check spelled twice. Three decisions worth knowing. **`applyAuthoredPlan` is now `async`** — the doc write is real I/O and its drift check has to land in preflight before any component mutation; pretending the filesystem is synchronous would have been the cheaper lie. **The editor writes docs first inside the apply and MCP writes them last**, optimising for opposite failures on purpose: the editor has an undo group it can roll back, so it does the only fallible thing first and rolls everything back if a later step throws; MCP has no rollback, so it leaves a doc that names components until those components are on disk. **A declined doc is `skipped`, not `failed`** — "ARCHITECTURE.md already says this" is right often enough that a red row would teach users to ignore it, and a submission byte-identical to the baseline is treated the same way, because an empty diff is not a review. **Running the lint found a defect in it**: it keyed on catalog `typeName`s, but the picker shows `displayName`s and a model writes what a human sees — "Repeater" is `For Each`, "Array" is `Collection2` — so the first version was blind to every restatement written in the vocabulary of the actual UI. It now unions both (179 distinctive names, verified clean against the honest-prose fixtures). Verified: editor Electron **1573 specs / 0 failures** (the first run's single failure was the known seed-dependent `Git local tests` flake, in untouched code, green on re-run), `noodl-mcp` **87/87** under node 22 (was 84), +27 specs including the write path on real files — bytes land, one undo restores the previous bytes, undo of a created doc deletes it, drift refuses before any mutation, and a failed doc write rolls the whole plan back. Residuals are all measurement, not mechanism: criterion 1 (live plan quality), criterion 6 (cost-corpus re-run) and the *quality* of what the doc turn writes need a real provider, and the panel flow has still not been driven in a running editor. First slice (2026-07-27, parallel batch): A validated **plan** step (`plan.ts`, `PlanningSession`), fan-out through the *unchanged* `AuthoringSession` (`PlanRun`), one transactional write path (`planStaging.ts`), a panel scope toggle, and four MCP tools. **Staging-before-apply is structural, not conventional**, which was the whole point: `PlanRun` never imports staging and holds no `ProjectModel` — it produces plain data, and there is no per-operation apply API anywhere; the only write path takes the complete accepted set in one call, with `files` a *required* field whose only source is a gate-passing candidate, so "apply op 1 before op 3 validated" has no expressible code path; every refusal runs in a preflight pass before the first mutation; and cross-op dependency closure makes "keep Cart-links-to-Checkout, drop create-Checkout" unrepresentable in both the panel and MCP. **Criterion 4 passes against real files, not mocks**: the 44-component corpus loaded into a real `ProjectModel`, saved through the product's own `project.toDirectory`, a 3-component plan applied, one undo, saved again, `Buffer.equals` on the two `project.json` files. Verified: editor 1537/1 (the 1 is the pre-existing order-dependent import flake), `noodl-mcp` 84/84. Criterion 7 was the one open criterion at the end of that slice; it is closed above |
| AIX-012 | AI Project Creation | 🔴 Not Started | Specced 2026-07-27. "Start with AI" at the launcher, where creation today collects only name + location + style preset (`projectTemplate: ''`). A scoping conversation — explicitly not an authoring turn, and not allowed to build — produces the project, its docs, the decision transcript, and an AIX-011 plan it deliberately does **not** execute. Stopping short of building is the point: a user can leave a scoping session with something durable and no half-built graph |

---

## Status Legend

- 🔴 **Not Started** - Work has not begun
- 🟡 **In Progress** - Actively being worked on
- 🟢 **Complete** - Finished and verified

---

## Recent Updates

| Date       | Update                                                                 |
| ---------- | ---------------------------------------------------------------------- |
| 2026-07-27 | **AIX-011 criterion 7 closed — the last open criterion in the AIX-009…012 batch.** Worth recording because the hand-off mis-stated the work: it described wiring a seam, and the seams were in place and refusing loudly, but a `doc` plan operation carried only `{target, intent}` and `PlanRun` skipped doc operations in the fan-out — so **nothing ever authored a body**, and there was no `proposed` for the write path to write. The missing piece was a producer, not a connection. It is now a `DocSession` turn that runs in a **second fan-out pass after every component operation**: not interleaved, because relying on `orderPlanOperations` having sorted docs last would make the doc turn's context depend on how the caller happened to sort the plan, and a separate pass makes "the doc sees the finished work" true by construction. What it is handed is what the plan *achieved*, failures named explicitly with an instruction not to document them — a doc that records a component the plan failed to build is worse than no doc. Three things this slice settled that are not obvious from the criterion. **AIX-009's design line got a mechanism.** "No AI-generated markdown that describes the graph" is the premise the whole four-task group rests on, and it was enforced by a paragraph of prompt; `docLint` now checks it, advisory-only through the AIX-006 style-pass shape — one rewrite offered, and a usable document is never turned into a failure by a cosmetic suggestion. **The transaction went async, and the two write orders diverged on purpose.** A doc write is real I/O whose drift check must land in preflight before any component mutation, so `applyAuthoredPlan` is now `async`; the editor then writes docs *first* (it has an undo group, so it does the only fallible thing first and rolls everything back if a later step throws) while MCP writes them *last* (it has no rollback, so a doc that names components waits until those components are on disk). **A declined doc is `skipped`, not `failed`** — "the doc already says this" is right often enough that a red row would train users to ignore it, and a byte-identical submission is treated the same way, because an empty diff is not a review. **Running the lint found a defect in the lint**: it keyed on catalog `typeName`s, but the node picker shows `displayName`s and a model writes what a human sees — "Repeater" is `For Each`, "Array" is `Collection2` — so the first version was blind to every restatement written in the vocabulary of the actual UI, which is all of them. Also re-learnt, and now written into the notes: **the editor's Electron suite runs on jasmine and `tsc --noEmit` will not tell you** — `toHaveLength`, `toMatchObject` and `expect(...).rejects` type-check under the root tsconfig and then fail the test-CI webpack build; the gate for editor specs is `webpack.test-ci.js`. Gates: editor **1573 specs / 0 failures** (the first run's one failure was the known seed-dependent `Git local tests` flake, in untouched code, green on the re-run), `noodl-mcp` **87/87** under node 22, +27 specs including the write path exercised on real files. AIX-011 is complete at 8/8; its remaining residuals are measurement (live plan quality, the cost-corpus re-run, the quality of what the doc turn actually writes) and one live-editor smoke, not mechanism. **AIX-012 is now unblocked.** |
| 2026-07-27 | **DEP-008 + AIX-009 + AIX-011 built by three parallel worktree agents and merged in that order.** The batch was arranged so the dependency cleared inside the wave rather than across two: DEP-008 (phase 26) owns the deploy copy path exclusively and was told to write AIX-009's criterion 8 as a mechanical test, while AIX-009 was forbidden from touching `copy.ts` at all — its own spec refuses the one-more-hardcoded-name patch, and duplicating the fix would have dissolved the interlock into a coincidence. AIX-011 was allowed to run concurrently despite listing AIX-009 as a prerequisite, on the reading that its only real dependency is criterion 7's write path; `ContextBuilder.ts` was fenced as AIX-009's territory with AIX-011 limited to one additive method and one call site, and both were forbidden from reordering existing blocks because AIX-007's cache-stable prefix and its measured 73% hit rate depend on that order. That held: six shared files, and the only conflicts were two functions to which both sides had added an optional trailing parameter — resolved as the union, `docs` before `planContext`, one AIX-011 spec updated for the new slot. **Three findings the batch produced that no single agent would have.** (1) The deploy filter's `.git`/`.noodl` checks tested the *absolute* path, so any project stored anywhere under a directory containing `.git` in its path had **every file** dropped from its deploy — a silent under-copy, worse than the over-copy the spec was written about. (2) Excluding `docs/` does not remove a `docs/` an earlier deploy already wrote, and `clearFolders` only clears folders it is about to recreate — so an excluded folder is exactly the one it stops clearing, and every existing user is still serving theirs; DEP-008 reports stale exclusions as an error rather than deleting, because it cannot tell its own leftovers from the user's files. (3) `UndoActionGroup`'s constructor leaves its pointer at 0, so a group built with constructor `{do, undo}` and handed to plain `push` has a dead undo — found by AIX-009 running its own accept path, and true today of all four `StyleTokensModel` operations. **The one thing the batch did not deliver is AIX-011's criterion 7**, and the reason is worth recording: the hand-off described it as wiring a seam, but a `doc` plan operation carries only an intent and `PlanRun` skips doc ops in the fan-out, so there is no authored body for the write path to write. The seams refuse loudly rather than fake-succeed, so nothing is unsafe; the missing piece is a doc-authoring turn whose output is reviewed as a diff in the plan and applied in the shared undo group. Gates after the merges: editor **1537 specs / 1 failure** (a pre-existing order-dependent import flake that has produced 8, 8 and 4 for the same assertion across seeds), `noodl-mcp` **84/84** under node 22 — note the repo requires node ≥22 and a node 20 shell fails 29 MCP specs because the backend correctly refuses to start without a usable `node:sqlite` (RUN-004's loud failure doing its job). The tsfixme ratchet is red at +57 `any`, entirely pre-existing in `noodl-runtime` |
| 2026-07-27 | **AIX-009…012 specced as one feature in four shippable pieces**, from a scoping discussion about AI-maintained project documentation and about what the assistant can reach. Two findings shaped the whole group. First, the in-editor assistant is component-scoped in two independent places — the panel's single `componentPath` (`AiAuthoringPanel.tsx:142`) and the agent's single write tool (`authoring/tools.ts:117`) — while the *substrate* for project-scope work already exists in `noodl-mcp` (`create_`/`update_`/`delete_component`, `search_project`). So AIX-011 is not new primitives; it is bringing MCP's existing reach into the surface that has the canvas, the diff review and the undo stack. Second, `copyProjectFilesToFolder` copies the whole project folder into the deploy output past a five-name hardcoded filter with a `// TODO: Load something like .noodlignore file list` sitting on line 6 — so the `docs/` folder this feature is built around would be published to the app's public origin. That was filed as a **blocking dependency on the deployment overhaul** rather than patched with a sixth hardcoded name, because the general defect (any private file a creator puts in their project leaks) is deployment's to solve. Three design lines held across all four specs: **no AI-generated markdown that describes the graph** (Explain Mode narrates the live artifact on demand; a prose copy is a second source of truth that rots on the next node drag, and would quietly undo this phase's premise that the graph *is* the legible spec) — docs hold only intent, decisions, rejected alternatives, external contracts, and the rules for next time; **AI doc writes are reviewed diffs, never automatic**, because an assistant that rewrites `ARCHITECTURE.md` after every landed component produces changelog sludge nobody reads and soon nobody trusts; and **staging before applying is not negotiable**, because reject is currently safe for a structural reason rather than a promised one. The AIX-006 result is the evidence the group is built on: exposing the project's style vocabulary through `AuthoringContextBuilder` produced 2.4× more on-system styling for about a week of work, which generalises to — the agent is not short of capability, it is short of *this project's* context. |
| 2026-07-27 | AIX-008 added and built — the authoring surface showed the user a node graph and asked them to accept it. Specced from the gap directly: a staged candidate is a detached `ComponentModel` that never enters the project (deliberately — that is what makes reject the absence of a call), and the viewer only ever renders the project export, so there was structurally nothing to render. Two existing facts made it cheap: `ViewerConnection` already exports **per `clientId`**, and viewer clients mint their own id — so a second webview is a second, independently-fed client. The data half got the one seam it needed: a network shim over `fetch`/`XHR` in the sandbox window, rather than a sandbox branch in a dozen node types. Three decisions taken deliberately: sample data is **agent-first with inference as the floor** (a book list gets book titles, and a field the model forgot still renders); backend config is left **intact** rather than blanked, so nodes take the same code path they take in the real app and RUN-004's loud-failure branches do not fire; and the preview is **always sandboxed**, with a one-click toggle to the real backend, so a login wall or an empty table can never be what the user is looking at without knowing it. |
| 2026-07-26 | AIX-007 added — token cost reduction, specced from a cost review of the AIX-002 slice-5 measurements (the first task in this phase to start from a real measured baseline rather than a design goal). Found four unexploited levers: prompt caching is absent entirely; `initialUserMessage`/`updateUserMessage` place the varying request *ahead* of the stable context blocks, which defeats a cache breakpoint before one is even added; `thinking: adaptive` runs at the inherited default effort with no per-call-site control; and `AiUsage` has no cache-token fields, so landing caching first would silently corrupt our own cost reporting in the flattering direction. Sequencing puts accounting before optimisation for exactly that reason. Target ≥30% cost/component holding 8/8 first-attempt validity. |
| 2026-07-27 | AIX-005 built — all seven phase-3.5 AGENT tasks, by six agents in parallel worktrees. **The Gate G2 prerequisite was not satisfied and was overridden on instruction; the decision and its four unmet conditions are recorded at the top of the task doc** rather than left implicit. What the parallel structure bought, beyond throughput: the stale phase-3.5 specs turned out to contain a dozen real bugs, and independent readers caught them — a dispatcher whose unrecognised actions returned `{completed: true}` (an unknown remote command reporting success), reconnection gated on `event.wasClean` (true exactly when a server closes an idle connection, false for the ordinary 1006 drop), `JSON.parse(JSON.stringify())` history cloning (destroys Dates/Collections/Models, throws on cycles), and a Set node with no setters at all. Agents also cross-checked each other: one hit the `nodelibraryexport.js` edge and said so, which exposed that another's three nodes had shipped `inNodePicker: false` — registered but impossible to add. Two design lines held across all six: reuse the runtime's existing primitives rather than build beside them (a store is a `Model`, a history entry is a store snapshot, an undo is an ordinary commit), and never swallow a failure (the dispatcher's vocabulary is closed and opt-in, `FETCH` was removed as CSRF/exfiltration, and `fullyRestorable`/`byReferenceKeys` admit what time travel cannot restore instead of quietly under-delivering). Residual, and the reason this is not marked complete: **zero editor verification.** |
| 2026-07-22 | Phase created from NOODL-REVIVAL-ROADMAP.md Track C (C-01..C-05)       |
| 2026-07-24 | AIX-001 complete — provider-agnostic client, model registry, Anthropic/OpenAI/compatible/Ollama adapters, secure credential storage, settings UI, 69 specs. See NOTES.md. |
| 2026-07-24 | AIX-006 added from the [pre-revival salvage audit](../../reviews/PRE-REVIVAL-SALVAGE-AUDIT.md) §2 — the shipped phase-9 styles system has zero substrate connection; exposing it is the cheapest quality jump for the G2 demo. |
| 2026-07-24 | AIX-004 complete — read-only Explain Mode: bounded context assembly over the real-project corpus, per-scope prompts with node citations, sidebar panel + context-menu entry, citations link to canvas (hover-highlight, click-navigate), +47 specs (1060 → 1107). Live editor pass caught and fixed the sidebar-switch-deselects defect. Register/accuracy tuning deferred to a live provider run. See AIX-004-NOTES.md + the task-doc CHANGELOG. |
| 2026-07-24 | AIX-003 slices 1–5 — graph-native review: staged proposals expressed through SUB-007's diff engine (change-set adapter with stable ids + dependency closures), annotated merged component on the read-only diff canvas (badges/dash/thickness — change types readable without colour), ChangeReviewDocument (grouped sentence list, click-to-canvas navigation, per-key parameter detail, Before/Changes/After, per-row exclude/restore with closure enforcement, walkthrough stepper, Accept N of M validated through the SUB-006 gate), +14 specs (1139 → 1153). Two SUB-007 upstream fixes (derived componentName extra; port-index normalization, found live). Residuals in AIX-003-NOTES.md. |
| 2026-07-25 | AIX-002 complete — slices 2–6 since the last tracker update: staging + accept/refine/reject with byte-identical undo (slice 2), the Build panel with streaming feed (slice 3), live canvas rendering during authoring (slice 4), live provider measurement — claude-sonnet-5 8/8 first-attempt valid, ~22% of context budget, $1.18/corpus (slice 5), and slice 6: **update mode** (revise an existing component: whole-candidate contract, id-keeping for legible diffs, inexpressible-field carryover, `updateAuthoredComponent` replace-with-undo — one undo byte-identical incl. root component) + **opt-in local-first telemetry** for Gate G2 (anonymous JSONL under userData, no server, enums/numbers only — spec-asserted). test:ci 1283/0 (also resurrected 16 unregistered slice-4 specs). Full trail in AIX-002-NOTES.md. |
| 2026-07-24 | AIX-002 slice 1 — the headless authoring loop: `authoring/` module (context → author → validate → repair), pull-based context through a hard charged budget (the no-whole-project rule is structural + spec-asserted), MCP-policy validation gate from shared validators, injectable chat seam, +22 specs (1107 → 1129). `normalizeV2Component` moved to the pure normalize module (barrel-exported; MCP untouched). See AIX-002-NOTES.md. |

---

## Dependencies

- **Phase-13 (format-ai-substrate):** SUB-004 (node catalog) and SUB-006 (semantic validator) block AIX-002/004; SUB-007 (graph diff) blocks AIX-003.
- **Gate G2 (~Month 9):** AIX-002 demo shipped a full quarter + two LEARN-006 pilots → do users return unprompted?
