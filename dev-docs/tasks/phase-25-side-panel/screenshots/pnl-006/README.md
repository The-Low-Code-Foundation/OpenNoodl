# PNL-006 screenshots

Written by `dev-docs/tasks/phase-23-visual-refresh/corpus/components-tree.mjs`,
run from the PRIMARY checkout against a live editor with a project open:

    components-tree--dark.png
    components-tree--dark--narrow240.png
    components-tree--light.png
    components-tree--light--narrow240.png

The directory is empty in the PNL-006 commits by design: an editor launched from
the task worktree executes main-checkout code, so any image captured there would
have shown a different build. See PNL-006-NOTES.md, "Could not verify".
