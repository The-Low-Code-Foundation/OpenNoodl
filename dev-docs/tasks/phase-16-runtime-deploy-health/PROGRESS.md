# Phase 16 Progress — Runtime & Deploy Health

**Created:** 2026-07-22, from [NOODL-REVIVAL-ROADMAP.md](../../reviews/NOODL-REVIVAL-ROADMAP.md) Track D
**Overall status:** 🟡 In progress — 1 / 4 tasks complete (RUN-001, 2026-07-25); RUN-002 slices 1–5 + render-trigger done 2026-07-25 (SEO, hydration, node server-compat crux, server-side data resolution, SSG all landed); RUN-003 re-scoped to **BYOB consolidation** (decision 2026-07-25) with slices 1–6 done (incl. M2O relation traversal, 2026-07-25) — see the RUN-003 row

## Status vocabulary

Not started · In progress · **Built–not wired** · Complete · Superseded

## Tasks

| ID | Title | Status | Estimate | Notes |
|---|---|---|---|---|
| RUN-001 | Runtime React 17 → 19 | **Complete** (2026-07-25) | ↓ from 4–6 wks | All 7 slices done, see [RUN-001-ASSESSMENT.md](./RUN-001-ASSESSMENT.md). Spec premise was stale: runtime ships vendored React **18.3.1** UMD (`1477a29` half-migrated it upstream); React 19's missing UMD build was the real problem. **Two live defects found + fixed en route**: (1) `0d075b2` — findDOMNode replacement never worked for any built-in, `setStyle` fast path silently no-oped since Dec 2025, fixed via the setDOMElement contract; (2) the corpus pass proved Router + Page Stack were missed by that contract — under 19 their bounding/screenPosition outputs never fired and their own setStyle no-oped; fixed (`noodlRootRef` + `noodlNodeAsProp`), re-probed identical. React 19 global bundles (`1c6790c`); `runtimeVersion` selects the React pair everywhere (`db571b8`); editor surface truthful (`414a3eb`); **corpus pass 2026-07-25**: 16 projects × both runtimes, probes for signal ordering / animation fast path / router / stack transitions / repeaters — no behavioural differences ([corpus/](./corpus/)); user docs at [docs/runtime/REACT-19-RUNTIME.md](../../../docs/runtime/REACT-19-RUNTIME.md). Cloud runtime React-free (excluded); SSR harness unwired (RUN-002's) |
| RUN-002 | SSR / SSG support | **In progress** — Step 1 audit done (2026-07-25) | 4–6 wks (holds) | **Finding: foundation, not artifact** — see [RUN-002-ASSESSMENT.md](./RUN-002-ASSESSMENT.md). The original team's unshipped SSR pipeline builds on every `npm run build` and, assembled into a real deployment and run headlessly, **renders meaningful HTML server-side today** for both a trivial project and the multi-bundle router probe (HTTP 200, content + computed styles in `#root` pre-JS, server-side bundle fetch works). Three real gaps stand between it and shippable: (1) **hydration is broken by the React upgrade** — `renderDeployed` detects hydrate-vs-createRoot via `data-reactroot`, a marker React 18+ `renderToString` no longer emits (confirmed absent), so post-RUN-001 the client always `createRoot`s and re-renders from scratch instead of hydrating; (2) **SEO injection is an unfinished TODO** (`static/ssr/index.js:163`) — title/meta buffered server-side but never written to `<head>`, so the headline SEO benefit isn't delivered end-to-end; (3) **node server-compatibility unhandled** — a JS node touching `window` throws `ReferenceError` server-side and is silently skipped (the spec's named crux, unstarted). Plus version skew (SSR pins React 18.3.1 vs the react19 opt-in), a fragile 1000-iteration render trigger (`SSR_PageReady` commented out), no tests, and stale `external/* N/` duplicate dirs. Reproducible smoke harness recipe in the assessment §5. Prereq (React 19 runtime) satisfied by RUN-001. **Follow-on (2026-07-25): the two "cheap wins" were attempted + browser-verified and both proved not cheap** — flipping `renderDeployed` to hydrate on a `data-ssr` marker made the client hydrate but *duplicated* the DOM (server div + a client-fresh div), because the deployed runtime loads the root component async so hydrateRoot's first render is empty; reverted to a safe explicit `createRoot` (verified single `#root` child). SEO head-injection was implemented (14/14 unit incl. escaping) but appeared inert end-to-end. **Slice 1 DONE (2026-07-25): SEO now renders end-to-end.** The "no-op" reading was itself a misdiagnosis — the runtime *does* populate `Noodl.SEO` server-side (router navigates, `setTitle` fires; title is `"Home"` right before `renderToString`). The culprit: `renderToString` constructs `Viewer`, whose constructor re-runs `NoodlJSAPI` (`viewer.jsx:147`) which did `Noodl.SEO = new SeoApi()` — a fresh empty instance clobbering the populated one mid-render. Fix: idempotent SEO (`noodl-js-api.js`) + `SeoApi.reset()` called per-request by the SSR server (no cross-page bleed) + `injectSeo` extracted to a committed, unit-tested `static/ssr/inject-seo.js`. **Verified against the real `webpack.ssr.prod` bundle**: `hello-world` → `<title>Home</title>`; meta variant → injected `description`/`og:title` tags; `probe-router` → `Home` on `/`, `Second` on `/second`, no bleed; headless-Chrome client `document.title` = `Home`, no hydration warnings. 16 new tests (10 injectSeo + 6 SeoApi), full viewer-react jest green (21/21). Corrects the assessment's §3.2 root cause. **Slice 2 DONE (2026-07-25): synchronous-first-render hydration works.** `renderDeployed` detects `data-ssr="1"` and pre-settles the graph through the *same* machinery the server renders with (awaited `setData`, `triggerDidMount`, `settle()` with a shared `createFetchTracker` around `window.fetch`, SSR_PageReady gate) before `hydrateRoot`, so the first client render matches the served HTML; falls back to `createRoot` on any pre-settle failure. The pre-settle's double-didMount hazard was found and guarded (componentDidMount skips when `didCallTriggerDidMount`, cleared on unmount; Router/Page Stack `props.didMount` made idempotent — they registered twice in RouterHandler). Headless-Chrome verified (dev React + a prod-React run): hello-world, probe-router `/`+`/second`, and the 400 ms gated-data project all hydrate with a single `#root` child, the **server element adopted by identity**, zero mismatch warnings, and the gated page's first render containing the late data; click-navigation works on the adopted DOM (back-nav quirk reproduces identically in CSR — pre-existing). 5 new fetch-tracker tests, viewer-react jest 38/38. **Slice 3 DONE (2026-07-25): node server-compat — the spec's crux (Step 2).** Every registered node (incl. `nodes-deprecated/`) audited for load-path browser-API usage; classification shipped as a structural `ssr: {compat: safe|partial|client-only, note?}` field flowing defineNode → metadata → catalog → enriched → MCP `get_node_type` (**119 safe / 9 partial / 4 client-only**; SCHEMA.md documents it; absent = cloud-only). **Client-only deferral:** on the SSR server (`platform.isSSRServer`, set only by the windowless viewer entry — NOT "no window", which would break the cloud runtime; NB `NoodlRuntime`'s constructor allow-lists platform fields, the flag had to be added there) a client-only node instantiates **inert** — ports live, initialize/setters/getters never run — with per-type + per-page server-log reporting; 6 unit tests. **Crash paths fixed:** Page Stack's unguarded `window` in didMount (the guaranteed SSR crash), Router willUnmount/pushState/window.open, REST2's `_noodl_cloud_runtime_version` misdetection (threw on `window.XMLHttpRequest`; now `typeof window` → fetch path), Open File Picker (`document.createElement` in initialize — now deferred), Text Input's `placeHolderOpacity` stylesheet injection, transitions' `window.performance.now()`, RouterHandler's `window.Noodl`, Navigate To Path/External Link action no-ops. **E2E-verified** against the rebuilt `webpack.ssr.prod` bundle: previously-fatal deferral project serves full HTML + named deferrals; probe-pagestack serves real markup **and hydrates cleanly in headless Chrome** (zero mismatches); hello-world + probe-router regressions green. Also fixed: `catalog:merge` silently broken since DEBT-006 (stale For Each Actions `runtimeBehavior`); both catalog gates green again. **Known residual:** a client-only node feeding visual content (Screen Resolution → Text) still trips React's mismatch recovery at hydration (server "unset" vs client value → whole-root client re-render; degraded, not broken) — fix shape: defer during pre-settle + activate post-`hydrateRoot`. **Slice 4 DONE (2026-07-25): server-side data resolution via real query nodes.** Transport survey found the crux: the Parse cloud data nodes (Query Records/Record/CRUD via `cloudstore.js`) use **bare XHR, never fetch**, on the SSR server (their env gate is `_noodl_cloud_runtime_version`, not `typeof window`) and Query Records **auto-fetches on graph load** — the most important data path was invisible to the fetch-only gate. `createXhrTracker` added to `render-gate.js` (completion = `readystatechange` DONE, covers success/error/abort in browser + node polyfill) and wired both sides: server wraps its XHR polyfill, client pre-settle wraps `window.XMLHttpRequest` alongside `window.fetch`. **Auto-gating decision: transport-level tracking IS the auto-gate** — no per-node opt-in; `onPageReady` stays for orchestration the transport can't see. E2E-verified with two **committed** corpus probes + mock backend (300 ms latency): probe-rest-data (fetch path, signal-on-load) and probe-cloud-query (XHR path, auto-fetch, zero user wiring) both serve HTML containing the fetched data; a **control run with the fetch-only predicate serves the placeholder** (tracker proven load-bearing); headless-Chrome hydration (dev+prod React) adopts the server DOM by identity with the data in the first client render, zero mismatches; hello-world/probe-router regressions green. 6 new unit tests (viewer-react jest 44/44); catalog gate green. Traps recorded: mock backends need CORS for the hydration checks; headless export strips `metadata.cloudservices` (`environment: null`) — splice the endpoint like the real deploy flow does. **Slice 5 DONE (2026-07-25): SSG mode.** Per-path render extracted from the Express template into shared committed modules (`server-core.js` renderPage + `runtime-globals.js` globals/trackers — server and SSG provably share one render path; localStorage mock supports bracket access because cloudstore reads it that way). New `ssg.js` build entry (same `{{#export#}}` splice + deploy layout): enumerates routes from the export's own `routerIndex` (unit-tested `ssg-paths.js`; **dynamic `{param}` routes reported + skipped → CSR fallback**, root first, directory-index layout `second/index.html` so static hosts need no rewrites), copies `public/*`, renders each route, per-route failure → CSR fallback file + non-zero exit. E2E: probe-router 3/3 routes correct titles; probe-rest-data + probe-cloud-query **bake fetched data (both transports) into the static HTML at build time**; served via plain `python3 -m http.server` — `/`, `/second/` and the data pages hydrate in headless Chrome, server element adopted by identity, zero mismatches; refactored Express server re-verified unchanged. 8 new unit tests (jest 52/52); webpack's static/ssr copy propagates the new modules (rebuild verified). Still open: no state serialization (data fetched once per side; SSG additionally bakes data until next build — inherent), React-18-only server (§3.4 skew), client-only post-hydration activation residual, deploy UI/settings (step 6), docs (step 7), dynamic-route SSG enumeration. |
| RUN-003 | Finish UBA → **finish BYOB** (re-scoped) | **In progress** — audit + decision done (2026-07-25) | 3–5 wks (consolidate + deepen) | **Two-stage finding — see [RUN-003-ASSESSMENT.md](./RUN-003-ASSESSMENT.md) (read its correction box first).** Stage 1: first contact with a real containerised Directus proved the built **UBA** is a config-FORM renderer for a hypothetical UBA-protocol middleware server — `configure` 404s, `SchemaParser.parse` fails on Directus metadata; it could never be the backend-adapter vehicle (reproducible in [uba-e2e/](./uba-e2e/)). Stage 2 (same day): the assessment's "no adapter anywhere" conclusion was itself falsified — **BYOB** (`BackendServicesPanel` + `models/BackendServices/` + 4 `byob-*` runtime nodes, commits `ae7d3b8`/`73b5a42` of 2025-12-30/31) is a wired, working native-backend adapter that every UBA-focused doc missed. **Empirically verified against live Directus**: introspection (collections/fields/PKs/enums) works, runtime CRUD round-trip works, field→port mapping works. Known gaps: relations dropped (`foreign_key_table` unread), **zero tests**, Supabase/Pocketbase parsers unverified, filter UX short of Parse's QueryEditor. **Decision (Richard, 2026-07-25): consolidate + deepen** — BYOB is *the* external-backend story; RUN-003 = finish it to Parse-node quality (relations fix → test suite → committed Directus e2e → real-Supabase parser verification → visual-filter parity → M2O traversal → realtime node → full Supabase adapter); **UBA formally retired**; **phase-19 constraint: `nodegx-backend` surfaces as one more `BackendServicesPanel` preset** — one panel, one node UX. Prereq (REV-001) satisfied. **Slices 1–4 DONE (2026-07-25, `bf843c7` + `e6f1f76`):** parsers extracted to pure unit-tested `schemaParsers.ts`; **three live defects found + fixed** — (1) Directus relations dropped (`foreign_key_table` unread → now `relationTarget`/`relationType`); (2) **cached-shape seam bug**: byob-create/update feed cached `SchemaField`s (no `meta`) into `getEnhancedFieldType`/`shouldShowField` which only read raw Directus `meta` — enum dropdowns and hidden-field filtering were silently dead in the real editor flow (both utils now accept both shapes; `SchemaField.hidden` added); (3) `parseSupabaseSchema` crashed on null + only read type/format — now recovers postgres ENUMs→`enumValues`, `<pk/>`→primaryKey, `<fk/>`→M2O relations, `required[]` (full Directus parity), **verified against a live PostgREST stack** (Supabase's REST layer is PostgREST; compose profile `supabase` in [uba-e2e/](./uba-e2e/)). Tests: 26 jest (runtime 283/283) + 22 Jasmine (editor 1303/1303). **Slice-5 premise revised:** `ByobFilterBuilder` already exists (~1.9k lines — nested and/or, drag-drop, operators, Directus conversion incl. relation paths; `filter_*` connected-value ports in the query node) — remaining parity gap vs Parse's QueryEditor is **unmeasured, needs the live-editor UX smoke first**, not assumed-missing construction. **Slice 5 DONE (2026-07-25): live UX smoke + measured parity fixes.** Full editor-UI smoke against live Directus (real Add-Backend dialog → connect 209ms → Sync Schema 31 collections → query node → filter modal): builder is *ahead* of Parse on operators/nesting/drag-drop/JSON-edit; the measured gaps were (1) **connected-value UI missing** — model+runtime+SCSS all anticipated a static/connected toggle but `FilterCondition.tsx` never rendered it (Parse's "VALUE\|INPUT" equivalent); now built — per-condition Link toggle → port indicator with generated `filter_<field>_<id>` name, saved param verified to materialise a "Filter Values" dynamic input port on the node; (2) **enum/boolean value editors** — enum fields got a plain TextInput despite `enumValues` in schema; now a dropdown (booleans too), verified live; (3) **totalCount defect found live**: node requested `meta=total_count` only, so a filtered list paginated against the whole-collection count — now requests `filter_count` too and prefers it (`pickTotalCount` in byob-utils). The editor-authored filter (`status _eq published AND rating _gt "2"`) verified row-exact against live Directus. Tests: runtime 291/291 jest, editor 1309/1309 Jasmine. seed.mjs now seeds records (1 author + 4 articles). Remaining parity gaps deferred by design: relation-path picker → slice 6 (M2O traversal), multi-field sort → judged minor. **Session traps:** editor SPA pushState's `file:///dashboard/projects` at boot, so any renderer reload dies with ERR_FILE_NOT_FOUND — fix URL first via `history.replaceState(null,'','file://<repo>/packages/noodl-editor/src/editor/index.html')`, then reload; a failed HMR compile ("Reload prevented") leaves stale modules until that reload; BaseDialog renders content twice (MeasuringContainer + real) — drive the copy NOT inside `[class*='MeasuringContainer']`; custom Select dropdowns need trusted CDP clicks and close on CDP session detach — keep one WebSocket session for multi-step choreography (see scratchpad drive-filter.js pattern); multiple `dev:debug` stacks fight over port 8080 → webpack-dev-server disconnect → live-reload death (pkill dev:debug + start.ts + webpack before relaunch); runtime jest's reporter crashes on missing `terminal-link` AFTER tests pass — use a minimal custom `--reporters=<file>`. **Slice 6 DONE (2026-07-25): M2O relation traversal, live-verified end-to-end.** One-hop expansion of M2O/O2O relations into dotted pseudo-fields (`author.name`, displayName `Author → Name`, type/enumValues carried from the target field so operators and value editors stay correct) via pure `getRelationFields`/`expandRelationFields` in byob-utils — hidden fields skipped both sides, no depth-2 paths, unresolvable targets skipped; wired into the query node's filter-port schema **and** Sort Field enums (Directus accepts dotted sort). **Outputs**: one `Include <relation>` boolean port per traversable relation (group "Related Data") → `buildFieldsParam` appends `fields=*,author.*` so records carry the related record as a nested object; stale include params (toggled on, then collection switched — port gone but param survives) are validated against the backend's cached collections (now exposed on `resolveBackend`) so they can't poison the request. **Live smoke found a real defect the unit tests couldn't:** the runtime's `_toDirectusFilter` (duplicate of the editor converter) never nested dotted paths — it sent a flat `{"author.name":{...}}` key which live Directus rejects with 403 FORBIDDEN; editor-side conversion was always correct, so only real fetches hit it. Fixed (extracted as exported pure `toDirectusFilter`, nests paths + maps `_null`/`_empty` operators to literal `true` like the editor converter). Full in-editor smoke against live Directus: field dropdown offers `author → name` + `hero_image → *` (file M2O to directus_files), condition `author.name _eq Ada` built in the modal alongside slice-5's static + connected conditions, Include author toggled, runtime fetch (public token injected into live metadata for the session) → URL `fields=*,author.*` + nested 3-condition filter → **exactly Alpha with nested author object, totalCount 1 from filter_count** (row-exact: Beta fails rating, Delta is Bob's, Gamma is draft). Tests: runtime jest 303/303 (12 new: expansion 4, buildFieldsParam 3, toDirectusFilter 4, resolveBackend collections 1); editor Jasmine 1309/1309 (no editor code change needed — converter/port-naming already handled dotted paths; the whole gap was runtime-side). **New traps:** `Page.reload` on the *viewer webview* CDP target reloads the embedder editor page too → the pushState death; after a runtime-code change prefer relaunching/reopening the project over reloading the webview. Viewer runtime internals reachable in CDP via `Noodl.Navigation._noodlRuntime` → `rootComponent.nodeScope.nodes` (node instances; `scheduleFetch()` triggers a real fetch) and `Noodl.getMetaData('backendServices')` returns a **live mutable ref** (session-only publicToken injection for smokes). Editor CDP target after project open: match page title `NodeGX`, never the default. Open: realtime node (slice 7), UBA panel removal + docs + CHANGELOG (slice 8); O2M/M2M relations still unparsed (need GET /relations — noted in schemaParsers.ts); persistent publicToken/role setup on the backend remains manual. |
| RUN-004 | Stabilise local SQLite backend | Not started | 2 wks | `better-sqlite3` currently falls back to an in-memory mock — data silently does not persist |

## Inherited state worth knowing

- **UBA is further along than its top-level tracker says.** `phase-6-uba-system/PROGRESS.md` reports 0%; the per-developer notes and git history show UBA-001…009 delivered (types, SchemaParser, field renderers, ConfigPanel, UBAClient with HTTP+SSE, UBAPanel, sidebar registration, health indicator). REV-006 corrects that record; RUN-003 assumes the work exists.
- **The local backend's failure is silent.** The fallback to an in-memory mock means a user can build an entire feature against a database that discards everything on restart. RUN-004 treats making the failure *loud* as a first deliverable, ahead of fixing the native build.

## Blockers

- RUN-004 should wait for **REV-004** (Electron 31 → 43), which changes the native ABI and forces a `better-sqlite3` rebuild regardless.
- ~~RUN-001 overlaps `noodl-viewer-react` with **PLAT-003** (Phase 14). Agree boundaries before both start.~~ Resolved 2026-07-24: PLAT-003 already typed the binding hub and all of `src/nodes/`; boundary agreed in RUN-001-ASSESSMENT.md §5 (RUN-001 owns `highlighter.js`, `Drag.tsx`, the entry, `webpack-configs/`, `static/`; PLAT-003 keeps typing the rest of `src/` root; `catalog:check` is the shared gate).
