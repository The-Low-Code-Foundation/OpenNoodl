# Phase 16 Progress — Runtime & Deploy Health

**Created:** 2026-07-22, from [NOODL-REVIVAL-ROADMAP.md](../../reviews/NOODL-REVIVAL-ROADMAP.md) Track D
**Overall status:** 🟡 In progress — 1 / 4 tasks complete (RUN-001, 2026-07-25); RUN-002 slices 1 + render-trigger done 2026-07-25; RUN-003 Step 1–2 done 2026-07-25 → **blocked on a scope decision** (its premise proved stale — see the RUN-003 row)

## Status vocabulary

Not started · In progress · **Built–not wired** · Complete · Superseded

## Tasks

| ID | Title | Status | Estimate | Notes |
|---|---|---|---|---|
| RUN-001 | Runtime React 17 → 19 | **Complete** (2026-07-25) | ↓ from 4–6 wks | All 7 slices done, see [RUN-001-ASSESSMENT.md](./RUN-001-ASSESSMENT.md). Spec premise was stale: runtime ships vendored React **18.3.1** UMD (`1477a29` half-migrated it upstream); React 19's missing UMD build was the real problem. **Two live defects found + fixed en route**: (1) `0d075b2` — findDOMNode replacement never worked for any built-in, `setStyle` fast path silently no-oped since Dec 2025, fixed via the setDOMElement contract; (2) the corpus pass proved Router + Page Stack were missed by that contract — under 19 their bounding/screenPosition outputs never fired and their own setStyle no-oped; fixed (`noodlRootRef` + `noodlNodeAsProp`), re-probed identical. React 19 global bundles (`1c6790c`); `runtimeVersion` selects the React pair everywhere (`db571b8`); editor surface truthful (`414a3eb`); **corpus pass 2026-07-25**: 16 projects × both runtimes, probes for signal ordering / animation fast path / router / stack transitions / repeaters — no behavioural differences ([corpus/](./corpus/)); user docs at [docs/runtime/REACT-19-RUNTIME.md](../../../docs/runtime/REACT-19-RUNTIME.md). Cloud runtime React-free (excluded); SSR harness unwired (RUN-002's) |
| RUN-002 | SSR / SSG support | **In progress** — Step 1 audit done (2026-07-25) | 4–6 wks (holds) | **Finding: foundation, not artifact** — see [RUN-002-ASSESSMENT.md](./RUN-002-ASSESSMENT.md). The original team's unshipped SSR pipeline builds on every `npm run build` and, assembled into a real deployment and run headlessly, **renders meaningful HTML server-side today** for both a trivial project and the multi-bundle router probe (HTTP 200, content + computed styles in `#root` pre-JS, server-side bundle fetch works). Three real gaps stand between it and shippable: (1) **hydration is broken by the React upgrade** — `renderDeployed` detects hydrate-vs-createRoot via `data-reactroot`, a marker React 18+ `renderToString` no longer emits (confirmed absent), so post-RUN-001 the client always `createRoot`s and re-renders from scratch instead of hydrating; (2) **SEO injection is an unfinished TODO** (`static/ssr/index.js:163`) — title/meta buffered server-side but never written to `<head>`, so the headline SEO benefit isn't delivered end-to-end; (3) **node server-compatibility unhandled** — a JS node touching `window` throws `ReferenceError` server-side and is silently skipped (the spec's named crux, unstarted). Plus version skew (SSR pins React 18.3.1 vs the react19 opt-in), a fragile 1000-iteration render trigger (`SSR_PageReady` commented out), no tests, and stale `external/* N/` duplicate dirs. Reproducible smoke harness recipe in the assessment §5. Prereq (React 19 runtime) satisfied by RUN-001. **Follow-on (2026-07-25): the two "cheap wins" were attempted + browser-verified and both proved not cheap** — flipping `renderDeployed` to hydrate on a `data-ssr` marker made the client hydrate but *duplicated* the DOM (server div + a client-fresh div), because the deployed runtime loads the root component async so hydrateRoot's first render is empty; reverted to a safe explicit `createRoot` (verified single `#root` child). SEO head-injection was implemented (14/14 unit incl. escaping) but appeared inert end-to-end. **Slice 1 DONE (2026-07-25): SEO now renders end-to-end.** The "no-op" reading was itself a misdiagnosis — the runtime *does* populate `Noodl.SEO` server-side (router navigates, `setTitle` fires; title is `"Home"` right before `renderToString`). The culprit: `renderToString` constructs `Viewer`, whose constructor re-runs `NoodlJSAPI` (`viewer.jsx:147`) which did `Noodl.SEO = new SeoApi()` — a fresh empty instance clobbering the populated one mid-render. Fix: idempotent SEO (`noodl-js-api.js`) + `SeoApi.reset()` called per-request by the SSR server (no cross-page bleed) + `injectSeo` extracted to a committed, unit-tested `static/ssr/inject-seo.js`. **Verified against the real `webpack.ssr.prod` bundle**: `hello-world` → `<title>Home</title>`; meta variant → injected `description`/`og:title` tags; `probe-router` → `Home` on `/`, `Second` on `/second`, no bleed; headless-Chrome client `document.title` = `Home`, no hydration warnings. 16 new tests (10 injectSeo + 6 SeoApi), full viewer-react jest green (21/21). Corrects the assessment's §3.2 root cause. **Slice 2 DONE (2026-07-25): synchronous-first-render hydration works.** `renderDeployed` detects `data-ssr="1"` and pre-settles the graph through the *same* machinery the server renders with (awaited `setData`, `triggerDidMount`, `settle()` with a shared `createFetchTracker` around `window.fetch`, SSR_PageReady gate) before `hydrateRoot`, so the first client render matches the served HTML; falls back to `createRoot` on any pre-settle failure. The pre-settle's double-didMount hazard was found and guarded (componentDidMount skips when `didCallTriggerDidMount`, cleared on unmount; Router/Page Stack `props.didMount` made idempotent — they registered twice in RouterHandler). Headless-Chrome verified (dev React + a prod-React run): hello-world, probe-router `/`+`/second`, and the 400 ms gated-data project all hydrate with a single `#root` child, the **server element adopted by identity**, zero mismatch warnings, and the gated page's first render containing the late data; click-navigation works on the adopted DOM (back-nav quirk reproduces identically in CSR — pre-existing). 5 new fetch-tracker tests, viewer-react jest 38/38. Still open: data resolution via real query nodes (§3.5; XHR untracked), node server-compat crux (§3.3, Step 2), no state serialization (data fetched on both sides), React-18-only server (§3.4 skew), SSG, deploy UI/settings, docs. |
| RUN-003 | Finish UBA | **Blocked on a scope decision** — Step 1–2 done (2026-07-25) | 3–5 wks (premise stale) | **Finding: the premise is wrong, not the estimate** — see [RUN-003-ASSESSMENT.md](./RUN-003-ASSESSMENT.md). The spec treats UBA as a near-complete backend data-adapter needing only E2E + a reference impl + a second adapter. First contact with a real containerised Directus (the spec's own prescribed first move) shows the built UBA is a **schema-driven config-FORM renderer for a hypothetical UBA-protocol middleware server**, not a system that reads a real backend's collections/fields and lets you build against them. Driving the **real** `UBAClient`/`SchemaParser` at Directus (reproducible in [uba-e2e/](./uba-e2e/)): `health` **works** (maps to `/server/health`); `configure` **404s** (Directus has no config-ingest endpoint); `SchemaParser.parse` **fails** on `/collections` + `/fields` (Directus serves no `UBASchema`, and nothing translates its metadata into one); there is **no adapter interface/registry and no runtime data-node wiring** anywhere. So the Desired State ("point at Directus, see your real collections and fields, build against them") is **net-new construction**, not finishing — changing the task's scope, executor tier, and definition of done. Three ways forward laid out in the assessment §4 (A: build the real native-BaaS data-adapter; B: build the reference UBA-protocol server; **C, recommended: split — harden+test what exists at its real altitude, re-scope native BaaS as its own task**). Awaiting Richard's call before building. Prereq (REV-001) satisfied. Banked: reusable Directus harness + rich seed schema (spec's step-1 deliverable). |
| RUN-004 | Stabilise local SQLite backend | Not started | 2 wks | `better-sqlite3` currently falls back to an in-memory mock — data silently does not persist |

## Inherited state worth knowing

- **UBA is further along than its top-level tracker says.** `phase-6-uba-system/PROGRESS.md` reports 0%; the per-developer notes and git history show UBA-001…009 delivered (types, SchemaParser, field renderers, ConfigPanel, UBAClient with HTTP+SSE, UBAPanel, sidebar registration, health indicator). REV-006 corrects that record; RUN-003 assumes the work exists.
- **The local backend's failure is silent.** The fallback to an in-memory mock means a user can build an entire feature against a database that discards everything on restart. RUN-004 treats making the failure *loud* as a first deliverable, ahead of fixing the native build.

## Blockers

- RUN-004 should wait for **REV-004** (Electron 31 → 43), which changes the native ABI and forces a `better-sqlite3` rebuild regardless.
- ~~RUN-001 overlaps `noodl-viewer-react` with **PLAT-003** (Phase 14). Agree boundaries before both start.~~ Resolved 2026-07-24: PLAT-003 already typed the binding hub and all of `src/nodes/`; boundary agreed in RUN-001-ASSESSMENT.md §5 (RUN-001 owns `highlighter.js`, `Drag.tsx`, the entry, `webpack-configs/`, `static/`; PLAT-003 keeps typing the rest of `src/` root; `catalog:check` is the shared gate).
