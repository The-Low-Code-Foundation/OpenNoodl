# Phase 64 — the tasks (VFN: the visual function in use)

**Created:** 2026-08-13, out of [README.md](README.md) and eleven reports from one session of
building with the floating Logic Builder.

> ## Status — 2026-08-13, after the close-out merge (`e61986a5`, `3acf2941`, `082e2c37`, `db4f6d72`)
>
> **All fourteen tasks are BUILT and merged into `cline-dev`. Every `vfn-*` branch is merged; `git
> branch --no-merged cline-dev` lists no `vfn-` lane.** What is left is one thing: **a drive.**
>
> **Gates on `cline-dev`, verified after the four close-out merges:**
>
> ```bash
> cd packages/noodl-editor
> npx jest                              # 185 suites / 2806 passing, 0 failures
> npx tsc -p tsconfig.json --noEmit     # clean
> npm run cloud-library:check           # up to date
> ```
>
> 🔴 **`npx tsc -p tsconfig.json` WITHOUT `--noEmit` is a trap that looks like a pass.** It emits
> ~3096 `.js`/`.js.map` files into `packages/*/src` beside their sources; jest then resolves
> `BenchRunner.js` over `BenchRunner.ts` and **158 of 178 suites fail to *run*** while the log
> prints `Tests: 246 passed` and zero failures. The artefacts are untracked, so `git status` looks
> unalarming. **Always `--noEmit`.** Clean-up: `git status --porcelain | grep '^??'` filtered to
> `packages/.*\.(js|map)$` — never `git clean`, which takes the new specs with it. Full account in
> [NOTES-contrast-sweep.md](NOTES-contrast-sweep.md).
>
> ### 🔴 Read the three states apart. This register lost track of them once and it cost a day.
>
> | state | means | proves |
> |---|---|---|
> | **built** | the code exists and is merged | nothing on its own |
> | **spec-proved** | a headless runner grades it, with a control watched red | the decision, the string, the geometry — *never* the pixels or the gesture |
> | **driven** | observed working in the running editor | the consequence |
>
> **VFN-004 was built, merged, and spec-proved by 52 green specs — and was completely dead on
> screen for a day**, because the field every layer agreed on (`ComponentModel.id`) was `undefined`.
> Nothing in this file may claim a drive that did not happen.
>
> ### 🔴 2026-08-13 — the close-out drive ran: [DRIVE-2026-08-13-G.md](DRIVE-2026-08-13-G.md)
>
> **20 of the brief's 31 items driven; 7 defects found, 4 fixed with controls watched red, 3 filed.**
> Six tasks that had **never** been on screen are now driven. What is still owed is listed at the
> foot of that file — start there, not at -F, whose list it supersedes.
>
> 🔴 **A Chrome with `--remote-debugging-port=9222` started on this machine mid-session and took the
> port.** Every reading after that came from Chrome, and it looked exactly like the editor crashing.
> **Check `lsof -i:9222` first, and drive on `NOODL_REMOTE_DEBUG_PORT=9333`.**
>
> 🔴 **[NEXT-SESSION-2026-08-13-F.md](NEXT-SESSION-2026-08-13-F.md)** remains the reference for the
> items not reached; its driving conditions all held, and G adds two: `cdp click` scrolls before it
> clicks (so a rect read a moment earlier is stale), and a `.blocklyToolboxCategory` **toggles** —
> always confirm `aria-selected` rather than assuming the flyout you asked for is the one that is up.
>
> ⚠️ **[DRIVE-2026-08-13-C.md](DRIVE-2026-08-13-C.md) and
> [DRIVE-2026-08-13-D.md](DRIVE-2026-08-13-D.md) have STALE COORDINATES.** VFN-005 moved the window
> and added two title-bar buttons. Their *findings* stand; every click point in them does not.

### Where the drive has actually reached

| Task | Built | Spec-proved | Driven |
|---|---|---|---|
| VFN-001 | ✅ | ✅ | ✅ **fully** — criteria 1–4 |
| VFN-002 | ✅ | ✅ | 🟡 field measured live; criterion 3's *rendered* ring and criterion 4 owed |
| VFN-003 | ✅ | ✅ | 🟡 criteria 1, 2, 3, 5 driven; 4 at the seam; one gesture owed |
| VFN-004 | ✅ | ✅ | 🟡 AC 1–4 driven; AC 5 driven **for a definition tab only**, AC 6 owed |
| VFN-005 | ✅ | ✅ | ✅ **criteria 2, 3, 4, the yield and the 640 px title bar all driven (G)** — 🔴 and the drive found **D-5**: the yield can put Home, Park and *Done* off the viewport |
| VFN-006 | ✅ | ✅ | ✅ **outline, hover trigger and the dialog's pin driven (G)** — 🔴 and found **D-7**, the outline never went away; fixed |
| VFN-007 | ✅ | ✅ | ✅ **criteria 3 and 4 driven in both themes (G)** |
| VFN-008 | ✅ | ✅ | 🟡 the ports fix driven; the description proved **on disk and in the flyout** (G); the quit-and-reopen half and the wire gesture owed |
| VFN-009 | ✅ | ✅ | ✅ **the section, the propagation warning and *Edit blocks* into the floating window all driven (G)** — 🔴 and found **D-1** and **D-2**; both fixed |
| VFN-010 | ✅ | ✅ | ✅ **launcher section, headings in both themes, cross-project check, delete refusal, clipboard import and criterion 6 all driven (G)** |
| VFN-011 | ✅ | ✅ | 🟡 the flagship gesture driven; 🔴 **D-6 — all seven strip sentences still clip at 640 px, and the budget's instrument is 16 % low**; items 4, 6, 8, 9 owed |
| VFN-012 | ✅ all three §§ | ✅ | 🟡 §2/§3's category, flyout, empty state and settings button driven (G) — 🔴 found **D-3** (the caution label can never fit) and **D-4** (the yield never fired from it; fixed). §1 items 3–5 and a real registered library owed |
| VFN-013 | ✅ | ✅ | ✅ **re-swept live** — 266 and 371 px² to zero; one residual recorded |
| VFN-014 | ✅ | ✅ | ✅ **criteria 1, 2 and 5 driven** — *View Code* leads with the readable program; warnings **16 → 1** |

**Every claim about existing code in these files was read in source**, and every claim that was
*not* pinned says so and carries a reproduce-first instruction. Six of the eleven reports have a
mechanism; two do not; three are design work.

| Task | File | One line | Report | Tier | State |
|---|---|---|---|---|---|
| VFN-001 ⭐ | [VFN-001-ONE-DELETE-STILL-MEANS-TWO.md](VFN-001-ONE-DELETE-STILL-MEANS-TWO.md) | the focus guard reads `activeElement` *after* Blockly removed the focused block | 2 | **1 — ship-blocking** | ✅ **BUILT** (`0067304d`) · ✅ **SPEC-PROVED** — 13 specs (6 jasmine + 7 jest), incl. a negative control taken at the same instant on the same event · ✅ **DRIVEN 2026-08-13 — criteria 1–4 all pass**, each with a control (canvas nodes 8→8→8→8 across three Deletes; the control deletes the node with focus outside). 🔴 **The task's own fix sketch was insufficient and the spec caught it (VFN-001b):** a detached element has no ancestors, so `path[0].closest('[data-keyboard-scope]')` is `null` on the very block that was removed — the resolver walks the captured composed path and takes the first element still `isConnected`. Also made correct: closing tabs is one transition, because a batched loop of `closeTab` never emits `AllTabsClosed`. **OWED: nothing.** |
| VFN-002 | [VFN-002-THE-FIELD-YOU-CANNOT-READ.md](VFN-002-THE-FIELD-YOU-CANNOT-READ.md) | our own `padding` takes 18 px out of a box Blockly sized to fit the text exactly | 3 | **1 — ship-blocking** | ✅ **BUILT** (`0067304d`; ring token fixed `5452720e`) · ✅ **SPEC-PROVED** — 6 stylesheet tests + 9 contrast cases, both with controls watched red · 🟡 **DRIVEN in part.** The clip is **measured live and passes**. 🔴 **`scrollWidth > clientWidth` is the WRONG instrument** (integer rounding gives a false 1 px failure); `scrollLeft` is the honest one — 0 with the fix, up to 9.5 px of real clip with the pre-fix rule re-injected. Border and padding gone, ring is a `box-shadow`, `font-family` override deleted — the font half *confirmed in source*: `WidgetDiv.show` copies the renderer and theme class names onto the widget div, so Blockly's own `FIELD_TEXT_FONTFAMILY` rule reaches the input. 🔴 **Lane D found criterion 3's ring was invisible: `--theme-color-border-default` on its own `bg-3` fill measures 1.01:1 (dark) / 1.11:1 (light)** — the same colour as the surface it delimits. Now `--theme-color-border-control`, 3.17 / 3.16, graded from the token names read *out of the stylesheet*. **OWED to the drive:** the ring rendered on a block in both themes (its outer edge is over a hue and measures 1.00), and **criterion 4 — dropdowns**, never touched. |
| VFN-003 | [VFN-003-THE-DIALOG-ELECTRON-DOES-NOT-HAVE.md](VFN-003-THE-DIALOG-ELECTRON-DOES-NOT-HAVE.md) | Blockly asks for a name with `window.prompt`, which Electron does not implement | 4 | **1 — ship-blocking** | ✅ **BUILT** (`0067304d`, `d9f41d20`) · ✅ **SPEC-PROVED** — 10 specs, incl. the whole prompt → taken → alert → re-prompt gesture · ✅ **DRIVEN 2026-08-13 — criteria 1, 2, 3, 5 pass; 4 proved at the seam.** The control re-registers `window.prompt` and reproduces the silence on demand. All three registered (`setPrompt`/`setConfirm`/`setAlert`), contract split into a React-free module so it is assertable. 🔴 **Registered from `BlocklyWorkspace`, NOT `initBlocklyIntegration()`** — the latter is reachable from the plain-Node runner and a `@noodl-core-ui` import in its graph broke two suites *to run*. **OWED to the drive:** the full *delete a variable in use* gesture. |
| VFN-004 | [VFN-004-THE-WINDOW-SAYS-WHERE-IT-BELONGS.md](VFN-004-THE-WINDOW-SAYS-WHERE-IT-BELONGS.md) | the tab names the component, and clicking it takes you back to the node | 1A | 2 | ✅ **BUILT** (`ca0ef476`), **SHIPPED DEAD**, **FIXED** (`da090f0b`) · ✅ **SPEC-PROVED** — 52 + 16 specs, **7 of the 16 go red under the pre-fix body**, printing the live symptom · ✅ **DRIVEN 2026-08-13 — AC 1, 2, 3, 4 pass live**, each with a control. 🔴 **It merged with 52 green specs and did nothing in the running editor**: every layer correct, and the field they all agreed on — `ComponentModel.id` — was `undefined`. **v2 assigns every component a guid (0 of ~180 missing); v1 legacy assigns 2 of 7.** Fixed with `componentInstanceId` (`models/componentIdentity.ts`), a `WeakMap`-held session identity minted lazily and **never serialised** — proved on disk, `project.json` still holds exactly 2 of 7 ids after a full drive. 🔴 **Minting an `id` on load was rejected against a live gate** (DSG-007). Keying on `fullName` rejected: a rename orphans the tab. The away mark is **italic, not colour** (identical `rgb(139,149,161)` in both states), captured both ways. 🔴 **`ed.selection` does not exist — it is `ed.selector._selected`**, and the non-existent one reads `[]`, which nearly filed a false failure. See [DRIVE-2026-08-13-D.md](DRIVE-2026-08-13-D.md). **OWED to the drive:** AC 5 (two tabs on different components, distinguishable from labels alone) and AC 6 (deleted component → tab stays open, named refusal toast); both need a second Logic Builder node in another component. |
| VFN-005 | [VFN-005-THE-APP-IS-BEHIND-THE-WINDOW.md](VFN-005-THE-APP-IS-BEHIND-THE-WINDOW.md) | 82% of the viewport, centred, is on top of the running app — park it, snap it, or place it better | 1B | 2 | ✅ **BUILT 2026-08-13 on `vfn-e-place`** (`3c37ca71`, `70fc6008`; merged `e61986a5`) · ✅ **SPEC-PROVED** — geometry graded headlessly, **7 negative controls watched red**; one guard was removed because it was decoration · 🔴 **NOT DRIVEN.** ✅ Criterion 1 answered **before** the build: it is **OCCLUSION**, not interception (window measured 74.1% × 69.5%; every blocked point geometrically inside the window rect, every reachable point outside it). **The decision: Park + Home. Snap-to-halves REJECTED** — the running app is a full-width band across the top, not a half, so a half-snap frees half a preview; **Home** places the window over the node graph frame and frees the preview entirely. Park is a **presentation** state (`height: auto`, `.TabContent` out of flow, the four geometry custom properties untouched), so nothing is serialised and no workspace is unmounted. Plus a **yield**: the window moves or parks when `openSettingsPanel` fires — which is DRIVE-C's finding that *Open app settings* opened the panel **behind** the window that offered it. 🔴 **This invalidates every coordinate in DRIVE-C and DRIVE-D.** **OWED to the drive:** all five items at the foot of the task file — criterion 3 (`elementFromPoint` after Home), criterion 2 (park/restore with an `.injectionDiv` measurement, the Blockly-caches-the-zero risk), criterion 4 (a **fresh profile**, `logic_overlay_rect` cleared), the yield end to end, and the title bar at the 640 px floor with two tabs open. |
| VFN-006 | [VFN-006-SHOW-ME-WHAT-I-AM-SAVING.md](VFN-006-SHOW-ME-WHAT-I-AM-SAVING.md) | "5 blocks" becomes *those* five blocks, outlined on the workspace | 5 | 2 | ✅ **BUILT** (`7384df78` / `22f9e1fe`; contrast fixed `5452720e`) · ✅ **SPEC-PROVED** — 31 specs in `tests-unit/vfn-006` + 9 contrast cases sweeping the whole hue circle in both themes · 🔴 **NOT DRIVEN — the painting is the whole feature and nobody has seen it.** Criteria 3, 4, 5 closed with controls; 1 and 2 closed at the model and the source. The outline is a `cloneNode` of each block's own `<path>` appended into its `<g>` — no Blockly API touched, gated by name. 🔴 **A control caught criterion 5 passing vacuously**: Blockly fires events on a `setTimeout`, so a synchronous "no events" assertion measures the event loop, not the code. 🔴 **Lane D found the outline was white in the light theme**: casing `--theme-color-bg-1` = `#ffffff`, **2.58:1** on hue 60 and 2.72 on hue 55 — the My Blocks hue, the one a save outline is most often wrapped around. Now untokened (`#0b0e12` / `#4da3ff`, worst 3.21:1), sharing VFN-013's palette. ⚠️ The gate grades the **resolved** colour, not `spec.fallback` — a fallback-grading spec would have stayed green through the entire defect. 🔴 The report's *"5 blocks"* is **6**. Also fixed here: `BaseDialog`'s measuring copy is `inert`, closing the double tab stop in **every** dialog in the editor. **OWED to the drive:** the outline painting, its **hover trigger** (the lane calls it *"the weakest thing I built"* — a guess about Blockly 12's menu DOM), and `inert` confirmed in a real dialog. |
| VFN-007 | [VFN-007-THE-SHELF-THAT-DOES-NOT-SAY-WHICH.md](VFN-007-THE-SHELF-THAT-DOES-NOT-SAY-WHICH.md) | the backpack radio takes the choice and does not show it — reproduce, then replace the control | 6 | 3 | ✅ **BUILT 2026-08-13 on `vfn-saveblock`** (`136fa3be`, merged via `7411c265`) · ✅ **SPEC-PROVED** — 23 tests, **three negative controls** · 🔴 **NOT DRIVEN.** 🔴 **Criterion 1 answered: it is NOT contrast.** `BaseDialog` renders the body twice, so 4 radios shared one document-scoped native group and the check landed on the **invisible measuring copy**. The picker is now an **ARIA radiogroup of themed option cards** — no `<input type="radio">` anywhere in the dialog, so there is no group for the second render to join; the selected state is React's and nothing else's. Criterion 2 is a **measured number**, not a screenshot: `--theme-color-primary` on `bg-3` = 6.45 / 3.99, `border-control` on `bg-2` = 3.66 / 3.43. ⚠️ The `<label>` wrapper is gone (a `<label>` with no labelable control labels nothing); the property it bought — the whole row including its note is the click target — is kept and asserted. **OWED to the drive:** criteria 3 and 4 — the selected state **on screen** in both themes, and the keyboard model. ⚠️ Filter `:not([class*=MeasuringContainer])` when driving. |
| VFN-008 ⭐ | [VFN-008-A-SAVED-BLOCK-THAT-DESCRIBES-ITSELF.md](VFN-008-A-SAVED-BLOCK-THAT-DESCRIBES-ITSELF.md) | a description that is asked for, stored, and shown everywhere the block appears | 7 | 2 | ✅ **BUILT** (`136fa3be` + `bf4529d7` / `ad02b43c`) — **all six criteria** · ✅ **SPEC-PROVED**, each half watched red, and measured on the real `vfn64-qa` artefacts · 🟡 **DRIVEN in part.** ✅ **Criterion 4 was FALSE AS WRITTEN and is fixed:** a call block now states its definition's ports in `extraState.ports` and `detectIO` reads them, so the port the generated code writes to exists — *a call site states the signature it was bound against*. 🔴 **Both filed routes were rejected**: the backpack shelf can never reach the viewer and the reported block was backpack-only. It also fixes the interface rails and the bench, which the finding never mentioned. ✅ **The migrate path IS driven** (DRIVE-C): after a flush the node's outputs gained `result`, typed `number`, and the OUTPUTS rail read **`result number 3`** beside `total any 21` — three merged lanes agreeing in one screenshot. **OWED to the drive:** the **canvas half** — drag a saved block into a *second* Visual Function and prove the port **accepts a wire** (a real drag; only migrate has been driven) — and criterion 1's round trip: save with a description, **quit and reopen**, hover the call block, open the My Blocks category. |
| VFN-009 ⭐ | [VFN-009-THE-LIBRARY-IN-THE-PROJECT.md](VFN-009-THE-LIBRARY-IN-THE-PROJECT.md) | open, rename, delete and **edit** a saved block, with a warning that names every call site | 8, 9 | 2 | ✅ **BUILT 2026-08-13 on `vfn-b-library`** (`a5cab23e` / `00261768`) · ✅ **SPEC-PROVED** · 🔴 **NOT DRIVEN — nothing in this section has ever been on screen.** Ruled: build it in full, in app settings. The engine (`myblocks/store.ts`) was already complete and had **zero callers outside its own tests**; this is the way in. Richard's second question answers itself in the direction he suspected — the deep half of the breakage warning waits for the testing phase; the line is drawn on evidence, not effort. 🔴 **AMENDED by lane B (`618deb7e`), and both amendments apply to this instance too:** (1) `EditorSettings.set` debounces its disk write by **1000 ms**, and this manager inherited that window on **five doors** — now flushed via `EditorSettings.store()`; (2) `handleExport` announced success for an **empty** export and rendered *"and its **-1** dependencies"*. **OWED to the drive:** does the section **render at all**? Does *Edit blocks* open the floating window **while the settings panel is open** — a canvas overlay reached from the settings route is a question only a drive answers. Then the rename/delete/propagation-warning gestures end to end. |
| VFN-010 | [VFN-010-THE-BACKPACK-IN-THE-LAUNCHER.md](VFN-010-THE-BACKPACK-IN-THE-LAUNCHER.md) | backpack blocks are marked in the picker and managed from the launcher | 8 | 3 | ✅ **BUILT 2026-08-13 on `vfn-f-backpack`** (`618deb7e`; merged `3acf2941`) · ✅ **SPEC-PROVED** — 4 new suites / 44 tests · 🔴 **NOT DRIVEN.** **A second *instance* of VFN-009's manager, not a second implementation** — the lane was told to stop and reuse if it found itself writing a parallel store, and it did not. Picker headings partition by shelf (*This project* / *My backpack*) via `kind: 'label'`. The cross-project usage count shares `collectReferences → scanNodeUsage` with the editor, **so the two counts cannot disagree**. **OWED to the drive:** all seven items at the foot of the task file — the picker headings screenshotted in both themes (a `kind: 'label'` in a flyout has never been rendered here), the launcher section rendering at all (`SavedBlocksSection` reaches `ProjectModel.instance`, `ToastLayer` and `DialogLayerModel` from a page that has never hosted it; a dialog inside a dialog is ordered but unseen), the cross-project check against **real** folders, 🔴 criterion 6's *write a rename, quit within a second, reopen* (the only way to see whether the bytes land), the delete refusal end to end, and clipboard import. |
| VFN-011 ⭐ | [VFN-011-THE-BENCH.md](VFN-011-THE-BENCH.md) | set sandbox values, press Run, watch the badges fill — with no app running | 10 | **1 — the flagship** | ✅ **BUILT** (`4420812e`, `e1219147`, `8d4bf513`; copy trimmed `5452720e`) · ✅ **SPEC-PROVED** — the drift gate runs 8 programs through `runOnBench` **and** through the runtime's real `_executeLogic`, diffing outputs, signals, refusals and the recorded frame, with four controls; criterion 8 asserts the serialised workspace is **byte-identical** across two runs · 🟡 **DRIVEN — and the flagship works.** ✅ Criterion 1 end to end; ✅ **a bench frame reaches `BlockValueBadgeLayer`** — five badges painted `7, 3, 21, 7, 7` — which was the item flagged *"most likely to be quietly wrong"*; ✅ criterion 8 **on disk**, non-vacuously; ✅ the 152 px rail fits. 🔴 **The drive found the strip sentence ellipsised** — *"…Press ▶ Run below to work them out here, with the app stop…"*, the teaching half gone. Lane D then measured in real glyph advances and found **six of the seven** sentences clipping at the 640 px minimum (`no-node` worst at 802 px against 564 px of room) — **the reported one was only the second longest.** All seven now 422–480 px against a 489 px budget; the word *"below"* was **removed as a false direction** (Run is to the note's LEFT). **OWED to the drive:** items 4 (criterion 7 with a preview running — two sources in one scrubber), 6 (the focus-preserving repaint, which runs on every settled change), 8 (a genuinely stale node for `no-probes`), 9 (Part 1's `generatedCode` thread, only partly observed), plus **one screenshot of the strip at a 640 px window** — the budget is arithmetic with a ±5% band. |
| VFN-012 | [VFN-012-THE-BLOCKS-THE-APP-ALREADY-HAS.md](VFN-012-THE-BLOCKS-THE-APP-ALREADY-HAS.md) | app-config variables, registered libraries and `window` become blocks | 11 | 3 | ✅ **ALL THREE SECTIONS BUILT — nothing dropped.** §1 `96869d5f`; **§2/§3 `69a13622` / `a3556bd7` on `vfn-g-config`** (merged `082e2c37`) · ✅ **SPEC-PROVED** — **8 negative controls watched red** for §2/§3; 85 new specs · 🟡 **DRIVEN in part.** ✅ §1's flyout **renders `kind: 'label'` and `kind: 'button'`** and *Open app settings* **reaches the panel** (DRIVE-C) — which is also where the panel was found opening **behind** the Logic Builder window, now VFN-005's yield. 🔴 §2/§3 **never driven.** One new category `Libraries & Browser` below `App Config`; `noodl_library_global` → `window.<global>`, `noodl_window` → `window["a"]["b"]`; no setter for either. 🔴 **`listRegisteredLibraries()` is async where §1's `getConfigVariables()` was sync, and a flyout callback is not** — so *"an async injection sampled early looks like a disposal"* is a **live defect** here, not a caution: a two-state cache would mark every library block `⚠` at the moment the editor opened and tell a project with five libraries it has none. Tri-state snapshot (`unknown`/`loaded`/`unavailable`) + a generation guard; 🔴 `scanModuleManifests(undefined)` returns `[]`, so the loader **throws** rather than reporting a confident empty project. ⚠️ **The Logic Builder IS registered in the cloud runtime** (`noodl-runtime.ts:190`, not in the `type !== 'cloud'` subtraction) — no `window`, no injected library — so both blocks say so and `window.x` is left to throw rather than guarded into silence. 🔴 A pasted `window.` prefix is stripped: `window["window"]["location"]` **works**, because `window.window === window`, so only a string assertion could catch it. The sharpest control: the `window` block is in *every* flyout, so an empty-state predicate written the obvious way reports no empty state ever. **OWED to the drive:** §1 items 3–5 (the `⚠` mark's contrast on a hue-90 block — needs a variable declared then deleted; the stale mark; a runtime round trip) and all of §2/§3 — 🔴 **`Libraries & Browser` is the longest label in this toolbox (19 chars, four more than `Inputs / Outputs`) and has never been measured against the pane width**, which is exactly how VFN-011's strip was lost. |
| VFN-013 | [VFN-013-THE-VALUES-YOU-CANNOT-READ.md](VFN-013-THE-VALUES-YOU-CANNOT-READ.md) | run values are drawn on top of the block they belong inside | live test 2026-08-13 | 2 | ✅ **BUILT** (`8b243610` / `c7af5581`) · ✅ **SPEC-PROVED** — 17 specs, with the negative control built from the *old* formula (it prints 4 real overlaps) · ✅ **DRIVEN — re-swept live after merge** (`d496f8b2`). Same instrument, same fixture, same gesture: the two badges the task filed, **266 px² and 371 px², both go to 0**, and the two top-level nulls stay null. Placement is now nesting-aware: a top-level block keeps LGC-003 §5.1's `-(width+6)` anchor to the pixel; a nested one goes above (or below) its **own** left edge, never left of it, with badge-vs-block and badge-vs-badge collisions resolved. New pure module `badgeLayout.ts`. 🔴 **Criterion 3 found a REAL failure and it was not the placement**: the badge fill was `--theme-color-bg-2`, which measures **2.58:1** against a block in the LIGHT theme — Blockly hues are theme-independent, so no light fill can clear 3:1; the badge is now dark in both themes (worst 3.21:1 over the whole hue circle). ⚠️ **One residual, recorded rather than rounded up to a pass:** the deepest nested badge still overlaps 152 px² of block *body* and 36 px² of an **empty** `blocklyFieldRect`. Criterion 1 is met for readable text and **not literally met** for one field rect in the densest nesting; the layout is bounded at six candidates and there is genuinely no free space there. **OWED: nothing further, unless the residual is ruled a defect.** |
| VFN-014 | [VFN-014-THE-CODE-NOBODY-CAN-READ.md](VFN-014-THE-CODE-NOBODY-CAN-READ.md) | *View Code* shows the probe-instrumented build, not the program | live test 2026-08-13 | 2 | ✅ **BUILT** (`8b243610` / `c7af5581`) · ✅ **SPEC-PROVED** — 19 specs, incl. **criterion 3 with a negative control**: instrumented → readable render → instrumented is byte-identical, and a deliberately leaked generator makes that comparison go red; criterion 4 proved by *running* both and diffing outputs **and signal order** · 🟡 **DRIVEN in part.** *View Code* now leads with a probe-free rendering of the same program and keeps the instrumented one behind one *Show tracing* press, labelled as what runs. Each inlined saved block is marked `// test1` (criterion 6). ✅ **mechanism pinned**: `generatedCode` is generated inside `withBlockProbes`, which is **scoped** — a clean rendering is one call away. ✅ **Criterion 5 REPRODUCED AND CLOSED live: `Outputs["result"]` is the inlined body of saved block `test1` (user backpack), and `generatedCode` is NOT stale** — the serious candidate is eliminated, and that near-miss (a clean, confident, wrong conclusion) is why it produced **criterion 6**: the code could not say which lines came from a saved block, which is why `result` was unfindable. **OWED to the drive:** criteria 1 and 2 — **one press of *View Code***, including whether the 14 lint warnings go. |

## Suggested order, and why

⚠️ **This section is the original build plan and is now history — every task in it is built.** It is
kept because the reasoning still explains the dependencies, and because a re-open of any task should
know why it sat where it sat. **For what to do next, go to
[NEXT-SESSION-2026-08-13-F.md](NEXT-SESSION-2026-08-13-F.md).**

**Tier 1 first, and it is genuinely a tier rather than a preference.** VFN-001, 002 and 003 are each
a builder losing work or being unable to proceed: a node deleted that nobody touched, a number you
cannot see while typing it, a button that does nothing at all. All three are small — the largest is
VFN-003 and it is one seam and one dialog — and none of them depends on anything else in the phase.
Shipping the rest of this phase on top of them is shipping polish over a data-loss bug.

**VFN-011 next, alone if necessary.** It is the flagship and it is the one task that changes what the
node *is*: a visual function you can exercise without running the app is a different tool from one
you can only watch. It is also the task most likely to find that something else is wrong, because it
is the first thing in this feature's history that executes a block program on demand. — *And it did:
the bench's own drive is what found the ellipsised strip and confirmed the ports fix end to end.*

**Then the library, in order: VFN-008 → VFN-009 → VFN-010.** They are one job in three sizes, and
008 is the cheapest half of the value: a saved block that says what it does removes most of the
reason to open it. 009 needs 008's description field to have somewhere to edit it. 010 needs 009's
manager to exist before it can be a second instance of it. — *All three built in that order, and 010
being a second **instance** is what let it fix two defects in 009's merged code rather than fork it.*

**VFN-004, 005, 006, 007 in any order.** All four are independent, all four are small, and none of
them blocks anything. VFN-005 is the only one with a reproduce step in front of it. — *VFN-004 was
not small: it shipped dead and cost a session.*

**VFN-012 last**, because it adds vocabulary to a language the previous eleven tasks are busy making
work. It is also the one task whose scope can be cut without leaving a hole: shipping the app-config
variables and not the libraries is a coherent partial answer. — *It was not cut. All three sections
landed.*

## Two things to check before starting *any* task here

1. 🔴 **Reproduce the report.** Two of eleven have no pinned mechanism and both are written to look
   obvious. This register has filed a "not there" finding that was a grep of the wrong surface, a
   guard that was decoration, and a spec containing the sentence it forbade. Reproduce first.
2. ⚠️ **`test:main` and `test:ci` before and after.** `test:main` measured 144 suites / 2107 green
   on 2026-08-12; `test:ci` has a floor of 6 failures at 2670 specs. Compare **names**, not counts,
   and read `tests/test-results.json` rather than the log. `cloud-library:check` is a required PR
   gate and drifts red on any port-group rename — VFN-012 adds ports and must run it.
3. 🔴 **And a third, learned the hard way in this phase:** `npx tsc -p tsconfig.json` **without
   `--noEmit`** poisons the jest run with emitted `.js` files and both gates then report a pass
   they did not earn. See the status block at the top.
