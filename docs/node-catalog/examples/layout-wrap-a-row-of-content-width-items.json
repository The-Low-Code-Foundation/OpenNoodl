{
  "id": "layout-wrap-a-row-of-content-width-items",
  "title": "A wrapping row of content-width items, with no CSS",
  "description": "Items that keep their own width and flow onto the next line when they run out of room — the layout a chip row, a filter bar or a tag list wants, and one people reach for a `CSS Definition` node to get. It needs no CSS: the container is a **Group** with `Multi Line Wrap` on, `Layout` set to row, a `Column Gap` and a `Row Gap` (the two gap ports only appear once wrapping is on) and `Align Y` at top, and each item is a **Group** at `Content Size`. ⚠️ That last part is the half that is usually missed. An item at `Content Size` gets no width of its own, and the runtime gives every node `flex-shrink: 0` and only grants `flex-grow` to a percentage width — so `Content Size` already *is* `flex: 0 0 auto`, and an item left at `100%` instead becomes `flex-grow: 100` and swallows the whole line. 🔴 This is not masonry: rows are laid out independently and nothing balances columns or packs items upward by height.",
  "demonstrates": ["Group", "For Each", "Static Data"],
  "components": [
    {
      "name": "/Chip Row",
      "nodes": [
        {
          "id": "wrap",
          "type": "Group",
          "label": "Wrapping row",
          "parameters": {
            "flexDirection": "row",
            "flexWrap": "wrap",
            "alignY": "top",
            "sizeMode": "contentHeight",
            "width": { "value": 100, "unit": "%" },
            "columnGap": "var(--space-2)",
            "rowGap": "var(--space-2)"
          },
          "children": ["repeat"]
        },
        { "id": "repeat", "type": "For Each", "label": "One chip per item", "parent": "wrap", "parameters": { "template": "/Chip Row/Chip" } },
        {
          "id": "items",
          "type": "Static Data",
          "label": "Sample labels",
          "parameters": {
            "type": "json",
            "json": "[{\"Label\":\"Design\"},{\"Label\":\"Engineering\"},{\"Label\":\"Go to market\"},{\"Label\":\"Ops\"},{\"Label\":\"Research and insights\"},{\"Label\":\"QA\"}]"
          }
        }
      ],
      "connections": [
        { "fromId": "items", "fromProperty": "items", "toId": "repeat", "toProperty": "items" }
      ]
    },
    {
      "name": "/Chip Row/Chip",
      "nodes": [
        { "id": "item", "type": "net.noodl.ParentComponentObject", "label": "This item" },
        {
          "id": "pill",
          "type": "Group",
          "label": "Pill",
          "parameters": {
            "flexDirection": "row",
            "sizeMode": "contentSize",
            "alignY": "center",
            "paddingTop": "var(--space-1)",
            "paddingBottom": "var(--space-1)",
            "paddingLeft": "var(--space-3)",
            "paddingRight": "var(--space-3)",
            "backgroundColor": "var(--muted)",
            "borderRadius": 999
          },
          "children": ["label"]
        },
        { "id": "label", "type": "Text", "label": "Label", "parent": "pill", "parameters": { "text": "Tag" } }
      ],
      "connections": [
        { "fromId": "item", "fromProperty": "prop-Label", "toId": "label", "toProperty": "text" }
      ]
    }
  ]
}
