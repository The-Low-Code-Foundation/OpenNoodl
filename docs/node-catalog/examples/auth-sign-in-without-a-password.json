{
  "id": "auth-sign-in-without-a-password",
  "title": "Sign in with a emailed link, or with Google",
  "description": "Two ways past the password field, and both have a behaviour that decides how the screen around them must be built. Request Magic Link posts the address and the backend mails a single-use, short-lived link; `done` means THE REQUEST WAS ACCEPTED and never that an account exists — the endpoint answers identically either way, deliberately, because a different answer would turn the sign-in form into a way to test whether somebody has an account here. So the message on `done` has to be 'if that address has an account, a link is on its way', and a screen that says 'check your inbox' for one address and 'no such user' for another has reintroduced exactly the leak the endpoint is avoiding. Sign In With is more drastic: triggering it NAVIGATES THE BROWSER AWAY to the provider, so nothing downstream of that signal runs and the graph on this page is torn down mid-flight. Treat it as the end of the flow, not a step in it — there is no 'after' to wire, and a spinner set by the same click is a spinner nobody ever sees stop. `signingIn` is the output for the brief moment before the redirect. Both take a `redirect` that must be same-origin with the backend or on its redirect allow-list; an off-list target means no mail is sent and no provider round trip starts, which is a configuration failure that looks exactly like nothing happening. Provider ids ('google', 'github', or whatever the operator named their OIDC provider) come from the backend, not from this graph. One wiring detail that catches people: `done` is a SIGNAL, and a signal wired straight into a value input like `visible` is played as true and then false in the same pass, so the confirmation would flash and vanish. A Switch latches the pulse into remembered state, which is what 'we have sent it' actually is.",
  "demonstrates": [
    "net.noodl.user.RequestMagicLink",
    "net.noodl.user.SignInWith",
    "Switch",
    "net.noodl.Log"
  ],
  "components": [
    {
      "name": "/Passwordless Sign In",
      "nodes": [
        {
          "id": "root",
          "type": "Group",
          "label": "Sign in",
          "parameters": {
            "flexDirection": "column"
          },
          "children": [
            "email_input",
            "link_button",
            "sent_text",
            "google_button",
            "error_text"
          ]
        },
        {
          "id": "email_input",
          "type": "net.noodl.controls.textinput",
          "label": "Email",
          "parent": "root"
        },
        {
          "id": "link_button",
          "type": "net.noodl.controls.button",
          "label": "Email me a link",
          "parent": "root"
        },
        {
          "id": "sent_text",
          "type": "Text",
          "label": "If that address has an account, a link is on its way",
          "parent": "root"
        },
        {
          "id": "google_button",
          "type": "net.noodl.controls.button",
          "label": "Continue with Google",
          "parent": "root"
        },
        {
          "id": "error_text",
          "type": "Text",
          "label": "Why the request could not be made",
          "parent": "root"
        },
        {
          "id": "magic_link",
          "type": "net.noodl.user.RequestMagicLink",
          "label": "Ask for a sign-in link"
        },
        {
          "id": "google",
          "type": "net.noodl.user.SignInWith",
          "label": "Leaves this page — nothing after this runs",
          "parameters": {
            "provider": "google"
          }
        },
        {
          "id": "provider_log",
          "type": "net.noodl.Log",
          "label": "The provider round trip did not start",
          "parameters": {
            "level": "warning"
          }
        },
        {
          "id": "link_sent",
          "type": "Switch",
          "label": "Remember that the request was accepted",
          "parameters": {
            "onFromStart": false
          }
        }
      ],
      "connections": [
        {
          "fromId": "email_input",
          "fromProperty": "onTextChanged",
          "toId": "magic_link",
          "toProperty": "email"
        },
        {
          "fromId": "link_button",
          "fromProperty": "onClick",
          "toId": "magic_link",
          "toProperty": "send"
        },
        {
          "fromId": "magic_link",
          "fromProperty": "error",
          "toId": "error_text",
          "toProperty": "text"
        },
        {
          "fromId": "google_button",
          "fromProperty": "onClick",
          "toId": "google",
          "toProperty": "signIn"
        },
        {
          "fromId": "google",
          "fromProperty": "error",
          "toId": "provider_log",
          "toProperty": "message"
        },
        {
          "fromId": "google",
          "fromProperty": "failure",
          "toId": "provider_log",
          "toProperty": "log"
        },
        {
          "fromId": "magic_link",
          "fromProperty": "done",
          "toId": "link_sent",
          "toProperty": "on"
        },
        {
          "fromId": "link_sent",
          "fromProperty": "state",
          "toId": "sent_text",
          "toProperty": "visible"
        }
      ]
    }
  ]
}
