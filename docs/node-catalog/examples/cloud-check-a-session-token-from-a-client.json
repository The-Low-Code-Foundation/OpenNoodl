{
  "id": "cloud-check-a-session-token-from-a-client",
  "title": "Check a session token that arrived in the request body",
  "description": "This node is for a token that came in the BODY — from a mobile client, a partner integration, a callback — and not for the caller's own session, which the Request node has already resolved onto `auth` and `userId` before any of this runs. If you find yourself verifying the caller's own token, read those outputs instead; the lookup has already happened. It is also not JWT Verify, and the difference is whose token it is: JWT Verify checks somebody ELSE's JWT against a key you hold, while this asks our own backend about our own `_Session` rows. Token is connection-only and cannot be set as a parameter, which is deliberate — a session token typed into a parameter is a live credential committed to a project file. Nothing is read and nothing is ever written until `Do` fires. `userId` is cleared before every check and is blank on every branch except `done`, so a stale id from a previous call can never be mistaken for this one's answer; `valid` is the value form of the same branch, for wiring into a condition rather than a flow. `unchanged` is the token that is simply not a live session — expired, revoked, never ours — which is an ordinary answer rather than an error, and it is routed to the same Response so the caller gets a clear 'no' instead of a timeout. `failure` is reserved for the lookup itself going wrong.",
  "demonstrates": ["noodl.cloud.verifysessiontoken", "noodl.cloud.request", "noodl.cloud.response"],
  "components": [
    {
      "name": "/Cloud Functions/Check Client Session",
      "nodes": [
        {
          "id": "fn_request",
          "type": "noodl.cloud.request",
          "label": "Request",
          "parameters": { "params": "sessionToken", "allowNoAuth": true }
        },
        { "id": "check", "type": "noodl.cloud.verifysessiontoken", "label": "Is this one of ours, and whose" },
        {
          "id": "ok_response",
          "type": "noodl.cloud.response",
          "label": "Valid, and who",
          "parameters": { "params": "valid,userId,username" }
        },
        { "id": "fail_response", "type": "noodl.cloud.response", "label": "The lookup failed", "parameters": { "status": "failure" } }
      ],
      "connections": [
        { "fromId": "fn_request", "fromProperty": "sessionToken", "toId": "check", "toProperty": "token" },
        { "fromId": "fn_request", "fromProperty": "receive", "toId": "check", "toProperty": "verify" },
        { "fromId": "check", "fromProperty": "valid", "toId": "ok_response", "toProperty": "valid" },
        { "fromId": "check", "fromProperty": "userId", "toId": "ok_response", "toProperty": "userId" },
        { "fromId": "check", "fromProperty": "username", "toId": "ok_response", "toProperty": "username" },
        { "fromId": "check", "fromProperty": "done", "toId": "ok_response", "toProperty": "send" },
        { "fromId": "check", "fromProperty": "unchanged", "toId": "ok_response", "toProperty": "send" },
        { "fromId": "check", "fromProperty": "error", "toId": "fail_response", "toProperty": "errorMessage" },
        { "fromId": "check", "fromProperty": "failure", "toId": "fail_response", "toProperty": "send" }
      ]
    }
  ]
}
