{
  "id": "app-catch-every-error-in-one-place",
  "title": "Catch the errors nobody wired a Failure port for",
  "description": "Per-node `Failure` outputs cover the errors an author expected and wanted to branch on where they happen. This node covers everything else, which in a real app is most of them — no library will ever carry complete failure ports on every node type, and without a boundary the only record of an unwired error is a console line nobody reads in a deployed app. It subscribes as soon as it is created, so errors raised while the graph is still starting are caught rather than missed during setup, which is exactly the window where a misconfigured backend or a missing secret shows up. The ordering guarantee is what makes it usable: the value outputs — Message, Code, Node Type, Node Id, Component Name — are updated BEFORE the `error` signal fires, so reading them on the pulse always describes this error rather than the previous one. Filter is blank here, and blank is the right default for a boundary; a code prefix narrows it to one node type or one specific error, which is what you want for a second, more specific handler rather than for the catch-all. Error Object wires straight into a string input and renders as JSON, which is what to send to a logging service. Put one of these at the root of the app, not one per page: a boundary that only exists on the screen where you were debugging catches nothing on the screen where the bug actually reaches a user.",
  "demonstrates": ["On App Error", "net.noodl.Log", "String Format", "Text"],
  "components": [
    {
      "name": "/App",
      "nodes": [
        {
          "id": "root",
          "type": "Group",
          "label": "App root",
          "parameters": { "flexDirection": "column" },
          "children": ["error_banner"]
        },
        { "id": "error_banner", "type": "Text", "label": "Something went wrong", "parent": "root" },
        { "id": "boundary", "type": "On App Error", "label": "Every unhandled failure in the app" },
        {
          "id": "where",
          "type": "String Format",
          "label": "Which node, in which component",
          "parameters": { "format": "{code} in {component} — {nodeType} ({nodeId})" }
        },
        { "id": "error_log", "type": "net.noodl.Log", "label": "Send it somewhere durable", "parameters": { "level": "error" } }
      ],
      "connections": [
        { "fromId": "boundary", "fromProperty": "code", "toId": "where", "toProperty": "code" },
        { "fromId": "boundary", "fromProperty": "componentName", "toId": "where", "toProperty": "component" },
        { "fromId": "boundary", "fromProperty": "nodeType", "toId": "where", "toProperty": "nodeType" },
        { "fromId": "boundary", "fromProperty": "nodeId", "toId": "where", "toProperty": "nodeId" },
        { "fromId": "boundary", "fromProperty": "message", "toId": "error_banner", "toProperty": "text" },
        { "fromId": "boundary", "fromProperty": "message", "toId": "error_log", "toProperty": "message" },
        { "fromId": "boundary", "fromProperty": "errorObject", "toId": "error_log", "toProperty": "data" },
        { "fromId": "boundary", "fromProperty": "error", "toId": "error_log", "toProperty": "log" }
      ]
    }
  ]
}
