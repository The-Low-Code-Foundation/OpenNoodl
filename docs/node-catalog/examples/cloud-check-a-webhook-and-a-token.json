{
  "id": "cloud-check-a-webhook-and-a-token",
  "title": "Prove an inbound request really came from who it says",
  "description": "Two different 'is this genuine' questions, side by side, because choosing the wrong one is the common mistake. HMAC is for a webhook: the sender computed a keyed digest of the body with a shared secret and put it in a header, and you recompute it and compare. The input that decides whether this works is Value — it must be the RAW body, byte for byte, exactly as it arrived. Re-serialising the parsed object first produces a different string (key order, whitespace, number formatting) and therefore a different signature, and the symptom is every webhook failing verification with no clue why. JWT Verify is for a token somebody else ISSUED to you, and it refuses any token whose header names an algorithm other than the one you selected — which is what defeats both `alg: none` and algorithm confusion, and is why Algorithm is set here rather than read from the token. Nothing inside a token may be believed before the signature check, so Claims is left alone on failure rather than handed over unchecked; reading claims off a token you have not verified is the same bug as trusting the webhook body. Clock Tolerance exists for issuers whose clock is not quite yours and defaults to 0. Both keys come from Secret nodes by name, so neither credential is in the graph. Note what neither node is for: your own users' sessions. That is Verify Session Token, which asks this backend about its own `_Session` rows.",
  "demonstrates": ["noodl.cloud.hmac", "noodl.cloud.jwtverify", "noodl.cloud.secret", "noodl.cloud.response"],
  "components": [
    {
      "name": "/Cloud Functions/Verify Inbound",
      "nodes": [
        {
          "id": "fn_request",
          "type": "noodl.cloud.request",
          "label": "Request",
          "parameters": { "params": "rawBody,partnerToken", "allowNoAuth": true }
        },
        {
          "id": "webhook_key",
          "type": "noodl.cloud.secret",
          "label": "The webhook's shared secret",
          "parameters": { "name": "WEBHOOK_SIGNING_SECRET" }
        },
        {
          "id": "partner_key",
          "type": "noodl.cloud.secret",
          "label": "The partner's JWT key",
          "parameters": { "name": "PARTNER_JWT_KEY" }
        },
        {
          "id": "body_signature",
          "type": "noodl.cloud.hmac",
          "label": "HMAC of the RAW body",
          "parameters": { "algorithm": "SHA-256", "encoding": "hex" }
        },
        {
          "id": "check_token",
          "type": "noodl.cloud.jwtverify",
          "label": "HS256 only — a token claiming anything else is refused",
          "parameters": { "algorithm": "HS256", "clockTolerance": 5 }
        },
        {
          "id": "ok_response",
          "type": "noodl.cloud.response",
          "label": "What we computed and what verified",
          "parameters": { "params": "signature,tokenValid" }
        },
        { "id": "fail_response", "type": "noodl.cloud.response", "label": "Failure", "parameters": { "status": "failure" } }
      ],
      "connections": [
        { "fromId": "fn_request", "fromProperty": "receive", "toId": "webhook_key", "toProperty": "fetch" },
        { "fromId": "fn_request", "fromProperty": "receive", "toId": "partner_key", "toProperty": "fetch" },
        { "fromId": "fn_request", "fromProperty": "rawBody", "toId": "body_signature", "toProperty": "value" },
        { "fromId": "webhook_key", "fromProperty": "value", "toId": "body_signature", "toProperty": "key" },
        { "fromId": "webhook_key", "fromProperty": "done", "toId": "body_signature", "toProperty": "sign" },
        { "fromId": "fn_request", "fromProperty": "partnerToken", "toId": "check_token", "toProperty": "token" },
        { "fromId": "partner_key", "fromProperty": "value", "toId": "check_token", "toProperty": "key" },
        { "fromId": "partner_key", "fromProperty": "done", "toId": "check_token", "toProperty": "verify" },
        { "fromId": "body_signature", "fromProperty": "signature", "toId": "ok_response", "toProperty": "signature" },
        { "fromId": "check_token", "fromProperty": "valid", "toId": "ok_response", "toProperty": "tokenValid" },
        { "fromId": "check_token", "fromProperty": "done", "toId": "ok_response", "toProperty": "send" },
        { "fromId": "body_signature", "fromProperty": "error", "toId": "fail_response", "toProperty": "errorMessage" },
        { "fromId": "check_token", "fromProperty": "error", "toId": "fail_response", "toProperty": "errorMessage" },
        { "fromId": "body_signature", "fromProperty": "failure", "toId": "fail_response", "toProperty": "send" },
        { "fromId": "check_token", "fromProperty": "failure", "toId": "fail_response", "toProperty": "send" }
      ]
    }
  ]
}
