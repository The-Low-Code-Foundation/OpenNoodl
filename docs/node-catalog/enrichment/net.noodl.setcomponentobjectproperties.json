{
  "typeName": "net.noodl.SetComponentObjectProperties",
  "summary": "Set Component Object Properties: writes declared values onto this component instance's own object when store fires.",
  "description": "The writing half of Component Object: property values set on this node's declared inputs are committed to the enclosing component instance's object when `store` fires, and every Component Object / Parent Component Object reader of that instance sees the change immediately. `done` confirms the write, and `completed` follows it on every invocation.",
  "whenToUse": "Mutating instance-scoped state: resetting a wizard, recording a selection local to this instance. For state shared across instances or screens use Objects (Model2) or Variables instead.",
  "ports": {
    "properties": "Declared property names; each becomes a value input written on store.",
    "store": "Signal; commits the current input values to the instance's object.",
    "done": "Signal; the write completed.",
    "completed": "Signal; fires after `done` on every invocation. This node has no `failure` — its record is its own component instance's and cannot be missing."
  },
  "runtimeBehavior": "Property inputs are generated from the per-instance `properties` list (runtime-discovered).",
  "examples": ["comp-wizard-component-object"],
  "relatedNodes": ["net.noodl.ComponentObject", "net.noodl.SetParentComponentObjectProperties", "SetModelProperties"]
}
